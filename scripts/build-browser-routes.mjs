import fs from 'node:fs'
import path from 'node:path'
import {fileURLToPath} from 'node:url'
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..')
const data=JSON.parse(fs.readFileSync(path.join(root,'src/shared/browserRoutes.json'),'utf8'))
const out=path.join(root,'docs/browser');fs.mkdirSync(out,{recursive:true})
fs.writeFileSync(path.join(out,'routes.json'),JSON.stringify(data,null,2)+'\n')
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))
const groups=data.categories.map(c=>'<section id="'+c.id+'"><h2>'+esc(c.name)+'</h2><p><a href="eas-favorites://home?folder='+c.id+'">在收藏夹中打开分类</a></p><div class="row">'+data.sites.filter(s=>s.category===c.id).map(s=>'<article id="'+s.id+'"><h3>'+esc(s.name)+'</h3><p>'+esc(s.description)+'</p><small>'+s.intents.map(esc).join(' · ')+'</small><p>'+(s.url?'<a href="'+esc(s.url)+'" target="_blank" rel="noopener noreferrer">打开网站 ↗</a>':'网址待确认')+'</p></article>').join('')+'</div></section>').join('')
const form='<section id="bookmark"><h2>收藏网站</h2><p>在 Eas-Term 内嵌浏览器中使用：提交仅打开预填表单，需再次确认收藏。</p><form id="bookmark-form"><label>名称 <input name="name" maxlength="120" required></label> <label>网址 <input name="url" type="url" required></label> <label>分类 <select name="folder">'+data.categories.map(c=>'<option value="'+c.id+'">'+esc(c.name)+'</option>').join('')+'</select></label> <button>打开收藏表单</button></form></section><script>document.getElementById("bookmark-form").addEventListener("submit",e=>{e.preventDefault();const values=new FormData(e.target);const u=new URL(values.get("url"));if(!["http:","https:"].includes(u.protocol)||u.username||u.password){alert("仅支持不含账号密码的HTTP(S)网址");return}location.href="eas-favorites://save?"+new URLSearchParams(values).toString()})</script>'
fs.writeFileSync(path.join(out,'index.html'),'<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Eas-Term 网站路由入口</title><style>body{background:#18181a;color:#eee;font:14px -apple-system,sans-serif;padding:30px;max-width:1200px;margin:auto}h1{font-size:26px}p,small{color:#aaa;line-height:1.8}nav{display:flex;gap:20px;flex-wrap:wrap}a{color:inherit}section{padding-top:30px;scroll-margin-top:20px}.row{display:flex;gap:16px;overflow:auto;padding-bottom:15px}article{border:1px solid #38383b;border-radius:10px;padding:20px;flex:0 0 260px}article:target{border-color:#bbacde}section:target h2{color:#bbacde}</style></head><body><h1>网站入口</h1><p>本地路由目录 · 点击打开，不会自动发布。收藏表单只预填，必须由用户确认保存。</p><nav>'+data.categories.map(c=>'<a href="#'+c.id+'">'+esc(c.name)+'</a>').join('')+'</nav><p><a href="eas-favorites://home">打开应用收藏夹</a> · <a href="eas-favorites://save">打开收藏表单</a></p>'+groups+form+'<script type="application/json" id="site-routes">'+JSON.stringify(data).replace(/</g,'\\u003c')+'</script></body></html>')
console.log('Generated docs/browser/index.html + routes.json')

const bundled=path.join(root,'resources/browser');fs.mkdirSync(bundled,{recursive:true});for(const name of ['index.html','routes.json'])fs.copyFileSync(path.join(out,name),path.join(bundled,name))

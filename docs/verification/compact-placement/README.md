# Frame 新建模块紧凑选址验收
2026-09-09：canvas 目录 107 项测试通过，typecheck 和 build 成功。
独立临时 userData / CDP 9448 实例，不改正式数据、不调用模型。
通过实际 store.addAgentNode 连续新建四次；模块最终尺寸 640×380，形成 2×2，已有节点坐标保持。
截图 four-modules.png。纯函数另覆盖删除空位补入、不同尺寸与子 Frame 障碍等几何条件；未做 Windows 真机验证。
手动落点创建的文件/组件及拖拽保留原规则；本次修改自动选址入口。

发版前复验：修正 AI 预选址实际宽度，并改为宽高比评分 + 邻边约束；新增真实尺寸测试。最终干净全量 2832 项（2819 通过、13 跳过）；发布 arm64 包以 EAS_VERIFY 隔离模式再次验证 2×2 并截图。初次打包验证脚本误用 EAS_SMOKE，未暴露 __store；改用规定的 EAS_VERIFY 后通过，未修改产品代码。删除补位和旧模块不动见 refill.json。没有调用模型。

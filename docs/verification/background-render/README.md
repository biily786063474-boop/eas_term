# 后台数据与视觉预算回归（2026-09-10）

隔离实例：worktree构建产物，独立临时userData，CDP9460；未替换/重启用户正式0.4.88或原开发实例。

## 行为
- 移除全局三项禁节流开关；主窗口backgroundThrottling:false仍保留，避免异步解析/状态调度挂起。
- 后台CSS暂停，思考球/代码星空同时检查焦点和视口，仪表盘后台停止15秒界面查询（主进程记账不停止）。
- 终端后台不用rAF等待，每块最多65536字符、一个write在途，解析完成再取下一块；前台仍约60Hz合帧。
- 已有5000行scrollback、网页离屏120秒回收保持。总待解析队列并非硬上限：无限高速生产超出解析能力仍有风险，本轮没有以丢文本掩盖积压。

## 已取证
1. 单测包含rAF不回调、慢解析、顺序、分块、卸载后的回调清理；代码星空有焦点/视口门和监听清理的源码契约测试（不等同UI行为测试）。
2. 独立应用最小化，Python每10ms输出一行，6000行中文，逐行完整有序；未恢复前台前xterm buffer已解析END。background-output.json。buffer为5000滚动历史+24可视行。
3. 持续输出回归的每分钟CPU/RSS快照在soak-process-samples.txt。这只是分钟级回归，不是数小时泄漏证明；样本含测试数据收集器，不能用RSS增长直接判断产品泄漏。
4. npm run check：typecheck/钩子/对比度/CSS平衡/动画静态检查通过；2866测试，2853通过，13跳过，0失败。npm run build通过；已有动态/静态混合import分包警告（pairing/CodeGraphView），未掩盖。

## 仍需明确的边界
- 当前运行的旧正式版renderer高CPU具体组件未定位：无调试端口，采样符号不足；新隔离简化场景不能作为旧实例同比降幅。
- 未运行Windows、三CLI真实长任务、数小时多模块/高清网页/语音并发压测。网络任务/CLI主进程逻辑没有改动，但不等于这些路径都完成端到端回归。
- 同一窗口内webview合成预算可能受主窗口backgroundThrottling:false影响；不能宣称每个网页完全休眠。
- 底层Clash进程另有高CPU，属于外部软件，本轮未动其配置。

## 持续回归结果与未通过步骤
- 30000行中文持续输出已完成（约5分钟以上，Python sleep受调度影响），全部有序且后台解析到END；详见soak-output.json。
- 六次每分钟快照：renderer CPU1.8–2.6%，RSS207968–218144KiB（中途215056/218224/216976）；GPU1.1–2.2%。这是简化隔离场景，不代表真实大画布的降幅。
- 前台恢复截图未完成：System Events恢复测试PID窗口报“不能获得window 1，无效的索引”；CDP Browser.getWindowForTarget不支持，截图等待未返回。CUA按同一路径解析到了原0.4.91开发窗口，为避免动错用户实例未继续操作。因此前台恢复视觉、星空/思考球具体UI动效均标记未验证，不能由静态测试冒充。

# 设置层级落地 · 2026-09-10
依据根工作区 docs/prototype/2026-09-10-settings-hierarchy.html。沿用真实 glass 令牌，10页/3组；MCP单独页，黑匣子进性能；审批三档保留原 setApproval 路径；隐私统计说明限定为该项统计，避免误解为所有网络请求不传对话。

验证：类型、构建、hooks/对比度/CSS括号/动画检查通过；全量2862项，2849通过、13跳过、0失败（并发4）。
隔离 Electron CDP9452、原开发实例PID69235；仅刷新renderer，未关闭开发实例/生产应用。主进程版本标签仍为该已启动实例的0.4.90，当前源码版本0.4.91，本次未发版。
- 10页真实渲染全部无横向溢出，搜索“黑匣子”只匹配性能页，说明展开正常。
- 主题实际切亮色再恢复暗色；720px窄窗口6个长页无横向溢出；截图已检查。
- Esc关闭通过；MCP入口初次未出现（空记录且开关开启的既有行为），在隔离实例暂时关闭MCP后显示，点击准确进入MCP页并恢复原开关。
- 画板“快照后清空标记”真实IPC保存及原值恢复通过。
- 无更改真实用户OMP审批/Claude statusLine、手机配对、扩展卸载/安装；这些有外部副作用的操作本轮未端到端执行，仅保留原处理器与静态回归。
- 暂不新增权限、默认值、主进程API或偏好存储；原有更新/手机/扩展/快捷键子组件保持完整。

截图：theme/ai/privacy/light/narrow等；ui.json / responsive.json / controls.json为实际探针。
responsive.json末尾空对象是首次MCP入口未出现，后续真实入口验证见controls.json；不将不存在的入口假报成功。

# 插件市场详情验收 · 2026-09-23

隔离工作树 `feat/plugin-market-detail-20260923`，不触碰正式应用和生产市场目录。

- `npm run build` 通过。
- `npm run check`：3614 通过、19 跳过、0 失败。
- 8 份详情按线上公开 v2 目录的名称、版本、SHA-256 绑定；逐个下载公开包并对照包内工具声明。仅文案，不安装或执行这些包。
- `verify-jev-market.mjs` 在隔离 Electron 中通过：卡片内容/边距开详情、Esc 返回及焦点、阅读不下载、窄窗口无横向溢出；原安装、更新、权限确认、离线缓存等链路仍通过。详情截图使用测试夹具文案，不冒充线上 Jev 0.1.1 截图。
- `detail.png` 暗色详情；`detail-narrow-light.png` 亮色窄窗口详情。其他截图是同脚本的安装/更新回归。

未验证：在线 Jev 模型真实调用、物理 Windows 界面、生产目录实际携带新 `detail` 字段。新客户端可对精确匹配的 8 个已审核包用内置详情回退；旧客户端不会得到此界面。生产目录发布、桌面正式版替换和自动更新不在本次范围。

# Frame 右键菜单验收（2026-09-25）

从截图指出的同一位置验证：一级菜单新增「组件 ›」，列出注册表中的版本管理、设计模块、团队面板、代码地图、插件面板；「新建 ›」只含 AI 对话、终端、浏览器。原有「移动到看板区 ›」仍保留箭头。「删除 Frame」文字与普通项同色、不再醒目红色，危险动作语义及删除回调未改。

`scripts/verify-frame-context-menu.mjs` 在独立 profile、沙箱 Electron 中验证，暗/亮截图见本目录；结果见 `result.json`。修改前脚本按预期失败于缺少一级「组件」；修改后八项检查通过，包括点击「版本管理」后确实插入组件。`npm run build` 通过。真实用户项目、删除确认流程和在线插件安装不在本脚本范围。

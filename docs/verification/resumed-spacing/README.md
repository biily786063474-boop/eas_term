# 待续对话底部留白

2026-09-07：仅修改待续页上下文选项行样式，与输入框间距 12px、底部 16px，左右与输入框同宽。不改发送逻辑或新建对话布局。

`npm run build` 与 `git diff --check` 通过。未注入测试夹具的隔离 Electron，通过既有 saveHistory API 保存示例历史并加载真实待续页；标准宽度与 480px 模块宽度下 DOM 测量均为 gap=12、bottom=16，选项行与输入框宽度一致。窄栏通过调整测试实例 pane 宽度触发真实容器查询。未调用模型。

截图：1100.png、480.png；测量：results.json。

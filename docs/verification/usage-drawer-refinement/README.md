# 用量仪表盘视觉微调（2026-09-10）

按用户现场验收反馈，只改展示，不改账本与统计：

- 大趋势图和每个项目的小图使用轻度平滑三次曲线；真实数据点不移动，控制点不超出相邻值范围，未知区间保持断线。
- 页签恢复到抽屉本体左侧、上下排列，沿用原竖排胶囊样式。用量页签简写「用量」，页内标题仍为「用量仪表盘」。移除顶部横排覆盖规则。
- 底层质感原样保留：`--glass-1` 背景、`--blur` 模糊、原边框/圆角/高光/阴影，没有新增抽屉底色。
- 外侧页签仍属于 `.wk-shell`，切换不会被 outside-click 当成外部点击；关闭位移包含左侧页签宽度。

验证：23 项定向测试通过（5 项新增视觉/曲线约束 + 用量核心回归），TypeScript、CSS 括号检查、构建通过。
真实开发实例只刷新渲染界面，没有关闭或重启应用进程。确认无运行中的 AI 对话后刷新。
`ui.json`：三张图均使用三次路径，四个未知点保留；页签在抽屉外左侧且上下排列，Skill/wiki 点击不收起；无面板错误、无横向溢出。
实际玻璃参数：`rgba(15,17,23,.5)`、`blur(28px) saturate(1.6)`，与原组件变量一致。
截图 `curves-and-tabs.png` 已亲眼检查。开发应用仍由 macOS 独立运行，保留给用户体验。

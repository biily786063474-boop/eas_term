# 设计选型分类吸顶与源码引用 · 2026-09-22

已实现，尚未提交/发版。保留现有设计系统。

- 分类和明暗筛选同组吸顶；真实隔离应用向下滚动两屏仍可见。
- 「引用源码」与「预览提示词」并排。无目标输入框明确拒绝；取到完整HTML后保存至目标项目 `.eas/design-references/<slug>-<sha256>.html` 并挂源码chip，不自动发消息。
- CUA实点 ChatGPT 示例，下载612133字节；与预览URL完整响应逐字节一致（见source-integrity.json）。原HTML含内联CSS/JS，外链依赖未下载，不等同于产品完整工程。
- 真实输入框粘贴 `@ChatGPT · 源码 只参考导航栏，其他部分保持当前项目风格。`，发送预览包含完整本地文件路径和局部要求，没有数百KB源码塞入CLI argv。
- 全量check：3520 tests，3502 pass，18 skip，0 fail；随后补充两项接线回归，总计六项专项通过。typecheck与build通过。
- 首次测试复制Electron后修改bundle ID但未重签，CUA找不到；仅退出自身PID，临时app ad-hoc签名后正常。CUA typeText中文不完整，改用paste并核实实际文字；该中间输入不计验收。保存截图后旧AX索引失效，未触发关闭按钮；随后CmdQ正常退出隔离应用。未动用户正式应用/全局CUA服务。

限制：没有实际向付费模型发送，因此「模型最终生成是否符合局部参考」未验证；远程agent不能访问本机文件的场景未验证；未逐一下载307个示例；运行中对话态、窄窗和亮色视觉尚未单独实测，接线回归覆盖两种输入状态。2MB上限与20秒超时下失败不截断、不用摘要代替源码。

## 2026-09-22 引用提示词二级选择（最终复验）

- 「预览提示词」改为「引用提示词」；仅点击后出现「仅参考配色 / 参考完整设计系统」，点选直接加入输入框引用，无中间确认，不自动发送。「引用源码」保留。
- 实测发现 portal 菜单被辞典外部点击捕获误关闭，导致完整规范异步读取失效；通过明确菜单归属修正，回归测试先红后绿。
- 连续切换发现原 addChip 同 ID 去重会静默保留旧范围；仅对 design: 引用允许原位更新内容，普通辞典去重不变。新增行为测试先红后绿（19/20 → 20/20）。
- 最终构建真实 UI 依次选择完整规范 → 仅配色 → 完整规范；每次查看实际发送内容预览，标签和正文均随范围更新，始终只有一条引用。没有向模型发送。
- 截图：prompt-menu.jpg（点击后选项）；prompt-colors.jpg（配色正文）；prompt-system.jpg（完整规范正文）。
- 最终 typecheck、build、npm run check 均成功：3525 tests，3507 pass，18 skip，0 fail。
- 仅使用隔离应用 /private/tmp/EasDesignVerify.app 和测试项目；验收后 CmdQ 退出。未发版，未提交。

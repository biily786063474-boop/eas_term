# OMP 登录收尾优化 · 2026-09-08（未发版）

- 隔离实例 9448，真实 bundled OMP Antigravity broker，无浏览器授权、无账号凭证复制。
- 等待输入/回调时：遮罩 mousedown + Escape 后进程仍为 input，面板保留。
- 点击 × 出现面板内确认；继续等待后进程保留；再次 × 并确认取消后 inFlight=null、面板关闭。
- 修正确认区按钮样式后重建、刷新实例，已截图实际界面；测试发起的登录已主动取消。
- 原生进度通过解析器/控制器回放与 React 组件渲染测试，覆盖无换行手动问句后继续输出、逐分块、验证→账号信息→失败清理。没有将这次回放标为真实账号登录验收。
- 完整 check 2772通过/12跳过/0失败；其中10 Windows专用、2显式开启的原生探针跳过。最终定向39/39，构建通过。其他并行工作新增测试使计数与0.4.86不同。
- 未新增项目ID配置入口、未改其他CLI、未发布新版本。

# Eas-Term 0.4.104 发布验收（已完成；历史阶段记录保留）
- 用户明确要求发版；当前插件分支已包含最新origin/main eda14a4。根工作区未提交改动不动。
- 包含独立插件更新入口、Eas格式外部来源入口与原源更新、时间线离线独立迁移、办公/本地文件市场宿主和配置授权基础。并非32项插件全部真实账号验收。
- 使用本工作树独立npm ci与Node22.23.2，不再使用共享node_modules软链。
- 首轮Node22 check：1失败（CLI夹具启动8秒超时），5取消（OMP approval等待unref定时器导致测试进程退出），3489通过/19跳过。CLI单跑通过；OMP取消单跑复现，仅测试增加自身keepalive，不修改产品unref安全超时逻辑。复测专项11通过；全量3514/3496通过/18跳过/0失败/0取消。
- 生产依赖audit最初2个high（fast-uri、ip-address）；按现有兼容约束更新至3.1.8、10.7.2，生产audit为0。开发依赖未整体升级，不宣称所有开发依赖无漏洞。
- 公网来源1.0.0→1.1.0、时间线迁移/历史/重启已在开发构建CUA验收；本轮正式包仍待验。
- Computer Use外部指针残留仍开放；不得全局杀外部服务，不宣称修复。

## 正式候选验收通过，发布进行中
- 产品提交 efbb5bf；Windows 35726706720 全流程 success，安装包已下载。
- Mac 初轮忘记开启公证；一次包装脚本工作目录指向旧树，发现后终止，未上传；正确树公证首次 Apple DNS -1003，查询确认没有新任务后重试，双架构 Accepted/staple。
- 最终用已签名公证 app 生成归档；x64 ZIP 遇构建缓存跳过，旧 ZIP 移到临时保留目录后重新生成。两架构 app/ZIP/DMG 的签名、staple、Gatekeeper、app.asar 与主二进制哈希一致。
- ARM 正式包 CUA：真实终端回显、市场已发布7包/来源入口/检查更新入口、时间线独立版、原历史记录、重启恢复1项成果与全局开关关闭。历史/manifest/授权三份哈希重启不变。
- x64 Rosetta CUA：真实终端回显及隔离项目目录，另测打包内 node-pty。未验物理Intel。
- CUA 同名路径多次timeout；改用原字节签名副本及规范化 /private/tmp 路径后连接成功，两份副本asar/主二进制哈希一致且签名通过。未改正式 /Applications 应用。
- 官网/GitHub尚未切换，不能把本节当发布完成。

## 官网已发布，GitHub镜像待完成
产品efbb5bf，tag e915531。插件更新入口、Eas兼容外部来源与原源更新、时间线离线独立迁移。Mac双架构公证/签名/ZIP-DMG哈希一致性通过，ARM市场/终端/时间线历史与重启CUA通过；x64 Rosetta终端CUA与原生PTY通过，未验物理Intel；Windows35726706720 success。五包逐个SCP并核对size/SHA256，三个网页后latest最后切换。公开latest/页面hash/包HEAD+Range通过；八站HTTP与五个PM2 PID/状态前后一致，无服务重启，无删除旧版。回退备份 /www/wwwroot/eas-release-backups/0.4.104-20260922T124920Z。GitHub仍draft上传中。Computer Use指针问题仍开放，不宣称32插件全部账号验收。

## 最终发布完成
官网与GitHub均已发布0.4.104。GitHub 2026-09-22T13:06:35Z 公开/latest，五包size与sha256 digest与本地/官网完全一致。main已包含本次产品和发布提交，tag e915531、产品efbb5bf。所有自有验收实例退出，未替换用户 /Applications 应用；已知Computer Use外部指针、32插件真实账号覆盖限制保留。

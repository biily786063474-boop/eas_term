# Eas-Term 0.4.84 发布验收

## 审查

独立审查发现并修复 3 项 Important：异步 fatal 与成功 ACK 竞态丢失待发项、调整方向绕过本地斜杠确认、OMP 握手期间不能撤销旧方向。复审无剩余阻断项。重试按稳定消息身份替换失败提问，避免重复历史。

## 验证

- npm run check：2559 通过，0 失败、0 跳过。
- 三端队列真实 Electron UI：39 项通过，控制台错误 0；CLI 传输为隔离夹具，非真实模型端到端测试。
- 既有导航与续发回归上一轮 23 项通过。
- 排队内容当前不跨应用重载保存。
- 外部 Codex Computer Use 指针残留仍开放，本次不宣称修复；详见 computer-use-lifecycle.md，更新日志明确此限制。

## 正式包与发布结果

2026-09-08 已发布 https://eas.biily.top ，macOS arm64/x64 与 Windows 均为 0.4.84，源提交 07c6c6b。内置能力插件化的后续未提交改动不在这些冻结包中。

- macOS 两个 .app 均通过 codesign --verify --deep --strict、stapler validate 和 Gatekeeper（Notarized Developer ID）。两个 DMG 经 hdiutil 校验，两个 ZIP 经 unzip 校验。
- 两个实际 app.asar 的版本均为 0.4.84，139 个 out 文件分别与冻结源构建逐字节一致，无测试 transport。
- ARM 原生、x64 在 Rosetta 下实际启动；界面、47 个 preload 命名空间、真实终端回显、IPC、代码图谱依赖分析、随包 OMP 18.1.2 均通过，无渲染 JS 异常。测试独立 userData，并用系统沙箱隔离用户 CLI/规则目录读写。只写保护首次暴露旧版 refreshInstalledRules 的 EPERM 启动边界，记录到插件化诊断，未声称本版修复。
- Windows GitHub Actions run 34195281416（同一源提交）构建和实际安装包冒烟通过。下载 artifact 的 SHA256 与 GitHub digest 一致，未在本机运行 Windows。
- 五个包逐个上传、核对远端完整 SHA256 后才切换 index/download/changelog 与 latest.json；未 reload 服务、未删除旧版本。公网五个包 HTTP 200，长度正确；页面与 latest.json 逐字节校验通过。七个生产站点发布前后状态均为 301。
- 备份与发布数据见 0.4.84-live.json；本地产物位于 ~/Eas-Term-release/0.4.84-prep/artifacts/。未安装替换用户正在运行的应用。


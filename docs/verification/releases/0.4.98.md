# Eas-Term 0.4.98 发布验证（2026-09-14）

源码：`f18ca5f`（`fix/background-render-budget`）。内容：流式识别 worker 常驻 + 语音启动退出准入队列（`docs/verification/voice-warm/2026-09-14-真机验收.md`）。

- 单测 3205 条 0 失败；typecheck 0 错；build 通过。
- mac arm64 / x64：stapler validate + spctl「Notarized Developer ID」通过，版本号 0.4.98。
- Windows：CI run 34936154244 成功，`Eas-Term-0.4.98-x64-setup.exe` 197462298 bytes，SHA256 前缀 `116e079c32eef7aa`。
  拉取时 `gh run download` 两次 EOF：Azure blob 直连 TLS 握不上（HTTP 000），代理节点 delay 526ms 可达；按 playbook 把 `blob.core.windows.net` 规则从 DIRECT 改为节点后一次成功。
- `bash scripts/publish-site.sh`：五包 SHA256 校验通过；`latest.json` 切到 0.4.98；下载链接 HEAD 200 且 Content-Length 一致；五站 reload 前后 301；清理删除 v0.4.96。
- GitHub release `v0.4.98`：五包资产上传（后台）。
- 本机 `/Applications` 未装（用户自行通过更新角标升级）。

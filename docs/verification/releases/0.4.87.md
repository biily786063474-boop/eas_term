# Eas-Term 0.4.87 正式发布验收

用户于2026-09-08授权正式发版。最终产品源码 `9e3af6bbb16d30ae95305b471986b649d6ea2ca4`；之后仅发布文档与官网内容变更。

## 范围
- 可选笔纵 MCP 发现失败不阻塞 OMP 握手与普通对话；必要依赖、安全与权限失败仍然阻止操作。
- OMP 登录取消保护、验证进度、图片预览 FIFO、徽标固定镂空边光。
- Git 检出/分支/标签/比较/复制、改动配色和行数；写入口 guardDir 验证工作树与元数据，未提交改动阻止切换。
- AI 分屏打开启动页，不复制会话、初始任务或团队归属。
- Windows fsGuard 使用 native realpath 统一8.3短路径与长路径，保留越界与 junction 检查。
- 不包含私测诊断日志上传；不宣称原始 Windows 闪退或外部 Computer Use 指针残留已修复。

## 最终验证
- 完整 check：2794项，2782通过、12跳过、0失败；typecheck/static通过。日志 `/tmp/release87-native-check.log`。
- Mac arm64/x64 均重新构建，Developer ID 签名、Apple公证、staple、codesign与spctl通过。
- 两架构实际打包应用 smoke、Git UI/受保护 IPC 15项、明暗徽标边光验证通过；截图已亲眼检查。
- Windows Actions 34311214941 / job102338103831，源码9e3af6b，所有构建、可选MCP/真实本地OMP fixture、smoke、Git/分屏/fsGuard、打包Git UI、内置能力与CLI回归通过。
- Windows实际打包应用Git检出/返回分支/脏工作区拒绝/越界拒绝15项通过。安装向导、真实付费模型与Intel实体Mac未人工测试。
- 最终asar含safe-regex、新Git IPC、native realpath；无私测上传端点。
- 证据在 `0.4.87-packaged/`；五包大小/SHA256见 `0.4.87-artifacts.json`。

## 部署
五包逐个scp、逐个大小与SHA256验证后先切包，再官网三页，latest.json最后切换。无服务重启，八站点状态前后一致。公开HTTPS页面哈希及五包HEAD大小/Range验收见 `0.4.87-live.json`。
保留0.4.86及回退文件：`/www/wwwroot/eas-release-backups/0.4.87-20260909T044342Z`。

## 失败候选记录（未对外发布）
- 早期依赖目录symlink造成safe-regex漏包、smoke失败；改真实APFS克隆依赖目录后重打，最终asar确认存在。
- 早期codexCapabilityLauncher时序超时；未删除测试或放宽门槛，最终全量通过。
- Windows34309925243：清理EBUSY掩盖Git UI失败；修复验证脚本等待/保留原错误/仅结束所属进程树。
- Windows34310496797：真实checkout超时；停止叠补丁，用独立只读probe34311024303证实RUNNER~1与runneradmin realpath不一致。
- 最终9e3af6b Windows-only native canonicalization红绿回归通过；未削弱目录安全边界，全部产物重新构建。

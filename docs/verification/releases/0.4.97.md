# Eas-Term 0.4.97 发布验证（2026-09-14）

源码：`e6ff281`（`fix/background-render-budget`）。内容：运行中心收进设置「系统管理 › 运行与资源」（`docs/verification/runtime-settings/`），标题栏拆掉 MCP 灯与运行按钮改为临时提示。

- 单测 3198 条 0 失败；typecheck 0 错；build 通过。
- mac arm64 / x64：`EAS_NOTARIZE=1 npm run dist`，两个架构 stapler validate + spctl「Notarized Developer ID」通过，版本号 0.4.97。
- Windows：CI run 34932722952 成功，`Eas-Term-0.4.97-x64-setup.exe` 197461759 bytes，SHA256 前缀 `7353e19e8c960b65`。tag 推送触发的重复 run 34933688151 已取消。
- `bash scripts/publish-site.sh`：五包 SHA256 校验通过；`latest.json` 切到 0.4.97；三个下载链接 HEAD 200 且 Content-Length 一致；五站 reload 前后 301；清理删除 v0.4.94（`ssh -n` 修复后清理正常）。
- GitHub release `v0.4.97` 五包资产上传完成。
- 本机 `/Applications` 未装（需先退出运行中的旧版；用户自行通过更新角标升级）。

# Eas-Term 0.4.103 发布验收（已完成）

- 用户暂停插件接入，要求合并其他分支后发版。基线 origin/main 0613056，合入本地 main ffe7022；合并提交 d9bc78e。
- 包含全局时间线、用量/时间线小票、项目用量明细、画布平移优化、Codex 原生目标续跑修复。
- 未包含暂停的插件候选、旧诊断分支或主工作区未提交改动。
- npm run check && npm run build：3308 项，3289 通过、19 跳过、0 失败；构建通过。
- Codex 隔离真实 Electron + native CLI：首次启动 Runtime.evaluate timeout，未改代码复测 5 项通过；使用本地协议夹具，不是在线模型端到端。完成截图已查看，首次失败证据保留。
- 其余 UI、双架构签名公证、Windows CI、线上发布尚未完成。
- 外部 Computer Use 指针残留仍是已知问题，不宣称修复；不全局清理服务、不替换用户运行中的应用。
- 画布回归：480 次输入合并为 60 次更新，最终位置准确、组件身份不变，释放/失焦收尾通过；截图已查看。
- 时间线生命周期 5 项通过。小票初测按钮等待超时；同一产品代码使用 Page.bringToFront 的诊断脚本复测 7 项通过，截图已查看。
- 用量 IPC 独立真实 Electron 验收通过，包含图片复制/保存、取消、非法数据及写路径保护。
- 附加用量 UI 回归发现两项验收环境限制：凌晨夹具按 35 分钟间隔生成，今日项目不足；后台窗口 CSS 主动暂停打印动画，按钮会等待回到前台。诊断已确认 paused/time=0/focus=false，未通过修改产品代码掩盖。后续在正式包前台继续验收。
- Windows 候选 CI：35703021002，产品提交 97494eb，尚在运行。
- 服务器发布前核查：可用约 11.5GB、latest=0.4.102、5 个 pm2 online；mini.biily.top 已为 504（发布前既有），其余已查 6 站 200。本次未修改线上内容。
- Windows CI 35703021002 最终 success。
- 首轮 Mac 双架构公证成功，但 arm64 真实包启动两次失败（40 秒无 CDP）。进程采样定位 NSAlert；在包内逐个 require 实测 yauzl 报 Cannot find module 'fd-slicer'。electron-builder 已提示依赖路径 undefined，共享 node_modules 链接造成依赖收集异常；未切线上。正在改用独立 npm ci 重打，首次失败与公证证据保留。
- 今日项目明细回归改用 5 分钟间隔测试数据（产品源码不变）全部通过，确认原失败是凌晨夹具导致今日项目不足。
- 独立 npm ci 首次 Node 26 因 yargs 扩展名解析报 require is not defined；换 Node 22.23.2 后成功。没有更改 lockfile 或依赖版本。
- 干净依赖重打后，包内 node-pty/node-forge/yauzl require 全通过；arm64 原始 smoke 全通过（实际 PTY、文件读取、桥接、OMP、JS 错误检查），截图已查看。正式包时间线小票 7 项通过。
- 用量周/月小票在独立 Electron 窗口 CUA Raise 后全通过（动态效果/图片文字复制/键盘等）；背景暂停是既有节能行为，不是卡住的产品逻辑。截图已查看。
- 后续 Mac 发版必须使用独立 node_modules + Node 22，不复用其他工作树的依赖软链；仅共享已校验的 OMP 二进制缓存。
- 干净依赖下再次全量 check：3308 项，3290 通过、18 跳过、0 失败。
- arm64 ZIP 解包、DMG 只读挂载后，app.asar/主二进制与已测 app 全部 SHA256 相同；三者签名、staple、Gatekeeper 全部通过。
- x64 原始 smoke 全通过，截图已查看（Apple Silicon 下 Rosetta，未验 Intel 物理机）。
- x64 ZIP/DMG/已测 app 三者 app.asar 与主二进制 SHA256 相同，签名/staple/Gatekeeper 全通过。
- 全部发布门槛通过；官网五包上传/校验进行中。GitHub Release 仍 draft，暂未公开。

## 官网已上线（GitHub 镜像待收尾）
五包逐个 scp 并核对大小/SHA256；三个网页后 latest 最后切换，公开页面 hash、安装包 HEAD/Range、latest 通过。八站状态和五个 pm2 PID/状态保持一致，未重启服务、未删旧版。mini 发布前后均504，未宣称修复。磁盘剩余10,421,346,304字节。回退备份 /www/wwwroot/eas-release-backups/0.4.103-20260922T083423Z。GitHub仍draft。

## 最终完成
GitHub v0.4.103 于 2026-09-22T08:46:39Z 正式发布/latest，五个 asset 的 size/digest 与本地及官网逐个一致。官网与 GitHub 双分发完成；main 已合入，tag=418d4d0，产品源码97494eb。未替换用户运行中的应用。插件/WPS工作继续保持暂停，未纳入本版。

# Eas-Term 0.4.89 发布验证

- 产品源码：78d2f65（语音 + OMP 调整方向修复 + Gemini 模型额度）。Windows 与 Mac 均用同一提交；隔离干净 worktree 构建，不包含根工作区未提交的 token-dashboard 等改动。
- 干净分支全量：2827 项，2814 通过，13 跳过，0 失败；根工作区此前 2830 项，2817 通过，13 跳过，0 失败。差异来自未纳入发布的既有变更。
- OMP 额度专项 38 项通过；类型检查、构建、独立评审通过。真实 Electron 及 Mac arm64 分发包验证模型额度与无数据状态，hover 剩余/刷新提示截图已查看。
- Windows CI 34432773903 成功：OMP/MCP 52 项、额度 38 项、安装包 smoke、CLI resolver、Git/分屏、browser gallery/workflow 均通过。EXE 下载 ZIP 与 Actions digest 一致。
- Mac arm64 / x64 已签名公证，codesign strict、stapler validate、Gatekeeper accepted 全通过；双架构分发包 smoke（PTY、IPC、图谱、随包 OMP）通过。x64 在 Rosetta 上测试，非 Intel 物理机。
- 五包逐个 scp、大小/SHA256 核对后，三张网页更新、latest 最后切换；公开页面哈希、五包 HEAD 与 Range 验证通过。
- 八站 HTTP 状态前后一致；未 reload 或重启生产服务，未删除旧版。回退备份 `/www/wwwroot/eas-release-backups/0.4.89-20260910T032954Z`。

## 边界
- 当前受管 OMP 真实 reports 为空，未验证真实 Gemini 账号额度数值；模型额度截图为明确的合成测试数据。多账号无法确定当前账号时不展示数字，不冒充剩余余额。
- OMP 查询仍异步、限频、超时，不参与对话/握手。额度分屏与大量长模型名专项交互未验。
- 语音不做本人声纹识别，现场硬件麦克风与轻声识别率未量化；不保证过滤旁人/电视讲话。
- 不包含 Windows 私测日志回传；不宣称用户原始 Windows 闪退、外部 Computer Use 指针残留已解决。
- 测试实例及其隔离数据已关闭删除，正式安装实例未替换、未关闭。

GitHub Release v0.4.89 五包上传完成并发布；五个 asset digest 与本地/生产逐个一致。发布 tag 触发同源码重复 run 34434645149，已请求取消以防重新打包覆盖已核验 EXE；正式成功构建依据为 34432773903。生产磁盘剩余 4.8G。

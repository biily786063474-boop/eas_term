# Eas-Term 0.4.90 发布验证

产品源码：78f0a4e。只包含 Frame 新建模块紧凑选址；其他工作区改动未混入。

- 独立审阅发现 AI 选址前宽度与实际 640px 不一致，已修普通新建和恢复入口。
- 发布前真实 UI 又发现长边评分使 640×380 连续三个模块纵排，已取消旧 Windows 候选 34446880441、终止旧 Mac 构建。改用宽高比优先并要求边邻接（避免对角线制造空白），新增失败回归后修正。
- 最终干净发布树全量检查：2832 项，2819 通过，13 跳过，0 失败；类型/样式/hooks 检查通过。
- 较早根工作区检查曾有 codexCapabilityLauncher 的 5 秒子进程记录等待失败；单独复跑 10 项通过，随后干净全量通过。未修改此测试掩盖失败。
- 真实隔离 Electron：连续 4 个 AI 对话以真实640×380形成2×2；删除第3个再新建补回原洞，其他坐标不变。截图与 JSON 在 docs/verification/compact-placement。
- 最终独立复审通过，另抽验常用尺寸20节点及100组30个混合尺寸无碰撞。
- Windows 最终 run34447151058 成功；Mac 双架构签名公证完成。官网/latest 已切换并核验。

边界：手动落点文件/组件与拖拽仍尊重指定位置；无 Windows 真机实操结论；外部 Computer Use 指针残留、原始 Windows 闪退、私人日志上传不在此版修复范围。GitHub 备份进度另记。

## 安装包验证补充
Mac arm64/x64 已签名、公证并通过 codesign strict、stapler、Gatekeeper 检查。arm64 原始 smoke 通过，真实分发包四模块2×2截图已查看。Windows CI34447151058成功，EXE artifact ZIP与Actions SHA256一致。
x64原始smoke两次在固定3秒UI门槛失败，后续截图正常且PTY/IPC/图谱通过；只读启动对照：新版15.052秒、旧0.4.89为29.183秒（不同冷缓存，不作为性能提升指标）。另用限时DOM就绪诊断版复验，不篡改原失败。x64仅Rosetta，非Intel物理机。

## 官网发布
x64限时DOM就绪版完整冒烟通过，保留3秒门槛原失败，不将Rosetta测试外推真实Intel。五包逐个scp、大小与完整SHA256核对，网页更新后latest最后切换；公网页面hash、五包HEAD/Range通过。八站HTTP状态前后一致，未reload/重启生产服务，旧版本保留。回退备份 `/www/wwwroot/eas-release-backups/0.4.90-20260910T070611Z`。GitHub五包备份已上传，逐个size/digest与本地及生产一致，Release已发布。

GitHub tag额外触发同源码run34449711013，已请求取消，避免重新构建覆盖核验EXE；正式Windows证据为34447151058。

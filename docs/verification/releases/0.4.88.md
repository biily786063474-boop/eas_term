# Eas-Term 0.4.88 发布验证

- 验收功能提交：9614b9f；发布候选：05b0ac5。
- 本地 `npm run check`：首次 2805 tests，2792 pass，13 skip，0 fail；OMP 缓存就绪后最终复跑 2793 pass，12 skip，0 fail。
- Windows 成功 run：34325720098（9aa4afc），原有 OMP/MCP、PTY、Git、分屏、安全回归与新增 packaged browser gallery 35 项 / bookmark workflow 19 项全部通过。
- Mac arm64 / x64：Developer ID 签名、公证、staple 校验通过；两架构 packaged smoke、browser gallery 35 项、workflow 19 项、词典 hover 2 项通过。x64 在 Apple Silicon Rosetta 上运行，未覆盖真实 Intel 硬件。
- 正式下载与 latest 已切换 0.4.88；五包逐个 scp、大小和完整 SHA256 校验，网页三文件后 latest 最后切换，八站状态一致，不重启服务。回退备份 `/www/wwwroot/eas-release-backups/0.4.88-20260909T080021Z`，保留 0.4.87。

## 已知边界
- Motion Sites、短视频助手准确网址未确认，保留不可点击待补入口，不猜链接。
- 不包含私测 Windows 日志回传；不声称用户原始 Windows 闪退已复现解决。
- 外部 Codex Computer Use 指针生命周期仍 OPEN，见 computer-use-lifecycle.md。
- 缩回动画减少重复布局，不承诺所有设备完全无掉帧；未验证真实付费模型重负载。
- 打包前 omp 下载经 Clash 代理较慢，改用本机已有的同版本缓存；两架构均按 manifest SHA256 校验，再由正式 bundle checker 验证 ACP 握手。未改安全或网络策略。

## 失败记录与定位
- Windows 候选 run 34324410703 / 34325077592：动画终点测试失败。第二轮诊断为 missing animation；最终日志确认 host reduced motion=true。测试明确切换 no-preference 后验证动画，再切回 reduce 验证无动画，两条分支均通过；未修改产品动画或跳过断言。
- x64 初次 gallery 在首屏/最大化固定 900/400ms 等待处失败；改为有上限的 DOM 就绪等待，同一签名包通过。workflow 首次固定 250ms 的 deep link 检查过早，等待真实表单值后同包 19 项全部通过。失败日志不作为成功证据。
- Mac 包构建源码 05b0ac5；Windows 源码 9aa4afc，之间只有测试脚本变化，产品源码相同。

GitHub Release v0.4.88 五包已完成异地备份并正式发布，asset digest 与生产及本地逐个一致（见 0.4.88-github-assets.json）。发布后磁盘剩余 5.7G，未删除旧版本。

发布 tag 自动产生重复 run 34327838928（同一 9aa4afc）。为防止其重新打包覆盖已核验 Release EXE，在依赖安装阶段主动取消；构建/发布步骤未执行。正式 Windows 成功依据仍为 34325720098；取消后五包 digest 再核验未变。

# Eas-Term 0.4.96 发布验证（2026-09-14）

源码：`14328f8`（`fix/background-render-budget`），内容 = `722447b` 空闲看门狗 + 三处待机开销，真机验收见 `docs/verification/idle-watchdog/2026-09-14-真机验收.md`。

## 构建
- 单测 3186 条 0 失败；typecheck 0 错；build 通过。
- mac：`EAS_NOTARIZE=1 npm run dist`，arm64 / x64 各 DMG+ZIP，钩子日志两次「✓ 公证 + staple 完成」。挂载 DMG 后对内部 `.app`：`stapler validate` → The validate action worked；`spctl -a -t exec` → accepted, source=Notarized Developer ID（两个架构都验）。
- Windows：CI run 34870433256（workflow_dispatch，6m56s，success），artifact `eas-term-win` → `Eas-Term-0.4.96-x64-setup.exe` 197463557 bytes，SHA256 前缀 `761df84a34979c1a`。tag 推送触发的重复 run 34872563186 已取消。

## 发布（`bash scripts/publish-site.sh`）
- 五包逐个 scp 到 `/www/wwwroot/eas-dl/v0.4.96/`，逐个 SHA256 校验通过；网页与 `latest.json` 切到 0.4.96（4 条 notes）。
- 线上此前最新 0.4.94（0.4.95 从未发过），本版直接跨过。
- nginx -t 通过，reload 前后五站均 301；线上自检 8 项 200；三个下载链接 HEAD 200 且 Content-Length 与本地一致。
- GitHub release `v0.4.96`（tag 在 14328f8）五包资产备份。

## 顺手修的
- `scripts/publish-site.sh` 清理循环里 `ssh` 未加 `-n`，吞掉 `while read` 的 stdin，每次只删得掉一个旧版本；已改 `ssh -n`，并手工补删 v0.4.92 / v0.4.93（下载页未引用）。线上现存 v0.4.94 / v0.4.96，磁盘 68%。

## 未做
- 本机 `/Applications` 安装需先退出正在运行的 0.4.95（见 memory《Eas-Term 发版流程》），待用户确认。
- Windows 无真机实操；x64 mac 仅 Rosetta。

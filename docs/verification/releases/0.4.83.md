# Eas-Term 0.4.83 发版准备

日期：2026-09-07。当前为本地候选，未上传官网、未创建发布标签。

## 范围与提交

- 三批功能提交：26585b6（对话错误与 MCP）、d307223（跨 CLI 界面）、73dd22c（CLI 自动更新）。
- 061ce76 合入 main 的角色章程/分支台账、画布动画和遥测隐私改动。
- 260d927 修复证书随机序列号和高光动画检查，同时同步工具语义类型及 Codex 错误的预期快照。
- bce6e4e 将导航锚点改为所属 AI 对话模块，移除 Frame 边界定位。旧实现先在新回归中失败，修复后 16 项界面检查及 2526 项全量测试通过。
- 版本号与锁文件同步为 0.4.83，CHANGELOG.md 和本地官网更新日志已生成；下载链接未提前指向未发布安装包。

## 验证

- npm run check：2526 项通过，0 失败，0 跳过；类型、hook、对比度、CSS 和动画检查通过。
- 聊天真实 Electron 界面交互 16 项通过：安装登录引导、CLI 切换清理提示、草稿保留、提问导航、抽屉层级、文件图标、分屏 worktree、Frame 独立移位不影响导航、模块独立移动后仍保持 12px 间距。安装/认证传输为模拟，非真实安装。
- CLI 更新：鼠标切换、IPC 持久化、默认关闭、重启启用待更新版本及实际新路径探测通过；重启流程使用可执行 fixture。
- 高光：真实窗口检查三颗按钮旋转、hover 整排暂停、当前高光隐藏、减少动效停止；截图和结果位于 ../agent-chat/release-start-ring.*。
- 原生助手通用二进制可运行；随包 omp 18.1.2 校验及 ACP 握手通过。
- 初次安装包普通启动通过，但新增代码图谱扫描确定发现 safe-regex 依赖缺失。原因是打包时使用共享 node_modules 符号链接；已改为完整本地副本，保持原生 ABI，不运行依赖安装/重建。

## 最终安装包复验

- Apple Silicon 与 Intel 包均通过：窗口、47 个 preload 命名空间、真实终端回显、IPC、代码图谱懒加载及 index.js → value.js 依赖分析、随包 omp，无渲染异常。Intel 在 Apple Silicon 机器的 Rosetta 下运行，未在 Intel 实机测试。
- 两个 .app 均通过 codesign --verify --deep --strict、stapler validate 和 Gatekeeper（Notarized Developer ID）；两个 DMG 经 hdiutil 校验通过，两个 ZIP 经 unzip -tq 校验通过。
- 两个实际 app.asar 版本均为 0.4.83，全部 138 个 out 文件与 bce6e4e 修复后的构建逐字节一致，preload 未包含测试 transport。
- 官方 npm 原生包 Codex 0.153.4、Claude Code 2.1.263：完成下载、SHA512 校验、解包、版本与对话参数检查；未改变用户当前 CLI。Codex 第一次连接中断，独立重试成功。详见 0.4.83-cli-packages.json。
- 更新、图谱、签名和原生包校验分别覆盖相应边界，不等同于所有第三方服务/模型均已在线测试。
- 四个已公证本地产物及最终 SHA256 见 0.4.83-artifacts.json，目录为 ~/Eas-Term-release/0.4.83-prep/。归档在 staple 完成后生成，已替换此前未含导航修复的候选包及校验和。

代码图谱测试使用真实规范路径。测试中另发现 /var → /private/var 别名可令图谱返回跨目录样式路径；这项既有符号链接路径问题未在本次发布准备中修改。

## 发布边界

用户明确确认后，包含导航锚定修复的双架构包已重新签名、上传 Apple，并获得 Accepted、完成 staple。arm64：030b0dd9-d78d-467e-8fb1-1d02f486417c；x64：59cadfca-bed2-4783-9d0f-906cc1b40e1d。公证记录与包内文件核对见 0.4.83-notarization.json。

Windows 包需要 Windows CI，目前未构建。未推送远端、未发布官网，用户已安装应用和原工作区的个人改动保持不动。

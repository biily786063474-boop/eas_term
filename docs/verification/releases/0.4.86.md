# Eas-Term 0.4.86 发布验收（官网已上线）

范围：原生 OMP 登录界面与光标（1e2a01c）、Google Cloud 项目缺失提示、OMP ACP 空目录防护、Codex resume 工具编号隔离。

- 完整 check：2777 项，2766 通过，11 跳过，0 失败；类型/静态检查通过。
- 独立审查：相关六文件 117/117，无已确认发布阻断问题。
- Codex 真机界面中断后 resume，两次新工具结果与旧结果隔离；证据 ../codex-direction/。
- 用户实测最新隔离实例 Antigravity 登录及对话可用。其他 Google 账号仍需其供应商要求的 Cloud 项目配置；本次不新增项目 ID 设置入口。
- 外部 Computer Use 指针生命周期仍开放，不标为已修复。Windows 已登录真实模型覆盖仍不完整。
- 不包含仓库其他未提交改动，不替换用户正在运行的正式版或测试实例。

发布顺序：签名公证/正式包检查 → 五包上传与完整 SHA256 → 官网页面/更新清单 → 公网核对；旧版本保留回退。正式结果待补。

## 正式包验证

源码提交 e818df66af6c026c7a29e11a5ca1fc8884a2ef46。Mac arm64/x64 均签名、公证、staple、Gatekeeper 通过；两个正式包均实际启动通过渲染、preload、PTY、IPC、代码图谱与随包 OMP 检查，无 JS 报错。两包主进程产物与本地构建逐字相同。
Windows CI 34257455903 构建、真实包启动、CLI/control 与内置能力检查通过；下载的 CI ZIP SHA256 与 GitHub artifact digest 完全一致。五包哈希见 0.4.86-artifacts.json。

迁移：本次不改凭证/对话存储结构，不需要重新配置账号；仍需满足供应商自身项目要求。回退安装 0.4.85 时保留 userData，不删除凭证、对话或生成请求日志。官网回退仅恢复发布备份的四个静态文件，先检查哈希防止覆盖后续更新。
本机 /Applications/Eas-Term.app 的 asar 仍为 e74f36a35762efd2b7ba70f83a093c9f26fa747ba42abec28faaf6d0ccd39373，未替换。

## 官网发布结果
五个安装包远端完整 SHA256 均一致；官网 index/download/changelog 与 latest.json 公网字节哈希一致。五包公网 HEAD 大小与 1024 字节 Range 核对通过（非公网完整下载）。七站前后状态不变，未 reload、未删旧包。
备份：`/www/wwwroot/eas-release-backups/0.4.86-20260908T173905Z`。GitHub备份核对仍进行中。

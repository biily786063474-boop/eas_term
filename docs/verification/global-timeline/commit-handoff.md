# 时间线独立提交交接（2026-09-18）

## 范围
基于 fdafd0f，只提交全局时间线、候选记录、项目筛选与插件生命周期相关实现/测试/图纸/验收证据。
共享 session.ts、processHandoff.test.ts、模块图和活动日志按片段暂存；没有夹带聊天退出修复、Token 统计、能力指引、AGENTS、画布状态等其他修改。原工作区文件保持不变。

## 本提交独立快照验证
从 Git 暂存区导出独立副本，不借用工作区其他 agent 的代码：
- 类型检查、构建通过。
- 定向测试 56 项通过。
- 真实隔离 Electron：21 项界面检查通过；更新/卸载旧 PID 退出、新代码重新握手、重装恢复通过；实际重启恢复开关/排除项目/历史通过。
- 全量结果见下方追加，不混用此前混合工作区的测试计数。

## 既有真实模型证据
Codex 真实既有、未绑定时间线的会话在全局开启后产生候选，已通过（本提交准备阶段没有重复付费模型调用）。

## 受阻、未验证和未完成
- Claude：组织禁用了 Claude Code 订阅访问，未完成有效模型任务；不能算通过。
- OMP：隔离环境没有配置模型服务商；不是正式版没有配置，没有复制正式凭证。
- Windows 未验证；完整三 CLI 实机取消与正式回执矩阵未完成。
- 跨轮候选关联已有成果 UI 尚未实现；当前可收录/忽略，不能宣称跨轮语义自动去重。
- 更新/卸载后旧面板失效，点击重试恢复，不是无感替换面板。
- 外部 Computer Use 服务生命周期不在本时间线验收结论内。

## 发布边界
仅本地提交，不合并、不推送、不发布、不替换正式应用；用户目录的旧同名插件可能覆盖内置版本，首次上线还需升级宿主并检查插件覆盖。

## 本次提交快照最终结果
暂存内容独立导出验证：全量 2873 项，2859 通过、14 跳过、0 失败；定向 56 通过；typecheck/build、21项真实隔离UI、生命周期PID退出/新代码重握手/重装及真实应用重启恢复全部通过。数字不同于先前混合工作区，原因是排除了其他agent未提交代码。本地提交，不合并/推送/发布；Claude、OMP、Windows及跨轮关联的边界仍如上。

## 主线整合审查（基于 origin/main f711419）
只移植 a27acf7 的时间线变更，不合入旧分支上的其他提交；保留主线 guarded IPC、资源准入、手动停止闩锁、运行时计量及退出修复。原始共享工作区保持不动。

独立复审发现并修正：
1. 异常轮次后恢复同一会话，新的 turn.start 丢弃失败缓冲。
2. 已发往插件、尚未落盘的 RPC 在用户撤权后仍可能写入：新增共享提交锁 + 授权 epoch，撤权/排除与最终落盘串行化。真实 SIGSTOP 插件→已发送 RPC→撤权→SIGCONT 的回归通过。
复审相关测试 10 项通过，未发现新的阻断项。

明确限制：授权提交锁争用会报错，不会假称撤权成功；进程异常死亡可能留下锁，需确认该 profile 所属宿主及插件均退出后人工清理 `.commit-lock`，当前没有自动锁恢复。全局记录不包含任意外部软件、未托管 CLI 或未登记项目。

### 整合后最终复验
- typecheck、build 通过。
- 全量 3284 项：3265 通过、19 跳过、0 失败（含本轮撤权真实 stdio 测试）。
- 隔离 Electron UI 21 项、生命周期更新/卸载/新进程握手、实际应用重启恢复全部通过；已查看全局热力图截图。
- 首轮全量失败原因：主线 pluginHostAdmission 测试夹具没有 Hosted.info，补全真实字段后回归通过，不改变运行时准入行为。
- UI 首轮末段等待与一次 CDP 超时保留为历史问题；脚本改为等待异步保存按钮恢复后再执行下一操作，串行复验通过。早期 CDP 超时见 integration-earlier-timeout.json。
- 此结论不是 Windows 交互验收，也不是已发布新安装包；Claude/OMP 受阻和跨轮关联未实现仍适用。

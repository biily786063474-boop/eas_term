# 可选笔纵不阻塞 OMP（2026-09-08）
原因：受管 initialize/tools/list 强制调用笔纵目录；BIZONE_MISSING 直接冒成 JSON-RPC -32603。
最小修复：仅发现阶段的笔纵后端失败返回空工具目录，宿主状态仍 failed；调用与授权错误仍拒绝。
红测先复现原报错，绿测验证空目录、失败状态、禁止调用生成、重新发现恢复、权限撤销及工作台异常不吞。

真实 bundled OMP 18.1.2 + 真实 stdio shim + 实际 mcpBridge HTTP 路由通过。
缺失笔纵为测试注入；模型为本机 loopback SSE 固定响应，验证回复正文 OPTIONAL_MCP_CHAT_OK 和 end_turn、请求一次。
未使用真实模型账户/付费生成，未在报错 Windows 机器复现。不是所有第三方 MCP 故障的通用降级。
构建后的隔离 Electron 设置 IPC/三个模块开关验证通过（6项）；未替换用户正式应用。
源码修复不会自动更新已交付的 0.4.85-diag.1 Windows 安装包，也未打新包/发版。

最终验证：独立真实 OMP 回归7项全通过；类型检查通过；当前工作区完整回归2783项，2771通过、12跳过、0失败。工作区另有并行登录UI修改，未纳入本修复提交；本次业务改动仅mcpBridge发现分支。

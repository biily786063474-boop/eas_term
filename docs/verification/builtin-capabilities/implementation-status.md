# 内置能力实施记录 · 2026-09-08

分支 `feat/builtin-capabilities`，基础提交 `90f295a`。候选版本 0.4.85 尚未发布，保留原有未提交改动。0.4.84 不包含本改造。

## 已落地

- 运行 0.4.83 与 Codex 0.153.4 的父子环境核对，证明旧全局注册未转发 EAS 环境导致零工具；已记录真实 MCP 握手证据。
- 随包三模块、唯一指引源、同一个 HostRegistry、受管会话租约与不可变配置快照，覆盖 AI 对话和 PTY 启动。关闭/取消只撤销本会话，保留在途调用引用。
- Codex 原生 config/read 合并保留用户规则、禁用工具和技能。selected stdio 凭证仅在私有快照；不能安全转换的权限字段明确拒绝，不能静默丢失。
- Bizone 正式 MCP 客户端、按需启动、端口/凭证版本变更重连和持久生成 guard 已接入生产宿主。九种生成入口使用请求身份，未知提交不重发。
- 精确旧规则迁移已连接到真实 workbench 成功调用；仅处理可靠归属段，完整备份、校验、记录迁移/回退意图，失败不改变已成功的工具结果。
- `canvas_open_image` 专用于当前项目内本地光栅图片，校验真实路径、文件头、大小和 Frame；满额拒绝，不执行 HTML、不关闭旧内容。38 项共享目录声明真实副作用，不覆盖原生审批策略。

## 当前证据与边界

| 范围 | 证据 | 仍待完成或不支持 |
| --- | --- | --- |
| 回归 | 最近完整检查 2709/2709、0 跳过；后续审查修复另有专项测试 | 最终冻结源码全量检查 |
| Claude Code / omp | 0.4.85 第一候选签名包，真实新建、恢复、应用重启后三轮正确 Frame | 新图片工具的最终包复测 |
| Codex | 第一候选包真实发现并调用工具；通用文件工具因原生审批被拒，未绕过 | 新专用图片工具正式包复测 |
| 配置隔离 | 系统沙箱拒绝全局规则写入；保留各文件前后哈希 | `.claude.json` 并发变化归属不明，不声称所有全局文件未变 |
| PTY | 原生 launcher、父子租约、POSIX Ctrl-C 与 omp 插件真实握手 | 三个交互 CLI 的正式包完整模型验收 |
| Windows | 注册协议路径/中文空格路径契约测试；新增 CI 正式包设置检查 | 本轮 CI、隔离 Windows 用户的真实 CLI 验收 |
| 笔纵 | 1.21.31 已先行发布；协议 19/19，Mac 包启动/旧接口/重启查询 3/3，Windows CI 成功 | 4 墨水实生成批准待回复；未扣费 |

## 兼容限制

- Codex profile-v2 无可靠原生 config/read 接口时明确停止；不伪造合并成功。绝对 CLI/alias 绕过 PATH 不由受管入口保证；Windows `.cmd`/`.bat` 原生启动仍 fail closed。
- 远程业务插件保留原生降级路径：Claude 接收受支持配置，Codex 保留已有原生注册，OMP 明确报告不支持 HTTP/SSE；不能声称三端统一支持远程第三方插件。
- 旧版或用户修改过的托管段无可靠归属时保留；第三方全局 MCP 注册不批量删除。迁移记录与备份位于 app-owned `capability-migrations`。
- 外部 Computer Use 指针生命周期问题保持开放。

实际调用、失败证据、迁移与发布进度分别保存在本目录；模拟传输测试不能代替正式包模型验收。

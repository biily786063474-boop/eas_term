# 2026-09-12 设计选型台落地验收

工作区：voice-regression；未提交、未发版。

- 实现真实辞典第三页签、本地 307 套检索和明暗筛选、悬停/焦点预览与点击选择分离、配色默认/系统摘要可选、可编辑提示词确认。
- CUA 实际开发实例（out/renderer/index.html）验证：打开真实顶栏辞典后切页显示 307 套；搜 Claude 得 1 套；点击后右侧标题与摘要同步；完整范围提示词含所选 Claude 字体/圆角。默认配色提示词保留项目设计基因。
- 成功向已聚焦 AI 输入框附加 ChatGPT · 配色 chip，未发送模型；测试 chip 已移除。刷新后无目标确认按钮禁用，文案明确。
- 发现原生 dialog 被全局 reset 取消居中，补显式 fixed/inset/margin 后重新构建、刷新、截图确认居中，背景弱化；Esc 返回正常。摘要默认收起，标准窗口主要操作无需滚动。
- `npm run check`：2906 tests，2893 pass，13 skipped，0 fail；最新 `npm run typecheck` 和 `npm run build` 通过。

边界/未验：本轮实际 UI 为暗色；亮色与窄分屏未实际复核；hover 鼠标驻留未独立验收；目标对话中途切换保护仅代码检查；未发真实模型消息。卡片为颜色示意，不是产品截图；“完整设计系统”当前附带的是本地库已有摘要，不是完整 token 源文件。确认的动作是加入现有输入框，随用户需求发送，而非自动启动模型。

## 02:50 用户截图反馈：缩小与入口竖排
- 根因：360px 头部中的数量、三个入口与固定宽搜索竞争同一行；入口默认可收缩。
- 调整：搜索独占下一行，入口与数量 nowrap/flex:none；选型上限760×540，卡片示意90px、详情250px、内边距12px。
- typecheck/build通过；CUA刷新隔离开发实例，实际360px词条页三个入口水平完整显示；切选型后760×540浮窗及两列卡片可见。未发版。

## 02:53 固定操作区与原站
原始 DATA 只有 source=design/preview，没有 URL。已核实并补四条官网：https://chatgpt.com/ 、https://claude.ai/ 、https://elevenlabs.io/ 、https://www.perplexity.ai/ （2026-09-12 web open）。概念设计不虚构原站。
拆开详情滚动与操作栏；CUA在1147×659开发窗口验证底部预览按钮可见、滚动后坐标点击成功弹出确认，确认按钮可见。原站按钮可见；外部浏览器实际打开尚未验收。typecheck/build通过；未穷举分辨率。

## 03:24 真实封面替换
- 实际执行原HTML离线截图，307文件产出，304张非空封面接入；vortex-gallery/aristidebenoist/three-html-to-canvas 近空白，manifest标不可用，不作为成功封面显示。未逐张视觉验收。
- 人眼检查ChatGPT、ElevenLabs、platform-hero-lime-remix截图，内容对应各自页面；卡片与详情的统一mock组件已移除，改成本地JPEG img，出错显式提示；307条原始预览地址已接入。
- covers.test.mjs 2通过；typecheck通过；最终build通过。原先完整性测试失败暴露meta缺失及超时页面，已补镜像来源映射并显式标记缺失封面。
- UI验证受阻：CUA getAXState与重置后的getApp连续120秒超时，报Sky Computer Use request timed out。因此未刷新验证最终应用界面，不能声称全部完成；正式版未变、未提交发版。

## 2026-09-12 界面类型初分
- 新增网页 / Web 应用 / 手机端 / 桌面客户端 / 硬件 UI / 组件与动效 / 待确认，保留原业务标签；支持多类型数据、中文类型搜索及组合筛选。
- 307 条：网页139、Web应用26、手机端17、硬件UI14、组件动效102、待确认9。桌面客户端暂无确证，不按品牌硬分。
- 根据本地原始渲染封面人工初分，28条组件依据源标签，非完整交互逐页审计。每条 classificationNote 记录依据。
- 测试先失败再通过（4/4）；typecheck/build 通过。
- CUA连接 worktree Electron，Cmd+R后打开辞典→设计选型台，选择手机端，实际显示17/307、真实手机封面、卡片类型；1147×659窗口底部预览提示词按钮可见，未增大760×540弹窗。未提交/未发版。

## 完整规范与遮罩返回
- 274份规范页及正文+全部tokens已本地导入；33条无页不冒充完整规范。颜色模式保持独立。
- 实际开发实例验证ChatGPT规范页展示品牌、字体、AI Context等原始设计页面（只读，原页脚本不执行）；完整提示词含原页正文，不再仅摘要。
- 实测点击提示词弹窗外部返回选型台。5项单测、类型检查、构建通过。
- 最后一处文案修订：完整模式移除有误导的卡片摘要前缀，明确产品AI Context优先于文档展示壳；该文案经单测构建，尚未再次刷新UI。全部274页未逐页验收，脚本驱动内容可能不能在只读页显示。
- 未提交、未发版。

## 04:50 规范弹窗返回按钮漏样式
用户截图右上角返回选型为浏览器默认按钮。补齐 scoped header button 基础、hover、active、focus-visible 样式，复用应用变量，不影响iframe内原始规范。构建通过，刷新开发实例并实际打开规范弹窗，确认右上角已为紧凑深色圆角按钮。未发版。

### 2026-09-12 分类下拉改标签
- 界面类型改为横向可点标签，复用亮暗筛选样式；删除旧 select 样式，超宽横向滚动。
- npm run typecheck / npm run build 均通过。
- 已刷新 worktree 开发实例，实际点击手机端，选中态与 17 / 307 结果同步；1147×659 窗口中标签无竖排、底部预览提示词可见。未发版。

### 2026-09-12 规范入口显著性
- 高对比整行查看按钮；右侧样例可点击，常显角标、hover/focus 描边、指针旁提示。无规范不展示虚假入口。
- typecheck 和 build 通过，开发实例实看按钮显著性，点击样例已打开原始规范弹窗。
- 指针跟随提示未单独实测（当前 CUA 未提供 hover/move API）；亮色本轮未验证。未提交/发版。

### 2026-09-12 移除重复规范按钮
typecheck / build 通过；刷新开发实例截图确认样例下方白色规范按钮消失，摘要上移、样例入口及底部预览提示词保留。未发版。

### 2026-09-12 网页 / 规范同排与内置浏览器
类型检查、构建通过。开发实例实测同排短文案；点击规范后本地 ChatGPT 规范加载在画布浏览器并聚焦，辞典自动收起；再点击图片，原始 CDN 预览页面在内置浏览器加载并聚焦。未发版；无 Frame 分屏回退和亮色本轮未实测。

### 2026-09-12 06:32 统一 popup
- 网页文案改「预览效果」，效果与规范同一 modal + WebView；不再写 Frame 节点。
- typecheck/build 通过。旧开发实例已不在运行，重新使用原隔离 userData eas-verify-Tn07Pu 启动（pid 31058）。
- 实际验证 ChatGPT 预览效果、设计规范均成功渲染于弹窗；两种弹窗点击左侧外部遮罩均返回选型，当前选择保留。亮色/Windows 本轮未验证。未发版。

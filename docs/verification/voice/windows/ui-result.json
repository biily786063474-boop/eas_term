{
  "passed": true,
  "checks": [
    "codex: middle insertion + selection replacement",
    "codex: one final = one undo",
    "claude: middle insertion + selection replacement",
    "claude: one final = one undo",
    "omp: middle insertion + selection replacement",
    "omp: one final = one undo",
    "cross editor continuous recording; late old text returns only to old editor; duplicate ignored; no focus theft",
    "manual document changes reject late final and retain candidate",
    "second microphone cannot steal recording or stop handler",
    "send during pending manual-stop discards late tail",
    "send during initialization cancels deferred microphone acquisition",
    "five start/stop cycles release every MediaStream track",
    "startup: actual editor receives ordered batch finals",
    "startup settings fully inside viewport; strong mode persisted",
    "terminal: actual editor receives ordered batch finals",
    "native textarea batch finals preserve order, persist immediately, undo individually; ordinary input invalidates voice undo",
    "IME active composition defers voice final until compositionend"
  ],
  "scope": "Mock ASR final events and silent capture; not microphone/noise recognition validation"
}
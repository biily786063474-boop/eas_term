# 外部市场接入口验收 · 2026-09-22

用户最终范围：留一个通用接入口，不逐家适配市场，不等待用户提供市场地址。

最终 `npm run check && npm run build` 成功：3505项，3486通过、19跳过、0失败。16专项覆盖规范化来源身份、持久化/损坏配置拒绝、跨源同名拒绝、未知旧安装拒绝、内置遮挡拒绝、真实安装IPC来源收据、去源后待确认安装拒绝。用临时文件与确定性网络响应验证，不冒充供应商账号验收。

真实构建应用（唯一临时bundle/profile，CUA操作与截图已亲眼查看）：
- 市场来源选择器、添加外部来源表单与Eas格式说明正确显示。
- 输入公开目录、原生默认取消的信任确认、点击添加、源配置实际写入、下拉切换均通过。
- 实际网络请求因本机Clash DNS返回198.18.0.152，被公开地址守卫拒绝，UI明确显示DNS包含非公开地址，没有放宽安全规则。
- 移除来源的原生确认明确保留插件和数据；确认后回到官方源，源文件为空列表。安装包、历史fixture与全局记录关闭状态均保持。

**尚未验证**：真实公网外部来源下载→安装→更新UI全链（受上述DNS环境限制）；任意第三方生态格式不在支持范围；正式分发包/Windows UI未验。本次没有主程序发版、没有改正式应用与真实用户插件目录。网络下载所用TLS/公开地址/代理与重定向策略仍沿用既有真实传输测试。

暂存运行 `/tmp/eas-plugin-update-ui`，只关闭本次实例；没有杀全局Computer Use服务。CUA首次按路径查app超时，按唯一bundle ID重取后成功。

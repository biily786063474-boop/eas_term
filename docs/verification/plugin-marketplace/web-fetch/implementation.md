# 网页抓取实包实现与验收

优先响应用户“先接入剩余插件，用户安装后自行授权”；本插件不需要账号，非目录占位。实现静态HTML/纯文本→标题/正文/链接，非截图或JS页面渲染。

解析器依据：https://github.com/fb55/htmlparser2 。固定10.0.0，独立package-lock；npm ignore-scripts安装7项依赖、audit 0已知漏洞，7份完整许可证随离线包分发。无root依赖变更。

TDD先缺模块红；首轮正文断言只允许一个换行，而实现使用段落空行，调整断言允许一个或多个换行（不改业务行为），5项专项通过：实体解码/脚本与隐藏元素过滤/输出边界/URL拒绝/pinned DNS/重定向/混合DNS/响应上限/真实打包解包和McpClient子进程抓取。网络fixture仅适配确切自有域名到本地HTTP，不放宽生产地址策略。

实际应用验证脚本verify-web-fetch-plugin.mjs：独立userData/home、临时builtin包，临时包内仅插入确切自有域名DNS/HTTPS拨号适配；生产包不含该适配。验证市场展示与3真实shim业务调用，非真实模型/公网TLS/生产安装。公网、Windows与JS渲染未验；后者不在本工具静态抓取范围。

87745全量3424项/3406通过/18跳过/0失败，构建成功，初版builtin隔离UI6项通过。72905进一步改为真实市场下载/哈希校验/权限确认/解包安装，7项通过，三shim读取受控网页，截图已眼验（已安装、不显示随软件更新）。生产插件不包含测试适配。

公网实测 https://example.com/ 返回 `DNS 包含非公开地址`，命令退出2，保存live.json；没有绕过DNS守卫，不宣称公网通过。与用户个人登录无关。按用户“先完成接入”要求，将真实实现且安装链路已验的web-fetch加入默认本地v2构建；v1因兼容要求排除。此为工程接入，不是已发布或所有网络环境可用。

98240最终全量检查退出0：3425项/3407通过/18跳过/0失败，实际双目录构建v1=2/v2=4。清理验收脚本遗留Bearer标题后12814实际市场7项再次通过。许可证原文两处空行尾空格导致diff-check失败，构建器统一仅裁行尾空格后复验6专项通过；不删许可证内容。未发布。

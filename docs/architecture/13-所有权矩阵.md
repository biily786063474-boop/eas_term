# 13 · 模块所有权矩阵

> **与 [10-模块领地图](10-模块领地图.md) 同步维护。**
> `✅ 自由改` · `⚠️ 先读注释再改` · `🔴 改前必须读 03-3B` · `⛔ 不要手改（分发/构建产物）`

## 主矩阵

| 目录 / 文件 | 责任域 | 权限 | 禁止的具体动作 | 关联 MCP 工具 | 文档 |
|---|---|---|---|---|---|
| `src/renderer/src/features/canvas/` | 无限画布 | ⚠️ | 给 `CanvasStage` 加 `canvas.shapes` 订阅；改 `CanvasContextMenu` API 不搜引用 | `canvas_*`、`todo_list`（读 `canvas.todos`） | [10](10-模块领地图.md#-耦合警报改动前必看)、`docs/画布组件协议.html` |
| `src/renderer/src/features/canvas/components/registry.tsx` | 画布组件协议 | ✅ | 为图方便改渲染框架本身（应只 push `CanvasComponentDef`） | — | `docs/画布组件协议.html` |
| `src/renderer/src/features/agentChat/` | AI 对话面板 | ✅ | — | `team_*` 间接 | — |
| `src/renderer/src/features/workspace/` | 应用外壳 / 面板路由 | ⚠️ | 加面板类型只改 `PaneView` 而漏 `layout.ts`+`tabsSlice` | — | [10](10-模块领地图.md) |
| `src/renderer/src/features/terminal/` | xterm | ⚠️ | 任何导致 xterm 重挂载的改动 | — | `memory/终端滚动条-altscreen.md` |
| `src/renderer/src/features/git/` | 分支图 / 历史 + 暂存 · 丢弃 · 提交 | ⚠️ | 移除 `SidebarGit.tsx` / `HistoryView.tsx` 的确认弹窗 —— 那是破坏性 git 操作**仅有**的护栏（没有测试、类型挡不住、主进程不复核）。新增入口照抄它们的写法 | — | 见 `src/main/git.ts` 那一行 |
| `src/renderer/src/features/status/RunMonitor.tsx` | 运行监控（左上角，只报"进行中"） | ⚠️ | 改方向/坐标不看注释（左右写反过一次，正是「右上角通知不见了」那场事故的起因）；往这里加待处理 / 通知类 UI —— 那是右侧 `PendingList` 与标题栏铃铛的地盘 | — | — |
| `src/renderer/src/features/status/`（`machine.ts` `useStatus.ts` `PendingList.tsx` `useIslandFeed.ts`） | 待处理与通知信号 | ⚠️ | 把判据换回 `top !== 'running'` 而不是 `attentionPtys`（症状：`notify` 打了标记却一处都不亮）；在 `useIslandFeed` / `PendingList` 里第二次手写 kind 判定或排序 —— 口径的唯一权威是 `machine.attentionKindOf` / `urgencyCmp` | `notify` | [10](10-模块领地图.md) |
| `src/renderer/src/features/*/`（其余） | 各自单一职责 | ✅ | — | `wiki_*`、`dict_add` | — |
| `src/renderer/src/store/index.ts` | store 组合 + 撤销订阅 | 🔴 | 在 action 里手写 `record()`；改 250ms 合并窗口时序 | — | [03-3B](03-agent角色边界.md) |
| `src/renderer/src/store/canvas/persist.ts` | 画布存档 | 🔴 | 改写入格式不同步放宽读取校验（症状：下次启动一片白） | — | [03-3B](03-agent角色边界.md) |
| `src/renderer/src/store/uiSlice.ts` | 运行 / 待处理标记（`flagAttention` `clearAttention` `setPtyRunning`） | ⚠️ | 动 `setPtyRunning` 里「非运行 → 运行 时顺带清 attention」那段：`if (has === running) return s` 的早退**排在清除之前**是承重的 —— `notify` 敢在 spinner 转着时打标记全靠它 | `notify` 落点 | — |
| `src/renderer/src/mcpHandler.ts` | **MCP 工具真正执行体** | 🔴 | 加工具不同步 `eas-mcp.mjs` schema | 全部 | [11](11-MCP工具网络.md) |
| `src/renderer/src/layout.ts` | 共享 leaf 契约 | 🔴 | 加 `PaneKind` 不改另外两处 | — | [10](10-模块领地图.md) |
| `src/renderer/island/` | 灵动岛独立窗口 | ✅ | 试图共享主窗口 React 树 | — | `memory/eas-term-灵动岛窗口坑.md`（全局） |
| `src/shared/` | **契约层** | 🔴 | `import electron` / `fs` / `net`（会破坏 `node --test`） | — | [02](02-分层架构.md) |
| `src/shared/shortcuts.ts` | **快捷键注册表** —— 这个 app 有哪些键以它为准 | ⚠️ | 在组件里写死键的组合（设置界面渲染的是这份注册表，写死在别处的键在设置里看不见）；改 `id`（改键的存储 key 是它，改了等于用户的自定义全丢） | — | [12](12-skill与hook流程.md) |
| `src/renderer/src/features/agentChat/userMessages.ts` | 用户消息合并回轮次流 | 🔴 | ① 用 `beforeTurnCount` 时不减 `view.trimmedFromHead` —— 归约器会从**头部**删轮次，不减就会让用户消息**成批消失**（不是位置歪）· ② 记 `beforeTurnCount` 用 `view().turns.length` 而不是 `turnCursor()` —— 前者到 `MAX_LIVE_TURNS` 就不再增长，所有提问会一起塌到下标 0，**吸顶路标从答案里整片消失**（2026-09-01 实测） | — | 见同步清单 |
| `src/preload/index.ts` | contextBridge API | ⚠️ | 暴露未经 guard 的写能力 | — | [01](01-系统上下文.md) |
| `src/main/fsGuard.ts` | **主写路径白名单**（项目根 ＋ 知识库根） | 🔴 | 弱化 `realResolve` symlink 防绕；放宽根边界 —— 这两个根之外的写操作应在自己模块里立一道**更窄**的守卫，不是把这里放宽（见同步清单） | — | [03-3B](03-agent角色边界.md) |
| `src/main/fs.ts` | 文件 IPC | 🔴 | 新增写通道**不加** `guardPath`/`guardDir` | `canvas_open_file` | [03-3A](03-agent角色边界.md) |
| `src/main/secrets.ts` | 密钥柜 | 🔴 | 删 `assertReady()`；删 seal/open checksum；只给 `secretsEnv` 加闸门而漏了 `secretsForRun`（见下方 `pty.ts` 一行） | `secret_check`、`request_secret`、`report_secret_invalid` | [03-3A](03-agent角色边界.md) |
| `src/main/agentHistoryKey.ts` | 路径穿越防线 | 🔴 | 放宽文件名校验 | — | — |
| `src/main/islandVisibility.ts` | 灵动岛显不显示的**唯一判定** | ⚠️ | 在 `island.ts` 里另写一处判定（总规则是「岛和前台的主体软件互斥」，散成两处必然只有一处是对的）；把 `held` 从「只管前台那一档」改成后台也管（后台跑着任务的岛会彻底不出现） | — | — |
| `src/main/island.ts` 的 `held` | 「用户主动要它留着」 | ⚠️ | 绕过 `releaseHold()` 直接赋值 —— 它有五个清除点（岛上点进来 / 主窗口拿焦点 / 主窗口里点别处 / 窗口销毁 / 退出），漏一个 `held` 就会**活到下一次**，表现是岛凭空压在前台软件的标题栏上；靠渲染层回传 `hold(false)` 来记账也不行，那是两跳 IPC，窗口正在销毁时就丢了 | — | — |
| `src/main/index.ts` | 启动编排 | 🔴 | 调整 `whenReady()` 内注册顺序；把协议注册挪到 ready 之后 | — | [02](02-分层架构.md#启动顺序硬依赖打乱是静默失效而非报错) |
| `src/main/pty.ts` | 终端 + env 注入 | 🔴 | 改 `secretsEnv` / `mcpEnv` 注入而**只看这一处**。`mcpEnv()` 定义在 `mcpBridge.ts`（改注入内容只改那一处），但有两个调用点 —— `pty.ts` 与 `agentChat/session.ts`（后者不传 `ptyId`，所以 agentChat 会话天生没有 `EAS_PTY_ID`），改调用侧的条件或 token 策略要两处一起改。密钥明文也有两条出口：`secretsEnv()`（PTY env 注入）与 `secretsForRun()`（经 `mcpBridge` 的 `/secret-env` 供 `mcp/eas-secret.mjs` 用，不经渲染层，门更紧：`mcpEnabled` 开关 → 解锁态 → 每 PTY 一张的 `x-eas-secret-token` → `ptyGrants`（认「哪个终端在要」，不是只认「有没有 token」）），**新加的闸门 / 脱敏 / 审计必须两条都加**（见同步清单） | — | [11](11-MCP工具网络.md) |
| `src/main/mcpBridge.ts` | MCP HTTP 网关 | 🔴 | 破坏超时不等式（**四道闸，见同步清单**）；改 `LONG_WAITS` 只改一处；放宽鉴权（`/secret-env` 是在全局 `x-eas-token` 之上**再加**一张每 PTY 的秘密 token，不是替换） | 全部 | [11](11-MCP工具网络.md) |
| `src/main/roles.ts` | 角色权限配置 | 🔴 | 改 `illustrator` 的 deny 通配符（=打开生图红线） | — | [03-3A](03-agent角色边界.md) |
| `src/main/agentChat/adapters/claude.ts` | Claude 适配 | 🔴 | 加 `--bare` / `--permission-mode manual` | — | [03-3A](03-agent角色边界.md) |
| `src/main/agentChat/adapters/codex.ts` | Codex 适配 | 🔴 | 把 stdin 从 `ignore` 改掉（会卡住） | — | 同上 |
| `src/main/cliAuth/` | CLI 登录状态与登录流程 | ⚠️ | 把登录预检 `await` 进 `agentChat:start` —— 那个 handler 的**同步性是承重的**，预检必须留在渲染层（文件头有整段论证）· 放宽 `redact.ts`（一次性凭证会跟着日志落盘，`log.test.ts` 会红）· **在自己真实的 `~/.claude` / `~/.codex` 上试跑登录**：`startLogin` 的 `spawn` 拿的是 `loginEnv()`，而它只在 `PROBE_ENV`（= `process.env` 的副本）上垫了 no-op `open` 与 `BROWSER`，`codex login` 会直接覆盖真实 auth.json。测之前先隔离配置目录：`CLAUDE_CONFIG_DIR=… CODEX_HOME=… npm run dev`，**别拿 `HOME` 当隔离**（`app.getPath('home')` 不跟随它，会得到「以为隔离了其实没有」的假安全感）。只读的 `checkAuth` 不改写凭证，安全 | — | `src/main/cliAuth/index.ts` 头部注释 |
| `src/main/agentChat/approvalRoute.ts` | 审批路由 | 🔴 | 改 `hookResponseBody()` 不同步 `responseBody.mjs`；把 hook 侧超时改成与服务端相等（见同步清单） | — | [12](12-skill与hook流程.md) |
| `src/main/agentRules.ts` `agentHook.ts` `agentSkill.ts` | 规则注入 | 🔴 | 往常驻区塞正文（token 教训）；破坏围栏/`_easTerm` 标记 | — | [12](12-skill与hook流程.md) |
| `src/main/skillLibrary/` | skill 面板 | ⚠️ | 改分类只改一处（**见同步清单**）；写用户 skill 文件时绕开 `write.ts` 那道更窄的守卫 | `skill_list`、`skill_categorize` | `src/main/skillLibrary/README.md` |
| `src/main/wiki/` | 知识库 | ⚠️ | taxonomy broken 时回落覆盖用户结构 | `wiki_*` | [12](12-skill与hook流程.md) |
| `src/main/dict.ts` | 辞典 | ⚠️ | 放宽 `sanitizeSvg` 的白名单 —— 词条的 `svg` 走 `dangerouslySetInnerHTML`，而这段内容是模型写的；给 `UserTerm` 加新字段只改 `dict:add` 而漏改 `readUser()` 的字段白名单（见同步清单） | `dict_add` | [12](12-skill与hook流程.md) |
| `src/main/git.ts` | git 状态 / 历史 ＋ **两条破坏性写通道** | ⚠️ | 新增 `git:discard` / `git:resetHard` 的调用方却不先弹确认 —— 前者 `checkout --` 覆盖已跟踪文件、把未跟踪文件扔进废纸篓，后者 `reset --hard` 丢弃其后提交与全部未提交改动；以为它们过 `fsGuard`（**不过**：git 子命令按 cwd 跑，`guardPath` 用不上，cwd 由渲染层传入） | — | [10 领地图·系统集成](10-模块领地图.md) |
| `src/main/canvas.ts`（`easfile` 段）· `bizone.ts` · `dictClips.ts` | 三个自定义协议的文件读取闸门 | ⚠️ | **canvas.ts**：往 `MEDIA_MIME` 加非媒体扩展名、给 mime 加 `?? 'application/octet-stream'` 兜底、去掉 `path.isAbsolute` —— 任一项都把 `easfile://` 变成任意文件读取通道 · **bizone.ts**：放宽 `MEDIA_ID_RE`，它是唯一挡住 `../` 走出 `media/` 的东西（mime 本来就兜底成 octet-stream，没有第二道门）· **dictClips.ts**：放宽 `NAME_RE`，或以「正则已经挡过了」为由删掉 `startsWith(clipDir() + path.sep)` —— 那行是**故意的**冗余二道门，不是死代码 | — | — |
| `src/main/phone/server.ts` | 手机服务端 | 🔴 | 绑 `0.0.0.0` | — | [01](01-系统上下文.md) |
| `src/main/phone/`（其余） | 配对与身份 | ⚠️ | 改 SPKI 指纹钉死机制 | — | — |
| `src/main/`（其余业务文件，**不含上面三个协议 handler 与 `git.ts`**） | 各自持久化/系统集成 | ✅ | 把多类数据合并进一个 JSON | — | — |
| `src/tunnel/` | 隧道服务端 | 🔴 | **任何"中间解密再转发"的改动**（`hub.test.ts` 会红）；改完不重新打包 `deploy/tunnel/hub.mjs` | — | 项目 `CLAUDE.md` |
| `mcp/eas-mcp.mjs` | MCP stdio 入口 | 🔴 | 改字段格式（用户已注册配置会连不上） | 全部 | [11](11-MCP工具网络.md) |
| `mcp/bizone-mcp.mjs` | 画板启动包装器 | ⚠️ | 在这里实现工具（工具属画板自己） | 画板全部 | [11](11-MCP工具网络.md) |
| `mcp/eas-secret.mjs` | shell 命令（**不是 MCP server**） | 🔴 | 让密钥值进 stdout / 命令行 / history | — | `skills/eas-term/secrets.md` |
| `skills/eas-term/` | 分发给用户 CLI 的指引 | ⚠️ | 改了等于改用户机器上 agent 的行为 | — | [12](12-skill与hook流程.md) |
| `hooks/scan-commit.mjs` | 提交钩子**源码** | ⚠️ | 把它当产物去找上游 —— **它就是源码，没有生成器**，`extraResources` 只是把整个 `hooks/` 原样拷进包，所以改它＝改用户机器上的提交钩子；改 `agentHook.ts` / `dict.ts` 以为能改到钩子行为（钩子是独立 node 进程跑 `Resources/hooks/scan-commit.mjs`，任何异常一律 exit 0 → 静默失效）；把已拆掉的「自动沉淀」加回去（文件头写了为什么拆） | `dict_add` 间接 | [12](12-skill与hook流程.md) |
| `hooks/dictionary-bundle.json` | 钩子自带的词库**手工副本** | ⚠️ | 当它是构建产物 —— **没有任何脚本会生成或同步它**；只改 renderer 那份就以为钩子跟着变了（见同步清单） | — | [12](12-skill与hook流程.md) |
| `resources/agent-hooks/` | 独立 hook 进程脚本 | 🔴 | 改 `responseBody.mjs` 不同步主进程同名函数 | — | [12](12-skill与hook流程.md) |
| `scripts/publish-site.sh` | 站点发布 | 🔴 | 改 `OTHER_SITES`（漏项=误判生产站）；改小 `KEEP`（=下载 404）；版本排序不用 `sort -V`；往 `site/` 加新页面或新根级资源却不加进脚本的传输清单（症状：线上 404，而脚本一路绿灯，只有真打开页面才看得出） | — | 项目 `CLAUDE.md`、见下方「发版链路」 |
| `scripts/publish-tunnel.sh` | 隧道发布 | 🔴 | 跳过 SHA256 核对 | — | 项目 `CLAUDE.md` |
| `scripts/check-*.mjs` `verify-*.mjs` | 质量闸 | ⚠️ | 放宽断言以让 CI 通过 | — | — |
| `build/afterPack.js` `notarize.js` | 签名与公证 | 🔴 | 改签名逻辑（产出"能打包但用麦克风就崩"） | — | — |
| `.github/workflows/build.yml` | Windows CI | 🔴 | 升级 `windows-2022`（node-pty 编译失败） | — | `memory/` |
| `package.json` 的 `build` 段 | 打包配置 | 🔴 | 改 `asarUnpack` / `x64ArchFiles` / `mac.identity`；加随包资源不同步 `extraResources`（见同步清单） | — | — |
| `site/` | 营销站 | ⚠️ | 手改 `site/vendor/spb-design/`（⛔ 分发产物）；手改 `index.html` / `download.html` 里的版本号与下载链接（发布脚本 sed 回填） | — | 项目 `CLAUDE.md` |
| `site/changelog.html` | 更新日志页 | ⛔ | 手改 —— `scripts/changelog.mjs` 的 `buildHtml()` 整份覆写，每次发布无条件重生成（`--site-only` 也照跑）。改内容改 `CHANGELOG.md`，改版式改 `buildHtml()` | — | 见下方「发版链路」 |
| `deploy/tunnel/hub.mjs` | 隧道部署产物（**git 跟踪、未压缩，长得像源码**） | ⛔ | 手改 —— 应改 `src/tunnel/main.ts` 后重新打包；`scripts/publish-tunnel.sh` 第一步就用 esbuild 覆写它。它会被源码 grep 命中，⛔ 这个标记是唯一的护栏 | — | 项目 `CLAUDE.md` |
| `deploy/eas-stats.py` `deploy/dashboard/` `deploy/install-dashboard.sh` | 埋点聚合与看板（**手维护源码，不是产物**） | ⚠️ | 改字段解析键不同步 `telemetry.ts` / `site/analytics.js`；改 `stats.json` 的输出键不同步 `dashboard/index.html` | — | 见同步清单 |
| `out/` `release/` `node_modules/` | 构建产物 | ⛔ | — | — | — |
| `docs/` · `memory/` | 方案与评审 HTML · 踩坑档案 | ✅ | 把长期结构文档 / 结构性内容丢这里（应进 `architecture/`） | — | [README](README.md) |
| `docs/architecture/` | **本套图纸** | ✅ | 改代码不同步更新 | — | [README](README.md) |
| `.plans/` | 多 agent 产出 | ✅ | — | `team_*` | [03-3A](03-agent角色边界.md) |
| `CLAUDE.md` | 部署纪律 | ⚠️ | 让它与脚本实际值脱节（`KEEP` / `OTHER_SITES` / 域名 / 端口改了要回填） | — | — |
| `AGENTS.md`（仓库根） | 给只读 `AGENTS.md` 的 CLI 的工程约定 | ⚠️ | 在 `<claude-mem-context>` 块里手写（claude-mem 插件会整段替换那一块）；改了不同步 `CLAUDE.md`（两份同源）。手写区在 `<!-- eas-term:arch:begin/end -->` 围栏内，插件只做定点替换，围栏里的内容不会被覆写 | — | — |

## 跨文件同步清单（改一处必须改另一处）

| 改这个 | 必须同步改 | 不同步的症状 |
|---|---|---|
| `mcp/eas-mcp.mjs` 的 `LONG_WAITS` | `src/main/mcpBridge.ts` 的 `LONG_WAITS`（成员名单必须一致） | 用户看到连接错误而非业务提示 |
| **四道超时闸里的任一个数值** | 回核不等式：`eas-mcp.mjs` 的 `CALL_TIMEOUT_MS` 15min ＞ `mcpBridge.ts` 的 `invokeRenderer` 10min ＞ 渲染层两处 —— `mcpHandler.ts` 的 `TEAM_WAIT_MS` 8min（`team_status` 长轮询）与 `features/team/batchRequest.ts` 的 `WAIT_MS` 9min（`team_spawn` 派活弹窗） | 外层太短 → 用户看到连接错误而不是业务提示；内层反超外层（②≤③ 只差 1 分钟，最容易踩）→ 主进程先超时只清自己那份 pending，渲染层弹窗还挂着，用户随后点「开工」把 Frame 标记成「有批次在跑」而实际一个 agent 都没起，之后派活永久被挡，除非重启 app |
| `approvalRoute.ts` 的 `hookResponseBody()` | `resources/agent-hooks/responseBody.mjs` | 审批链路失效（跨进程无法 import） |
| `approvalRoute.ts` 的 `APPROVAL_TIMEOUT_MS` | `resources/agent-hooks/eas-pretooluse.mjs` 的 `FETCH_TIMEOUT_MS`，**必须严格更大**（现留 10s 缓冲），好让服务端自己先超时 | 设成相等或更小 → hook 侧先断，用户拿到的是一个 AbortError，而不是服务端那句写清楚的「等待超时」 |
| skill 分类能力 | `eas-mcp.mjs` schema ＋ `mcpHandler.ts` 执行 ＋ `skillLibrary/index.ts` 落盘（IPC / `saveConfig` 的 patch 语义 / `skippedLocked`）与 `category.ts` 校验 ＋ `skill-organizer/SKILL.md` 说明 | 任一处漏改都出问题，而且没有测试会红 |
| `layout.ts` 的 `PaneKind` | `store/tabsSlice.ts` 的 `setPaneKind`（编译器挡）＋ `PaneView.tsx` 的 `KIND_LABEL`（`Record<PaneKind, …>`，编译器也挡）、**渲染分支链**（`pane.kind === …` 的 `&&` 串，没有穷尽检查，**编译器挡不住，最易漏**）、视情况还有 `KIND_OPTIONS`（要不要出现在下拉框） | 新面板类型渲染空白 |
| 新增 MCP 工具 | `eas-mcp.mjs` schema ＋ `mcpHandler.ts` 执行体 ＋ [11](11-MCP工具网络.md) 的清单 | 工具报错；或图纸过期 |
| 快捷键的键位 | `src/shared/shortcuts.ts` 的 `keys` ＋ 实现处。**实现处按 id grep `shortcutHit` / `hitKey` 找，别在这儿记条数** —— 这行原来写「App.tsx 分屏五条 / CanvasStage 画布三条」，加了两条键之后就不对了。当前分布：`App.tsx`（scope global 与 split）、`CanvasStage.tsx`（scope canvas，两个 effect：依赖选中的 / 不依赖选中的）。**别在组件里写死组合** —— 设置界面渲染的是注册表 | 设置里显示的键和实际能按出来的键对不上 |
| `canvas.pan`（空格）的键位 | 注册表的定义 ＋ `CanvasStage.tsx` 里那段 `e.code === 'Space'`。**这条刻意没走注册表分发**（hold 语义），两处各存一份 | 设置里改了没反应，或反过来 |
| `activeProjectId` 的**每一个**改动点 | 同时用 `pickActiveTab()` 算出新的 `activeTabId`（`projectsSlice` 的 `setActiveProject` 与 `canvasSlice` 的 `followSel` 各一处） | 造出「激活的标签属于另一个项目」这种状态：空态与 PaneLayer 判据打架（「没有打开的终端」透在跑着的终端底下）、⇧⌘[ / ⇧⌘] 翻标签按项目过滤得到空数组而静默失效 |
| `reduce.ts` 里任何新的 `turns.splice(0, N)` | 同步累加 `trimmedFromHead`；压缩那种「删完又 unshift 一个标记」的还要抵掉 1 | 用户自己发的消息成批消失（`userMessages.ts` 按绝对下标定位，全靠这个偏移量修正） |
| `features/agentChat/history.ts` 的 `MAX_TURNS` / `MAX_USER_TURNS` | 两条额度是**分开**的：assistant 走 `MAX_TURNS`，user 走 `MAX_USER_TURNS`。把它们合回一个预算，提问就会被答案挤光 —— 一次提问要 6–39 段回答（实测） | 重开对话节点后一条提问都看不到，**吸顶路标随之整份消失**（路标挂在 user 轮次上） |
| `src/shared/ompQuota.ts` 的 `usedFraction()` 四级优先 | omp 上游 `packages/ai/src/usage.ts` 的 `resolveUsedFraction`（**抄的不是 import 的** —— 那是上游包，进不了我们的构建）。上游改了优先级而这边没跟，症状是「`omp usage` 自己显示得好好的，我们的额度条却空着」，用户会认为我们坏了 |
| `quotaStore.ts` 里 omp 那条采集路的 env | `agentChat/omp/launch.ts` 的 `ompBaseEnv()`（会话 / 冒烟 / `omp usage` / `omp models` **四个调用点共用一份**）。少设一处的症状是那条命令去读用户**真实的 `~/.omp`** —— 拿回来的是别人账号的额度，而且不报错 |
| 新增 IPC 写通道 | ① `preload/index.ts` 暴露 ② **必须有边界**：目标落在「项目根 ＋ 知识库根」里 → 加 `guardPath`/`guardDir`；落在这两个根之外 → 在本模块写一份**更窄**的守卫，**不要放宽 fsGuard 的 roots**（那会把所有写通道的边界一起放宽）。已知的见下表 | 安全边界出洞；或写操作静默失败后被"修"成放宽 fsGuard |
| 新增走 `process.resourcesPath` 的随包资源（脚本 / 模板 / 页面 / 片子） | `package.json` 的 `build.extraResources` 加 `{from, to}`，且 `to` 的字面量与代码里 `path.join(process.resourcesPath, '<to>', …)` **逐字对上** | dev / `npm run check` / `node --test` **全绿**（取路径的代码都有 `app.getAppPath()` 的 dev 分支兜底），只有装出来的包静默找不到文件、功能整块死掉。`scripts/verify-agent-chat.mjs` 静态钉了这条，但只覆盖 `agent-hooks` 一项，且它不在 `check` / `dist` 里 |
| `src/tunnel/*.ts` | 重新打包 `deploy/tunnel/hub.mjs`（esbuild，或直接跑 `scripts/publish-tunnel.sh`，它第一步就会覆写） | 线上跑的还是旧字节，而本地 `hub.test.ts` 全绿 |
| `src/renderer/src/features/dict/dictionary-bundle.json`（**正本**：`scripts/dict-svg/apply.mjs` 只写它，`src/shared/dictTaxonomy.test.ts` 只钉它） | `hooks/dictionary-bundle.json`（钩子自带那份，`scan-commit.mjs` 的 `loadDict()` 只在自己目录里找） | 新词条永远进不了 `docs/knowledge-manual.html`，老词条还配着过时示意图。**没有任何脚本会重新生成 hooks 那份，只能手工同步** |
| `UserTerm` 新增字段（`src/shared/types.ts`） | `dict.ts` 的 `dict:add` 落盘对象 **和 `readUser()` 的字段白名单** | 写盘有、读出来没有；更坏的是 `dict:add` 开头就 `readUser()` 再整个写回，于是此后每加一条词，就把已有条目的该字段洗掉一遍 |
| `src/main/cliAuth/index.ts` 的 `LOGIN_ARGS` | `features/agentChat/CliSetupPanel.tsx` 的 `TERMINAL_LOGIN`（渲染层 import 不到主进程模块，只能手抄一份；`STATUS_ARGS` 没有渲染层孪生，别一起找） | GUI 跑 A 命令，面板里的终端兜底却教用户敲 B 并一键执行；没有编译或测试拦得住，只有用户点开兜底那一刻才看得见 |
| `src/main/pty.ts` 的 `secretsEnv` 注入闸门 | `src/main/secrets.ts` 的 `secretsForRun` 闸门（`/secret-env` 那条路） | 安全开关只挡住一条路，另一条照发明文 —— `mcpEnabled` 原来只挡 `/invoke`、挡不住 `/secret-env`，就是这个形状 |
| **CLI 装在哪的候选目录**（`src/main/probeEnv.ts` 的 `userBinDirs()`） | **没有孪生了 —— 这就是修法本身。** `agentInstall.hasBin` / `agentSkill.hasCli` 已改成引它，新增探测点也一律 import，**不要再列第四份**。真正的兜底是 `applyLoginShellPath()`（启动时问登录 shell 要真实 `$PATH`），候选目录只是它回来之前的止血 | 2026-09-01 实况：三份清单里只有 `PROBE_ENV` 那份漏了 `~/.local/bin`，而 Claude Code 2.x 升级后自己迁到了那里 —— 于是「安装引导说已装、探测说未装」，用户界面永远停在「未安装」、不给启动按钮。同机 codex 在 homebrew 里照常显示，更像 app 坏了 |
| `src/main/prefs.ts` 的 `Prefs` | `src/preload/index.ts` 的 `PrefsSnapshot`（手抄，刻意不跨进程 import 类型，所以没有编译约束） | 新加的偏好项在渲染层读不到 |
| `telemetry.ts` 的 `EVENTS` 白名单（新增或改名一个功能计数） | `deploy/eas-stats.py` 的 `FEAT_NAME` ＋ `site/privacy.html` §四「会发的·功能计数」 | 看板显示英文 key；更要紧的是**隐私页少申报了实际在采的数据** |
| `telemetry.ts` / `site/analytics.js` 的 **query key 名** | `deploy/eas-stats.py` 的 `load_events()` 里逐个 `one("…")` 的字面量 | 该维度静默归零（取不到就默认空串），图表变空但不报错；脚本跑在服务器 cron 上，进不了 CI |

**刻意不走 `fsGuard` 的两条写通道**（就这两条，多出第三条即红旗；它们的守卫比 fsGuard **更窄**，不是更宽 —— 别为了"统一"把它们并回去）：

| 通道 | handler | 守卫 | 为什么不套 fsGuard |
|---|---|---|---|
| `skillLibrary:writeFile` / `skillLibrary:copySkill` | `skillLibrary/index.ts` | `skillLibrary/write.ts`：已登记的 skill 目录 ∪ 各项目 `.claude/skills`，且落点必须在某个 skill 子目录**之内** | skill 目录全在「项目根 ＋ 知识库根」之外，那正是这个面板存在的意义 |
| `projects:renameFolder` | `projects.ts` | `projectPaths.ts` 的 `planRename`（不引 electron，可被 `node --test` 直接打）＋ 调用处补 `realResolve` 解 symlink | 操作的正是项目根**本身**，而 `guardPath` 要求「在某个项目内部」 |

## 发版链路

流程本身在全局 memory 的「Eas-Term 发版流程」里，这里只记会静默出错的耦合：

- **版本号唯一来源是 `package.json`。** `publish-site.sh` 读它，再用 `sed` 把 `site/index.html` / `site/download.html` 里的版本号与下载链接回填。手改那两处会被 sed 刷掉，或被后面的 STALE 检查拦下。
- **`CHANGELOG.md` 一份来源两处消费**：`changelog.mjs html` 生成 `site/changelog.html`，`changelog.mjs notes <版本>` 塞进 `latest.json` 的 `notes`（被 `updater.ts` 的 `LATEST_URL` 取走）。漏写本版条目时，`publish-site.sh` 第一步的 `check` 直接 exit 1 —— 这条是**响亮**的；但 `notes` 找不到版本时**返回空数组不报错**，所以绕开 check 的路径上，应用内更新提示会是空的。
- mac 走 `EAS_NOTARIZE=1 npm run dist`（产物落 `~/Eas-Term-release`，即脚本的 `PKG_DIR`）；Windows 走 CI 的 `dist:ci`，产物要 `gh run download` 拉进**同一个** `PKG_DIR`。下载页指向新版 exe 而本地没有它时脚本会中止。**Windows 链接允许落后一版**，这是刻意的。

## 📌 已发现的文档脱节（待修）

| 位置 | 文档/配置写的 | 代码实际 |
|---|---|---|
| `hooks/dictionary-bundle.json` | 与 renderer 那份同源（打包分发那次两边逐字节相同） | 已漂：条目数比 renderer 少（多出的那批全是 `fx-*`），共有条目里还有大量 `svg` 不同。**renderer 是正本**，没有脚本会补上这道差 |
| `site/privacy.html` §四「会发的·功能计数」 | 未列 `approve`（灵动岛审批）与 `agent`（新建 AI 对话面板） | `telemetry.ts` 的 `EVENTS` 里有 —— **对外少申报了两项** |
| `deploy/eas-stats.py` 的 `FEAT_NAME` | 少 `agent` | 看板上那一行会显示英文 key |
| `site/README.md` | 「无 JavaScript」「下载链接全是占位 `#`」 | 站点已带 `analytics.js` / `proto.js`，下载链接也已是真实的 `/download/v<版本>/…` |
| `.gitignore` 里 `resources/models/` 那条注释 | 「打包时经 extraResources 附带」 | 语音模型体积很大（`resources/models/` 已 gitignore），走运行时下载到 `userData/models`，**故意不进 `extraResources`** —— 这是唯一一个走 `process.resourcesPath` 却不登记的资源，别当成缺漏去"修" |

> 发现新的脱节就补一行；修其中一条时顺手核一遍同一条链上的其余数字。

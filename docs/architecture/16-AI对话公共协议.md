# AI 对话公共协议

2026-09-07。类型真相在 `src/shared/agentChat.ts`；本图纸解释约束，不定义第二份协议。

## 能力与生命周期

`CliAdapter` 负责 CLI 参数、传输、探测与原始事件翻译。会话层管理进程和公共事件，前端根据 `CliCapabilities` 渲染，不能按 Codex/Claude/omp 名称决定控件。

`agentChat:event` 使用固定通道和 sessionId 信封；preload 提前监听的顺序不变。`session.ready.sessionId` 是 CLI 的恢复标识，`model` 为空表示 CLI 未确认实际模型，不能用用户选择填造事实。`turn.start` 表示已投递；`turn.done` 结束当前轮，进程退出和会话删除是不同状态。Codex 保持 stdin ignore，恢复参数为 `exec --sandbox X resume ID …`。

## 模型目录

`ChatModelOption` 包含 id、label，可选 effortLevels、defaultEffort。按模型给出的档位优先；没有时保留 harness 的全局档位。模型选择和强度选择通过 setParams 在下一轮应用；切模型清除原强度覆盖。

`capabilities` 事件按字段更新，数组整体替换；缺席字段不清空其他能力。可选 `modelCatalog` 包含 status（loading/ready/error）、source（probe/cache/fallback/none）、updatedAt 和 note。旧 harness 不提供该字段时维持旧行为。

首次探测失败也保留状态与刷新入口；缓存、内置目录须标明来源。`agentChat:refreshModels` 校验 sender 所有权，复用原探测通路，不重启对话。目录更新只要求会话对象和请求代次仍匹配，不要求本轮进程存活；删除或替换会话后忽略晚到结果。

Codex 通过共享 PROBE_ENV 启动 app-server，依次 initialize → initialized → model/list，读取 nextCursor。握手失败、坏响应、循环分页、超时均降级，收尾先 TERM 后 KILL。成功探测在进程内缓存 60 秒，以可执行文件和环境的哈希隔离来源；刷新绕过成功缓存，同时复用正在进行的同源请求。磁盘目录仍按 CLI 保存，只能证明上次成功，不能证明当前账号可用；账号变化后应刷新，失败时明确显示缓存来源。

## 文本、工具与插件

`text.delta/text.done` 继续交给已有 Markdown 渲染器。工具使用 `exec.start/exec.done`，可选 tool（server/name）和 resources（uri/name/mimeType）；缺省仍按 label/detail/output 显示。三个翻译器将工具内容归一为可读文本，未知内容保留降级信息；Codex MCP 的 isError 不能显示为成功。

资源元数据随历史保存。HTTP(S) 入口再次验证协议和凭证，使用对话 leafId 定位同 Frame 浏览器。`ui://` 只映射当前会话插件清单中 entry 完全匹配的面板，交给既有 PluginPanel 的 resources/read、握手和权限检查；未知或缺失资源显示不可打开，不执行任意 URI。

`plugin.status` 的 selected/missing 来自用户选择和已安装清单，只读展示，不代表已注入、已认证或连接成功。实际调用结果由 exec 事件表达。插件选择仍保持单插件语义；本次没有新增安装或连接授权流程。

## 同步与验证

修改能力/事件时同步 shared、adapter/translator、session、preload、reducer、toolbarModel、history 和隔离基线；UI 文案不得伪造未报告的运行状态。验证记录在 `docs/verification/agent-chat/README.md`。测试回放覆盖公共组件，不能替代每个真实服务的端到端验证。

## 首轮前模型选择（2026-09-07 补充）

`agentChat:modelCatalog(cli, force)` 返回 AgentChatModelCatalog，不要求 sessionId，不创建对话。与会话后的刷新共用 resolveAdapterModels、缓存与探测器。StartupModelPicker 在选择 CLI 时立即请求，组件以 CLI id 为 key，卸载后忽略旧响应；选择按 CLI 隔离保留。启动页显示启动模型、思考强度和首条消息模型摘要。显式选择优先于角色默认；更换模型不继承角色的强度覆盖；未选择时保留角色/CLI 默认，不填造未知的默认模型。首轮与恢复失败重试共用 startupParams。

## 共用视觉布局（2026-09-07）

主布局经过 Figma Review 01 审查。启动卡片宽度上限 720px；StartupModelPicker 通过 actions 插槽把模型/强度/刷新和语音/发送放入同一卡片，实际首轮摘要保留。项目/CLI/角色/分支移到卡片下方，历史入口默认折叠且仅无已恢复记录时出现。

聊天正文和 ChatToolbar 对齐到 760px 阅读宽度。主操作行放模型、强度下拉、刷新和消息动作；插件、目录状态、新对话/压缩和沙箱说明下置，显示条件仍依赖公共能力。刷新/语音/发送/停止使用图标及 aria-label。停止按钮在明暗主题、常态和悬停态均使用白底黑色图标，继续调用 interrupt 保留会话。用户后续要求禁止主操作换行：AI pane 最小内容宽度 640px，主操作保持单行，长模型名截断。样式复用现有主题变量；进入淡变和按钮过渡支持 prefers-reduced-motion。

`paneSizing.ts` 集中维护最小宽度与分屏树约束。画布 resizeNode 和 PaneView 旧节点恢复使用相同下限；PaneLayer 对分屏计算受约束的临时布局，空间不足横向滚动，保留原 pane DOM 与 store 中的分割比例。不能通过重挂载面板来实现布局切换。`verify-agent-chat-ui.mjs --width` 检查旧节点修正、缩小下限、控件同排、分屏不重叠与 DOM 保留。

MessageList 的提问吸顶、零 padding-top、历史恢复与会话生命周期保持原有约束。`QuestionNavigator` 从真实 user turns 派生刻度和相邻回复摘要，以消息节点定位；不能按可变 turns 引用 memo。导航 portal 以所属 AI 对话模块 .pane 为唯一定位基准，外侧固定 26px 导航区与 12px 屏幕间距，不读取 Frame 边界；Frame 独立移动/扩张不改变导航与模块关系。空间不足内收并仅增加左侧留白；按 pane-layer 可见交集裁切，隐藏面板不显示。ResizeObserver、消息变更、滚动和 store 变更合并到 rAF，最大化/还原的短 FLIP 动画期间逐帧跟随，空闲不轮询。普通画布和分屏导航层级 40，低于抽屉 45；仅最大化且抽屉隐藏时提高到 220，越过最大化 pane 的 200。展开抽屉占据导航位置时导航让位。必须测试实际命中与层级，不能仅检查 DOM 存在。

## 启动安装和认证（2026-09-07 接入原型）

`StartupSetupCard` 从当前 selected CLI、检测状态和认证结果派生展示。选择未安装 CLI 也必须先更新 selected，禁止使用与选择脱离的 cliNote。切换递增请求代次、清理旧认证与设置面板；重复选择同一 CLI 也重查认证，不能因引用未变绕过登录。listClis 的晚响应只在代次仍有效时应用，认证响应只属于对应 CLI。

外部 CLI 复用 CliSetupPanel 的安装→CliLoginPanel 流程；provider-key 复用 OmpSetupPanel。完成后重查清单和认证，保留草稿，不自动发送。内置运行文件缺失显示修复说明，不执行外部安装；无安装方案提供手动安装与重查；空清单重查后应选择可用项。启动按钮在安装、认证、能力未就绪时禁用。模型入口仍走原来的 modelCatalog，不以视觉状态伪造可用模型。

## 语义图标（2026-09-07）

`ui/SemanticIcons.tsx` 提供自有双色 SVG，`semanticIconKinds.ts` 根据可靠文件名/扩展名分类；FileTree 共用于资源抽屉和分屏侧栏。BranchBadge 使用当前 leaf 的 worktree 和 effectiveCwd，不能取全局 Frame。待创建工作区必须明确标注待创建。菜单的 leadingIcon 与原右侧 hint 独立，不改变操作保护。

可选 `ExecKind` 随 exec.start/done 贯通 reducer 和历史：Codex 按 item type，Claude 按 tool name，omp 仅可靠 execute 类型映射 terminal；未知及旧历史使用 generic。类型图标不代替执行成功/失败状态，不能根据自然语言输出猜品牌或工具类别。

## 交互验证入口（2026-09-07）

`verify-agent-chat-ui.mjs` 默认流程使用当前 CLI 下拉菜单与发送按钮的可访问名称；真实坐标点击等待布局稳定并检查命中。沙箱提示检查 `.ac-bar-note`，审批保护入口已在设置，不再要求旧工具栏 chip。16 项断言输出 `interaction.json`，控制台错误使脚本失败。`--startup` 和 `--compat` 分别覆盖首轮参数与三 harness 事件回放；这些测试替换 start/onEvent，不能标作真实远端完整端到端。全量回归额外发现未改动的手机证书模块序列号首字节 00 时编码失败，复现记录见验证 README。

## Codex 启动沙箱权限（2026-09-07）

启动页仅在选中且可用的 Codex 时显示三态沙箱图标（与 adapter 的三个 sandboxLevels 对应）。用户指定默认 danger-full-access；角色 caps.write:false 锁定只读，并仍由 adapter 强制执行。startupSandboxParams 将选择同时传入首次 start 和 resume 失败重试；切换到 Claude/omp 不发送该字段，切回 Codex 保留本次页面选择。权限行独立于模型和消息操作，保持 640px 最小宽度。运行中的会话仍不提供权限切换。

## 工具调用列表与详情（2026-09-07）

折叠列表继续使用 visibleExecs：最近 3 条加所有失败项，按原顺序去重。“展开全部”只显示全部调用行；每行用原生按钮独立展开 detail/output，并提供 aria-expanded/controls。资源链接保持可访问。列表最大高度由所属 ac-messages 的 ResizeObserver 计算为可视高度三分之一（96–320px），超出内部滚动；收起列表按钮在列表外，展开条目的标题吸顶以便随时收起详情；正文与审批卡片不纳入工具滚动区域。不得以 memo 冻结可变 ExecItem 的状态更新。

## 警告与错误视觉（2026-09-07）

启动错误、聊天 notice、工具失败行统一中性 s-2 底色与 t-2 正文，取消红/黄半透明铺底。ChatStatusIcon 用三角警告和圆形叉号区分级别，细侧线及图标使用语义色；工具失败保留明确“失败”标记。警告/错误的去重、自动消失、关闭和登录入口保持原逻辑，通知容器仍限高 96px。

## 参数选择框（2026-09-07）

启动页与聊天页的 ac-param-select 使用 32px 胶囊、弱背景和自绘下拉箭头，field-sizing:content 随所选名称收缩；模型上限 200px，强度上限 136px。移除父级 220/140px 固定占位与 pending 强描边，模型图标位于胶囊内部。仍使用原生 select，保持键盘/读屏、能力目录、选择回调和下轮生效逻辑。

- CLI 品牌图标：`ui/CliBrandIcon.tsx` 为聊天上下文按钮及菜单、FrameStart、CanvasAgentBar 共用入口。Claude 使用官网资源，Codex 使用 OpenAI 官方组织标识，omp/内置 harness 直接引用 `build/icon.png`；资源随包离线加载，未知外部 CLI 回退终端图标。切换不改变清单、安装提示或启动参数。

## 启动沙箱图标（2026-09-07）

用户指定 Codex 启动页默认完全放开（DEFAULT_STARTUP_SANDBOX），仅影响首轮显式参数，adapter 其他入口默认值不变；只读角色仍优先锁定只读。StartupSandboxButton 位于 StartupModelPicker 操作行，单个 36px 按钮通过解锁 / 盾牌 / 眼睛三种形状区分完全放开、可改工作区、只读。点击按此顺序轮换并以中性气泡说明当前状态，3.2 秒后收起；hover/focus 显示“调整沙箱状态”，Escape 和点击外部收起。气泡 portal 到 body 并按视口避让，不占输入框布局；切换 CLI 卸载，角色/可用状态变化清除旧提示。安装未完成不显示此启动控件。

启动模型摘要（“首条消息…”）属于辅助说明：11px 常规字重，使用输入框 placeholder 同源 fg-dim 的 85% 不透明度；与控件间距 8px。明暗主题下均弱于 13px 的输入预置文字，状态及模型信息继续完整显示。

沙箱说明气泡的位置在布局阶段和操作行 ResizeObserver 回调内直接更新 DOM 坐标，随模型名及容器宽度移动；避免测量后反复更新 React 状态形成布局循环。

## 对话强度滑块（2026-09-07）

ChatToolbar 使用 EffortSlider 替代对话态强度下拉；toolbarModel 根据当前模型提供离散档位，不按 CLI 名称分支。0 档代表不覆盖 CLI 默认，后续刻度与能力清单一一对应；154px 控件显示当前档位，支持拖动及原生 range 的键盘操作，aria-valuetext 说明下一条消息起生效。无档位隐藏，换模型清空旧强度；目录刷新移除旧档位时同样清空覆盖。继续调用既有 onSetParams，不改变会话或启动页强度控件。

## CLI 身份与可选工具故障（2026-09-07）

ChatToolbar 上下文栏展示会话当前 CliInfo 的名称和 CliBrandIcon，作为只读身份标记，不提供中途切 CLI。Codex translator 翻译顶层 error 为非致命提示、turn.failed 为真正的致命失败；stderrDiagnostics 按完整行和结构化 error.message 保留最相关原因，避免最后 600 字的 MCP 关闭警告覆盖模型版本错误。wireProc 对 MCP 连接错误只发一次非致命工具提示，不停进程；已有结构化 fatal 时不再重复通用退出错误。cliAuth.detect 排除明确的 MCP 握手 401，避免误触发模型主账号登录。遵循 CLI 自身的可选 MCP 行为，不修改全局 MCP 配置、不关闭可用工具，也不伪造失败后的成功。

恢复已有 paneSessionId 时，AgentChatView 优先保留 pinnedCli 对应的清单项，即使暂时 unavailable；只有未启动的新会话才按可用性选择替代 CLI。MCP 容错不覆盖用户显式 required 配置，也不掩盖真正的进程退出或模型失败。

## CLI 版本更新与会话稳定性（2026-09-07）

Codex / Claude 自动更新独立开关，默认关闭；损坏/旧配置亦关闭。打开后立即检查、启动后 30 秒及每 6 小时复查。只更新已经安装的 CLI，只接收官方稳定数字版本，不降级。官方原生包下载到 userData/cli-versions，SHA512、归档路径和文件类型校验后解包，--version/--help 检查通过才写 pending；这不承诺所有上游能力兼容。下载十分钟超时，关闭开关取消在途下载并丢弃晚到结果；已完成 pending 保留且在界面说明。无全局安装脚本或账号修改。

下次软件启动在窗口和会话开放前校验并将 pending 切为 active，保留上一托管版；校验失败保留旧版。probeEnv.setManagedCliPaths 只能在此调用，登录 shell 的晚到 PATH 合并必须保留此前缀；PTY 使用同一目录，zsh 启动文件最后重新前置。应用内 Claude 禁止自行后台更新，对话进程同时禁用手动自更新以保持运行版本固定。外部终端安装与更新设置不改变。回退也是 pending 操作，并关闭自动更新。内置 omp 继续随应用分发，不参加独立 CLI 更新。

新增 IPC：cliUpdates:get / setEnabled / retry / rollback，严格只认 codex、claude 和 boolean，不接受路径、URL 或 shell 命令。事件 cliUpdates:changed 推送设置状态；配置写入失败不假报已保存。

### 0.4.83 隔离基线同步

两层 isolation 快照已审阅后同步：工具事件增加 edit/terminal 语义类型；Codex 原始 error 为非致命、turn.failed 为致命，过去未呈现的认证错误现在进入提示。仅上述有意行为变化，既有会话、文本、用量及工具执行结果均保留。

# 画布交互批量需求 · 2026-07-24

一次会话连续提出的需求。状态：✅已验 / ⚙️已改待验 / ⏳待做 / 🖼️待截图。

## ✅ 已在 dev 眼验通过

- **#2 抽屉「更多信息」引导**：收起态右缘竖排「更多信息」+ 向右长扁箭头，持久显示，点击展开。改 `CanvasDrawer.tsx` + `canvas.css`。
- **#3a 模块 resize 放大去重叠**：`resizeNode` 改走 `reflowSeparate`。验证：撑大节点 → 邻居 Frame 自动下移让位。
- **#5 终端防误删**：
  - (a) 键盘：`CanvasStage` keydown 去掉 `Backspace` 触发；终端节点(leafId)+ Frame 一律不由键盘删。验证：同一次 Delete 里终端存活、形状被删。
  - (b) 关闭确认：已内建（`closeLeafSafely` → `busyByIds` → 运行中弹确认）。
- **#9 框选不选 Frame**：`startBoxSelect` 去掉 frame-add，只选节点。验证：拖框选 5 节点 0 Frame；frame 头部点击仍能选中。

## ⚙️ 已改，dev 不复现（打包态）

- **#1 麦克风崩溃**：根因 `identity:null` 跳过签名 → bundle 封签残缺 → TCC 杀进程。修复 `build/entitlements.mac.plist` + `build/afterPack.js`（dist 后自动 ad-hoc `--deep` 重签+校验），已接进 package.json。**只能等授权打包时验。**

## ✅ 已在 dev 眼验通过（续）

- **#8 分屏任务完成通知**：标题栏铃铛（`TerminalAttention`，已完成项目数）+ Sidebar 项目条目完成徽标点。点铃铛「依次」跳到下一个已完成项目并清除；`setActiveProject` 切到某项目即清除其 attention。验证：flag pty→铃铛/徽标出现；点铃铛→跳该项目+全清。
- **#4 版本管理「回退到该版本」**：`HistoryView` 提交行右键 → `CanvasContextMenu`「回退到该版本」(stopPropagation 防串画布节点菜单) → `requestConfirm` 破坏性确认 → `git:resetHard`（新增主进程 handler + preload）。验证：一次性仓库 reset 生效；UI 菜单+确认框正常，真实仓库未动。
- **#7 浏览器双指按选中态区分**：`WebView` 接 `selected`，未选中(`false`)时 `.web-body` 盖 `.web-shield` 透明遮罩（z-index>webview）→ 双指 wheel 冒泡到画布 viewport 触发 pan；选中/分屏不盖。验证：未选中 wheel→画布 pan(位移=−delta)；选中→遮罩消失。

## 🖼️ 待截图澄清

- **#3b 画布 zoom 放大去重叠**：世界坐标等比 zoom 不新增重叠，`deoverlapFrames` 空操作。若 zoom 时确实看到重叠 → 多半是终端字号缩放的渲染层错配，需截图定位。

---
**当前 dev 会话**：临时加了 CDP 调试端口行（`src/main/index.ts`），全部验完、提交前删除。canvas.json 已备份 `bakE`，测试改动结束时恢复。**用户明确：完事不要打包，dev 验证。**

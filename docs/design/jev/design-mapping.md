# Jev 面板设计映射 · 2026-09-22
用户选定 Master Strategy 设计套件。参考为用户粘贴的本地选型库正文，不代表官方最新规范。
产品映射：强调 #2563EB；画布 #000；表面 #111；1px rgba(255,255,255,.1) 边界；Flat；8px 节奏；4/6px 控件圆角；Ease / 300ms；低强度呼吸点阵。
字体参考 Playfair Display + Space Mono。原稿展示标题 96px/400/96px/-.05em；正文 Space Mono 12px/400/15px/.1em。面板按空间缩放标题，中文采用系统中文回退；原型未嵌入字体，不能视为精确字体验收。
冲突：橙/洋红粒子仅为背景气氛例外，不扩展操作色。原文 WebGL 标签下示例实际为 Canvas2D；本版采用 DOM/CSS 点阵降级，不宣称实现 WebGL。减少动态偏好时关闭动画。
--kit-*、expanded-sidebar-* 等文档展示壳 tokens 不复制到产品。
范围：本地交互原型；连接/总开关/分组能力/记录空态。未接真实 API，不接受真实凭证。尚未实现错误状态全集、宿主权限及持久化。

## 第三版：连接引导 → 能力仪表台
用户允许放弃 Master Strategy 氛围风格，改为清晰工具型。本版删除侧栏、大字及粒子。
未连接或未首次确认启用仅显示引导。连接成功后确认启用，再展示两组能力网格。暂停保留仪表台；断开回引导。
模块标题图标统一从 pluginMetadata.icon 获取。当前未找到 Jev 市场注册条目，icon=null 明确兜底；不是已接通正式市场。正式插件面板、画布模块标题需共用市场 manifest 图标，仍待生产实现。

# 多 Agent 编排 · 交接报告

> 2026-08-19 · 从 v0.4.30 做到 v0.4.33 · 本轮 26 个提交
> 写这份是因为上下文快满了。**新会话接手时先读这份，再读 `memory/eas-term-多agent编排.md`**（那份是架构决定，这份是待办）。

---

## 一、当前状态

**已装到你机器上的是 0.4.33**（`/Applications/Eas-Term.app`）。但**仓库里还有 3 个提交没打进任何包**：

| 提交 | 内容 | 装了吗 |
|---|---|---|
| `e54e736` | 全批叫停 + team_status | ❌ 未打包 |
| `f35e9d9` | P1 · shim 超时（fetch → node:http） | ❌ 未打包 |
| `f4cb006` | 两个 P0（Frame 锁死 / 超时死代码） | ❌ 未打包 |
| `7dbe0f1` | 面板显示角色名 | ❌ 未打包 |
| `526e289` | 团队 agent 不进状态系统 + 派活挂节点 | ✅ 0.4.33 |
| `2895e69` | 开关一开就出面板 + notify 拦截 | ✅ 0.4.33 |

下一次打包要带上 `7dbe0f1` 和下面修的所有东西。

**第一、二期功能上跑通了**：开关拦得住、清单弹得出、进程真起得来、画布上有节点、面板列得出角色。真派过一次活，两个 agent 都交了 findings，其中 cross-checker 抓到三个我没发现的问题（见下）。

---

## 二、必须先修的（按严重程度）

> **2026-08-19 更新：三条全部修完并验证。**
> `f4cb006`（两个 P0）· `f35e9d9`（P1）。下面保留原文作为记录 ——
> 每条的教训比修法本身更值钱，尤其「别自己维护一份会和现实脱节的状态」。
> P1 那条还留下一个可复用的结论：**Node 的 fetch 在这个 shim 里不能用于长等待**
> （undici headersTimeout 301 秒是独立内部闸，AbortSignal 拦不住），
> 要长等待就用 `node:http`。


### 🔴 P0 · `finishBatch` 从不在成功路径调用 —— 派一批就锁死一个 Frame

**这是当前最要命的一条,已经在真实使用中触发了。**

`mcpHandler.ts` 里 `finishBatch(where.frameId)` **只在「起到一半失败」的 catch 里**调；
成功 return 那条路不调。`batchRequest.ts` 的 `running` Set 于是永久记着这个 frameId。

后果：**这个 Frame 再也派不了活**，`askForBatch` 会在弹窗之前就抛
「这个项目已经有一批 agent 在跑了」，而它提示的补救动作（去面板里停掉）
跟这个闸门**根本没接线** —— `TeamPanel` 的停按钮走 `agentChat.stop`，
不碰 `batchRequest`。唯一出路是重启 app。

**修法**：两处都要接
1. `TeamPanel` 停掉某个会话时，如果这个 Frame 上已经没有 owner:'team' 的活会话了 → 调 `finishBatch(frameId)`
2. 或者更彻底：`running` 不再自己维护一份状态，改成**现算**——「这个 Frame 上还有没有 owner:'team' 且 alive 的会话」。那样它不可能和现实脱节

我倾向第 2 个。自己维护一份「谁在跑」的状态，就一定会有忘记清的路径。

### 🔴 P0 · 渲染层那条 9 分钟超时是死代码

`batchRequest.ts` 的超时守卫写成了：

```ts
if (pending?.resolve !== resolve) return   // ← 恒为 true，永远从这儿返回
```

`pending.resolve` 存的是**包装闭包**，`resolve` 是 Promise 的原始 resolve，
两者永远不是同一个引用。cross-checker 用 `mock.timers` 把时钟推过 9 分钟实测确认：
定时器回调跑了、`pending` 还在、比较是 false，**直接 return，什么都没做**。

**修法**：守卫比一个稳定的身份，不要比 resolve

```ts
let timer: ReturnType<typeof setTimeout>
const mine = { req, resolve: (d) => { clearTimeout(timer); resolve(d) } }
timer = setTimeout(() => { if (pending !== mine) return; /* … */ }, WAIT_MS)
pending = mine
```

**并且把那条实测收进 `batchRequest.test.ts`** —— 现有 7 条全在状态机层，
没有一条碰超时，所以这个 bug 才能带着「711 全过」的提交信息进来。

### 🟡 P1 · 真正最短的一道闸在第三层，只有 5 分钟

`mcpBridge.ts` 的注释把链路当成两层，实际是**三层**，而最外面那层最短：

| 层 | 位置 | 上限 | 实际 |
|---|---|---|---|
| ① MCP shim | `mcp/eas-mcp.mjs` 的裸 `fetch` | **300 秒** | undici 默认 headersTimeout，**最先炸** |
| ② 渲染层 | `batchRequest.ts` `WAIT_MS` | 9 分钟（名义） | 永不触发（上一条） |
| ③ 主进程 | `mcpBridge.ts` `WAITS_FOR_HUMAN` | 10 分钟 | 会触发但太晚 |

所以「清单能等 10 分钟」是假的，实际 5 分钟。
**修法**：给 shim 那个 fetch 显式设更长的 timeout（或 `AbortSignal.timeout`），
并在 `mcpBridge.ts` 那段注释里把三层都写上 —— 那段注释现在是错的。

---

## 三、第二期还没做完的

- ~~**`team_status`**~~ ✅ `e54e736`（描述里明写「别轮询」；只报进程状态不报任务完成度）
- **`team_send` / `team_dissolve`** 两个 MCP 工具还没做
  （`team_send` = 给某个 agent 追加指令纠偏；`team_dissolve` = 解散并归档 `.plans/`）
- **`.plans/<项目>/team.json` 落盘** —— 方案里的「状态层」还没建。现在批次状态全在内存，app 重启就没了（E-11/E-12 的兜底不成立）
- **面板上的用量与花费** —— 现在只有时长，方案里设计的是「本批累计 $X · N tok」
- ~~**全批叫停按钮**~~ ✅ `e54e736`（只停团队派生的，不碰你自己开的会话）
- **`owner:'team'` 关节点不杀进程**这条只有单测，没有真机验证（要等能稳定派活之后）

---

## 四、第三期（写码 agent）——**没开始，且不建议现在开始**

核心障碍是 **E-07 并发写同一个仓库是静默覆盖**（A 读、B 读、A 写、B 写 → A 的改动消失且无人知晓，不是 git 冲突那种至少会报错的情况）。
必须先做 **git worktree 隔离**。方案里的建议是：先把第二期（只读角色）真用一阵子再决定要不要上第三期。

---

## 五、这一轮里跟多 agent 无关、但还挂着的

1. **`docs/代码规范审查-2026-08-19.html` 里的三条建议一条都没做**
   - `check-css-dup.mjs`（24 个 CSS 选择器重复定义，我这轮亲手撞了两次）
   - `docs/invariants.md` + 检查脚本（142 处教训注释只有 22 处有测试兜底）
   - `npm run check` 串起 golden_rules / tsc / test
2. **场景包没随 app 分发** —— `canvasSkillFiles` 写死了 `skills/eas-term` 一个目录，
   且 Codex 那侧 `detailDir()` 把文件平铺，多个 skill 的 SKILL.md 会互相覆盖。
   `team-research` 现在是手动装在 `~/.claude/skills/` 的
3. **eas-term skill 的 description 优化工具** 修好了但放在 scratchpad（`opt/` 目录），
   没入库。里面两处修正值得保留：写进真 skill 目录、每次调用独立临时根
4. **`/Applications` 下有 8 个旧版本备份**，占空间，可以清

---

## 六、验证方法备忘

- **改 UI 必须真机验证**，方法见 `memory/eas-term-cdp-验证方法.md`（11 条坑）
- **装包别只打开 dmg** —— 这轮踩过：0.4.32/0.4.33 的 dmg 都挂载着，但 `/Applications` 里还是 0.4.31，两次派活白跑。直接 `ditto` 替换 + `lsregister -f` 最稳
- **换了 bundle 还要重启 app** —— 正在跑的进程不会因为文件被替换而更新
- 团队相关的端到端验证要**先确认跑的进程版本**：
  `ps -p $(pgrep -f "Eas-Term.app/Contents/MacOS") -o lstart=` 对比 bundle 的 `CFBundleShortVersionString`

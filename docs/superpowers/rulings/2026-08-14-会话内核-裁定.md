# 会话内核（子项目 A）执行期裁定存档

> 2026-08-14，SDD 执行 `docs/superpowers/plans/2026-08-14-会话内核.md` 期间，
> 控制方替用户做的 15 条决定。每条含理由与「判错的代价」。
> 原始账本随工作区删除，这份是留给仓库的。

Ruling 1（冲突 1 · text.delta 测不了）：ChatEvent 定义了 text.delta，但三份夹具都是
**没带 --include-partial-messages** 跑出来的，里面根本没有 partial 事件，Task 1 无法为它写测试。
决定：**Task 1 只实现 text.done，text.delta 保留在类型里但不实现**，等 Task 9 用活 CLI 验明
--include-partial-messages 的真实分块粒度后再补（spec §九 第 3 条本就要求那时验）。
这条会写进 Task 1 的 dispatch。代价若判错：Task 9 之后要回头给 claudeEvents.ts 补一段增量拼接，
是加法不是改写，成本低。

Ruling 2（冲突 2 · 跨进程的必要重复）：hook 脚本是**独立 Node 进程**，import 不到 TS 那份
hookResponseBody，只能各写一份。审查规则通常把逐字重复判为缺陷，此处是跨进程边界所迫。
决定：**保留重复**，但两个文件都必须写明「改一处必须改另一处」的交叉引用注释（计划已要求）。
代价若判错：两处漂移会让审批响应体失效，靠注释与 Task 9 的 e2e 兜。

Ruling 3（冲突 3 · T8 无单测）：Task 8 是纯胶水（spawn/喂行/推事件/定时器），判定逻辑全在
T1-T7 的纯函数里且已被覆盖。为胶水层写 mock 测试收益低于维护成本。
决定：**T8 不写单测，由 Task 9 的真实端到端验证兜底**；审查若提「缺测试」按此裁定。
代价若判错：胶水层的接线错误要等 e2e 才暴露，而 e2e 会花少量 CLI 额度。
T1 dispatched (base 523a117, model sonnet, agent ac7e276)

Ruling 4（计划缺陷 · 审批两路无共同关联键）：实测确认 hook 路 payload 含
`tool_use_id`（完整键：session_id, transcript_path, cwd, prompt_id, permission_mode,
hook_event_name, tool_name, tool_input, tool_use_id），而流里的 hook_started/hook_response
**只有 hook_id**，两者无法对应。计划 §Task 3 假设按 tool_use_id 缝合两路，该假设不成立。
决定：**审批完全由 hook 路单独驱动**，不做"缝合"——
  · `approval.request` 只从 hook payload 产生，approvalId = tool_use_id
  · `approval.resolved` 由我们自己回传决定时产生（我们知道回了什么）
  · 流里的 PreToolUse hook 事件**不产出任何中间事件**（与 SessionStart 噪音同等处理）
影响：Task 1 需一个修复轮删掉 hook_response→approval.resolved 那条；
Task 3 从"缝合器"降级为"hook payload → approval.request 的转换 + 待处理表"，测试相应简化。
代价若判错：若将来 Claude 在流里补上 tool_use_id，可以再把流那路加回来做冗余校验，是加法。
Task 1: 审查 Approved（无 Critical/Important）。审查者独立复核了夹具字节、两个 tsconfig、
  变异验证的还原状态，并确认 resolvedExecIds 去重 Set 是必要实现（permission_denied 与
  tool_result 是同一 tool_use_id 的两路独立信号，不去重会产出 2 条 exec.done，
  直接打破「被拒必须留下一条失败痕迹」）。
Task 1: minor (deferred): hook_started 分支不判 hook_event 直接丢弃，与简报字面「先判再丢」
  略有出入，行为等价。
Task 1: minor (deferred): 本机未配全局 git user.name/user.email，提交身份靠系统推断。
  属用户环境问题，不在本分支处理，最终报告里提一句。
Task 1: 进修复轮 —— 原因不是审查发现的缺陷，而是 Ruling 4 在审查之后才定：
  翻译器不该再从 hook_response 产出 approval.resolved。
--
Ruling 5（计划与 spec 不一致 · Codex 走哪条接口）：spec §A.4 写 Codex 走 app-server
（原生带审批），但计划 Task 2 的夹具与翻译器写的是 exec --json，两者事件格式完全不同。
pre-flight 扫描只对了任务间接口、没对任务与 spec，漏掉了这条。
决定：**子项目 A 内 Codex 先走 exec --json**，Task 5 的 Codex adapter 报
`capabilities.approval = []`（UI 据此退回沙箱级别选择——这正是能力声明机制要覆盖的情形，
spec §A.4 的退路条款已预留）。app-server 那条**推迟到子项目 A 之后单独做**，
理由：它是 experimental，摸清握手与消息格式需要另一个 spike，不该阻塞整个内核落地。
影响用户需求：「两个 CLI 一起做」仍然满足（两个 adapter 都在），但 Codex 侧一期只有
沙箱级权限、没有逐次审批卡片。**这一条要在最终汇报里明确告诉用户。**
代价若判错：app-server 落地时要加一个 codexAppServerEvents.ts 与新 adapter 分支，
exec 那条留作退路，是加法不是重写。

Ruling 6（Task 2 的失败判定是错的 · 已抓真样本证实）：实测 codex exec 的
command_execution item 形状为
  started:   {id,type,command,aggregated_output:"",exit_code:null,status:"in_progress"}
  completed: {id,type,command,aggregated_output:"...",exit_code:1,status:"failed"}
**没有 error 字段**，失败靠 status/exit_code 表达。当前实现判 error 有无 → 失败会被判成成功，
直接违反「执行结果以事件为准」这条硬约束。
决定：**审查前先修**（SDD 对 DONE_WITH_CONCERNS 的处理：正确性类顾虑先解决再审）。
新夹具 codex-exec-fail.jsonl 已入库供断言。
Task 2: fix round 1/5 — 2cb21c1，255 通过。ok 判据改为 status，output 用 aggregated_output，
  label 用 command。变异验证已做（恢复旧判据后正好 2 个依赖 ok 语义的测试失败）。
Task 2: minor (deferred): commandPreview 用 .slice(60) 截断，理论上可能切断 UTF-16 代理对
  （命令行里出现 emoji 才会碰到，汉字在 BMP 内不受影响）。
Task 2: minor (deferred): exec.start.detail 与 file_change 的 label 仍是类比设计，无夹具覆盖。
Task 2: 审查 Approved（无 Critical/Important）。审查者独立复现了 ok 判据的变异（恢复旧写法后
  精确 2 条失败），并**额外做了一次报告未提的变异**：把 outputTextOf 换成无条件 JSON dump，
  10 条测试全过 —— 因为序列化后的 item 里仍含 "No such file" 子串。
Task 2: minor (deferred): 「exec.done.output 带上真实命令输出」这条测试锁定太弱，
  实际只验证了「output 里能找到失败原因文本」。审查者给的修法：断言 output 不含 "status"
  这个 JSON 键名，或精确匹配 aggregated_output + '\n(exit code 1)'。**最终评审请 triage 这条。**
Task 2: minor (deferred): 两个翻译器对空文本的处理不一致——Codex 要求 text.length>0 才产出
  text.done，Claude 只要求 typeof === 'string'（空串也产出）。影响面小，值得后续统一。
--
Ruling 7（我的计划质量问题 · 简报测试漏断言字段）：Task 2 与 Task 3 连续两次被审查者用
自设变异发现「看起来在测、其实没测」的字段（codex 的 output 降级优先级、registry 的 detail）。
两次实现者都是**逐字照抄简报测试**，根子在我写计划时只断言了事件存在与部分字段。
决定：**从 Task 4 起，在每个 dispatch 里加一条要求** —— 「若发现简报的测试没有覆盖你实现的
某个字段或分支，自己补一条断言，并在报告里说明补了什么」。已积累的三条 deferred minor
交最终评审 triage。代价若判错：实现者可能补出一些低价值断言，成本远低于假测试留在仓库里。
T4 dispatched (base 68b69d9, model sonnet)
Task 4: 实现完成 f5b13af，276 通过。**按 Ruling 7 补了 5 条断言**（简报漏断言的字段：
  15 分钟边界、pending 跨次合并、非 model/effort 字段不受影响、restart 的 opts.model/cwd、
  send 分支的 opts 内容）。两次变异验证均精确命中。
Task 4: 待审查判定 —— planSend 的 now 参数在决策逻辑里未被使用（实现者推断回收由外部
  定时器 + shouldReap 驱动，planSend 只读 alive 标志）。需判断这是多余参数还是遗漏了 lazy check。
Task 4: 审查「需修复」（范围极小，只补一条测试，不动实现代码）。
  【Important】effectiveOpts 里 pending.model 是否被读取，14 条测试全测不出——
  把它改成无视 pending 后 14/14 仍全绿。后果：将来若手滑改坏，用户改了模型、UI 说
  「下条起生效」、发送后 CLI 仍静默沿用旧模型，无任何测试报警。实现本身是对的，只缺覆盖。
  审查者另做 7 次变异，抽查的 3 条补充断言全部证实为真断言。

Ruling 8（planSend 的 now 参数 · **我倾向的方案被审查推翻，采纳审查意见**）：
我原倾向 (b)「planSend 应自己 lazy 判 shouldReap，防定时器被系统挂起」。审查者判 (c) 保留现状，
理由更硬：planSend 是纯函数、**没有能力杀进程**；若它仅凭时间判 restart 而 alive 仍为 true，
Task 8 骨架「restart 直接 spawn」会造成两个进程同时存活、stdout 灌进同一个 translator——
比要防的问题更严重。且 alive 由单线程、唯一写者在同步临界区维护，不是会过期的缓存；
休眠期间子进程同样被挂起，不产生资源浪费。
决定：**采纳 (c)**，now 参数保留（跨任务锁定接口，T8 已按此签名调用）。
**并把审查者的前瞻建议带进 Task 8**：restart 分支必须无条件 `live.proc?.kill()` 后再 spawn，
那才是真正兜住「休眠导致 alive 滞后」的地方。
代价若判错：若真出现 alive 长期滞后，症状是给已死进程发消息失败——由 spawn 层可见地报错，
不是静默错误。
Task 4: minor (deferred): applyParamChange 对当前 s.effort 的「不提前生效」保证只在 model
  通道被直接测试，effort 通道没有（实现对称正确，纯覆盖不对称）。
Task 4: fix round 1/5 复审 —— 两条均 ADDRESSED，独立变异确认新测试精确命中。
  复审顺带印证：泄漏 effort 后，走 planSend 的旧测试仍通过（pending ?? current 的回退让两值凑巧相等），
  只有直接断言原始字段才抓得到——这类盲区的成因清楚了。
Task 4: complete (commits 68b69d9..59248e6, review clean)

Ruling 9（接口缺口 · 退回沙箱模式没有可用字段）：按 Ruling 5，Codex 走 exec --json、
capabilities.approval = []，spec 要求「UI 自动退回沙箱级别选择」。但 Task 1 定的
CliCapabilities 与 StartOpts 里**没有任何字段能表达沙箱级别**——UI 退回后无从取值。
决定：**扩两个字段**（纯加法，不破坏已完成的任务）：
  · `CliCapabilities.sandboxLevels?: { id: string; label: string }[]`
  · `StartOpts.sandbox?: string`
「sandbox」是行业通用词不是 Codex 专有概念，不违反「中间模型不含 CLI 特有字段」。
并在 Task 5 加一条自洽性测试：**任何报了 approval:[] 的 adapter 必须给出非空 sandboxLevels**，
否则 UI 会显示一片空白。
代价若判错：字段是可选的，将来若发现更好的抽象（比如统一成 permissionLevel），改名成本低。
T5 dispatched (base 59248e6, model sonnet)
Task 5: 实现完成 aab7ab8，308 通过（10 条照抄 + **20 条自补**，9 轮变异验证）。
  按 Ruling 7 抓到简报两个真缺口：① Claude 的 buildArgs 测试传了 model 却不检查 --model 是否
  落进 args（删掉那行 push 全绿）；② Codex 的 --sandbox 只检查 flag 存在不检查值，
  静默翻成 read-only 能过全部测试——正是「写文件被静默拒绝」那个坑。两处已被新断言锁死。

Ruling 10（接口缺口 · adapter 无法声明 stdin 需求）：实现者报告 `< /dev/null` 这个实测得来的
Codex 启动要求**在 CliAdapter 接口里没有位置**——buildArgs 只返回 {bin, args}，没有 stdio 配置。
不修的话 Task 8 会踩「Codex 卡在 Reading additional input from stdin」这个已知坑，
而且靠 dispatch 口头传达，接第三个 CLI 时必然再漏一次。
决定：**buildArgs 返回值加 `stdin: 'pipe' | 'ignore'`**。Claude 用 pipe（stdin 是送消息的通道），
Codex exec 用 ignore（等价于 < /dev/null）。这符合能力声明的精神：adapter 知道自己怎么启动，
不靠下游记住每个 CLI 的怪癖。加一条测试锁住两者的值。
代价若判错：字段是必填的联合类型，将来若有 CLI 需要第三种 stdio 形态，加一个成员即可。
Task 5: fix round 1/5 — 3b50c7a，310 通过。buildArgs 加必填 stdin 字段，双向变异各精确命中。
Task 5: 审查 Approved（无 Critical/Important）。核心判据「加第三个 CLI 时 UI 零改动」经核实成立：
  index.ts 无任何 CLI 特判，capabilities 足以驱动 UI 渲染。审查者独立复验了 4 组变异
  （含要求的 2 条缺口 + 自设 2 条），全部精确命中且证实缺口属实。
Task 5: minor (deferred): WHICH_BIN 常量与 detectByWhich() 在 claude.ts / codex.ts 里
  **逐字节重复**，第三个 CLI 接入时会第三次复制粘贴。建议抽到 adapters/detect.ts。
  这条与「加第三个 CLI 零改动」的目标相关，**最终评审请优先 triage**。
Task 5: minor (deferred): buildArgs 返回类型在两个 adapter 里各手写一遍内联类型，
  已被 `: CliAdapter` 外层约束覆盖，冗余但无害。
Task 5: complete (commits 59248e6..3b50c7a, review clean)
T6 dispatched (base 3b50c7a, model sonnet)
Task 6: 实现完成 c11021a，324 通过（6 条照抄 + 8 条自补，8 组变异验证）。
  按 Ruling 7 抓到两个给定测试完全无感的坑：
--
Ruling 11（顾虑 1 · hook 超时上限 —— **实测推翻，不成立**）：实现者担心 Claude Code 的
PreToolUse hook 有 60s 默认上限，会先于我们的 APPROVAL_TIMEOUT_MS(5min) 触发。
实测（2.1.232，不设 timeout 字段，hook 睡 70s）：**hook 跑满 70 秒未被切断**，
正常返回 allow，文件真的被创建，总耗时 86s。
决定：**不需要给 hook entry 加 timeout 字段**，APPROVAL_TIMEOUT_MS 保持 5 分钟。
已验证到 70 秒；更长未验，Task 9 的 e2e 可顺带探。
代价若判错：若真实上限在 70s~5min 之间，症状是用户思考过久后审批卡片失效——
可观察（卡片没反应），且加 timeout 字段是一行修复。

Ruling 12（顾虑 2 · 打包清单缺 agent-hooks —— **成立，必须修**）：核实
package.json 的 extraResources 只有 mcp/skills/hooks 三项，**没有 agent-hooks**。
后果：打包后 hook 脚本不存在，装出来的应用**审批功能完全是死的**，而开发环境跑 e2e 却能过。
决定：**在 Task 8 修**（它本来就要算 hook 脚本的 dev-vs-packaged 绝对路径，
仓库里 mcpBridge.ts 的 serverScriptPath() 是现成的样板）。
同时 Task 9 的 e2e 必须**同时覆盖开发路径与打包路径的解析**，否则这个坑测不出来。
Task 7: minor (deferred): mcpBridge.ts 新增的 import 带 .ts 后缀，与该文件其余 import 的
  无后缀风格不一致（两者都能工作，已验证）。
Task 7: 审查批准（无 Critical、无阻塞 Important）。审查跑了 20 组畸形响应喂真实 hook 脚本
  （空 body / {} / 大写 ALLOW / 布尔 true / null / 数组 / 非 JSON / 500 里塞 "allow" / 401 /
  302 / 连接摧毁 / ECONNREFUSED / 缺端口 / 缺 token / 空 stdin / 垃圾 stdin / 永久挂起…），
  **19 条全部正确 deny，0 条意外放行**；另跑 500 次子进程压测 stdout 截断（0 次复现）；
  通读 mcpBridge.ts 确认是纯 additive、既有路由与 /health 豁免未受扰动。

Ruling 13（规格缺口 · createApprovalRegistry 未被消费 —— **在 Task 7 补，不推给 Task 8**）：
简报 Interfaces 写了消费 createApprovalRegistry，但实现只取了 approvalId，
**payload 的 tool_name/tool_input/cwd 被丢弃**——而那是审批卡片要显示的全部内容。
决定：**在 Task 7 补一轮小修**，而不是留给 Task 8。理由：Task 8 已经很重（进程管理 +
打包路径解析 + kill-before-spawn + stdin 声明），再叠一个"回头改 Task 7 的文件"会让责任边界糊掉；
这是 Task 7 自己的接口缺口，在本任务内修更内聚。
修法边界：**Task 7 只负责把 payload 存下来并可被订阅**（pending 表存 {payload, resolve}，
导出订阅接口），**不产出 ChatEvent、不碰 session 概念**——那仍是 Task 8 的事。
实现者当初正是怕跟 Task 8 的设计冲突才没做，这个顾虑是对的，所以边界要划清楚。
代价若判错：若 Task 8 发现订阅接口形状不合用，改签名的成本很小（只有一个消费方）。
Task 7: fix round 1/5 — 08921c5，351 通过。三条均落地（payload 留住+订阅接口 /
  process.exitCode 防截断 / fetch 加 AbortSignal 超时），且边界守住：
  mcpBridge.ts 未 import createApprovalRegistry，session 概念仍留给 Task 8。
  （实现者一度卡在等自己起的后台验证信号，由控制方代跑验证后解开僵局。）
Task 7: 复审者也卡在等自己起的后台任务（同 Task 7 实现者，本会话第二次出现该模式）。
  **控制方亲自完成对抗性验证**，不再依赖它：
  · 20 组畸形输入真 spawn hook 脚本（正常 allow/deny、空 body、{}、大写 ALLOW、布尔 true、
    JSON null、数组、非 JSON、decision 嵌别处、500 含 allow 字样、401、302、缺端口、缺令牌、
    连接被拒、连接被摧毁、空 stdin、垃圾 stdin、服务端永不回应）
    → **0 次意外放行**，输出均为完整合法 JSON（②防截断有效）
  · ③ 超时：把 FETCH_TIMEOUT_MS 临时改成 3000ms 重跑「服务端永不回应」
--
Ruling 14（hook 装不上时怎么办 —— **告知而非阻断**）：既然 hook 失效是 fail open，
必须让用户知道「这次会话没有审批保护」。可选：(A) 推一条非致命 error 事件告知并继续；
(B) 直接拒绝启动会话。
决定：**选 A**。理由：用户在未注册目录里临时用是合理需求，直接阻断会把正当用法也堵死；
但**必须**让他看见这次没有审批保护，而不是默默降级。
ChatEvent 里现成就有 { k:'error', fatal:false } 这个非致命信号位，目前完全没被利用。
代价若判错：若实测发现用户容易忽略这条提示，再收紧成阻断（那是加法）。
Task 8: minor (deferred): hookCommand 的引号只堵空格未转义内部引号（暴露面窄，路径非用户可输入）。
Task 8: minor (deferred): sessions Map 仅在显式 stop 时清理，渲染层忘记调用会留下小对象无界增长。
Task 8: 判定 —— 自加的 agentChat:setParams **合理**（审查同判）：骨架显式 import 了
  applyParamChange，不给入口的话「改模型/effort 下条生效」这条已拍板决定将永远无法从产品面触发，
  而 Task 8 是流水线最后一个实现任务，没有后续任务会回来补。
Task 8: fix round 1/5 — 3111736，354 通过。① nodeBinForHook 兜底改成 process.execPath +
  spawn 时注入 ELECTRON_RUN_AS_NODE=1（走进程环境继承，不依赖 shell 语法，Windows 也成立）；
  installApprovalHook 返回 {ok,reason?}，失败推 {k:'error',fatal:false} 并讲清后果。
  ② 照抄 preload 里 pty.create 的 startBuffering/stopBuffering 先例，start() 一 resolve 就缓冲，
  onEvent 订阅时先回放再切实时。

⚠️ 控制方疏漏（记下来，别再犯）：实现者验证时第一次未清环境变量，
  **向用户正在运行的正式版 Eas-Term 实例发了一次真实请求**（返回 404，旧构建无此路由，
  未落进 handler、无残留，影响为零），随后自行改用 env -u 重跑并如实交代。
--
Ruling 15（**该裁而没裁的那条 —— 我的重大疏漏**）：14 条裁定覆盖了计划缺陷、接口缺口、
范围取舍，**唯独漏了侵入性最强的这条**：往用户项目的 .claude/settings.json 装一个阻塞式
hook，要不要征求同意 / 备份 / 能卸载。它被当成 Task 6/7/8 的机械实现细节处理了，
而仓库早就为一模一样的场景立过规矩。C1 就是从这个空缺里长出来的。
决定：**遵守仓库既有规矩**，本轮修复必须做到三件：
  ① **不误伤**（最关键）：spawn 时注入 EAS_AGENT_CHAT_SESSION=<sessionId>，
     hook 读不到就 **exit 0 且不输出任何内容**（按 Claude Code hook 约定 = 「本 hook 无意见」，
     走正常权限流程）。这一条同时把审批路由从「拿 session_id 猜」改成「点名」，
     顺带关掉 I1 的一半。**不能复用 EAS_PROJECT/EAS_TERM_TOKEN——PTY 也有那些。**
  ② **写前备份**（照抄 agentHook.ts:writeJson）
  ③ **补卸载入口 + 状态展示**（对齐 hook:uninstall / hook:status 的既有形状）
  「显式征求同意」这条涉及 UI，UI 属子项目 B，**写进 B 的验收标准**，不在本轮。
代价若判错：标记隔离若失效，症状回到 C1（可观察：用户自己的 claude 卡住），不是静默。
最终评审修复波完成：87d8d37(C1) / 8972b82(I1,I3,I5,I6) / 957567e(6 条小的)。
  388 通过（354→388），typecheck/build 干净。
  C1 三件都做了：EAS_AGENT_CHAT_SESSION 隔离（hook 读不到就静默 exit 0）、写前备份、
  hookStatus/hookUninstall IPC。实现者直接验了 4 种 env 场景，**确认 PTY 终端误伤那一例现在是静默无操作**。
  I1 加了「用户超时后才点允许」的回归测试，确认不再把实际的 deny 标成 allow。
  小项第 4 条（effort 通道覆盖）**查实早已在 59248e6 修过**，是 deferred-minors.txt 没更新。
  实现者顾虑：**e2e 未重跑**。控制方判定必须重跑——C1 把审批路由从 resumeId 猜改成
  eas_session_id 点名，那是闭环的根本机制，不重跑等于让整个 e2e 证据作废。

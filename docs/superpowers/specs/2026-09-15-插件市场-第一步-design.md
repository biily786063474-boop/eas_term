# 插件市场 · 第一步：官方目录 + 一键安装 · 设计

> 讨论背景：竞品(OpenAI Apps SDK / Anthropic marketplace / Cursor)的插件市场都收敛到
> 「MCP server 目录 + 一键安装 + 审核」。Eas-Term 的自家插件系统已经具备协议层(MCP)、
> UI 层(`eas-plugin://` sandbox iframe + `panels`)、权限层(`permissions` + `CANVAS_CALL_ALLOWLIST`)——
> 缺的只有**分发层**。本文只定第一步:**做官方目录 + 一键装到 `~/.eas/plugins/`**,不碰第三方提交/审核(第二步)。

## 目标 / 非目标

**目标**
- 一个可订阅的**官方远程目录**(`registry.json`),托管在 `eas.biily.top`。
- 插件面板里能**浏览目录、一键安装/更新/卸载**自家(`cli: 'eas'`)插件,落到 `~/.eas/plugins/<name>/`。
- 安装前**展示插件要的权限**,用户确认后才装(延续「不静默装」红线)。
- 复用现有 `plugin.json` schema 与 `parseManifest`,不新造清单格式。

**非目标(留给第二步)**
- 第三方开发者提交 / 审核 / 签名信任链。
- MCPB(`.mcpb`)跨生态打包 —— 第一步只做自家 `.zip`,想跨 Claude/Cursor 生态时再上 MCPB。
- 给 Claude/Codex 生态插件做一键装(那两条仍是「预填命令让用户按回车」,`plugins.ts` 的既有红线不动)。

## 现状(已有的,别重造)

| 层 | 已有 | 位置 |
|---|---|---|
| 清单 schema | `plugin.json`(name/displayName/description/category/brandColor/composerIcon/defaultPrompt/mcp/panels/permissions) | `resources/plugins/*/plugin.json` |
| 清单校验(纯函数) | `parseManifest`:name 须等目录名、相对路径不许跳出目录、权限过 `CANVAS_CALL_ALLOWLIST`、能修的修修不了的整份拒 | `src/main/pluginManifest.ts` |
| 加载/聚合 | `easPluginsIn(root, builtin)` → `listPlugins()`(用户 `~/.eas/plugins/` + 内置,同名用户覆盖) | `src/main/plugins.ts` |
| IPC | 仅 `plugins:list`(只读) | 同上 |
| 渲染 UI | `CanvasFilePicker` 的「插件」tab(展示已装、开对话/开面板) | `renderer/…/CanvasFilePicker.tsx` |
| 下载/临时文件模式 | `updater.ts`:https + `.part` 临时文件 + rename | `src/main/updater.ts` |

## 架构与安装流程

```
官方目录(服务器)                         客户端(主进程)                   用户
eas.biily.top/plugins/registry.json  ──拉取──▶ plugins:registry(缓存+离线兜底)
eas.biily.top/plugins/<name>/<name>-<ver>.zip                         浏览目录/点「安装」
                                     ◀──下载── plugins:install
                                              1. https 下载 zip → .part
                                              2. 校验 sha256(registry 声明)
                                              3. 防 zip-slip 解压到临时目录
                                              4. parseManifest 校验(必过)
                                              5. 展示 permissions ──确认──▶ 用户确认
                                              6. 原子移入 ~/.eas/plugins/<name>/
                                              7. plugins:list 刷新
```

## Registry schema(`registry.json`)

```jsonc
{
  "schema": 1,
  "updated": "2026-09-15T00:00:00Z",
  "plugins": [
    {
      "name": "board",                    // 须匹配 plugin.json.name，落盘目录名
      "displayName": "看板",
      "description": "项目待办三栏看板…",
      "category": "Productivity",
      "brandColor": "#5E6AD2",
      "version": "1.0.0",                  // 语义版本，比对提示更新
      "url": "https://eas.biily.top/plugins/board/board-1.0.0.zip",
      "sha256": "…64hex…",                 // 强制校验，registry 与包不一致即拒
      "size": 6421,                        // 下载前告知体积；下载超限即中止
      "permissions": { "canvas": ["canvas_open_file"] }  // 安装前展示，与包内 plugin.json 一致
    }
  ]
}
```

- **来源信任 = HTTPS + 官方域名**。第一步官方目录自己维护,不做签名;第三方/签名留第二步。
- `permissions` 在 registry 里冗余一份,是为了**下载前就能展示**;装完以包内 `plugin.json` 的 `permissions` 为准(两者不一致 → 拒,防目录谎报权限)。

## 包格式(`.zip`)

- 内容 = 一个插件目录的完整内容:`plugin.json` + `server.mjs` + `ui/…`(即 `resources/plugins/<name>/` 原样打包)。
- 解压后根部必须直接是 `plugin.json`(不允许多套一层目录)。
- 打包脚本:`scripts/pack-plugin.mjs <plugin-dir>` → 产出 `<name>-<version>.zip` + 打印 sha256/size(填 registry 用)。

## 安全模型(六条,每条对应一个纯函数/守卫)

1. **写入边界**:新 `guardPluginDir(name)` —— 只允许 `~/.eas/plugins/<name>/`,`name` 过 `NAME_RE`(同 `pluginManifest`)。不复用 `fsGuard`(那个边界是项目根+知识库根)。纯函数,可测。
2. **zip-slip 防护**:解压前每个条目的目标路径 `path.resolve(dest, entry)` 必须仍在 `dest` 内,否则整包拒。纯函数 `safeExtractPaths(entries, dest)`,可测。
3. **sha256 强制**:下载完算 sha256,与 registry 声明比对,不符即拒 + 清理。
4. **清单必过**:解压后 `parseManifest` 必须 `ok`,否则拒 + 清理(装进去一个跑不了/越权的插件比不装更糟)。
5. **权限确认**:安装前弹确认框展示 `permissions`(哪些 canvas 工具),用户点确认才装。**延续「不静默装」红线**(同 `agentInstall`/`plugins.ts`)。
6. **卸载边界**:只删 `~/.eas/plugins/<name>/`;`builtin`(`resources/plugins/`)与 Claude/Codex 插件不可从这里卸载(`plugins.ts` 只读纪律不破)。

## 任务分解(实施时每个展开为 TDD 逐步)

| # | 任务 | 文件 | 接口 / 测试要点 |
|---|---|---|---|
| 1 | Registry 校验(纯函数) | `main/pluginRegistry.ts` (+test) | `parseRegistry(raw): {ok,entries[]}｜{ok:false,errors}`。坏条目丢弃记 warning、整份格式错才拒(同 parseManifest 原则);sha256/version/url 格式校验;url 必须 https + 官方域名 |
| 2 | 安装安全纯逻辑 | `main/pluginInstall.ts` (+test) | `guardPluginDir(name)`、`safeExtractPaths(entries,dest)`、`verifySha256(buf,expected)`;zip-slip / 越权 name / 哈希不符各一条红灯 |
| 3 | 主进程 registry + install/uninstall | `main/plugins.ts` 扩展 | `plugins:registry`(拉取 https,缓存 `userData/plugin-registry.json`,离线兜底);`plugins:install(name)`(下载→校验→解压临时→parseManifest→**返回待确认权限**→确认后落盘);`plugins:uninstall(name)`;下载复用 updater 的 `.part`+rename 模式 |
| 4 | 确认闸门 | `main/plugins.ts` | 安装分两段:`plugins:install` 先下载校验解压到临时、返回 `{name, permissions, size}`;渲染层展示权限、用户确认后调 `plugins:installCommit(token)` 才移入 `~/.eas/plugins`。token 绑定临时目录 + 30 秒期限(参考 runtime stopGate 的一次性凭证) |
| 5 | preload api | `preload/index.ts` | `api.plugins.registry()` / `install(name)` / `installCommit(token)` / `uninstall(name)` |
| 6 | 渲染 UI | `CanvasFilePicker.tsx` 插件 tab 加「发现」区 | 列 registry 条目(已装标「已安装/可更新」);「安装」→ 确认弹窗展示权限 → 装 → 刷新;卸载按钮(仅 `cli:'eas'` 用户装的) |
| 7 | 服务器 + 打包 + 图纸/验收 | `scripts/pack-plugin.mjs`、`scripts/publish-plugins.sh`、图纸 11/13、`docs/verification/plugin-marketplace/` | 打包脚本产出 zip+sha256;发布脚本逐个 scp 到 `/www/wwwroot/eas/plugins/`(复用 publish-site 模式);把内置 board/computer 作为首批目录条目;CDP 真机验收装一个插件 |

## 分两步的边界(别越界)

- **第一步(本文)**:官方目录、自家 `.zip`、HTTPS+域名信任、一键装到 `~/.eas/plugins`。纯工程,风险可控,几天可落地,对你和用户立刻有用(装官方插件更顺 + 建好分发管线)。
- **第二步(想清楚再做)**:第三方提交 + 审核 + 签名 + MCPB 跨生态。这才进入**生态运营**,取决于有没有第三方开发者需求信号——是长期投入,不是代码问题。

## 已定决策(2026-09-15)

1. **托管**:第一步先用 `eas.biily.top/plugins/` 静态目录(复用 `publish-site.sh` 模式)。用户的对象存储是**阿里云 OSS**,就绪后把发布脚本的上传目标 + registry 里的 `url` 切到 OSS/CDN 域名——**对客户端透明**(客户端只认公开 HTTPS URL)。OSS 发布凭证(AccessKey)不在密钥金库,切换时再向用户取位置(不贴 key)。
2. **信任**:第一步不做签名,只 HTTPS + sha256 + 官方域名信任(第一步只上自己的插件,来源可信)。第三方 + 签名留第二步。

## 收录范围(回答「会不会有竞品同款插件」)

MCP 标准化的最大红利:**竞品市场里的插件绝大多数是标准 MCP server,你的市场可以直接收录,不用重开发。**分两层:

- **纯工具插件(无 UI)**:任何标准 MCP server(官方 `filesystem` / `fetch` / `github` / `slack` / `notion` 等)都能进你的 registry、一键装,行为与竞品市场同款。`plugins.ts` 现在已经能显示用户在 Claude/Codex 里装的 MCP 插件——Eas-Term 本就是「多生态聚合器」。
- **带 UI 的插件(panels)**:各家 UI 协议不同(ChatGPT Apps 的 `_meta.ui.resourceUri` + MCP Apps bridge ≠ Eas-Term 的 `panels` + `eas-plugin://`)。底层工具通用,但富交互 UI 要适配成 `panels`。第一步先只收工具型 + 自家带 UI 的。

**第一步 registry 首批** = 自家插件(board / computer / eas-capabilities)+ 精选收录几个成熟开源 MCP server(工具型)。「市场有多丰富」取决于你收录多少 + 生态,不是技术卡点。

## 未来接口预留（账号 / 开发者贡献的落点，2026-09-15 补）

> 第一步只做「官方目录 + 一键装」。但为了以后接账号系统、开开发者贡献不返工，
> 这里把**接入点**先记清楚。**下面都是设计预留，尚未实现**——真做时照这些位置插。

### 一、账号系统：客户端只需在「拉取层」加一个口子

市场与身份**已解耦**——客户端就是「拉一个 URL 的 registry / 下 zip」。接账号时：

- **唯一改动点**：`pluginMarket.fetchBuffer(url)` 拉 registry / 下载时，给请求加 `Authorization: Bearer <token>`。
  token 从哪来（复用 app 已有登录 / 新开市场账号）是产品决定；拉取层加头是一处局部改动，渲染层与安装流程不动。
- registry 从「静态文件」升级成「带鉴权的 API」是**服务端**工作，客户端无感（它只认公开/受权的 HTTPS URL）。
- registry schema 加字段**向后兼容**（`parseRegistry` 丢弃不认识的字段、保留认识的）——老客户端不受影响。

### 二、registry 条目的预留字段（`parseRegistry` 里逐个加校验即可）

| 字段 | 用途 | 何时加 |
|---|---|---|
| `author` / `authorId` | 谁写的（开发者身份）| 开放贡献时 |
| `signature` | 作者私钥对包的签名，客户端验签（见下）| 开放贡献 + 签名信任时 |
| `verified` | 官方是否认证过这个作者 / 插件 | 有审核时 |
| `homepage` / `repo` | 详情页、源码链接 | 随时（展示用）|
| `ratings` / `downloads` | 评分、下载量（社区信号）| 有后端统计时 |
| `price` / `sku` | 付费插件 | 有账号 + 支付时 |

加字段的纪律同现有条目：**坏字段丢弃记 warning，不因一个新字段拒整份**。

### 三、信任链：两条路，签名插在现有的「校验」环节

现在的信任 = **HTTPS + 官方域名 + sha256**（保证没被篡改，不保证是谁写的）。第三方进来后：

- **路 A｜官方审核 + 托管**：所有第三方插件你过一遍、放你的域名。域名信任照旧，你是把关人。简单，但你是瓶颈。
- **路 B｜签名**：registry 条目带 `signature`，客户端用作者公钥 / 信任根验签。能规模化、去中心，但要建密钥基础设施。

**关键**：`installStage` 里**已经有一步 sha256 校验**（`verifySha256` 之后）。验签名就插在那个位置——
先验 sha256（完整性）再验 signature（authorship），两步都过才继续解压。install 流程的形状不变。

### 四、开发者贡献管线（第二步的服务端主体）

客户端**已就绪**：任何插件进来都过同一套 `parseManifest` 校验 + 权限确认 + zip-slip/sha256 安全安装，
第三方插件与自家插件同等对待、同等安全。要加的全在**服务端**：
1. 提交入口（开发者上传插件 zip + 元数据）
2. 审核 / moderation（自动检查 + 人工）
3. 把审核过的写进 registry（可能从静态文件升级成 DB + 生成 API）

### 五、必须先想清楚的产品决策（不是代码问题）

第三方插件会在用户机器上**跑真实代码**（MCP server 能联网、能读文件）。权限模型管住了画布这块，
但 MCP server 本身的能力管不住。所以「开放贡献」是**信任/审核的产品决策**：策展 allowlist ／ 开放 + 审核 ／ 签名 + 责任归属。
这是生态运营，取决于有没有第三方开发者的需求信号——是长期投入，先有信号再上。

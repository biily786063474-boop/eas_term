# 项目重命名与文件操作 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让项目区的「重命名文件夹」真改盘上的目录（与访达同步），历史会话不丢；顺带给文件面板加新建入口、给画板文件节点加「在访达中显示」。

**Architecture:** 改名是一次事务：先动盘（容易失败的先做）、再更新 `projects.json`。绝大多数功能（终端 cwd / Frame / 甘特图 / MCP 白名单 / git 面板）已经是「存 `projectId`、用时现查 `path`」，自动跟上、不需要改。唯一会断的历史会话，用「在 `projects.json` 记住旧路径、读 transcript 时把新旧编码目录一起找」解决，**不写 Claude Code 的目录**。

**Tech Stack:** Electron + React 18 + TypeScript + zustand；测试用 Node 26 原生 `node --test`（`.ts` 直接跑，零测试框架依赖）。

## Global Constraints

- **不放宽 `src/main/fsGuard.ts`。** 项目根改名走项目管理，这正是 `fsGuard.ts:86-87` 注释指明的路。新处理器自带**更窄**的守卫，不复用也不修改 `guardPath`/`guardDir`。
- **不写 `~/.claude/projects/` 下的任何东西。** 历史会话靠别名解析，只读不写。
- **不重写 `canvas.json` 里的 `filePath`。** 画布上已打开的文件节点改名后会显示「文件不存在」，这是设计定的。
- **需要被 `node --test` 覆盖的纯逻辑，必须放在不 import electron 的模块里。** `session.ts` 和 `projects.ts` 都 import 了 electron，`node --test` 加载不了它们。
- 测试一律 `node --test`，**不得引入任何测试框架依赖**，不得改 `package.json`。
- 现有测试一个都不能坏（当前 43 个，`node --test src/main/**/*.test.ts`）。
- `projects.json` 结构向后兼容：`pastPaths` 是**可选**字段，老配置不需要迁移。
- 改名失败时 `projects.json` **一个字节都不能变**。

---

## File Structure

| 文件 | 职责 | 新建/修改 |
|---|---|---|
| `src/main/sessionPaths.ts` | cwd → Claude Code transcript 目录的编码与候选解析。**不引 electron** | 新建 |
| `src/main/sessionPaths.test.ts` | 上面的测试 | 新建 |
| `src/main/session.ts` | 三个调用点改成走候选目录 | 修改 |
| `src/main/projectPaths.ts` | 改名的校验与新路径计算。**不引 electron** | 新建 |
| `src/main/projectPaths.test.ts` | 上面的测试 | 新建 |
| `src/main/projects.ts` | 新增 `projects:renameFolder` 处理器 | 修改 |
| `src/shared/types.ts` | `Project` 加 `pastPaths?`；新增 `RenameFolderResult` | 修改 |
| `src/preload/index.ts` | 暴露 `projects.renameFolder` | 修改 |
| `src/renderer/src/store/projectsSlice.ts` | `renameProjectFolder` action | 修改 |
| `src/renderer/src/features/workspace/projectMenu.ts` | 菜单加「重命名文件夹…」，原「重命名」改名为「改显示名」 | 修改 |
| `src/renderer/src/features/files/FileTree.tsx` | 加 `createRequest` prop（外部触发新建） | 修改 |
| `src/renderer/src/features/workspace/Sidebar.tsx` | 文件页签右侧加 `+` | 修改 |
| `src/renderer/src/features/canvas/CanvasFileNode.tsx` | 头部加「在访达中显示」 | 修改 |

---

### Task 1: transcript 目录的编码与候选解析

**Files:**
- Create: `src/main/sessionPaths.ts`
- Test: `src/main/sessionPaths.test.ts`

**Interfaces:**
- Consumes: 无
- Produces: `encodeCwd(cwd: string): string`、`candidateDirs(claudeProjectsRoot: string, cwd: string, pastPaths?: string[]): string[]`

**为什么单独一个文件**：`session.ts` 第一行就是 `import { ipcMain } from 'electron'`，`node --test` 加载不了它。把纯逻辑放这里才测得住，而这条判定错了会让整个项目的历史会话消失。

- [ ] **Step 1: 写会失败的测试**

创建 `src/main/sessionPaths.test.ts`：

```ts
import { test } from 'node:test'
import assert from 'node:assert'
import { encodeCwd, candidateDirs } from './sessionPaths.ts'

const ROOT = '/Users/me/.claude/projects'

test('encodeCwd：非字母数字一律换成短横线（照抄 Claude Code 的约定）', () => {
  assert.strictEqual(encodeCwd('/Users/me/Projects/foo'), '-Users-me-Projects-foo')
  assert.strictEqual(encodeCwd('/Users/me/vibe coding/terminal'), '-Users-me-vibe-coding-terminal')
  assert.strictEqual(encodeCwd('/a/b_c.d'), '-a-b-c-d')
})

test('没有旧路径时只有一个候选', () => {
  assert.deepStrictEqual(candidateDirs(ROOT, '/Users/me/foo'), [
    '/Users/me/.claude/projects/-Users-me-foo'
  ])
})

test('有旧路径时当前的排第一、旧的按给的顺序跟在后面', () => {
  const got = candidateDirs(ROOT, '/Users/me/new', ['/Users/me/old', '/Users/me/older'])
  assert.deepStrictEqual(got, [
    '/Users/me/.claude/projects/-Users-me-new',
    '/Users/me/.claude/projects/-Users-me-old',
    '/Users/me/.claude/projects/-Users-me-older'
  ])
})

test('旧路径编码后和当前撞了就不重复列（改回原名的情况）', () => {
  const got = candidateDirs(ROOT, '/Users/me/foo', ['/Users/me/foo', '/Users/me/bar'])
  assert.deepStrictEqual(got, [
    '/Users/me/.claude/projects/-Users-me-foo',
    '/Users/me/.claude/projects/-Users-me-bar'
  ])
})

test('空的/非法的旧路径条目直接跳过，不产生垃圾候选', () => {
  const got = candidateDirs(ROOT, '/Users/me/foo', ['', '   '] as string[])
  assert.deepStrictEqual(got, ['/Users/me/.claude/projects/-Users-me-foo'])
})
```

- [ ] **Step 2: 跑测试确认它失败**

Run: `node --test src/main/sessionPaths.test.ts`
Expected: FAIL —— `Cannot find module './sessionPaths.ts'`

- [ ] **Step 3: 写实现**

创建 `src/main/sessionPaths.ts`：

```ts
// cwd → Claude Code 存 transcript 的目录名。
//
// **不引 electron**：这样 node --test 能直接加载它。session.ts 引了 electron，
// 判定逻辑留在那边就测不住，而这条错了整个项目的历史会话会一起消失。
import path from 'path'

/** Claude Code 把 cwd 里的非字母数字全换成 '-' 当目录名。照抄它的约定，不要自己发挥。 */
export function encodeCwd(cwd: string): string {
  return cwd.replace(/[^a-zA-Z0-9]/g, '-')
}

/**
 * 这个 cwd 的 transcript 可能落在哪些目录里，当前路径排第一。
 *
 * 为什么要有「旧路径」：项目文件夹一改名，编码目录名就变了，改名前的会话留在老目录里。
 * 我们**不搬 Claude Code 的目录**（那是别人的地盘，而且它可能正开着在写），
 * 改成读的时候新旧一起找 —— 对用户来说改名前后无缝。
 */
export function candidateDirs(
  claudeProjectsRoot: string,
  cwd: string,
  pastPaths?: string[]
): string[] {
  const seen = new Set<string>()
  const out: string[] = []
  const push = (p: string): void => {
    if (!p || !p.trim()) return
    const enc = encodeCwd(p)
    if (seen.has(enc)) return
    seen.add(enc)
    out.push(path.join(claudeProjectsRoot, enc))
  }
  push(cwd)
  for (const p of pastPaths ?? []) push(p)
  return out
}
```

- [ ] **Step 4: 跑测试确认它通过**

Run: `node --test src/main/sessionPaths.test.ts && npm run typecheck`
Expected: 5 个用例全绿；类型检查无输出

- [ ] **Step 5: 提交**

```bash
git add src/main/sessionPaths.ts src/main/sessionPaths.test.ts
git commit -m "feat(session): transcript 目录的编码与候选解析

单独成文件是为了能被 node --test 加载 —— session.ts 引了 electron，
逻辑留在那边测不住，而这条判定错了整个项目的历史会话会一起消失。"
```

---

### Task 2: session.ts 接上候选目录

**Files:**
- Modify: `src/main/session.ts:20-23`（`projectDir`）、`:198`、`:211-219`、`:236-242`

**Interfaces:**
- Consumes: Task 1 的 `candidateDirs`
- Produces: 无（内部改造）

- [ ] **Step 1: 先看清三个调用点长什么样**

Run: `grep -n "projectDir(\|latestJsonl(" src/main/session.ts`
Expected: 5 行 —— 定义在 20，`latestJsonl` 定义在 25，调用在 198 / 211 / 219 / 236 / 242。
三处调用的形状是一样的：先算目录，再「有 sessionId 就按文件名找，否则取最新」。

- [ ] **Step 2: 加一个读 projects.json 拿 pastPaths 的辅助函数**

在 `session.ts` 的 import 区加：

```ts
import { app } from 'electron'
import { candidateDirs } from './sessionPaths'
```

（`ipcMain` 那行已有 `from 'electron'`，把 `app` 并进去即可，不要写成两条 import。）

然后替换掉原来的 `projectDir`（20-23 行整个函数）：

```ts
/** 直接读 projects.json 而不是从 projects.ts 导入：那边注册 IPC，import 进来会有副作用。
 *  和 fsGuard 用的是同一套做法（现读、不缓存），所以改名后立刻生效、不用重启。 */
function pastPathsOf(cwd: string): string[] {
  try {
    const raw = fs.readFileSync(path.join(app.getPath('userData'), 'projects.json'), 'utf8')
    const list = JSON.parse(raw) as { path?: string; pastPaths?: string[] }[]
    if (!Array.isArray(list)) return []
    return list.find((p) => p?.path === cwd)?.pastPaths ?? []
  } catch {
    return [] // 读不到就只找当前目录，退化但不出错
  }
}

/** 这个 cwd 的 transcript 可能落在哪些目录，当前的排第一。
 *  项目改过名的话，改名前的会话还在老编码目录里 —— 一起找，不搬别人的目录。 */
function projectDirs(cwd: string): string[] {
  const root = path.join(os.homedir(), '.claude', 'projects')
  return candidateDirs(root, cwd, pastPathsOf(cwd))
}

/** 在候选目录里定位 transcript：优先按 sessionId 精确命中，否则取所有候选里 mtime 最新的。
 *  三个 handler 原本各写一遍这套逻辑，收在这里，免得改一处漏两处。 */
function findJsonl(cwd: string, sessionId?: string): string | null {
  const dirs = projectDirs(cwd)
  if (sessionId && /^[\w-]+$/.test(sessionId)) {
    for (const d of dirs) {
      const p = path.join(d, `${sessionId}.jsonl`)
      if (fs.existsSync(p)) return p
    }
  }
  let best: string | null = null
  let bestM = -1
  for (const d of dirs) {
    const f = latestJsonl(d)
    if (!f) continue
    try {
      const m = fs.statSync(f).mtimeMs
      if (m > bestM) {
        bestM = m
        best = f
      }
    } catch {
      // 跳过读不到的
    }
  }
  return best
}
```

- [ ] **Step 3: 三个 handler 改成用 findJsonl**

`session:index`（原 198 行）：

```ts
      const file = findJsonl(cwd)
```

`session:exchange`（原 211-219 行，把 `const dir = ...` 到 `if (!file) file = latestJsonl(dir)` 整段替换）：

```ts
        const file = findJsonl(cwd, sessionId)
```

`session:last`（原 236-242 行，同样整段替换）：

```ts
      const file = findJsonl(cwd, sessionId)
```

改完 `projectDir` 这个名字应该在文件里彻底消失。

- [ ] **Step 4: 验证没有残留、类型通过**

Run: `grep -n "projectDir(" src/main/session.ts ; npm run typecheck && npm run build`
Expected: `grep` 无输出（只剩 `projectDirs`）；typecheck 无输出；build 成功

- [ ] **Step 5: 手工验证现有行为没变（还没有 pastPaths，等价于改造前）**

Run:
```bash
ls ~/.claude/projects/ | head -3
node -e "
const {candidateDirs}=require('./src/main/sessionPaths.ts');
console.log(candidateDirs(process.env.HOME+'/.claude/projects', process.cwd()));
" 2>/dev/null || echo '（.ts 直接 require 不行时跳过，Step 4 的 typecheck 已够）'
```
Expected: 打出来的目录名应该能在 `ls` 的结果里找到 —— 说明编码规则没抄错

- [ ] **Step 6: 提交**

```bash
git add src/main/session.ts
git commit -m "refactor(session): transcript 定位改成在候选目录里找

三个 handler 原本各写一遍「有 sessionId 就按文件名找、否则取最新」，收成 findJsonl。
候选目录 = 当前路径 + projects.json 里记的旧路径 —— 为项目改名做准备：
改名后老会话还在老编码目录里，一起找就不会丢，而我们一个字节都不写 Claude Code 的目录。"
```

---

### Task 3: 改名的校验与新路径计算

**Files:**
- Create: `src/main/projectPaths.ts`
- Test: `src/main/projectPaths.test.ts`

**Interfaces:**
- Consumes: 无
- Produces: `planRename(input): RenamePlan`，其中

```ts
export interface RenameInput {
  projects: { id: string; name: string; path: string; pastPaths?: string[] }[]
  projectId: string
  newName: string
  /** 知识库路径，用来判断它是不是落在这个项目里 */
  wikiPath?: string | null
}
export type RenamePlan =
  | { ok: false; error: string }
  | { ok: true; oldPath: string; newPath: string; renameDisplayName: boolean }
```

**为什么单独一个文件**：`projects.ts` import 了 electron。守卫逻辑错了就是安全问题，必须测得住。

- [ ] **Step 1: 写会失败的测试**

创建 `src/main/projectPaths.test.ts`：

```ts
import { test } from 'node:test'
import assert from 'node:assert'
import { planRename } from './projectPaths.ts'

const base = [
  { id: 'a', name: 'terminal', path: '/Users/me/Projects/terminal' },
  { id: 'b', name: '我的画板', path: '/Users/me/Projects/canvas' }
]
const run = (over: Partial<Parameters<typeof planRename>[0]> = {}) =>
  planRename({ projects: base, projectId: 'a', newName: 'eas-term', ...over })

test('正常改名：算出新路径，留在同一个父目录里', () => {
  const r = run()
  assert.strictEqual(r.ok, true)
  if (!r.ok) return
  assert.strictEqual(r.oldPath, '/Users/me/Projects/terminal')
  assert.strictEqual(r.newPath, '/Users/me/Projects/eas-term')
})

test('没自定义过展示名（name === 目录名）→ 展示名跟着改', () => {
  const r = run()
  assert.strictEqual(r.ok && r.renameDisplayName, true)
})

test('自定义过展示名 → 不动它，只改路径', () => {
  const r = planRename({ projects: base, projectId: 'b', newName: 'board' })
  assert.strictEqual(r.ok, true)
  assert.strictEqual(r.ok && r.renameDisplayName, false)
})

test('项目不存在 → 拒绝', () => {
  assert.strictEqual(planRename({ projects: base, projectId: 'zzz', newName: 'x' }).ok, false)
})

test('名字含斜杠 → 拒绝（借机移动位置是不允许的）', () => {
  const r = run({ newName: '../evil' })
  assert.strictEqual(r.ok, false)
  assert.match(r.ok ? '' : r.error, /斜杠|名称/)
})

test('名字以点开头 → 拒绝（会变成隐藏目录）', () => {
  assert.strictEqual(run({ newName: '.hidden' }).ok, false)
})

test('名字为空或全空白 → 拒绝', () => {
  assert.strictEqual(run({ newName: '   ' }).ok, false)
})

test('新名字和现在一样 → 拒绝（没有要做的事，别走一遍写盘）', () => {
  assert.strictEqual(run({ newName: 'terminal' }).ok, false)
})

test('知识库正好在这个项目里 → 拒绝，并说清原因', () => {
  const r = run({ wikiPath: '/Users/me/Projects/terminal/wiki' })
  assert.strictEqual(r.ok, false)
  assert.match(r.ok ? '' : r.error, /知识库/)
})

test('知识库在别处 → 不受影响', () => {
  assert.strictEqual(run({ wikiPath: '/Users/me/Documents/eas-wiki' }).ok, true)
})

test('知识库路径恰好等于项目根 → 也要拒绝', () => {
  assert.strictEqual(run({ wikiPath: '/Users/me/Projects/terminal' }).ok, false)
})

test('新路径会撞上另一个已注册项目 → 拒绝', () => {
  const r = planRename({
    projects: [...base, { id: 'c', name: 'eas-term', path: '/Users/me/Projects/eas-term' }],
    projectId: 'a',
    newName: 'eas-term'
  })
  assert.strictEqual(r.ok, false)
})
```

- [ ] **Step 2: 跑测试确认它失败**

Run: `node --test src/main/projectPaths.test.ts`
Expected: FAIL —— `Cannot find module './projectPaths.ts'`

- [ ] **Step 3: 写实现**

创建 `src/main/projectPaths.ts`：

```ts
// 项目文件夹改名的校验与新路径计算。
//
// **不引 electron**：守卫逻辑错了就是安全问题，必须能被 node --test 直接打到。
// 也**不复用 fsGuard**：那套是「必须在某个项目**内部**」，而这里操作的正是项目根本身。
// fsGuard.ts:86-87 的注释早就写了「改名/删除一个项目根应该走项目管理，不是文件树」——
// 这个文件就是那条路。它的守卫比 fsGuard **更窄**，不是更宽。
import path from 'path'

export interface RenameInput {
  projects: { id: string; name: string; path: string; pastPaths?: string[] }[]
  projectId: string
  newName: string
  /** 知识库路径。它在 wiki.json 里、是另一套配置，不跟着项目改名走，所以要挡住 */
  wikiPath?: string | null
}

export type RenamePlan =
  | { ok: false; error: string }
  | {
      ok: true
      oldPath: string
      newPath: string
      /** 展示名要不要跟着改：用户从没自定义过（name === 旧目录名）才改，
       *  自定义过的不动 —— 那是他特意设的，不能替他丢掉 */
      renameDisplayName: boolean
    }

/** 只允许改最后一段。这三条各自对应一种真实的坏结果：
 *  斜杠 → 借机移动到别的目录；点开头 → 变成隐藏目录，人在访达里找不到；
 *  控制字符 → 造出打不开的名字。 */
function badName(name: string): string | null {
  if (!name || !name.trim()) return '名字不能为空'
  if (name !== name.trim()) return '名字前后不能有空格'
  if (name.includes('/') || name.includes('\\')) return '名字里不能有斜杠'
  if (name.startsWith('.')) return '名字不能以点开头（那会变成隐藏文件夹）'
  // eslint-disable-next-line no-control-regex
  if (/[\x00-\x1f\x7f]/.test(name)) return '名字含不可见字符'
  if (name.length > 255) return '名字太长'
  if (name === '.' || name === '..') return '这不是一个合法的文件夹名'
  return null
}

const isInside = (child: string, parent: string): boolean =>
  child === parent || child.startsWith(parent + path.sep)

export function planRename(input: RenameInput): RenamePlan {
  const { projects, projectId, newName, wikiPath } = input
  const p = projects.find((x) => x.id === projectId)
  if (!p) return { ok: false, error: '找不到这个项目' }

  const bad = badName(newName)
  if (bad) return { ok: false, error: bad }

  const oldPath = p.path
  const parent = path.dirname(oldPath)
  const newPath = path.join(parent, newName)

  if (newPath === oldPath) return { ok: false, error: '新名字和现在一样' }
  // path.join 会把 'a/../b' 这类归一化掉，所以这条是最后一道防线：
  // 归一化之后仍然必须是同一个父目录下的直接子项
  if (path.dirname(newPath) !== parent) return { ok: false, error: '只能改名字，不能换位置' }

  if (wikiPath && isInside(wikiPath, oldPath)) {
    return {
      ok: false,
      error: '知识库就在这个项目里。知识库路径存在另一份配置里、不会跟着改名走，改了会让它失联——先把知识库挪出去，或者换个项目改'
    }
  }

  if (projects.some((x) => x.id !== projectId && x.path === newPath)) {
    return { ok: false, error: '已经有另一个项目用着这个位置' }
  }

  return {
    ok: true,
    oldPath,
    newPath,
    renameDisplayName: p.name === path.basename(oldPath)
  }
}
```

- [ ] **Step 4: 跑测试确认它通过**

Run: `node --test src/main/projectPaths.test.ts && npm run typecheck`
Expected: 12 个用例全绿；类型检查无输出

- [ ] **Step 5: 提交**

```bash
git add src/main/projectPaths.ts src/main/projectPaths.test.ts
git commit -m "feat(projects): 文件夹改名的校验与新路径计算

不引 electron 所以测得住；不复用 fsGuard 因为那套是「必须在项目内部」，
而这里操作的正是项目根本身 —— fsGuard.ts:86 的注释早就指明这条路该走项目管理。
守卫比 fsGuard 更窄：必须是已注册项目、只改最后一段、不许换位置、知识库在里面就拒绝。"
```

---

### Task 4: 主进程处理器与对外接口

**Files:**
- Modify: `src/shared/types.ts:19-27`（`Project`）
- Modify: `src/main/projects.ts`（新增 handler）
- Modify: `src/preload/index.ts:116-120` 附近

**Interfaces:**
- Consumes: Task 3 的 `planRename`
- Produces: IPC `projects:renameFolder(id, newName)` → `Promise<RenameFolderResult>`，
  preload 暴露为 `window.api.projects.renameFolder(id, newName)`

```ts
export interface RenameFolderResult {
  ok: boolean
  error?: string
  /** 成功时给回新的完整列表，渲染层直接换掉，不用再拉一次 */
  projects?: Project[]
}
```

- [ ] **Step 1: 类型**

`src/shared/types.ts` 的 `Project` 接口加一个字段（放在 `status?` 后面）：

```ts
  /** 这个项目曾经在哪些路径上。改名时追加旧路径，只用来找改名前的历史会话
   *  （Claude Code 按 cwd 编码存 transcript，改名后老会话留在老编码目录里）。
   *  上限 5 条，超了丢最老的——它不是审计日志。 */
  pastPaths?: string[]
```

同文件末尾加：

```ts
export interface RenameFolderResult {
  ok: boolean
  error?: string
  /** 成功时给回新的完整列表，渲染层直接换掉，不用再拉一次 */
  projects?: Project[]
}
```

- [ ] **Step 2: 主进程 handler**

`src/main/projects.ts` 的 import 区加：

```ts
import { planRename } from './projectPaths'
import { wikiPath } from './wiki/paths'
import type { RenameFolderResult } from '../shared/types'
```

在 `registerProjectHandlers()` 里，`projects:rename` 那个 handler **后面**加（不要动 `projects:rename` 本身，它还在用）：

```ts
  /** 真改盘上的目录名，与访达同步。
   *
   *  和 projects:rename 是两件事：那个只改 projects.json 里的展示名。
   *  两个入口在菜单里都留着，各自的 hint 说清区别。
   *
   *  顺序是「先动盘、再动配置」：盘上改失败是常见的（权限、重名、被别的程序占着），
   *  配置改失败几乎不会。先做容易失败的那个，失败就直接返回，不留半截状态。
   *  反过来的话——配置改了盘没改——项目在应用里就彻底找不到了，用户也不知道去哪找。 */
  ipcMain.handle(
    'projects:renameFolder',
    async (_e, id: string, newName: string): Promise<RenameFolderResult> => {
      const list = loadProjects()
      const plan = planRename({
        projects: list,
        projectId: id,
        newName: String(newName ?? ''),
        wikiPath: wikiPath()
      })
      if (!plan.ok) return { ok: false, error: plan.error }

      // 盘上先改。存在性检查放在 rename 之前只是为了给一句人话错误，
      // 真正的原子性靠 fs.rename 本身（它在同名已存在时会失败）
      if (fs.existsSync(plan.newPath)) {
        return { ok: false, error: '同级已经有一个叫这个名字的文件或文件夹' }
      }
      try {
        await fs.promises.rename(plan.oldPath, plan.newPath)
      } catch (err) {
        return { ok: false, error: err instanceof Error ? err.message : String(err) }
      }

      // 盘上成功了才动配置
      const p = list.find((x) => x.id === id)
      if (p) {
        const past = [plan.oldPath, ...(p.pastPaths ?? [])].filter(
          (v, i, a) => a.indexOf(v) === i // 去重：改回原名再改回去会重复
        )
        p.pastPaths = past.slice(0, 5) // 只用来找历史会话，不是审计日志
        p.path = plan.newPath
        if (plan.renameDisplayName) p.name = path.basename(plan.newPath)
      }
      saveProjects(list)
      return { ok: true, projects: list }
    }
  )
```

- [ ] **Step 3: preload 暴露**

`src/preload/index.ts` 的 `projects` 那一块，在 `rename` 后面加：

```ts
    /** 真改盘上的目录名。和上面的 rename（只改显示名）是两件事 */
    renameFolder: (id: string, newName: string): Promise<RenameFolderResult> =>
      ipcRenderer.invoke('projects:renameFolder', id, newName),
```

同文件顶部的 type import 里补上 `RenameFolderResult`。

- [ ] **Step 4: 类型检查 + 构建 + 现有测试**

Run: `npm run typecheck && npm run build && node --test src/main/**/*.test.ts`
Expected: 三个都过；测试数应为 43（现有）+ 5（Task 1）+ 12（Task 3）= 60

- [ ] **Step 5: 端到端手验（不启动 Electron）**

写一个临时脚本放 scratchpad（不要提交），用 esbuild 打包 `src/main/projects.ts`（只 mock electron，`app.getPath('userData')` 指到临时目录），在临时目录里造两个真实文件夹当项目，验证：

- 正常改名 → 盘上目录真的换名了、`projects.json` 的 `path` 跟着变、`pastPaths` 记下了旧路径
- 目标名已存在 → 返回错误，且**盘上和 `projects.json` 都一个字节没变**
- 自定义过展示名的项目 → 改名后 `name` 不变
- 连续改两次 → `pastPaths` 有两条且顺序是「最近的在前」

把实测输出贴进报告。

- [ ] **Step 6: 提交**

```bash
git add src/shared/types.ts src/main/projects.ts src/preload/index.ts
git commit -m "feat(projects): projects:renameFolder —— 重命名真改盘上的目录

先动盘、再动配置：盘上改失败是常见的（权限/重名/被占用），配置改失败几乎不会。
先做容易失败的那个，失败就直接返回，不留半截状态；反过来会让项目彻底找不到。

顺带把旧路径记进 pastPaths（上限 5 条），session.ts 靠它找改名前的历史会话。
projects:rename（只改显示名）原样保留，两个入口在菜单里各自说清。"
```

---

### Task 5: 项目菜单与确认弹窗

**Files:**
- Modify: `src/renderer/src/store/projectsSlice.ts:106` 附近
- Modify: `src/renderer/src/features/workspace/projectMenu.ts:41-53`

**Interfaces:**
- Consumes: Task 4 的 `window.api.projects.renameFolder`
- Produces: store action `renameProjectFolder(id: string, newName: string): Promise<string | null>`（返回错误文案，成功返回 `null`）

- [ ] **Step 1: store action**

`src/renderer/src/store/projectsSlice.ts`，在现有的 `renameProject`（106 行那个）**后面**加：

```ts
  /** 真改盘上的目录名。返回错误文案，成功返回 null。
   *  不自己弹提示：调用方知道该把错误显示在哪里 */
  renameProjectFolder: async (id: string, newName: string): Promise<string | null> => {
    const r = await window.api.projects.renameFolder(id, newName)
    if (!r.ok) return r.error ?? '改名失败'
    if (r.projects) set({ projects: r.projects })
    return null
  },
```

对应的类型声明加到这个 slice 的 interface 里：

```ts
  renameProjectFolder: (id: string, newName: string) => Promise<string | null>
```

- [ ] **Step 2: 菜单两项**

`src/renderer/src/features/workspace/projectMenu.ts`：

先把函数签名的第二个参数改成带模式（第 9-11 行那个 `onStartRename`）：

```ts
export function projectMenuItems(
  projectId: string,
  /** 让调用方接管「进入内联改名态」。mode 区分改的是哪一个：
   *  'name' = 只改应用内显示名，'folder' = 真改盘上的目录名。
   *  画布抽屉没有内联输入框，不传 —— 两项都不显示，和今天「重命名」的处理一致 */
  onStartRename?: (projectId: string, mode: 'name' | 'folder') => void
): CanvasMenuItem[] {
```

再把 41-49 行那段（`onStartRename` 那个条件项）替换成：

```ts
    ...(onStartRename
      ? [
          {
            label: '重命名文件夹…',
            hint: '真改盘上的名字，与访达同步',
            onClick: () => onStartRename(projectId, 'folder')
          } as CanvasMenuItem,
          {
            label: '改显示名',
            hint: '只改应用内的显示，不动文件夹',
            onClick: () => onStartRename(projectId, 'name')
          } as CanvasMenuItem
        ]
      : []),
```

`CanvasDrawer.tsx:538` 那处不传第二个参数，保持原样不动。

- [ ] **Step 3: 侧栏复用现有的内联输入框**

**不要用 `window.prompt` / `window.alert`** —— 全仓（除第三方 composer）没有一处用它们，
而侧栏第 127-146 行本来就有一个内联输入框在做改名，直接给它加一个模式。

`src/renderer/src/features/workspace/Sidebar.tsx`：

把状态从 `string | null` 换成带模式的（第 72 行）：

```ts
  const [editingProject, setEditingProject] = useState<{
    id: string
    mode: 'name' | 'folder'
  } | null>(null)
  const [renameError, setRenameError] = useState<string | null>(null)
  const renameProjectFolder = useStore((s) => s.renameProjectFolder)
```

双击仍然是改显示名（第 118 行）：

```tsx
              onDoubleClick={() => setEditingProject({ id: p.id, mode: 'name' })}
```

菜单那处（第 184 行）：

```tsx
          items={projectMenuItems(projMenu.id, (id, mode) => setEditingProject({ id, mode }))}
```

输入框那段（第 127-146 行）改成：

```tsx
                {editingProject?.id === p.id ? (
                  <input
                    className="project-rename"
                    // 改显示名时预填显示名；改文件夹时预填**目录名**（那才是要改的东西）
                    defaultValue={
                      editingProject.mode === 'folder' ? (p.path.split('/').pop() ?? '') : p.name
                    }
                    autoFocus
                    // SwipeRow 用 pointer 事件做横滑删除，不挡住就会一边打字一边把行滑走
                    onMouseDown={(e) => e.stopPropagation()}
                    onPointerDown={(e) => e.stopPropagation()}
                    onClick={(e) => e.stopPropagation()}
                    onDoubleClick={(e) => e.stopPropagation()}
                    onBlur={(e) => {
                      const mode = editingProject.mode
                      const val = e.target.value
                      setEditingProject(null)
                      if (mode === 'name') {
                        void renameProject(p.id, val)
                        return
                      }
                      const cur = p.path.split('/').pop() ?? ''
                      if (!val.trim() || val === cur) return
                      startFolderRename(p.id, val)
                    }}
                    onKeyDown={(e) => {
                      if (e.key === 'Enter') (e.target as HTMLInputElement).blur()
                      if (e.key === 'Escape') setEditingProject(null)
                    }}
                  />
```

在组件里加改名的执行函数（放在 `editingProject` 声明附近）：

```ts
  // 真改文件夹：有终端在跑就先把后果说清楚，让人决定
  const startFolderRename = (projectId: string, newName: string): void => {
    const s = useStore.getState()
    const running = s.tabs
      .filter((t) => t.projectId === projectId)
      .flatMap((t) => collectLeaves(t.root))
      .filter((l) => l.pane.kind === 'terminal').length
    const go = async (): Promise<void> => {
      const err = await renameProjectFolder(projectId, newName)
      setRenameError(err)
    }
    if (running > 0) {
      s.requestConfirm({
        message:
          `把文件夹改名成「${newName}」？\n\n` +
          `这个项目下有 ${running} 个终端开着。改名不会杀掉它们（系统认的是目录本身、不是名字），` +
          `但里面的 agent 记着的路径会对不上，它下次读写那些路径可能会失败。\n\n` +
          `画板上已经打开的该项目文件节点会显示「文件不存在」，重新打开即可。`,
        confirmLabel: '改名',
        onConfirm: () => void go()
      })
    } else {
      void go()
    }
  }
```

错误显示：在项目列表上方（`workspace-tabs` 之后）加一条，失败时才出现：

```tsx
      {renameError && (
        <div className="ws-inline-error" onClick={() => setRenameError(null)}>
          {renameError}
        </div>
      )}
```

`src/renderer/src/features/workspace/workspace.css` 末尾加：

```css
/* 改名失败的提示条：点一下消失。用 confirm 弹窗太重（这不是要人决定的事），
   用 toast 又会飘走（错误文案往往要照着改，得留得住） */
.ws-inline-error {
  margin: 6px 10px;
  padding: 7px 10px;
  border-radius: 8px;
  font-size: 12px;
  line-height: 1.5;
  color: #fda4af;
  background: rgba(244, 63, 94, 0.1);
  border: 1px solid rgba(244, 63, 94, 0.25);
  cursor: pointer;
}
```

import 补 `collectLeaves`（`../../layout`）。

- [ ] **Step 4: 类型检查 + 构建**

Run: `npm run typecheck && npm run build`
Expected: 两个都无错

- [ ] **Step 5: 提交**

```bash
git add src/renderer/src/store/projectsSlice.ts src/renderer/src/features/workspace/projectMenu.ts
git commit -m "feat(projects): 项目菜单加「重命名文件夹…」，原「重命名」改叫「改显示名」

两项并存：直接替换等于拿走「设一个和文件夹不同的显示名」这个能力，那不是无感。
两条 hint 各自说清区别。有终端在跑时先弹确认，把「进程不会死但路径会对不上」讲明白。"
```

---

### Task 6: 文件面板的新建入口

**Files:**
- Modify: `src/renderer/src/features/files/FileTree.tsx:60-70`（props）、`:108`（`startCreate`）
- Modify: `src/renderer/src/features/workspace/Sidebar.tsx:36-43`

**Interfaces:**
- Consumes: 无
- Produces: `FileTree` 新增可选 prop `createRequest?: { kind: 'file' | 'dir'; nonce: number }`

**为什么用 nonce 而不是 ref**：`Sidebar` 和 `FileTree` 已经在用 `refreshKey` 这个计数器通信了，沿用同一条线，不引入第二种机制。

- [ ] **Step 1: FileTree 接收外部触发**

`src/renderer/src/features/files/FileTree.tsx`，props 加一项：

```ts
export function FileTree({
  rootPath,
  refreshKey,
  editable = false,
  createRequest
}: {
  rootPath: string
  refreshKey: number
  editable?: boolean
  /** 外部（面板头部的 + 按钮）触发在根目录新建。nonce 变一次触发一次；
   *  用计数器而不是 ref，是因为 refreshKey 已经是这个模式了，不引入第二种机制 */
  createRequest?: { kind: 'file' | 'dir'; nonce: number }
}): JSX.Element {
```

在 `startCreate` 定义（108 行那个 `useCallback`）**之后**加：

```ts
  // 外部触发：nonce 变了就在根目录起一次新建。
  // 依赖里不放 createRequest 整个对象——它每次渲染都是新对象，会无限触发
  const createNonce = createRequest?.nonce ?? 0
  const createKind = createRequest?.kind ?? 'file'
  useEffect(() => {
    if (!createNonce) return
    startCreate(createKind, null)
  }, [createNonce, createKind, startCreate])
```

（`useEffect` 若还没 import 就补进 React 的 import 里。）

- [ ] **Step 2: Sidebar 加 + 按钮**

`src/renderer/src/features/workspace/Sidebar.tsx`：

```tsx
  const [createReq, setCreateReq] = useState<{ kind: 'file' | 'dir'; nonce: number } | undefined>()
  const [newMenu, setNewMenu] = useState<{ x: number; y: number } | null>(null)
```

在现有「刷新文件树」按钮的**前面**插入：

```tsx
          <button
            className="icon-btn"
            data-tip="新建"
            onClick={(e) => {
              const r = (e.currentTarget as HTMLElement).getBoundingClientRect()
              setNewMenu({ x: r.left, y: r.bottom + 4 })
            }}
          >
            <PlusIcon size={13} />
          </button>
```

并在组件返回的 JSX 末尾（和 `FileTree` 同级）加菜单：

```tsx
      {newMenu && (
        <CanvasContextMenu
          x={newMenu.x}
          y={newMenu.y}
          items={[
            {
              label: '新建文件',
              onClick: () => setCreateReq((p) => ({ kind: 'file', nonce: (p?.nonce ?? 0) + 1 }))
            },
            {
              label: '新建文件夹',
              onClick: () => setCreateReq((p) => ({ kind: 'dir', nonce: (p?.nonce ?? 0) + 1 }))
            }
          ]}
          onClose={() => setNewMenu(null)}
        />
      )}
```

`FileTree` 的调用处（51 行）加上 `createRequest={createReq}`。
import 补 `PlusIcon`（`../../ui/Icons`，**已存在**，是 `export const`）和
`CanvasContextMenu`（`../canvas/CanvasContextMenu`）。

- [ ] **Step 3: 类型检查 + 构建**

Run: `npm run typecheck && npm run build`
Expected: 两个都无错

- [ ] **Step 4: 提交**

```bash
git add src/renderer/src/features/files/FileTree.tsx src/renderer/src/features/workspace/Sidebar.tsx
git commit -m "feat(files): 文件面板头部加新建入口

新建文件/文件夹本来就有，只是藏在右键菜单里没人找得到。
用 nonce 计数器触发，和旁边的 refreshKey 是同一种模式，不引入第二种机制。"
```

---

### Task 7: 画板文件节点「在访达中显示」

**Files:**
- Modify: `src/renderer/src/features/canvas/CanvasFileNode.tsx:199-206`

**Interfaces:**
- Consumes: 无
- Produces: 无

- [ ] **Step 1: 加按钮**

在「复制相对路径」那个 `<button>`（199-205 行）**之后**、`</>`  之前插入：

```tsx
            <button
              className="cfile-btn"
              data-tip="在访达中显示"
              onClick={() => void window.api.fs.showInFolder(absPath)}
            >
              <FolderIcon size={11} />
            </button>
```

它和旁边两个复制按钮共用同一个 `{absPath && (...)}` 条件——没有真实路径时（比如网页节点）三个一起不显示。

`FolderIcon` **已存在**于 `src/renderer/src/ui/Icons.tsx`（是 `export const`），
加进第 10 行那条已有的 Icons import 里即可，不要新画图标。

- [ ] **Step 2: 类型检查 + 构建**

Run: `npm run typecheck && npm run build`
Expected: 两个都无错

- [ ] **Step 3: 提交**

```bash
git add src/renderer/src/features/canvas/CanvasFileNode.tsx
git commit -m "feat(canvas): 文件节点头部加「在访达中显示」

头部本来有复制绝对路径/复制相对路径/最大化/删除，唯独没法跳到访达。
和旁边两个复制按钮共用 absPath 条件——网页节点没有真实路径，三个一起不显示。"
```

---

### Task 8: 端到端真机验收

**Files:** 无（纯验证）

**Interfaces:**
- Consumes: Task 1-7 全部
- Produces: 无

> 这个任务**没有单元测试**，是有意的：要验的是「改名之后应用里各处还对不对」，
> 那只有真跑起来才看得出。审查时按这个标准判。

- [ ] **Step 1: 造一个可以随便改的测试项目**

```bash
SCRATCH=<本会话 scratchpad>
mkdir -p "$SCRATCH/renametest/proj-old/src"
echo "console.log(1)" > "$SCRATCH/renametest/proj-old/src/a.js"
```

在应用里把它加成项目，开一个终端，在画板上打开 `src/a.js` 那个文件节点。

- [ ] **Step 2: 改名前记录基线**

```bash
ls ~/.claude/projects/ | wc -l
cat "$HOME/Library/Application Support/Eas-Term/projects.json" | node -p "JSON.parse(require('fs').readFileSync(0,'utf8')).find(p=>p.path.includes('proj-old'))"
```
记下来。

- [ ] **Step 3: 改名，然后逐条核对**

项目区右键 → 重命名文件夹… → 改成 `proj-new`。然后核对：

| 要验的 | 怎么看 | 期望 |
|---|---|---|
| 盘上真改了 | `ls "$SCRATCH/renametest"` | 只有 `proj-new` |
| 配置跟上了 | 上面那条 node 命令，把 `proj-old` 换成 `proj-new` | `path` 是新的，`pastPaths` 里有旧的 |
| 展示名跟着改 | 看侧栏 | 显示 `proj-new` |
| **终端 cwd 跟上了** | 在**新开**的终端里 `pwd` | 新路径 |
| **MCP 白名单跟上了** | 让 agent `canvas_open_file("src/a.js")` | 能打开，不报「不在项目内」 |
| git 面板 | 切到「版本」页签 | 不报错 |
| **Claude Code 的目录没被动过** | `ls ~/.claude/projects/ \| wc -l` | 和 Step 2 的数字**相同** |
| 画布上原来那个文件节点 | 看它 | 显示「文件不存在」——**这是设计定的，不是 bug** |

- [ ] **Step 4: 验历史会话没丢**

改名**之前**先在那个项目的终端里和 claude 说两句话（产生 transcript），
改名之后打开「对话导航」，确认那两句还在。

- [ ] **Step 5: 验失败路径不留半截状态**

```bash
mkdir -p "$SCRATCH/renametest/occupied"
```
把项目改名成 `occupied` → 应该报「同级已经有一个叫这个名字的文件或文件夹」，
且 `projects.json` 与盘上目录**都不变**（改名前后各 `cat` 一次比对）。

- [ ] **Step 6: 验知识库保护**

把知识库临时指到测试项目里的某个子目录，再试改名 → 应该拒绝并说清原因。验完把知识库指回去。

- [ ] **Step 7: 验 ① 和 ③**

- 文件面板头部的 `+` → 新建文件 / 新建文件夹，都能建出来，且与右键菜单建出来的行为一致
- 画板文件节点右上角新按钮 → 访达打开并选中那个文件

- [ ] **Step 8: 收尾**

```bash
rm -rf "$SCRATCH/renametest"
```
在应用里把测试项目从列表移除。

---

## 实现顺序与依赖

```
Task 1 ─→ Task 2 ─┐
Task 3 ─→ Task 4 ─┴─→ Task 5 ─┐
                                ├─→ Task 8
Task 6 ────────────────────────┤
Task 7 ────────────────────────┘
```

Task 1-2（历史会话别名）和 Task 3-4（改名处理器）两条线互不依赖，可以并行。
Task 5 要两条线都在才有意义。Task 6、7 完全独立，随时可做。
Task 8 要全部做完。

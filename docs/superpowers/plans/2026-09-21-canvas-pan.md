# 画布平移性能第一阶段

范围：合并拖动事件、隔离位置更新与重内容渲染；不卸载面板、不改进程、不改沙箱。基于 main 471456f，独立 perf/canvas-pan-20260921。

评审：优先正确性，保持视口同步以维持命中检测与裁剪；本阶段按帧提交视口，不直接延迟到松手（否则坐标转换/中途缩放会读旧值）。先减少重复 React 更新和稳定 props 的重内容重渲染；容器直接变换与分批预热留待性能证据支持。

1. 添加按帧最新值合并器，测试合并、flush、cancel与重入。
2. 拖拽移动按帧合并，松手/失焦提交最后坐标，不留下延迟写；保持缩放/滚轮已有同步坐标语义。
3. 以 React.memo 隔离终端与AI对话内容，使用默认浅比较；内部 store/状态继续正常更新，禁止自定义比较忽略数据。
4. 构建隔离应用，固定闲置对话场景，前后相同拖动采集视口提交数/帧延迟/进程启动，验证位置、视图切换和内容更新。
5. 类型/回归测试，架构同步；未测正式场景不得宣称所有卡顿解决。原阶段不提交、不合并、不发布；2026-09-21 用户追加授权审查、提交并合入本地 main，不推送、不发布。

第一阶段已按上述边界完成：轻负载与长历史前后对照、松手/失焦/切换及活动对话更新回归通过。减少重复视口更新但未测得帧率改善；重媒体和持续终端输出压力场景未验证，暂不进入无证据的第二阶段补丁。

## 本地集成门禁
用户已明确授权提交并合入本地 main。最终只读审查未发现阻断项；提交前完整检查 3284 通过、19 跳过、0 失败。合并后另行复跑检查；不推送、不发布。

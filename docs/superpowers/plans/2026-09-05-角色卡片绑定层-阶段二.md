# 角色卡片绑定层 · 阶段二（界面与旁路）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把绑定层的报告渲染进界面（编辑器三列矩阵、工具栏降级标记），并让 `team_spawn` 能按 `role_id` 套角色卡。

**Architecture:** 所有文案从 `bindRole().report` 现算，不再手写。`shared/roleBinding.ts` 加三张标签表与两个纯函数（`capMatrix` / `degradedLines`）；编辑器与 RolePicker 只做渲染。`team_spawn` 的 `role_id` 走现有的 `openAgentPane({ roleId })`，对话节点起会话时自然带上角色边界与契约。

**Tech Stack:** TypeScript · Electron · React · `node --test`（Node 26 直接跑 `.ts`）。

**Spec:** `docs/superpowers/specs/2026-09-05-角色卡片三harness绑定层-design.md`（6.4、6.5 表末行、第七节「阶段二」）。阶段一已合入 main（`20b9f95`）。

## Global Constraints

- 阶段二只做三件事：编辑器矩阵、工具栏降级标记、`team_spawn` 加 `role_id`。终端命令条那项已随阶段一完成（无 UI 入口，仅保持同步）。阶段三的档位转正不做。
- **界面上关于「各家怎么落」的每一句话都来自 `bindRole().report`**，编辑器与 RolePicker 里不许再出现手写的 harness 落法文案（标签表 `CAP_LABEL` / `HARNESS_LABEL` / `LEVEL_LABEL` 是名词，不是落法）。
- 文案按 memory「失败要说人话」：一句原因，不堆术语。
- 降级标记**只在有 `degraded` / `unsupported` 行时出现**，不常驻。
- `checkBatch` 一条不合格整批拒绝的原则不变；`role_id` 不存在也是整批拒绝。
- 改颜色/间距先读 `docs/architecture/15-视觉令牌与主题.md`，只用邻近规则（`.re-hint` / `.re-chip` / `.rp-cur`）已经在用的 token，不新造颜色值。
- 测试命令 `npm test`；类型检查 `npm run typecheck`。每个任务结束两者都要绿。
- 提交信息中文，`feat(roles): …` 风格，末尾 `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`。
- 工作目录：`/Users/biily/Biily/Projects/vibe coding/terminal-wt/roles-phase2`（分支 `feat/roles-phase2`）。

---

## 文件结构

| 文件 | 责任 |
|---|---|
| `src/shared/roleBinding.ts`（改） | 加 `HARNESSES` / `HARNESS_LABEL` / `CAP_LABEL` / `LEVEL_LABEL`、`capMatrix()`、`degradedLines()` |
| `src/shared/roleBinding.test.ts`（改） | 矩阵行序与 active、预览格、降级行筛选 |
| `src/renderer/src/features/canvas/CanvasRoleEditor.tsx`（改） | 三个开关下面渲染 `.re-matrix`，删手写 `how` |
| `src/renderer/src/features/canvas/canvas.css`（改） | `.re-matrix` / `.re-lv*` / `.re-how` |
| `src/renderer/src/features/agentChat/RolePicker.tsx`（改） | 新 prop `cli`，触发按钮上的 `.rolepick-warn` |
| `src/renderer/src/features/agentChat/agentChat.css`（改） | `.rolepick-warn` |
| `src/renderer/src/styles/base.css`（改） | `.app-tooltip` 加 `white-space: pre-line` |
| `src/renderer/src/features/agentChat/AgentChatView.tsx`（改） | 给 RolePicker 传 `cli` |
| `mcp/eas-mcp.mjs`（改） | `team_spawn.agents[].role_id` schema |
| `src/renderer/src/features/team/batchSpec.ts`（改） | `AgentSpec.roleId`、`checkBatch(raw, { knownRoleIds })` |
| `src/renderer/src/features/team/batchSpec.test.ts`（新/改） | `role_id` 三条 |
| `src/shared/teamRoster.ts`（改） | `RosterAgent.roleId` |
| `src/renderer/src/mcpHandler.ts`（改） | 传 `knownRoleIds`、`openAgentPane({ roleId })`、花名册记 `roleId` |
| `src/renderer/src/features/team/TeamBatchModal.tsx`（改） | 行里显示角色卡名 |
| `skills/team-research/SKILL.md`（改） | `role_id` 说明 |
| `docs/architecture/03、11、12`、spec 状态行（改） | 图纸同步 |

---

### Task 1: 绑定层加标签表与两个纯函数

**Files:**
- Modify: `src/shared/roleBinding.ts`
- Test: `src/shared/roleBinding.test.ts`

**Interfaces:**
- Produces: `HARNESSES: readonly HarnessId[]`、`HARNESS_LABEL`、`CAP_LABEL`、`LEVEL_LABEL`、`MatrixRow`、`capMatrix(bounds, ctx?) → MatrixRow[]`、`degradedLines(bounds, kind, ctx?) → BindingLine[]`。Task 2、3 依赖。

- [ ] **Step 1: 写失败的测试**（追加到 `roleBinding.test.ts` 末尾，import 行补上新名字）

```ts
import { capMatrix, degradedLines, CAP_LABEL, HARNESS_LABEL, HARNESSES, LEVEL_LABEL } from './roleBinding.ts'

test('标签表齐全：六个 cap、三家、四档都有中文名', () => {
  for (const k of ['write', 'shell', 'imageGen', 'mcpServers', 'mcpTools', 'raw'] as const) assert.ok(CAP_LABEL[k])
  assert.deepEqual(HARNESSES, ['claude', 'codex', 'omp'])
  assert.equal(HARNESS_LABEL.omp, '默认 harness')
  for (const l of ['hard', 'soft', 'degraded', 'unsupported'] as const) assert.ok(LEVEL_LABEL[l])
})

test('capMatrix：空卡也给三行预览（write/shell/imageGen），全部 active=false，格子里是「点亮后会怎样」', () => {
  const rows = capMatrix(undefined)
  assert.deepEqual(rows.map((r) => r.cap), ['write', 'shell', 'imageGen'])
  assert.ok(rows.every((r) => !r.active))
  assert.equal(rows[0].cells.codex?.level, 'hard')
  assert.ok(rows[0].cells.codex?.how.includes('read-only'))
  assert.equal(rows[2].cells.omp?.level, 'degraded')
})

test('capMatrix：点亮的意图 active=true，且 write 的 Claude 附注随 shell 变化', () => {
  const a = capMatrix({ caps: { write: false } })
  assert.ok(a[0].active && !a[1].active)
  assert.ok(a[0].cells.claude?.how.includes('Bash'))
  const b = capMatrix({ caps: { write: false, shell: false } })
  assert.ok(!b[0].cells.claude?.how.includes('Bash'))
})

test('capMatrix：有 mcp / raw 时追加对应行，只在有内容时出现', () => {
  const rows = capMatrix({ caps: { mcp: { denyServers: ['s'] } }, raw: { codex: { disable: ['web_search'] } } }, { knownMcpServers: ['s'] })
  assert.deepEqual(rows.map((r) => r.cap), ['write', 'shell', 'imageGen', 'mcpServers', 'raw'])
  const srv = rows[3]
  assert.ok(srv.active)
  assert.ok(srv.cells.codex?.how.includes('s'))
  const raw = rows[4]
  assert.equal(raw.cells.claude, undefined, 'raw.codex 不该出现在 Claude 格')
  assert.equal(raw.cells.codex?.cap, 'raw')
})

test('degradedLines：只回 degraded / unsupported 的行', () => {
  const bounds = { caps: { write: false as const, imageGen: false as const } }
  assert.deepEqual(degradedLines(bounds, 'claude'), [])
  const codex = degradedLines(bounds, 'codex')
  assert.equal(codex.length, 1)
  assert.equal(codex[0].cap, 'imageGen')
  assert.equal(degradedLines(undefined, 'omp').length, 0)
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test src/shared/roleBinding.test.ts`
Expected: FAIL，`capMatrix` 等不是导出

- [ ] **Step 3: 实现**（追加到 `roleBinding.ts` 末尾）

```ts
// ── 给界面用的标签与派生 ─────────────────────────────────────────────────────
// 这里只有**名词**（cap 叫什么、harness 叫什么、档位叫什么）。
// 「各家怎么落」那些句子仍然只由 bindRole 生成 —— 界面不许再手写它们。

export const HARNESSES: readonly HarnessId[] = ['claude', 'codex', 'omp']
export const HARNESS_LABEL: Record<HarnessId, string> = { claude: 'Claude', codex: 'Codex', omp: '默认 harness' }
export const CAP_LABEL: Record<CapKey, string> = {
  write: '不许改文件',
  shell: '不许跑命令',
  imageGen: '不许生图',
  mcpServers: '禁用的 MCP server',
  mcpTools: '禁用的 MCP 工具',
  raw: '手写参数'
}
export const LEVEL_LABEL: Record<Enforcement, string> = { hard: '硬', soft: '软', degraded: '降级', unsupported: '不支持' }

export interface MatrixRow {
  cap: CapKey
  /** 这一行在当前草稿里是否点亮。没点亮的三个意图行仍给出「点亮后会怎样」的预览 */
  active: boolean
  cells: Partial<Record<HarnessId, BindingLine>>
}

const INTENTS = ['write', 'shell', 'imageGen'] as const

/** 编辑器三列矩阵的数据。三个意图行永远在（未点亮的按「假设点亮」预览）；
 *  mcpServers / mcpTools / raw 只在草稿里真有内容时追加。 */
export function capMatrix(bounds: RoleBounds | undefined, ctx: BindingContext = {}): MatrixRow[] {
  const caps = bounds?.caps ?? {}
  const rows: MatrixRow[] = []
  for (const k of INTENTS) {
    const active = caps[k] === false
    const preview: RoleBounds = active ? (bounds ?? {}) : { ...bounds, caps: { ...caps, [k]: false } }
    const cells: MatrixRow['cells'] = {}
    for (const h of HARNESSES) {
      const line = bindRole(preview, h, ctx).report.find((l) => l.cap === k)
      if (line) cells[h] = line
    }
    rows.push({ cap: k, active, cells })
  }
  for (const k of ['mcpServers', 'mcpTools', 'raw'] as const) {
    const cells: MatrixRow['cells'] = {}
    for (const h of HARNESSES) {
      const line = bindRole(bounds, h, ctx).report.find((l) => l.cap === k)
      if (line) cells[h] = line
    }
    if (Object.keys(cells).length) rows.push({ cap: k, active: true, cells })
  }
  return rows
}

/** 工具栏降级标记用：这张卡在这家上哪些限制打了折扣 */
export function degradedLines(bounds: RoleBounds | undefined, kind: HarnessId, ctx: BindingContext = {}): BindingLine[] {
  return bindRole(bounds, kind, ctx).report.filter((l) => l.level === 'degraded' || l.level === 'unsupported')
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test src/shared/roleBinding.test.ts && npm run typecheck`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/shared/roleBinding.ts src/shared/roleBinding.test.ts
git commit -m "feat(roles): 绑定层加标签表与 capMatrix / degradedLines，界面文案从此只能从报告派生

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 2: 编辑器三列矩阵

**Files:**
- Modify: `src/renderer/src/features/canvas/CanvasRoleEditor.tsx`（「能力边界」区，约 290-330 行）
- Modify: `src/renderer/src/features/canvas/canvas.css`（`.re-caps` 规则附近，约 3356 行）

**Interfaces:**
- Consumes: `capMatrix`、`HARNESSES`、`HARNESS_LABEL`、`CAP_LABEL`、`LEVEL_LABEL`（Task 1）；编辑器里已有的 `servers`（本机 Codex MCP 名）与 `draft`。

- [ ] **Step 1: 删手写 `how`，开关只留标签**

把三个开关的数组改成只有 `k` 与 `label`（删 `how` 字段与 `data-tip={it.how}`）：

```tsx
              {(
                [
                  { k: 'write', label: CAP_LABEL.write },
                  { k: 'shell', label: CAP_LABEL.shell },
                  { k: 'imageGen', label: CAP_LABEL.imageGen }
                ] as const
              ).map((it) => {
                const on = draft.caps?.[it.k] === false
                return (
                  <button key={it.k} className={`re-chip re-cap${on ? ' on' : ''}`} onClick={() => setCap(it.k, !on)}>
                    {it.label}
                  </button>
                )
              })}
```

- [ ] **Step 2: 在开关下面渲染矩阵**

在 `</div>`（`.re-caps` 结束）之后、第一条 `re-hint` 之前插入：

```tsx
            {/* 各家怎么落：**全部由绑定层现算**，这里一句落法都不手写。
                未点亮的行按「假设点亮」预览并压暗，用户不用先点再看。 */}
            <table className="re-matrix">
              <thead>
                <tr>
                  <th />
                  {HARNESSES.map((h) => (
                    <th key={h}>{HARNESS_LABEL[h]}</th>
                  ))}
                </tr>
              </thead>
              <tbody>
                {matrix.map((row) => (
                  <tr key={row.cap} className={row.active ? '' : 'off'}>
                    <th>{CAP_LABEL[row.cap]}</th>
                    {HARNESSES.map((h) => {
                      const c = row.cells[h]
                      return (
                        <td key={h}>
                          {c ? (
                            <>
                              <span className={`re-lv re-lv-${c.level}`}>{LEVEL_LABEL[c.level]}</span>
                              <span className="re-how">{c.how}</span>
                            </>
                          ) : (
                            <span className="re-lv re-lv-none">—</span>
                          )}
                        </td>
                      )
                    })}
                  </tr>
                ))}
              </tbody>
            </table>
```

在组件里 `rawDeny` 那几行 const 旁边加：

```ts
  const matrix = capMatrix({ caps: draft.caps, raw: draft.raw }, { knownMcpServers: servers })
```

import 改为从 `'../../../../shared/roleBinding'` 引 `capMatrix, HARNESSES, HARNESS_LABEL, CAP_LABEL, LEVEL_LABEL`。

两条 hint 改为：

```tsx
            <span className="re-hint">
              <b>只能收紧</b>：没点的就是允许。下表是每家实际落成什么，由绑定层现算，压暗的行是「点亮后会这样」。
            </span>
            <span className="re-hint warn">
              「不许改文件」在 Claude 与默认 harness 上留着命令行就仍能 <code>echo &gt; 文件</code>；要封死连「不许跑命令」一起点。
            </span>
```

- [ ] **Step 3: 样式**

先读 `docs/architecture/15-视觉令牌与主题.md` 的三条硬规矩，再看 `canvas.css` 里 `.re-hint`、`.re-chip`、`.re-chip.on` 用的是哪几个 token（颜色、边线、圆角），下面的 `<token>` 全部换成那几条规则里已经在用的名字：

```css
.re-matrix {
  width: 100%;
  border-collapse: collapse;
  margin-top: 6px;
  font-size: 11.5px;
  table-layout: fixed;
}
.re-matrix th,
.re-matrix td {
  text-align: left;
  vertical-align: top;
  padding: 4px 6px;
  border-top: 1px solid <.re-hint 或 .re-chip 用的边线 token>;
}
.re-matrix thead th {
  border-top: none;
  font-weight: 500;
  color: <.re-hint 的文字 token>;
}
.re-matrix tbody th {
  width: 88px;
  font-weight: 500;
  white-space: nowrap;
}
.re-matrix tr.off {
  opacity: 0.45;
}
.re-lv {
  display: inline-block;
  margin-right: 5px;
  padding: 0 5px;
  border-radius: 6px;
  font-size: 10.5px;
  line-height: 16px;
  background: <.re-chip 的底 token>;
}
.re-lv-hard {
  background: <.re-chip.on 的底 token>;
}
.re-lv-degraded,
.re-lv-unsupported {
  background: <.re-hint.warn 用的警示底 token>;
}
.re-how {
  color: <.re-hint 的文字 token>;
}
```

- [ ] **Step 4: 验证**

Run: `npm run typecheck && npm test`；再 `grep -n "Claude 去掉\|Codex 只读\|默认 harness 去掉\|实测未生效" src/renderer/src/features/canvas/CanvasRoleEditor.tsx`
Expected: 全绿；grep 无输出（手写落法已清零）

- [ ] **Step 5: Commit**

```bash
git add src/renderer/src/features/canvas/CanvasRoleEditor.tsx src/renderer/src/features/canvas/canvas.css
git commit -m "feat(canvas): 角色编辑器的能力矩阵从绑定层报告渲染，删掉全部手写落法

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 3: 工具栏降级标记

**Files:**
- Modify: `src/renderer/src/features/agentChat/RolePicker.tsx`（props、触发按钮）
- Modify: `src/renderer/src/features/agentChat/agentChat.css`
- Modify: `src/renderer/src/styles/base.css:323`（`.app-tooltip`）
- Modify: `src/renderer/src/features/agentChat/AgentChatView.tsx:1272`

**Interfaces:**
- Consumes: `degradedLines`、`CAP_LABEL`（Task 1）
- Produces: `RolePicker` 新 prop `cli?: HarnessId`

- [ ] **Step 1: RolePicker 加 prop 与标记**

props 改为：

```ts
export function RolePicker({
  roleId,
  cli,
  onPick
}: {
  roleId?: string
  /** 这次对话选的 harness。有了它才知道这张卡在这家上哪些限制打了折扣 */
  cli?: HarnessId
  onPick: (roleId: string) => void
}): JSX.Element {
```

`current` 之后加：

```ts
  // 只在有降级 / 不支持时出现，不常驻 —— 常驻的标记等于没有标记
  const warn = current && cli ? degradedLines({ caps: current.caps, raw: current.raw }, cli) : []
```

触发按钮里 `<span className="ac-ctxbar-name">…</span>` 之后插入：

```tsx
        {warn.length > 0 && (
          <span
            className="rolepick-warn"
            aria-label="部分限制在当前 CLI 上打了折扣"
            data-tip={warn.map((l) => `${CAP_LABEL[l.cap]}：${l.how}`).join('\n')}
          >
            {LEVEL_LABEL[warn[0].level]}
          </span>
        )}
```

import：`import { degradedLines, CAP_LABEL, LEVEL_LABEL } from '../../../../shared/roleBinding'` 与 `import type { HarnessId } from '../../../../shared/types'`。

- [ ] **Step 2: AgentChatView 传 cli**

```tsx
          <RolePicker
            roleId={roleId}
            cli={selected.id as HarnessId}
            onPick={(next) => setAgentRole(tabId, leafId, next)}
          />
```

- [ ] **Step 3: 样式**

`agentChat.css`（放在 `.rolepick-btn` 规则旁）：

```css
/* 角色在当前 CLI 上有降级时才出现的小标记；tooltip 里逐条列出哪几项打了折扣 */
.rolepick-warn {
  margin-left: 4px;
  padding: 0 4px;
  border-radius: 5px;
  font-size: 10px;
  line-height: 14px;
  background: <.re-hint.warn 同一个警示底 token>;
}
```

`base.css` 的 `.app-tooltip` 里加一行 `white-space: pre-line;`（tooltip 文本里的 `\n` 才能换行；先 grep 确认它现在没有 `white-space`）。

- [ ] **Step 4: 验证**

Run: `npm run typecheck && npm test`
Expected: 全绿

- [ ] **Step 5: Commit**

```bash
git add src/renderer/src/features/agentChat/RolePicker.tsx src/renderer/src/features/agentChat/agentChat.css src/renderer/src/styles/base.css src/renderer/src/features/agentChat/AgentChatView.tsx
git commit -m "feat(chat): 角色在当前 CLI 上有降级时，工具栏角色名旁出现标记并列出哪几项

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 4: `team_spawn` 加 `role_id`

**Files:**
- Modify: `mcp/eas-mcp.mjs`（`team_spawn` 的 `agents.items.properties`，约 288-308 行）
- Modify: `src/renderer/src/features/team/batchSpec.ts`
- Test: `src/renderer/src/features/team/batchSpec.test.ts`（没有就新建，照 `brief.test.ts` 的写法）
- Modify: `src/shared/teamRoster.ts`（`RosterAgent`、`parseRoster`）
- Modify: `src/renderer/src/mcpHandler.ts`（约 990 行 `checkBatch`、1070 行 `openAgentPane`、1095 行花名册）
- Modify: `src/renderer/src/features/team/TeamBatchModal.tsx:49-54`
- Modify: `skills/team-research/SKILL.md:42-45`

**Interfaces:**
- Consumes: `openAgentPane({ roleId })`（已存在，`tabsSlice.ts:69`）
- Produces: `AgentSpec.roleId?: string`、`checkBatch(raw, opts?: { knownRoleIds?: readonly string[] })`、`RosterAgent.roleId?: string`

- [ ] **Step 1: 写失败的测试**

```ts
import assert from 'node:assert/strict'
import { test } from 'node:test'

import { checkBatch } from './batchSpec.ts'

const base = { goal: 'g', agents: [{ role: 'researcher', task: 't' }] }

test('role_id 可选：没给就没有 roleId 字段', () => {
  const r = checkBatch(base)
  assert.ok(r.ok && !('roleId' in r.spec.agents[0]))
})

test('role_id 收 snake_case（MCP 入参）也收 roleId（内部）', () => {
  const a = checkBatch({ goal: 'g', agents: [{ role: 'r', task: 't', role_id: 'scout' }] }, { knownRoleIds: ['scout'] })
  assert.ok(a.ok && a.spec.agents[0].roleId === 'scout')
  const b = checkBatch({ goal: 'g', agents: [{ role: 'r', task: 't', roleId: 'scout' }] }, { knownRoleIds: ['scout'] })
  assert.ok(b.ok && b.spec.agents[0].roleId === 'scout')
})

test('role_id 不在已知角色卡里 → 整批拒绝，错误里列出可用的 id', () => {
  const r = checkBatch({ goal: 'g', agents: [{ role: 'r', task: 't', role_id: 'nope' }] }, { knownRoleIds: ['scout', 'builder'] })
  assert.ok(!r.ok)
  assert.ok(!r.ok && r.error.includes('nope') && r.error.includes('scout') && r.error.includes('builder'))
})

test('没给 knownRoleIds 时不校验存在性（纯函数不猜环境）', () => {
  const r = checkBatch({ goal: 'g', agents: [{ role: 'r', task: 't', role_id: 'whatever' }] })
  assert.ok(r.ok && r.spec.agents[0].roleId === 'whatever')
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test src/renderer/src/features/team/batchSpec.test.ts`
Expected: 后三条 FAIL

- [ ] **Step 3: 改 `batchSpec.ts`**

`AgentSpec` 加：

```ts
  /** 套哪张角色卡（`AgentRole.id`）。填了就把那张卡的契约 / 能力边界 / 默认模型套到这个 agent 上。
   *  MCP 入参叫 `role_id`（与 `estimate_tokens` 同风格），内部叫 `roleId`。 */
  roleId?: string
```

`checkBatch` 签名与循环体：

```ts
export function checkBatch(raw: unknown, opts: { knownRoleIds?: readonly string[] } = {}): BatchCheck {
  …
    const roleIdRaw = typeof a.role_id === 'string' ? a.role_id : typeof a.roleId === 'string' ? a.roleId : ''
    const roleId = roleIdRaw.trim() || undefined
    if (roleId && opts.knownRoleIds && !opts.knownRoleIds.includes(roleId)) {
      return {
        ok: false,
        error: `「${role}」的 role_id「${roleId}」不存在。可用的角色卡：${opts.knownRoleIds.join(', ')}（不填 = 无角色）`
      }
    }
    agents.push({ role, task, needs, prefer, ...(isolation ? { isolation } : {}), ...(roleId ? { roleId } : {}) })
```

- [ ] **Step 4: schema、花名册、handler、弹窗、skill**

`mcp/eas-mcp.mjs` 的 `isolation` 属性之后加：

```js
              role_id: {
                type: 'string',
                description:
                  '套一张角色卡（内置有 scout 勘探员 / inspector 验官 / builder 工匠 / writer 笔杆子 …，' +
                  '用户自建的也行）。填了就把那张卡的职责契约、能力边界（比如只读）、默认模型套到这个 agent 上。' +
                  '只读的调研 / 审查角色配 scout 或 inspector 最合适；不填 = 无角色。id 不存在整批会被拒。'
              }
```

`src/shared/teamRoster.ts`：`RosterAgent` 加 `roleId?: string`（注释：重派时要原样带上）；`parseRoster` 里清洗 agent 的那段加 `...(typeof x.roleId === 'string' && x.roleId ? { roleId: x.roleId } : {})`。

`src/renderer/src/mcpHandler.ts`：

- `checkBatch({ goal: …, agents: …, estimateTokens: … })` → `checkBatch({ … }, { knownRoleIds: st.roles.map((r) => r.id) })`（`st` 在上面已经取过）
- `openAgentPane({ … initialMessage: … })` 里加 `roleId: a.roleId,`
- 花名册 `agents: spec.agents.map((a) => ({ role: a.role, task: a.task, …worktree }))` 里加 `...(a.roleId ? { roleId: a.roleId } : {})`

`TeamBatchModal.tsx`：组件里取 `const roles = useStore((s) => s.roles)`（看文件顶部已有的 `useStore` import；没有就从 `'../../store'` 引），行里 `<span className="tbm-role">{a.role}</span>` 之后加：

```tsx
              {a.roleId && (
                <span className="tbm-card" title="套的角色卡">
                  {roles.find((r) => r.id === a.roleId)?.name ?? a.roleId}
                </span>
              )}
```

对应 css 文件里（`grep -rn "tbm-role" src/renderer/src --include='*.css'` 找到那份）加：

```css
.tbm-card {
  margin-left: 6px;
  padding: 0 5px;
  border-radius: 5px;
  font-size: 10.5px;
  background: <.tbm-role 用的底 token 或 .re-chip 的底 token>;
}
```

`skills/team-research/SKILL.md` 在「`prefer` 是软偏好，可以不填。」之后加一段：

```
`role_id` 可选：套一张角色卡（内置 `scout` 勘探员 / `inspector` 验官，用户自建的也行）。
填了就把那张卡的职责契约、能力边界（只读）、默认模型套到这个 agent 上 ——
本场景包里的角色都是只读的，配 `scout` 或 `inspector` 最合适。id 不存在整批会被拒。
```

- [ ] **Step 5: 验证**

Run: `node --test src/renderer/src/features/team/batchSpec.test.ts && npm run typecheck && npm test`
Expected: 全绿

- [ ] **Step 6: Commit**

```bash
git add mcp/eas-mcp.mjs src/renderer/src/features/team/batchSpec.ts src/renderer/src/features/team/batchSpec.test.ts src/shared/teamRoster.ts src/renderer/src/mcpHandler.ts src/renderer/src/features/team/TeamBatchModal.tsx skills/team-research/SKILL.md
git add -A src/renderer/src/features/team/*.css
git commit -m "feat(team): team_spawn 加 role_id —— 派出去的 agent 也能套角色卡，弹窗与花名册都记着

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 5: 图纸同步 + 真机验证

**Files:**
- Modify: `docs/architecture/03-agent角色边界.md`（3A 角色段末尾）
- Modify: `docs/architecture/11-MCP工具网络.md`（`team_spawn` 一行）
- Modify: `docs/architecture/12-skill与hook流程.md`（team-research 那段若提到字段表）
- Modify: `docs/superpowers/specs/2026-09-05-角色卡片三harness绑定层-design.md`（状态行、6.4、第十二节后追加「阶段二真机验证」）

- [ ] **Step 1: 图纸**

03：角色段末尾加「界面文案（编辑器矩阵、工具栏降级标记、team_spawn 弹窗）全部从 `bindRole().report` 派生，`capMatrix` / `degradedLines` 在 `shared/roleBinding.ts`；`team_spawn` 的 `role_id` 走 `openAgentPane({ roleId })`，与用户手选角色同一条路。」

11：`team_spawn` 行补「`agents[].role_id` 可选，`checkBatch` 按当前角色卡 id 校验，不存在整批拒」。

12：team-research 段若列了 `role / task / needs / prefer`，补 `role_id`。

spec：状态行改「阶段一、二已实现；阶段三未动」；6.4 两段各补一句「已实现（2026-09-05）」。

- [ ] **Step 2: 真机验证（隔离实例）**

先 `pkill -f verify-app.mjs`，再在 worktree 里 `npm run verify`（重新构建 `out/`）。用 `node scripts/eval-in-app.mjs "<js>"` 逐条核对，读 `/Users/biily/.claude/projects/-Users-biily-Biily-Projects-vibe-coding-terminal/memory/eas-term-cdp-验证方法.md` 里的坑（首启弹窗先关、按 title 选 page、`__store` 在 verify 实例里有、孤儿实例占 9333 先杀）：

| # | 场景 | 判据 |
|---|---|---|
| 1 | 打开画师的编辑器 | `.re-matrix tbody tr` 至少 3 行；`imageGen` 行 Codex 格的 `.re-lv` 文字是「降级」，Claude 格是「硬」 |
| 2 | 编辑器里点亮「不许改文件」 | `write` 行 `tr.off` 消失；Claude 格 how 含「Bash」；再点亮「不许跑命令」后不含 |
| 3 | 对话节点选画师 + Codex | 工具栏 `.rolepick-warn` 出现；hover 后 `.app-tooltip` 文本含「不许生图」且有换行 |
| 4 | 同一节点切到 Claude | `.rolepick-warn` 消失 |
| 5 | 选勘探员 + 任意 CLI | 无标记（write 三家都是 hard） |
| 6 | `team_spawn` 带 `role_id: 'scout'` | 需要多 agent 开关：在隔离实例里打开 Frame 的开关，用 `window.__store` 之外的方式不易触发，**允许只靠 Task 4 的单测 + 弹窗渲染检查**：把一份 `spec` 塞给 `TeamBatchModal` 不可行时记「未验证（需真实 MCP 调用）」 |

结果写进 spec 新小节，每条一行写「看到了什么」；未验证的如实写。

- [ ] **Step 3: 提交**

Run: `npm run typecheck && npm test`

```bash
git add docs/architecture/03-agent角色边界.md docs/architecture/11-MCP工具网络.md docs/architecture/12-skill与hook流程.md docs/superpowers/specs/2026-09-05-角色卡片三harness绑定层-design.md
git commit -m "docs(architecture): 角色卡阶段二 —— 界面文案改为报告派生、team_spawn role_id、真机验证记录

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

## 自查记录

- **Spec 覆盖**：6.4 编辑器矩阵 → Task 1/2；6.4 工具栏标记 → Task 1/3；6.5 `team_spawn roleId` → Task 4；6.5 终端路 → 阶段一已做；图纸同步 → Task 5。
- **类型一致性**：`MatrixRow.cells` 是 `Partial<Record<HarnessId, BindingLine>>`，Task 2 用 `row.cells[h]` 可空；`degradedLines` 返回 `BindingLine[]`，Task 3 用 `l.cap` / `l.how` / `l.level`；`AgentSpec.roleId` 与 `openAgentPane` 的 `roleId` 同名同型。
- **手写文案清零判据**：Task 2 Step 4 的 grep；Task 3 的 tooltip 只拼 `CAP_LABEL` + `how`。

# AI 对话执行可见性与等待续接（后续排期）

用户于2026-09-13明确：在运行资源闸门与统一服务中心任务完成之后实施；现在允许只读调查。

## 症状
用户只能看到文字汇报，看不到命令测试的执行过程；等待期间无法判断运行、排队、停止；担心工具结束后无法回到原对话。不要把测试通过或单轮回复结束显示为整个任务完成。

## 已核对
本会话shell父进程为codex PID69226；该进程子code-mode-host使用Eas-Term托管codex/0.154.0目录。不是Claude执行本次命令。此证据只能确认CLI链，不能证明事件丢失位置。

## 实施前调查与验收
1. 脱敏采集同一次调用的CLI原始事件、adapter解析、主进程转发、当前轮次归属、渲染结果，定位首个缺口。
2. 覆盖code-mode嵌套工具、exec等待句柄、工具输出和最终状态；不得凭猜测新增另一套事件流。
3. 当前用户消息下显示运行内容/时长/最近输出，区分排队、执行、等待用户、断线、中断、完成。
4. 结果关联原调用和原轮次，断线核对原任务而非重放副作用；验证可恢复范围，不承诺不具备的跨进程恢复能力。
5. 隔离开发实例亲眼验收：慢命令、持续输出、静默命令、失败、取消、断线/重连；任务结束状态不冒充整体目标完成。

状态：已排期，根因未确认，未实施。

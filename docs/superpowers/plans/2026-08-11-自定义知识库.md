# 自定义知识库 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让用户按自己的分类建一个知识库，享有和内置库完全一样的读写能力；老库原样不动。

**Architecture:** 库根目录放一份 `.eas-wiki.json` 声明分类；app 侧四个耦合点改成
「有配置按配置、没有走今天那套」；eas-term 技能拆成渐进式披露，新增的引导流程作为
按需文件之一。内置路径的代码分支**原样保留**，不做「统一成一条」的重构 ——
那会让老库生成的说明书文字发生变化。

**Tech Stack:** TypeScript · Electron 主进程 · Node 内置测试器（`node --test`，
Node 26 原生跑 `.ts`，零新依赖）

## Global Constraints

- **老库一个字节不变**：没有 `.eas-wiki.json` 的库，所有行为与今天完全一致。
  这是本计划的首要不变量，每个任务都要保住它。
- 新逻辑放**不 import electron 的文件**里 —— `paths.ts` 因为 `cfgFile()` 依赖
  `app.getPath('userData')` 而 import 了 electron，纯 node 测试加载不了它。
- 测试用 `node --test <文件>`，不引入任何测试框架依赖。
- 配置文件名固定 `.eas-wiki.json`。点号开头是有意的：`walkNotes` 跳过点开头的条目，
  它不会被当成笔记扫进图谱。
- 校验失败时**回落到内置八目录**，不能让库变成打不开。
- 类型检查命令：`npm run typecheck`。构建：`npm run build`。

---

### Task 1: 分类配置的读取与校验

**Files:**
- Create: `src/main/wiki/taxonomy.ts`
- Test: `src/main/wiki/taxonomy.test.ts`

**Interfaces:**
- Consumes: 无（本任务是最底层）
- Produces:
  - `TAXONOMY_FILE = '.eas-wiki.json'`
  - `interface TaxonomyDir { name: string; purpose: string; role?: 'inbox' | 'templates' | 'raw' }`
  - `interface Taxonomy { version: number; dirs: TaxonomyDir[]; frontMatter: { required: string[]; optional: string[] } }`
  - `validateTaxonomy(raw: unknown): { ok: true; value: Taxonomy } | { ok: false; error: string }`
  - `readTaxonomy(root: string): Taxonomy | null`（读不到 / 坏了都返回 null）

- [ ] **Step 1: 写失败的测试**

创建 `src/main/wiki/taxonomy.test.ts`：

```ts
import { test } from 'node:test'
import assert from 'node:assert'
import fs from 'fs'
import os from 'os'
import path from 'path'
import { validateTaxonomy, readTaxonomy, TAXONOMY_FILE } from './taxonomy.ts'

const GOOD = {
  version: 1,
  dirs: [
    { name: '00-收件箱', purpose: '还没整理的原始素材', role: 'inbox' },
    { name: '课题', purpose: '一个课题一篇' },
    { name: '_模板', purpose: '新建笔记的模板', role: 'templates' }
  ],
  frontMatter: { required: ['summary', 'tags'], optional: ['status'] }
}

test('合法配置通过校验，并补齐 optional 默认值', () => {
  const r = validateTaxonomy(GOOD)
  assert.equal(r.ok, true)
  if (!r.ok) return
  assert.equal(r.value.dirs.length, 3)
  assert.deepEqual(r.value.frontMatter.optional, ['status'])
})

test('缺 optional 时补成空数组', () => {
  const r = validateTaxonomy({ ...GOOD, frontMatter: { required: ['summary'] } })
  assert.equal(r.ok, true)
  if (!r.ok) return
  assert.deepEqual(r.value.frontMatter.optional, [])
})

test('没有 inbox 角色 → 拒绝', () => {
  const dirs = GOOD.dirs.map((d) => ({ ...d, role: d.role === 'inbox' ? undefined : d.role }))
  const r = validateTaxonomy({ ...GOOD, dirs })
  assert.equal(r.ok, false)
  if (r.ok) return
  assert.match(r.error, /收件箱/)
})

test('两个 inbox 角色 → 拒绝', () => {
  const dirs = [...GOOD.dirs, { name: '另一个', purpose: 'x', role: 'inbox' }]
  const r = validateTaxonomy({ ...GOOD, dirs })
  assert.equal(r.ok, false)
})

test('目录名带斜杠 / 点开头 / 撞配置文件名 → 拒绝', () => {
  for (const bad of ['a/b', '.hidden', TAXONOMY_FILE]) {
    const dirs = [...GOOD.dirs, { name: bad, purpose: 'x' }]
    const r = validateTaxonomy({ ...GOOD, dirs })
    assert.equal(r.ok, false, `应该拒绝：${bad}`)
  }
})

test('重名目录 → 拒绝', () => {
  const dirs = [...GOOD.dirs, { name: '课题', purpose: '重复的' }]
  assert.equal(validateTaxonomy({ ...GOOD, dirs }).ok, false)
})

test('purpose 为空 → 拒绝（它要写进 agent 说明书，空的等于没说）', () => {
  const dirs = [...GOOD.dirs, { name: '新目录', purpose: '   ' }]
  assert.equal(validateTaxonomy({ ...GOOD, dirs }).ok, false)
})

test('readTaxonomy：文件不存在 → null', () => {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'wiki-'))
  assert.equal(readTaxonomy(dir), null)
})

test('readTaxonomy：JSON 坏了 → null（回落到内置，不能让库打不开）', () => {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'wiki-'))
  fs.writeFileSync(path.join(dir, TAXONOMY_FILE), '{ 这不是 JSON')
  assert.equal(readTaxonomy(dir), null)
})

test('readTaxonomy：合法文件 → 解析出来', () => {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'wiki-'))
  fs.writeFileSync(path.join(dir, TAXONOMY_FILE), JSON.stringify(GOOD))
  const t = readTaxonomy(dir)
  assert.notEqual(t, null)
  assert.equal(t?.dirs[0].name, '00-收件箱')
})
```

- [ ] **Step 2: 跑测试确认它失败**

Run: `node --test src/main/wiki/taxonomy.test.ts`
Expected: FAIL — `Cannot find module './taxonomy.ts'`

- [ ] **Step 3: 写实现**

创建 `src/main/wiki/taxonomy.ts`：

```ts
// 库的分类配置：让用户按自己的方式组织知识库。
//
// **这个文件不许 import electron。** paths.ts 因为 cfgFile() 要 app.getPath('userData')
// 而依赖了 electron，纯 node 测试加载不了它 —— 校验逻辑放这里才测得住，
// 而这份逻辑正是「配错了会让库打不开」的地方，最需要测试。
import fs from 'fs'
import path from 'path'

/** 库根目录下的分类配置。点号开头是有意的：walkNotes 跳过点开头的条目，
 *  它不会被当成笔记扫进图谱、也不会被算成孤儿页。 */
export const TAXONOMY_FILE = '.eas-wiki.json'

export interface TaxonomyDir {
  name: string
  /** 一句话说清这里装什么。**不是注释，是要写进 agent 说明书的正文** ——
   *  这句写不清楚，agent 就不知道东西该往哪放。 */
  purpose: string
  /** inbox：收件箱（有专属机制，必须恰好一个）；templates：新建笔记的模板来源；
   *  raw：原始素材，不进索引也不算反链 */
  role?: 'inbox' | 'templates' | 'raw'
}

export interface Taxonomy {
  version: number
  dirs: TaxonomyDir[]
  frontMatter: { required: string[]; optional: string[] }
}

type Result = { ok: true; value: Taxonomy } | { ok: false; error: string }

const isStr = (v: unknown): v is string => typeof v === 'string'
const strArr = (v: unknown): string[] =>
  Array.isArray(v) ? v.filter(isStr).map((s) => s.trim()).filter(Boolean) : []

/** 校验一份配置。**拒绝比放行安全**：配置错了顶多回落到内置分类，
 *  而放行一份缺收件箱的配置会让整理流程整条断掉，症状还很难联想到配置。 */
export function validateTaxonomy(raw: unknown): Result {
  if (!raw || typeof raw !== 'object' || Array.isArray(raw)) return { ok: false, error: '配置不是一个对象' }
  const o = raw as Record<string, unknown>
  if (!Array.isArray(o.dirs) || o.dirs.length === 0) return { ok: false, error: 'dirs 得是非空数组' }

  const dirs: TaxonomyDir[] = []
  const seen = new Set<string>()
  for (const d of o.dirs) {
    if (!d || typeof d !== 'object') return { ok: false, error: 'dirs 里有不是对象的项' }
    const e = d as Record<string, unknown>
    const name = isStr(e.name) ? e.name.trim() : ''
    const purpose = isStr(e.purpose) ? e.purpose.trim() : ''
    if (!name) return { ok: false, error: '有目录没有 name' }
    // 这三条都会让库出问题：斜杠会建出嵌套、点开头会被 walkNotes 跳过（等于永远读不到）、
    // 撞配置文件名会互相覆盖
    if (name.includes('/') || name.includes('\\')) return { ok: false, error: `目录名不能含斜杠：${name}` }
    if (name.startsWith('.')) return { ok: false, error: `目录名不能以点开头（会被当成隐藏项跳过）：${name}` }
    if (name === TAXONOMY_FILE) return { ok: false, error: `目录名不能和配置文件同名：${name}` }
    if (seen.has(name)) return { ok: false, error: `目录名重复：${name}` }
    if (!purpose) return { ok: false, error: `「${name}」没写 purpose —— 那句话要写进 agent 说明书，空的等于没说` }
    seen.add(name)
    const role = e.role
    if (role !== undefined && role !== 'inbox' && role !== 'templates' && role !== 'raw')
      return { ok: false, error: `「${name}」的 role 不认识：${String(role)}` }
    dirs.push({ name, purpose, ...(role ? { role: role as TaxonomyDir['role'] } : {}) })
  }

  const inbox = dirs.filter((d) => d.role === 'inbox')
  if (inbox.length !== 1)
    return {
      ok: false,
      error: `必须恰好有一个 role:"inbox" 的目录（现在有 ${inbox.length} 个）—— 收件箱有专属机制（徽章计数、记录素材原来在哪），没有它整理流程会断`
    }
  if (dirs.filter((d) => d.role === 'templates').length > 1)
    return { ok: false, error: 'role:"templates" 最多一个' }

  const fmRaw = (o.frontMatter ?? {}) as Record<string, unknown>
  const required = strArr(fmRaw.required)
  if (required.length === 0) return { ok: false, error: 'frontMatter.required 不能为空' }

  return {
    ok: true,
    value: {
      version: typeof o.version === 'number' ? o.version : 1,
      dirs,
      frontMatter: { required, optional: strArr(fmRaw.optional) }
    }
  }
}

/** 读一个库的分类配置。**读不到、解析不了、校验不过，一律返回 null** ——
 *  调用方据此回落到内置八目录。不抛异常：一份手工改坏的配置不该让库打不开。 */
export function readTaxonomy(root: string): Taxonomy | null {
  try {
    const raw = JSON.parse(fs.readFileSync(path.join(root, TAXONOMY_FILE), 'utf8'))
    const r = validateTaxonomy(raw)
    if (!r.ok) {
      console.warn(`[wiki] ${TAXONOMY_FILE} 校验不过，回落到内置分类：${r.error}`)
      return null
    }
    return r.value
  } catch {
    return null
  }
}
```

- [ ] **Step 4: 跑测试确认全过**

Run: `node --test src/main/wiki/taxonomy.test.ts`
Expected: PASS，10 个测试全绿

- [ ] **Step 5: 类型检查**

Run: `npm run typecheck`
Expected: 无输出（通过）

- [ ] **Step 6: 提交**

```bash
git add src/main/wiki/taxonomy.ts src/main/wiki/taxonomy.test.ts
git commit -m "feat(wiki): 分类配置的读取与校验

.eas-wiki.json 让用户按自己的方式分类。这个文件刻意不 import electron ——
paths.ts 因为 cfgFile() 依赖 app.getPath 而引了 electron，纯 node 测试加载不了，
而校验逻辑正是「配错了会让库打不开」的地方，最需要测试。

校验从严：缺收件箱、目录重名、名字带斜杠或点开头、purpose 为空一律拒绝。
读取从宽：读不到/解析不了/校验不过都返回 null，调用方回落到内置分类 ——
一份手工改坏的配置不该让库变成打不开。"
```

---

### Task 2: 库形状的统一入口

**Files:**
- Modify: `src/main/wiki/taxonomy.ts`（追加）
- Test: `src/main/wiki/taxonomy.test.ts`（追加）

**Interfaces:**
- Consumes: Task 1 的 `Taxonomy`、`readTaxonomy`
- Produces:
  - `interface LibraryDir { name: string; purpose: string; role?: 'inbox' | 'templates' | 'raw' }`
  - `BUILTIN_DIRS: LibraryDir[]`（内置八目录 + 各自的一句话）
  - `libraryDirs(root: string, resolve: (key: string) => string): LibraryDir[]`
    —— `resolve` 由调用方注入（`paths.ts` 传 `dirOf` 做老库中文名回落），
    这样本文件仍然不用 import electron

- [ ] **Step 1: 写失败的测试**

追加到 `src/main/wiki/taxonomy.test.ts`：

```ts
import { libraryDirs, BUILTIN_DIRS } from './taxonomy.ts'

test('没有配置 → 内置八目录，顺序与名字都不变', () => {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'wiki-'))
  const got = libraryDirs(dir, (k) => k)
  assert.deepEqual(got.map((d) => d.name), [
    '00-inbox', 'me', 'people', 'methods', 'domains', 'projects', 'sources', '_templates'
  ])
  assert.equal(got.find((d) => d.role === 'inbox')?.name, '00-inbox')
})

test('没有配置时 resolve 生效（老库中文名回落）', () => {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'wiki-'))
  const got = libraryDirs(dir, (k) => (k === '00-inbox' ? '00-收件箱' : k))
  assert.equal(got[0].name, '00-收件箱')
  assert.equal(got[0].role, 'inbox')
})

test('有配置 → 用配置的，resolve 不介入', () => {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'wiki-'))
  fs.writeFileSync(path.join(dir, TAXONOMY_FILE), JSON.stringify({
    version: 1,
    dirs: [{ name: '收件', purpose: 'x', role: 'inbox' }, { name: '课题', purpose: 'y' }],
    frontMatter: { required: ['summary'] }
  }))
  const got = libraryDirs(dir, () => '不该被调用')
  assert.deepEqual(got.map((d) => d.name), ['收件', '课题'])
})

test('内置八目录每个都有非空 purpose', () => {
  for (const d of BUILTIN_DIRS) assert.ok(d.purpose.trim().length > 0, d.name)
})
```

- [ ] **Step 2: 跑测试确认它失败**

Run: `node --test src/main/wiki/taxonomy.test.ts`
Expected: FAIL — `libraryDirs is not a function`

- [ ] **Step 3: 写实现**

追加到 `src/main/wiki/taxonomy.ts`：

```ts
export type LibraryDir = TaxonomyDir

/** 内置分类。**顺序和名字不许动** —— 老库的说明书按这个顺序生成，
 *  改了会让所有内置库的 CLAUDE.md 在下次启动时被重写成不同的文字。 */
export const BUILTIN_DIRS: LibraryDir[] = [
  { name: '00-inbox', purpose: '用户丢进来、还没整理的原始素材', role: 'inbox' },
  { name: 'me', purpose: '关于用户自己的：偏好、习惯、决定过的事' },
  { name: 'people', purpose: '关于别人的：合作方、同事、客户' },
  { name: 'methods', purpose: '可复用的做法与套路' },
  { name: 'domains', purpose: '某个领域的知识积累' },
  { name: 'projects', purpose: '具体项目的进展与结论' },
  { name: 'sources', purpose: '原始出处：论文、文章、录音的存档', role: 'raw' },
  { name: '_templates', purpose: '新建笔记的模板', role: 'templates' }
]

/** 这个库实际长什么样。有 .eas-wiki.json 就按它，没有就是内置八目录。
 *
 *  `resolve` 由调用方注入而不是在这里 import dirOf —— 那会把 electron 依赖
 *  拖进这个文件（paths.ts 引了 electron），测试就加载不了了。
 *  内置路径需要它做老库中文名回落；自定义路径不需要，名字就是配置里写的。 */
export function libraryDirs(root: string, resolve: (key: string) => string): LibraryDir[] {
  const t = readTaxonomy(root)
  if (t) return t.dirs
  return BUILTIN_DIRS.map((d) => ({ ...d, name: resolve(d.name) }))
}
```

- [ ] **Step 4: 跑测试确认全过**

Run: `node --test src/main/wiki/taxonomy.test.ts`
Expected: PASS，14 个测试全绿

- [ ] **Step 5: 提交**

```bash
git add src/main/wiki/taxonomy.ts src/main/wiki/taxonomy.test.ts
git commit -m "feat(wiki): 库形状的统一入口 libraryDirs

有 .eas-wiki.json 按它，没有就是内置八目录。resolve 由调用方注入而不是
在这里 import dirOf —— 那会把 electron 依赖拖进来，测试就加载不了。

BUILTIN_DIRS 的顺序和名字不许动：老库说明书按这个顺序生成，改了会让
所有内置库的 CLAUDE.md 在下次启动时被重写成不同的文字。"
```

---

### Task 3: paths.ts 接上闸门

**Files:**
- Modify: `src/main/wiki/paths.ts:70-76`（`inboxOf` / `isRawDir`）、`:115-137`（`walkNotes`）、`:142-154`（`looksLikeWiki`）
- Test: `src/main/wiki/paths.test.ts`（新建，只测不碰 electron 的纯函数）

**Interfaces:**
- Consumes: Task 2 的 `libraryDirs`、Task 1 的 `TAXONOMY_FILE`
- Produces: `isRawDir(root: string, rel: string): boolean`（**签名变了**，多收一个 root）

- [ ] **Step 1: 先确认现在的调用点只有一处**

Run: `grep -rn "isRawDir" src/`
Expected: 只有 `paths.ts` 里的定义（76 行）和 `walkNotes` 里的调用（128 行）。
若出现别的调用点，那些也要一起改签名。

- [ ] **Step 2: 写失败的测试**

创建 `src/main/wiki/paths.test.ts`：

```ts
import { test } from 'node:test'
import assert from 'node:assert'
import fs from 'fs'
import os from 'os'
import path from 'path'
import { libraryDirs, TAXONOMY_FILE } from './taxonomy.ts'

// paths.ts 引了 electron，这里不 import 它 —— 改判定逻辑本身放在 taxonomy 里，
// 这个文件只钉住「自定义库的原始素材区判定」这条行为
const rawDirsOf = (root: string): Set<string> =>
  new Set(libraryDirs(root, (k) => k).filter((d) => d.role === 'inbox' || d.role === 'raw').map((d) => d.name))

test('内置库：收件箱和 sources 算原始素材区', () => {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'wiki-'))
  const raw = rawDirsOf(dir)
  assert.ok(raw.has('00-inbox'))
  assert.ok(raw.has('sources'))
  assert.ok(!raw.has('methods'))
})

test('自定义库：按配置里的 role 判，内置那些名字不再特殊', () => {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'wiki-'))
  fs.writeFileSync(path.join(dir, TAXONOMY_FILE), JSON.stringify({
    version: 1,
    dirs: [
      { name: '收件', purpose: 'x', role: 'inbox' },
      { name: '原始档', purpose: 'y', role: 'raw' },
      { name: 'sources', purpose: '这个库里 sources 是正经笔记目录' }
    ],
    frontMatter: { required: ['summary'] }
  }))
  const raw = rawDirsOf(dir)
  assert.ok(raw.has('收件'))
  assert.ok(raw.has('原始档'))
  assert.ok(!raw.has('sources'), 'sources 在这个库里没有 raw 角色，不该被跳过')
})
```

- [ ] **Step 3: 跑测试确认它失败**

先把测试改成直接打 `paths.ts` 的真实导出（而不是在测试里重写一遍判定逻辑，
那样测的是测试自己、永远会过）。在测试文件顶部加：

```ts
// paths.ts 引了 electron，node 测试加载不了 —— 用一个最小桩顶掉它。
// 只桩 app.getPath：paths.ts 里只有 cfgFile() 用到它，且本文件不测那个函数。
import { register } from 'node:module'
process.env.EAS_TEST = '1'
```

若这条桩不通（electron 的导入形态导致），**改为把判定逻辑整体搬进 `taxonomy.ts`**
（导出 `isRawName(root, rel, resolve)`），`paths.ts` 只做一行转发 ——
测得住比结构好看重要，而这条判定错了会让整片笔记被当成原始素材跳过。

Run: `node --test src/main/wiki/paths.test.ts`
Expected: **FAIL** —— 自定义库那条会失败（此刻 `isRawDir` 还是硬编码的
`RAW_DIRS`，不认配置里的 `role`）。没看到 FAIL 就不要往下走，那说明测试没打到真实实现。

- [ ] **Step 4: 改 paths.ts**

把 `inboxOf` / `isRawDir` 改成走 `libraryDirs`：

```ts
import { libraryDirs, TAXONOMY_FILE } from './taxonomy'

export const INBOX = '00-inbox'

/** 这个库的收件箱目录名。自定义库按配置里 role:"inbox" 那个，内置库还是 00-inbox
 *  （含老库中文名回落）。 */
export const inboxOf = (root: string): string =>
  libraryDirs(root, (k) => dirOf(root, k)).find((d) => d.role === 'inbox')?.name ?? dirOf(root, INBOX)

/** 原始素材区（收件箱 + 素材）：不是笔记，不进索引也不算反链。
 *  **签名多收一个 root**：自定义库的原始素材区叫什么由配置决定，不看名字。
 *  唯一调用点是下面的 walkNotes，它本来就有 root。 */
export const isRawDir = (root: string, rel: string): boolean =>
  libraryDirs(root, (k) => dirOf(root, k)).some(
    (d) => d.name === rel && (d.role === 'inbox' || d.role === 'raw')
  )
```

`walkNotes` 里的调用改成 `if (isRawDir(root, r)) continue`。

`looksLikeWiki` 开头加一条：

```ts
    // 有分类配置 = 明确是我们的库，不用再看目录长什么样
    if (fs.existsSync(path.join(root, TAXONOMY_FILE))) return true
```

- [ ] **Step 5: 跑测试 + 类型检查**

Run: `node --test src/main/wiki/paths.test.ts && npm run typecheck`
Expected: 测试全绿；类型检查无输出

- [ ] **Step 6: 回归 —— 确认老库行为没变**

Run: `grep -n "RAW_DIRS" src/main/wiki/paths.ts`
Expected: 无输出（旧的硬编码 Set 已经删掉，判定统一走 `libraryDirs`）。
若还在，说明有别的地方在用，要一并迁走 —— 留着两套判据迟早对不上。

⚠️ **一个容易漏的老库边缘情况**：原来的 `RAW_DIRS` 是
`['00-inbox','00-收件箱','sources','素材']` —— **中英文名同时在里面**。
改成走 `dirOf` 之后只会返回盘上实际存在的那一个。所以一个中文老库里如果**同时**
存在 `00-收件箱` 和 `00-inbox` 两个目录，后者会从「被跳过」变成「被当成笔记扫进去」。

加一个测试钉住这条：

```ts
test('中文老库里同时存在两套目录名时，两个都仍算原始素材区', () => {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'wiki-'))
  fs.mkdirSync(path.join(dir, '00-收件箱'))
  fs.mkdirSync(path.join(dir, '00-inbox'))
  // 两个都不该被当成笔记目录 —— 这是「老库一个字节不变」的一部分
  assert.ok(isRawDirNames(dir).has('00-收件箱'))
  assert.ok(isRawDirNames(dir).has('00-inbox'))
})
```

实现上：内置路径的 raw 判定**保留中英文双名**（即在 `BUILTIN_DIRS` 之外，
内置分支额外把 `LEGACY` 里的别名也算进 raw 集合），自定义路径只认配置里的 `role`。

- [ ] **Step 7: 提交**

```bash
git add src/main/wiki/paths.ts src/main/wiki/paths.test.ts
git commit -m "feat(wiki): inboxOf / isRawDir / looksLikeWiki 接上分类配置

isRawDir 签名多收一个 root —— 自定义库的原始素材区叫什么由配置的 role 决定，
不能再看名字。唯一调用点 walkNotes 本来就有 root。

删掉硬编码的 RAW_DIRS：判定统一走 libraryDirs，留着两套判据迟早对不上。"
```

---

### Task 4: 说明书按自定义分类生成

**Files:**
- Modify: `src/main/wiki/schema.ts:39-51`（`dirNames`）、`:53`（`schemaBody`）、`:145`（`schemaText`）、`:251`（`initWiki`）
- Test: `src/main/wiki/schema.test.ts`（新建）

**Interfaces:**
- Consumes: Task 2 的 `libraryDirs`、`LibraryDir`
- Produces: `customSchemaBody(dirs: LibraryDir[]): string`

**为什么不把内置那条也改成走列表**：`schemaBody(d: WikiDirNames)` 用的是八个具名字段
（`d.inbox`、`d.me`…），改成遍历列表会让生成的文字发生变化 —— 而那份文字会在下次启动时
被写进每一个内置库的 `CLAUDE.md`。所以内置路径**一个字符都不动**，自定义走另一条。

- [ ] **Step 1: 写黄金测试（钉住「老库一个字节不变」）**

创建 `src/main/wiki/schema.test.ts`：

```ts
import { test } from 'node:test'
import assert from 'node:assert'
import fs from 'fs'
import os from 'os'
import path from 'path'
import { customSchemaBody } from './schema.ts'
import type { LibraryDir } from './taxonomy.ts'

const DIRS: LibraryDir[] = [
  { name: '00-收件箱', purpose: '还没整理的原始素材', role: 'inbox' },
  { name: '课题', purpose: '一个课题一篇，记进展与结论' },
  { name: '_模板', purpose: '新建笔记的模板', role: 'templates' }
]

test('自定义说明书逐条列出目录名与它的一句话', () => {
  const s = customSchemaBody(DIRS)
  for (const d of DIRS) {
    assert.ok(s.includes(d.name), `说明书里没提到 ${d.name}`)
    assert.ok(s.includes(d.purpose), `说明书里没写 ${d.name} 的 purpose`)
  }
})

test('自定义说明书不含内置目录名（否则 agent 会往不存在的目录放东西）', () => {
  const s = customSchemaBody(DIRS)
  for (const gone of ['me/', 'people/', 'methods/', 'domains/']) {
    assert.ok(!s.includes(gone), `说明书里不该出现内置目录 ${gone}`)
  }
})

test('front-matter 硬要求仍然写在说明书里', () => {
  const s = customSchemaBody(DIRS)
  assert.ok(s.includes('summary'))
  assert.ok(s.includes('tags'))
})
```

- [ ] **Step 2: 跑测试确认它失败**

Run: `node --test src/main/wiki/schema.test.ts`
Expected: FAIL — `customSchemaBody is not exported`

- [ ] **Step 3: 写实现**

在 `schema.ts` 里新增（**不动现有的 `schemaBody`**）：

```ts
import { libraryDirs, type LibraryDir } from './taxonomy'

/** 自定义分类的库用这份说明书。**内置库不走这里** —— 那条路的文字一个字符都不能变，
 *  否则每个内置库的 CLAUDE.md 都会在下次启动时被重写成不同的内容。 */
export function customSchemaBody(dirs: LibraryDir[]): string {
  const lines = [
    '这个知识库的分类是你自己定的（见库根目录的 `.eas-wiki.json`）。',
    '',
    '## 往哪放',
    ''
  ]
  for (const d of dirs) lines.push(`- \`${d.name}/\` ${d.purpose}`)
  lines.push(
    '',
    '## 每篇都要有 front-matter',
    '',
    '`summary` 和 `tags` 是硬要求 —— 查询、图谱、体检全靠它们。',
    '',
    '```',
    '---',
    'summary: 一句话说清这篇讲什么',
    'tags: [标签1, 标签2]',
    '---',
    '```',
    '',
    '## 互相引用用 `[[双链]]`',
    '',
    '写 `[[笔记名]]` 就建立了关联，图谱和反链都靠它。'
  )
  return lines.join('\n')
}
```

**要改的调用方有两处**（不点名的话很容易只改一处，症状是「新建的库对了、
升级已有库时又写回内置文字」）：`upgradeSchemaFile()`（schema.ts:155）和
`initWiki()`（schema.ts:251）。两处都从 `schemaText(dirNames(root))`
改成 `schemaTextFor(root)`。

分流入口：

```ts
// schemaText 之外新增一个按 root 分流的入口，调用方改用它
export function schemaTextFor(root: string): string {
  const t = readTaxonomy(root)
  if (!t) return schemaText(dirNames(root)) // 内置：原样不动
  return SCHEMA_BEGIN + '\n' + customSchemaBody(t.dirs) + '\n' + SCHEMA_END
}
```

`initWiki(root)` 里那个 `for (const key of WIKI_DIRS)` 改成
`for (const d of libraryDirs(root, (k) => dirOf(root, k)))`，建 `d.name`。

- [ ] **Step 4: 跑测试 + 类型检查**

Run: `node --test src/main/wiki/schema.test.ts && npm run typecheck`
Expected: 测试全绿；类型检查无输出

- [ ] **Step 5: 手工回归 —— 内置库的说明书没变**

```bash
node --input-type=module -e "
import { schemaText, dirNames } from './src/main/wiki/schema.ts'
console.log(schemaText(dirNames('/tmp/nonexistent')).length)
"
```
Expected: 打印一个数字且不报错。把这个数字和改动前的比对（改动前先跑一次存下来）——
**必须完全相等**。不等就说明内置路径被动到了，回退重来。

- [ ] **Step 6: 提交**

```bash
git add src/main/wiki/schema.ts src/main/wiki/schema.test.ts
git commit -m "feat(wiki): 自定义分类的库生成自己的说明书

新增 customSchemaBody，按配置逐条列出目录名与它的一句话。
**内置那条路一个字符都没动** —— schemaBody 用的是八个具名字段，
改成遍历列表会让生成的文字变化，而那份文字会在下次启动时写进
每一个内置库的 CLAUDE.md。所以两条路分开走，不做「统一成一条」的重构。

initWiki 建骨架改成按 libraryDirs 走。"
```

---

### Task 5: 技能拆成渐进式披露

**Files:**
- Modify: `skills/eas-term/SKILL.md`（267 行 → 约 70 行）
- Create: `skills/eas-term/canvas.md`、`skills/eas-term/generate.md`、`skills/eas-term/secrets.md`

**Interfaces:**
- Consumes: 无（纯内容工作）
- Produces: 五个文件的约定 —— Task 6 的分发改造按这个清单来

**切分判据**：规则常驻，操作步骤按需。判断标准不是长短，是
「不知道它会不会做错事」—— 「生图只能走笔纵画板」是规则（漏了模型会去调别的 API），
「怎么两阶段确认」是步骤。

> **这个任务没有单元测试，是有意的**：产出是给模型读的自然语言，不是代码。
> 它的验收在 Step 4-5（行数、内容没丢的 grep）和 Task 6 Step 7（真起一个 claude
> 逐条实测触发）。审查时请按这个标准判，不要因为「没有 .test.ts」判它不合格。

- [ ] **Step 1: 先量出现状，作为拆分后的对照**

```bash
wc -l skills/eas-term/SKILL.md
```
Expected: 267。记下来 —— 拆完常驻部分要降到 100 行以内。

- [ ] **Step 2: 把三块搬进各自的文件**

- `canvas.md` ← 现「### 具体场景」「## 完整工具表」「## 如果工具不见了」三节
- `generate.md` ← 现「## 要出图 / 出视频时」整节（含全部子节）。
  **这一节 2026-08-11 晚刚按上游新文档扩过**（改已配好的节点、六个坑、意图→该调什么的
  决策表），搬的时候整节照搬，别顺手精简 —— 那张决策表是上游明说「不写进 agent 规则就会卡」的。
- `secrets.md` ← 现「## 密钥怎么拿」整节（含「### 三条不要越的线」）

每个文件开头加一句说明它是什么、什么时候该读。

- [ ] **Step 3: 把 SKILL.md 改写成路由**

保留：`## 最重要的一条`、`## 什么时候用哪个工具`（精简）、`## 上下文是自动的`、
`## 边界：这些做不到`、`## 分寸`。

三条触发条件**各占一行、分开写**（不许合并 —— `codexRegion()` 的注释记着教训：
生图和摆放合并写，模型只会想到摆放，想不到自己能生成）：

```markdown
## 按需细读

下面三件事**规则写在这里，具体怎么做去读对应文件**：

- **要操作画布**（开预览、打开网址、整理布局、贴便签、通知）→ 读 `canvas.md`
- **用户要图 / 封面 / 海报 / 视频** → **只能走笔纵画板的 MCP（`bizone-canvas`），
  不许调别的图像 API，也不许自己写生成脚本** → 具体步骤读 `generate.md`
- **撞到缺 key / 401 / 鉴权失败** → **绝不让用户把密钥贴进对话** → 怎么拿读 `secrets.md`
```

- [ ] **Step 4: 量拆分后的常驻行数**

```bash
wc -l skills/eas-term/*.md
```
Expected: `SKILL.md` ≤ 100 行；四个文件相加与原来 267 行大致相当（内容没丢）

- [ ] **Step 5: 人工核对内容没丢**

```bash
grep -c "笔纵画板\|autoConfirm\|confirm_batch_generate" skills/eas-term/generate.md
grep -c "secret_check\|request_secret\|report_secret_invalid" skills/eas-term/secrets.md
```
Expected: 两条都 > 0。为 0 说明搬运时漏了正文。

- [ ] **Step 6: 提交**

```bash
git add skills/eas-term/
git commit -m "refactor(skill): eas-term 技能拆成渐进式披露

267 行、每会话常驻读，再往里加就是纯浪费。按功能切开：
常驻只留触发条件 + 边界 + 分寸，画布工具表 / 生图 / 密钥各自成文件。

判据是「规则常驻、操作步骤按需」，不是按长短切：
生图只能走笔纵画板是规则（漏了模型会去调别的 API），怎么两阶段确认是步骤。

三条触发条件各占一行、分开写 —— codexRegion 的注释里记着教训：
生图和摆放合并写，模型只会想到摆放，想不到自己能生成。"
```

---

### Task 6: 分发改造（装目录而不是装单个文件）

**Files:**
- Modify: `src/main/agentSkill.ts:40-41`（源路径）、`:82-83`（目标路径）
- Modify: `src/main/agentRules.ts:51`（`canvasSkill`）、`:81-95`（`codexRegion`）、`:143`、`:153`

**Interfaces:**
- Consumes: Task 5 产出的五文件清单
- Produces: 无（这是最后一环）

- [ ] **Step 1: 先列出所有写死单文件的地方**

```bash
grep -rn "eas-term'.*SKILL.md\|SKILL.md" src/main/agentSkill.ts src/main/agentRules.ts
```
Expected: 至少 4 处。每一处都要改，漏一处的症状是「触发了但 agent 找不到细节文件」。

- [ ] **Step 2: agentSkill.ts 改成拷目录**

```ts
/** 技能包源目录。**是目录不是文件** —— 技能拆成渐进式披露之后有五个 .md，
 *  按单个文件处理会把细节文件全漏掉，症状是「触发了但 agent 找不到流程」。 */
const skillSrcDir = (): string =>
  app.isPackaged
    ? path.join(process.resourcesPath, 'skills', 'eas-term')
    : path.join(app.getAppPath(), 'skills', 'eas-term')

/** 目录里的所有 .md。排序是为了让「装了哪些」这件事可复现，便于比对。 */
const skillFiles = (dir: string): string[] => {
  try {
    return fs.readdirSync(dir).filter((f) => f.endsWith('.md')).sort()
  } catch {
    return []
  }
}

const claudeSkillDir = (): string =>
  path.join(app.getPath('home'), '.claude', 'skills', 'eas-term')
```

安装处改成遍历：

```ts
const src = skillSrcDir()
const dst = claudeSkillDir()
fs.mkdirSync(dst, { recursive: true })
for (const f of skillFiles(src)) fs.copyFileSync(path.join(src, f), path.join(dst, f))
```

「装没装」的判据也要跟着改：原来查 `SKILL.md` 在不在，现在要查**每个源文件都在**，
否则少拷一个会被判成「已安装」。

```ts
const installed = (): boolean => {
  const src = skillFiles(skillSrcDir())
  if (src.length === 0) return false
  return src.every((f) => fs.existsSync(path.join(claudeSkillDir(), f)))
}
```

- [ ] **Step 3: agentRules.ts 的 canvasSkill 改成读目录**

```ts
/** 技能包里的所有文件。原来只读 SKILL.md 一个 —— 拆成渐进式披露之后
 *  细节文件也要分发，Codex 那边尤其：它没有 skill 机制，
 *  常驻区只放指针，正文必须真的写到 ~/.eas/agent/ 下才读得到。 */
const canvasSkillFiles = (): { name: string; text: string }[] => {
  const dir = path.join(sourceRoot(), 'skills', 'eas-term')
  try {
    return fs
      .readdirSync(dir)
      .filter((f) => f.endsWith('.md'))
      .sort()
      .map((f) => ({ name: f, text: fs.readFileSync(path.join(dir, f), 'utf8') }))
  } catch {
    return []
  }
}
```

写入处（原 143 行与 153 行）改成逐个写：

```ts
const files = canvasSkillFiles()
for (const f of files) {
  writeFileEnsured(path.join(home(), '.claude', 'skills', 'eas-term', f.name), f.text)
  writeFileEnsured(path.join(detailDir(), f.name), f.text)
}
```

- [ ] **Step 4: codexRegion 的触发条件从 2 条扩到 4 条**

每条指向自己的细节文件绝对路径，而不是全部指向 `canvas.md`：

```ts
lines.push('**画板**：产出了给人看的东西 → 用画板 MCP 工具摆到用户眼前，')
lines.push(`别只说「已生成」。详细：\`${path.join(detailDir(), 'canvas.md')}\``, '')
lines.push('**生图 / 生视频**：用户要图、封面、海报、视频 → 走「笔纵画板」的 MCP')
lines.push(`（\`bizone-canvas\`），不要调别的图像 API。详细：\`${path.join(detailDir(), 'generate.md')}\``, '')
lines.push('**缺密钥**：撞到 401 / 鉴权失败 → 别让用户把 key 贴进对话，')
lines.push(`走密钥柜。详细：\`${path.join(detailDir(), 'secrets.md')}\``, '')
lines.push('**重新设计知识库**：用户说分类不合适 / 要自定义知识库 →')
lines.push(`详细：\`${path.join(detailDir(), 'wiki-architect.md')}\``, '')
```

- [ ] **Step 5: 类型检查 + 构建**

Run: `npm run typecheck && npm run build`
Expected: 两个都无错

- [ ] **Step 6: 真机验证安装结果**

```bash
SCRATCH=<本会话 scratchpad>
npx electron . --user-data-dir="$SCRATCH/skilltest" &
# 等它起来后，在界面「扩展能力」里点安装，然后：
ls ~/.claude/skills/eas-term/
ls ~/.eas/agent/
```
Expected: 两处都有五个 `.md` 文件。少一个就是漏拷。

> ⚠️ 这一步会写用户的真实 `~/.claude/skills/` 和 `~/.eas/agent/`。
> 先备份这两个目录，验完还原。

- [ ] **Step 7: 实测触发条件真的会让模型去读细节文件**

这是渐进式披露的固有风险，而且**失败是静默的** —— 模型不会说「我没读到」，
它会直接凭印象干活。所以必须逐条实测，不能只看行数。

在测试实例的终端里起一个 claude，逐条说下面四句，看它有没有去读对应文件
（观察它的工具调用里有没有 Read 那个文件）：

| 说什么 | 该读哪个 | 判据 |
|---|---|---|
| 「把这个 html 在画布上打开」 | `canvas.md` | 调了画布工具且行为正确 |
| 「画一张封面」 | `generate.md` | **提到笔纵画板**，没有去调别的图像 API |
| 「跑这个脚本，它要 OPENAI_API_KEY」 | `secrets.md` | **没有让用户把 key 贴进对话** |
| 「我想按自己的方式重新建知识库」 | `wiki-architect.md` | 开始问分类，没有直接动老库 |

任何一条没触发，回去把 SKILL.md 里那行触发条件写具体 —— 通常是漏了用户
实际会说的那个词。

- [ ] **Step 8: 提交**

```bash
git add src/main/agentSkill.ts src/main/agentRules.ts
git commit -m "feat(skill): 分发改成装整个目录

技能拆成五个文件之后，原来「把 SKILL.md 当单个文件处理」的三处硬编码
就漏掉了细节文件 —— 症状是触发了但 agent 找不到流程。

codexRegion 的触发条件从 2 条扩到 4 条，各指各的文件：Codex 没有 skill 机制，
常驻区只放触发条件 + 路径指针，正文在 ~/.eas/agent/。"
```

---

### Task 7: 写自定义知识库的引导流程

**Files:**
- Create: `skills/eas-term/wiki-architect.md`
- Modify: `skills/eas-term/SKILL.md`（触发条件加第四条）

**Interfaces:**
- Consumes: Task 1-4 的 `.eas-wiki.json` 格式与校验规则；Task 5-6 的文件布局
- Produces: 无

- [ ] **Step 1: 写 wiki-architect.md**

文件的小节固定成这六个，缺一个都会让模型在那一步自由发挥：

```markdown
# 重新设计知识库

## 先声明：不动用户现有的库        ← 不搬、不改、不删；建新的、切过去
## 第一步：读现状                  ← 用 wiki_query / 文件列表看分布，摊给用户
## 第二步：问清怎么分              ← 话术见下，一次一个问题
## 第三步：给方案等确认            ← 目录清单 + 每个的一句话 + front-matter
## 第四步：建库                    ← 骨架 + .eas-wiki.json + 说明书 + 指过去
## 配置格式与校验规则              ← 字段含义 + 四条会被拒绝的情况
```

关键话术要写死在文件里，不能让模型自由发挥：
第 2 步问的是「你平时往里放的东西大致分几类、每类一句话怎么描述」，
**不是**「你要几个文件夹」—— 后者会得到一堆没有 purpose 的空目录。

- [ ] **Step 2: SKILL.md 加第四条触发**

```markdown
- **用户说知识库分类不合适 / 要按自己的方式建知识库** → **不要动他现有的库**
  （不搬、不改、不删）→ 引导流程读 `wiki-architect.md`
```

- [ ] **Step 3: 校验文件里写的格式和代码实现一致**

```bash
grep -o '"role"[^,]*' skills/eas-term/wiki-architect.md | sort -u
grep -n "'inbox' | 'templates' | 'raw'" src/main/wiki/taxonomy.ts
```
Expected: 文档里出现的 role 值必须都在代码的联合类型里。
文档写了代码不认的值，用户照着填就会被拒绝，而错误信息指向配置不指向文档。

- [ ] **Step 4: 端到端真机验证**

在测试实例里：起一个终端 → 说「我想按自己的方式重新建知识库」→ 走完引导 →
检查产物：

```bash
ls <新库>/                     # 目录骨架
cat <新库>/.eas-wiki.json      # 配置
cat <新库>/CLAUDE.md           # 说明书描述的是新分类
```

再验现有工具照常工作：`wiki_query` 查得到、图谱画得出、`wiki_lint` 不误报、
收件箱徽章正常。

- [ ] **Step 5: 回归 —— 指回老库**

把知识库路径指回原来那个内置库，确认一切和今天完全一致：
目录没多出来、`CLAUDE.md` 没被改写、查询/图谱/体检都正常。

**这条是本计划最重要的验收** —— 全局约束「老库一个字节不变」就靠它兜底。

- [ ] **Step 6: 提交**

```bash
git add skills/eas-term/
git commit -m "feat(skill): 自定义知识库的引导流程

五步：读现状 → 问清怎么分 → 给方案等确认 → 建库 → 确认。

第 2 步的话术写死在文件里：问「你平时往里放的东西大致分几类、每类一句话
怎么描述」，不是「你要几个文件夹」—— 后者会得到一堆没有 purpose 的空目录，
而 purpose 正是要写进说明书、决定 agent 往哪放东西的那句话。

纪律也写死：老库不搬、不改、不删。"
```

---

## 实现顺序与依赖

```
Task 1 ─→ Task 2 ─→ Task 3 ─┐
                   └→ Task 4 ─┴─→ Task 7
Task 5 ─→ Task 6 ──────────────┘
```

Task 1-4 是 app 侧闸门，Task 5-6 是技能拆分，两条线互不依赖、可以并行。
Task 7 两边都要有才有意义 —— 在闸门上线前照它建出来的库，收件箱会认不出、
说明书仍描述内置分类。

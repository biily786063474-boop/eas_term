# 统一状态机与四个显示面 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把「一个终端此刻在干什么」从五处各自为政收拢成一个可测的纯函数状态机，四个显示面改为它的视图。

**Architecture:** 新建 `src/renderer/src/features/status/`。`machine.ts` 是纯函数（不引 React、不引 electron、不引 store），只吃一份只读快照 `RawSignals` 吐结果，`node --test` 直接跑。`useStatus.ts` 是唯一的 store 读取口。四个视图（抽屉项目行、双击列表、右上角气泡+已完成列表、灵动岛）与运行监视全部搬进这个目录，只消费 `useStatus`。

**Tech Stack:** TypeScript · React 18 · zustand · Node 原生 `node --test`（类型剥离，零依赖）

**Spec:** `docs/superpowers/specs/2026-08-14-统一状态机-design.md`

## Global Constraints

- **`machine.ts` 绝不能 import：React、electron、`../../store`。** 只允许 import `../../layout`（零依赖、已验证可被 `node --test` 加载）和纯类型。这条破了整个模块就测不了。
- **状态判据只看当前信号，不看历史、不自持状态。** 三态判据逐字为：`running` = `ptyId ∈ runningPtys`；`approval` = 不在 `runningPtys` 且 `∈ attentionPtys` 且 `ptyApproval[ptyId]` 有值；`done` = 不在 `runningPtys` 且 `∈ attentionPtys` 且 `ptyApproval[ptyId]` 无值。
- **不一致时 `attentionPtys` 是权威**：`ptyApproval` 有残留值但 `attentionPtys` 里没有该 ptyId → 状态是 `null`，不是 `approval`。
- **排序恒为**：`approval` > `done` > `running` > 其余；同档内按最近变化时间倒序。
- **清除判据是「发生了一次指向该终端的聚焦动作」**，**「它在画布上可见」不算**。画布模式下 `PaneLayer` 把所有 leaf 都渲染着，拿可见性当判据会让 `done` 在产生的同一帧被清掉。
- **不新增持久化字段。** `done` / `approval` 不落盘，重启后清空。
- **灵动岛的外观与交互一个像素都不改**，只把它内部的推导换成调用 `machine.ts`。
- **不要碰 `features/canvas/frameStatus.ts`** —— 那是看板列（用户手动打的标），与本状态机无关。
- 现有 72 个测试一个都不能坏。

---

## 文件结构

```
src/renderer/src/features/status/
  machine.ts        纯函数：三态判定 / locate / 按项目聚合 / 排序
  machine.test.ts   node --test 直接跑
  useStatus.ts      唯一的 store 读取口
  StatusIcon.tsx    三个动态 icon 的唯一实现
  DoneList.tsx      右上角气泡点开的已完成列表
  RunMonitor.tsx    ← 从 canvas/CanvasRunMonitor.tsx 搬来
  useIslandFeed.ts  ← 从 island/useIslandFeed.ts 搬来
  status.css        以上全部的样式
```

`src/renderer/src/features/island/` 目录随之删除（只有一个文件）。

---

## Task 1: 纯函数状态机 + 测试基建

**Files:**
- Create: `src/renderer/src/features/status/machine.ts`
- Create: `src/renderer/src/features/status/machine.test.ts`
- Modify: `package.json`（加 `test` 脚本）

**Interfaces:**
- Consumes: `collectLeaves` from `src/renderer/src/layout.ts`
- Produces: 类型 `TermState` / `RawSignals` / `LocateCtx` / `Located` / `ProjectRow`；
  函数 `statusOf` / `cleanTitle` / `locate` / `byProject` / `sortRows`

**类型名必须是 `ProjectRow`，不许叫 `ProjectStatus`** —— 后者在 `src/shared/types.ts` 里
已经被占了，指的是**看板列**（`frameStatus.ts` 用的那套用户手动打的标）。
用同一个名字会把「不要混」的两件事在类型层面混起来。

**`RawSignals` 只含四个字段，不是规格 §1.3 说的六个。** 另两个
（`silencedNotices` 灵动岛静音、`approvalSentAt` 写回后等复活）**只有灵动岛用**：
前者决定「岛上还冒不冒这一条」，后者决定「审批按钮要不要降级成跳转」。
它们不参与三态判定，放进状态机会让它认识一堆与状态无关的东西。留在 `useIslandFeed.ts`。

- [ ] **Step 1: 加测试脚本**

`package.json` 的 `scripts` 里加一行（放在 `typecheck` 之后）：

```json
    "test": "node --test 'src/**/*.test.ts'",
```

这条 glob 同时覆盖 `src/main` 与 `src/renderer`。**必须加引号** —— 不加的话 shell 会先展开，`src/renderer` 下还没有测试文件时会报 no matches。

Run: `npm test`
Expected: 现有 72 个通过（此时还没有新测试）

- [ ] **Step 2: 写会失败的测试**

创建 `src/renderer/src/features/status/machine.test.ts`：

```ts
import { test } from 'node:test'
import assert from 'node:assert'
import { statusOf, locate, byProject, sortRows } from './machine.ts'
import type { RawSignals } from './machine.ts'

const raw = (o: Partial<RawSignals> = {}): RawSignals => ({
  runningPtys: [],
  attentionPtys: [],
  ptyApproval: {},
  ptyTiming: {},
  ...o
})

// ── 三态判定 ──
test('在 runningPtys 里 = running', () => {
  assert.strictEqual(statusOf('p1', raw({ runningPtys: ['p1'] })), 'running')
})

test('不在 running、在 attention、有问句 = approval', () => {
  const r = raw({ attentionPtys: ['p1'], ptyApproval: { p1: { question: '要删吗' } } })
  assert.strictEqual(statusOf('p1', r), 'approval')
})

test('不在 running、在 attention、无问句 = done', () => {
  assert.strictEqual(statusOf('p1', raw({ attentionPtys: ['p1'] })), 'done')
})

test('三态互斥：running 优先于 attention', () => {
  // spinner 又转起来了但 attention 还没清 —— 以 running 为准
  const r = raw({ runningPtys: ['p1'], attentionPtys: ['p1'] })
  assert.strictEqual(statusOf('p1', r), 'running')
})

test('什么都不在 = null', () => {
  assert.strictEqual(statusOf('p1', raw()), null)
})

// ── 权威性：attentionPtys 说了算 ──
test('ptyApproval 有残留但不在 attentionPtys 里 → null 而不是 approval', () => {
  const r = raw({ ptyApproval: { p1: { question: '上一轮的残留' } } })
  assert.strictEqual(statusOf('p1', r), null)
})

// ── 聚合 ──
const ctx = {
  tabs: [
    { id: 't1', projectId: 'pr1', title: '演示', root: leafTree('l1', 'p1') },
    { id: 't2', projectId: 'pr1', title: '演示', root: leafTree('l2', 'p2') },
    { id: 't3', projectId: 'pr1', title: '演示', root: leafTree('l3', 'p3') }
  ],
  frames: [],
  projects: [{ id: 'pr1', name: '演示', path: '/tmp/demo' }]
}

test('3 个终端 2 done 1 running → 项目行是 done、计数 2', () => {
  const r = raw({ attentionPtys: ['p1', 'p2'], runningPtys: ['p3'] })
  const rows = byProject(['p1', 'p2', 'p3'], r, ctx)
  assert.strictEqual(rows.length, 1)
  assert.strictEqual(rows[0].top, 'done')
  assert.strictEqual(rows[0].count, 2)
})

test('同项目里有 approval 时，项目行显示 approval（比 done 更急）', () => {
  const r = raw({
    attentionPtys: ['p1', 'p2'],
    ptyApproval: { p2: { question: '要删吗' } }
  })
  const rows = byProject(['p1', 'p2'], r, ctx)
  assert.strictEqual(rows[0].top, 'approval')
  assert.strictEqual(rows[0].focusPtyId, 'p2', '点击要落到最紧急的那个终端')
})

test('项目里一个终端都没有 → 不产生行', () => {
  assert.strictEqual(byProject([], raw(), ctx).length, 0)
})

test('清掉一个 done，同项目里另一个 done 不受影响（计数从 2 变 1，行还在）', () => {
  // 规格 §5 点名要测的那条：「聚焦那个终端 → 清；聚焦同项目的**别的**终端 → 不清」。
  // 清除动作本身是 focusTerminal 调 clearAttention(ptyId)——只摘那一个 id。
  // 这里测的是它在聚合层的可见后果：另一个还在。
  const before = byProject(['p1', 'p2'], raw({ attentionPtys: ['p1', 'p2'] }), ctx)
  assert.strictEqual(before[0].count, 2)
  const after = byProject(['p1', 'p2'], raw({ attentionPtys: ['p2'] }), ctx)
  assert.strictEqual(after.length, 1, '另一个还是 done，这一行不该消失')
  assert.strictEqual(after[0].count, 1)
  assert.strictEqual(after[0].focusPtyId, 'p2')
})

test('终端查不到所属项目 → 不产生行，且不抛异常', () => {
  const r = raw({ attentionPtys: ['nope'] })
  assert.doesNotThrow(() => byProject(['nope'], r, ctx))
  assert.strictEqual(byProject(['nope'], r, ctx).length, 0)
})

// ── 排序 ──
test('approval 排在 done 前，done 排在 running 前', () => {
  const rows = [
    { projectId: 'a', top: 'running' as const, count: 1, focusPtyId: 'x', at: 3 },
    { projectId: 'b', top: 'done' as const, count: 1, focusPtyId: 'y', at: 2 },
    { projectId: 'c', top: 'approval' as const, count: 1, focusPtyId: 'z', at: 1 }
  ]
  assert.deepStrictEqual(sortRows(rows).map((r) => r.projectId), ['c', 'b', 'a'])
})

test('同档内新的排前面', () => {
  const rows = [
    { projectId: 'old', top: 'done' as const, count: 1, focusPtyId: 'x', at: 100 },
    { projectId: 'new', top: 'done' as const, count: 1, focusPtyId: 'y', at: 200 }
  ]
  assert.deepStrictEqual(sortRows(rows).map((r) => r.projectId), ['new', 'old'])
})

// ── locate ──
test('locate 解得出 ptyId 落在哪个 tab / 项目', () => {
  const loc = locate('p1', ctx)
  assert.strictEqual(loc?.tabId, 't1')
  assert.strictEqual(loc?.projectId, 'pr1')
  assert.strictEqual(loc?.project, '演示')
})

test('locate 对不存在的 ptyId 返回 null（终端已关）', () => {
  assert.strictEqual(locate('gone', ctx), null)
})

/** 造一棵只有一个终端叶子的布局树 */
function leafTree(leafId: string, ptyId: string): { type: 'leaf'; id: string; pane: { kind: 'terminal'; ptyId: string } } {
  return { type: 'leaf', id: leafId, pane: { kind: 'terminal', ptyId } }
}
```

- [ ] **Step 3: 跑一遍确认失败**

Run: `npm test`
Expected: FAIL —— `Cannot find module './machine.ts'`

不要跳过这一步。没见过它失败的测试，不知道它是不是真在测东西。

- [ ] **Step 4: 写实现**

创建 `src/renderer/src/features/status/machine.ts`：

```ts
// 「一个终端此刻在干什么」的唯一真相。
//
// **纯函数，不引 React / electron / store。** 只吃一份只读快照吐结果，
// 所以 node --test 能直接加载它（见 package.json 的 test 脚本）。
// 这不是洁癖：迁移规则和聚合规则写错了不会崩、不会报错，只会让人在某个时刻
// 少看到一条该看到的提醒——那种错只有测试抓得住。
//
// 判据一律**只看当前信号**，不看历史、不自己记状态。写成「刚从 running 落下」
// 那种历史依赖的话，状态机就得自己维护一份状态再跟 store 对齐，
// 那正是这次要消灭的东西。runningPtys 的维护时机（spinner 起落）已经在
// TerminalView 里做好了，本模块只读不写。
import { collectLeaves } from '../../layout'

export type TermState = 'running' | 'approval' | 'done'

/** store 里那六个字段的只读快照。由 useStatus.ts 取好传进来——本模块不认识 store。 */
export interface RawSignals {
  runningPtys: string[]
  attentionPtys: string[]
  ptyApproval: Record<string, { question?: string } | undefined>
  ptyTiming: Record<string, { lastDoneAt?: number; roundStart?: number } | undefined>
}

/** locate 需要的上下文。同样是只读快照。 */
export interface LocateCtx {
  tabs: { id: string; projectId?: string | null; title?: string; root: unknown }[]
  frames: { id: string; nodes: { id: string; leafId?: string; name?: string }[] }[]
  projects: { id: string; name: string; path?: string }[]
}

/** 一个终端在应用里的落点 */
export interface Located {
  ptyId: string
  tabId: string
  leafId: string
  projectId: string | null
  project: string
  term: string
  frameId?: string
  nodeId?: string
}

/** 项目那一行显示什么 */
export interface ProjectRow {
  projectId: string
  /** 最紧急的那个状态 */
  top: TermState
  /** 该项目处于 top 这个状态的终端个数 */
  count: number
  /** 点这一行要聚焦到哪个终端 */
  focusPtyId: string
  /** 最近一次变化的时刻，同档排序用 */
  at: number
}

/** 紧急程度。数字越小越急——排序和「取最紧急的那个」都用它，只有一处定义。 */
const URGENCY: Record<TermState, number> = { approval: 0, done: 1, running: 2 }

/**
 * 这个终端此刻是什么状态。
 *
 * **`attentionPtys` 是权威**：它回答「有没有待处理」，`ptyApproval` 只回答
 * 「待处理的内容是什么」。所以 ptyApproval 有残留值但 attentionPtys 里没有该 ptyId 时，
 * 状态是 null 而不是 approval——两者的「同生共死」是调用点手动维护的约定，
 * 不是结构保证，残留是可能的。
 */
export function statusOf(ptyId: string, raw: RawSignals): TermState | null {
  if (raw.runningPtys.includes(ptyId)) return 'running'
  if (!raw.attentionPtys.includes(ptyId)) return null
  return raw.ptyApproval[ptyId] ? 'approval' : 'done'
}

/** 剥掉标题开头的 spinner：agent 干活时会把转圈字符写进标题，
 *  带着它显示会让名字每 100ms 抖一下。 */
export function cleanTitle(s: string): string {
  return s.replace(/^[⠀-⣿◐-◓◴-◷\s✳✴✶✻✽✹*]+/u, '').trim()
}

/** 从快照解出某个 pty 的落点；找不到（终端已关）返回 null */
export function locate(ptyId: string, ctx: LocateCtx): Located | null {
  for (const t of ctx.tabs) {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    for (const leaf of collectLeaves(t.root as any)) {
      if (leaf.pane.kind !== 'terminal') continue
      if ((leaf.pane as { ptyId: string }).ptyId !== ptyId) continue
      const frame = ctx.frames.find((f) => f.nodes.some((n) => n.leafId === leaf.id))
      const node = frame?.nodes.find((n) => n.leafId === leaf.id)
      const project = ctx.projects.find((p) => p.id === t.projectId)
      return {
        ptyId,
        tabId: t.id,
        leafId: leaf.id,
        projectId: t.projectId ?? null,
        project: project?.name ?? '未归属',
        term: cleanTitle(node?.name || t.title || '') || '终端',
        frameId: frame?.id,
        nodeId: node?.id
      }
    }
  }
  return null
}

/**
 * 按项目聚合：每个项目一行，显示最紧急的那个状态 + 该状态的终端个数，
 * 点击落到最紧急的那个终端。
 *
 * 查不到所属项目的终端**直接跳过**，不产生行也不抛异常——
 * 终端可能刚被关掉，而状态数组是上一帧的。
 */
export function byProject(ptyIds: string[], raw: RawSignals, ctx: LocateCtx): ProjectRow[] {
  const acc = new Map<string, ProjectRow>()
  for (const ptyId of ptyIds) {
    const st = statusOf(ptyId, raw)
    if (!st) continue
    const loc = locate(ptyId, ctx)
    if (!loc?.projectId) continue
    const at = raw.ptyTiming[ptyId]?.lastDoneAt ?? raw.ptyTiming[ptyId]?.roundStart ?? 0
    const cur = acc.get(loc.projectId)
    if (!cur) {
      acc.set(loc.projectId, { projectId: loc.projectId, top: st, count: 1, focusPtyId: ptyId, at })
      continue
    }
    if (URGENCY[st] < URGENCY[cur.top]) {
      // 出现了更急的：整行改成它，计数从 1 重新起（count 数的是 top 这一档的个数）
      acc.set(loc.projectId, { projectId: loc.projectId, top: st, count: 1, focusPtyId: ptyId, at })
    } else if (st === cur.top) {
      cur.count += 1
      if (at > cur.at) cur.at = at
    }
  }
  return [...acc.values()]
}

/** approval > done > running；同档内按最近变化时间倒序 */
export function sortRows(rows: ProjectRow[]): ProjectRow[] {
  return [...rows].sort((a, b) => URGENCY[a.top] - URGENCY[b.top] || b.at - a.at)
}
```

- [ ] **Step 5: 跑测试**

Run: `npm test`
Expected: 现有 72 + 新增 13 全部通过

- [ ] **Step 6: 类型检查**

Run: `npm run typecheck && npm run build`
Expected: 两个都无输出/无错

- [ ] **Step 7: 提交**

```bash
git add package.json src/renderer/src/features/status/
git commit -m "feat(status): 纯函数状态机 machine.ts

三态判据只看当前信号不看历史；attentionPtys 是权威，ptyApproval 只说内容。
不引 React/electron/store，node --test 直接跑——迁移与聚合规则写错了
不会崩不会报错，只会让人少看到一条提醒，那种错只有测试抓得住。

顺带补上 npm test（一条 glob 覆盖 main 与 renderer 两侧）。"
```

---

## Task 2: `useStatus.ts` —— 唯一的 store 读取口

**Files:**
- Create: `src/renderer/src/features/status/useStatus.ts`

**Interfaces:**
- Consumes: Task 1 的 `RawSignals` / `LocateCtx` / `byProject` / `sortRows` / `statusOf` / `locate`
- Produces: `useProjectRows(): ProjectRow[]`、`useDoneRows(): DoneRow[]`、`focusTerminal(ptyId): void`

- [ ] **Step 1: 写实现**

创建 `src/renderer/src/features/status/useStatus.ts`：

```ts
// 状态机与 store 之间唯一的那道门。四个视图都从这里拿数据，
// 谁都不许自己去 store 里捞那六个字段——那样又会长出第二套推导。
import { useMemo } from 'react'
import { useStore } from '../../store'
import { byProject, locate, sortRows, statusOf } from './machine'
import type { LocateCtx, Located, ProjectRow, RawSignals } from './machine'

/** 把 store 里那六个字段取成一份快照。
 *
 *  **逐字段订阅，不订阅整个 store。** 订阅整个 store 的话，任何无关变化
 *  （比如别的终端标题变了）都会让所有消费者重渲染一轮。 */
function useRaw(): RawSignals {
  const runningPtys = useStore((s) => s.runningPtys)
  const attentionPtys = useStore((s) => s.attentionPtys)
  const ptyApproval = useStore((s) => s.ptyApproval)
  const ptyTiming = useStore((s) => s.ptyTiming)
  return useMemo(
    () => ({ runningPtys, attentionPtys, ptyApproval, ptyTiming }),
    [runningPtys, attentionPtys, ptyApproval, ptyTiming]
  )
}

function useCtx(): LocateCtx {
  const tabs = useStore((s) => s.tabs)
  const frames = useStore((s) => s.canvas.frames)
  const projects = useStore((s) => s.projects)
  return useMemo(() => ({ tabs, frames, projects }), [tabs, frames, projects])
}

/** 所有有状态的项目，已排好序（approval > done > running）*/
export function useProjectRows(): ProjectRow[] {
  const raw = useRaw()
  const ctx = useCtx()
  return useMemo(() => {
    const ids = [...new Set([...raw.runningPtys, ...raw.attentionPtys])]
    return sortRows(byProject(ids, raw, ctx))
  }, [raw, ctx])
}

/** 已完成列表要显示的一条 */
export interface DoneRow extends Located {
  at: number
}

/** 只列 done 的终端，新的在前 */
export function useDoneRows(): DoneRow[] {
  const raw = useRaw()
  const ctx = useCtx()
  return useMemo(() => {
    const out: DoneRow[] = []
    for (const ptyId of raw.attentionPtys) {
      if (statusOf(ptyId, raw) !== 'done') continue
      const loc = locate(ptyId, ctx)
      if (!loc) continue
      out.push({ ...loc, at: raw.ptyTiming[ptyId]?.lastDoneAt ?? 0 })
    }
    return out.sort((a, b) => b.at - a.at)
  }, [raw, ctx])
}

/**
 * 跳到某个终端并清掉它的状态。
 *
 * **清除就发生在这里，只发生在这里。** 规格 §1.2 第四条：
 * 「被聚焦到眼前」= 发生了一次指向该终端的聚焦动作，
 * **「它在画布上可见」不算**——画布模式下 PaneLayer 把所有 leaf 都渲染着，
 * 拿可见性当判据的话 done 会在产生的同一帧被清掉，整个功能等于没有。
 */
export function focusTerminal(ptyId: string): void {
  const st = useStore.getState()
  const loc = locate(ptyId, { tabs: st.tabs, frames: st.canvas.frames, projects: st.projects })
  if (!loc) return
  if (loc.frameId && loc.nodeId) st.focusCanvasNode(loc.frameId, loc.nodeId)
  st.setActiveLeaf(loc.tabId, loc.leafId)
  st.clearAttention(ptyId)
}
```

- [ ] **Step 2: 核对签名**

Run: `grep -n "focusCanvasNode\|setActiveLeaf\|clearAttention" src/renderer/src/store/canvas/types.ts src/renderer/src/store/uiSlice.ts src/renderer/src/store/tabsSlice.ts`
Expected: 三个都能找到。**若参数个数或名字对不上，以 store 里的实际签名为准改这里**，不要反过来改 store。

- [ ] **Step 3: 类型检查**

Run: `npm run typecheck`
Expected: 无输出

- [ ] **Step 4: 提交**

```bash
git add src/renderer/src/features/status/useStatus.ts
git commit -m "feat(status): useStatus —— 状态机与 store 之间唯一的门

四个视图都从这里拿数据，谁都不许自己去 store 捞那六个字段。
清除动作（focusTerminal）也只在这里发生。"
```

---

## Task 3: 三个动态 icon

**Files:**
- Create: `src/renderer/src/features/status/StatusIcon.tsx`
- Create: `src/renderer/src/features/status/status.css`

**Interfaces:**
- Consumes: Task 1 的 `TermState`
- Produces: `<StatusIcon state={TermState} size?={number} />`

- [ ] **Step 1: 组件**

创建 `src/renderer/src/features/status/StatusIcon.tsx`：

```tsx
// 三个状态 icon 的**唯一**实现。四个显示面都用它——
// 各画一份的话，改动效就得改四处，而漏掉的那处只会「看起来有点不一样」，很难被报上来。
import type { TermState } from './machine'

interface Props {
  state: TermState
  size?: number
}

export function StatusIcon({ state, size = 14 }: Props): JSX.Element {
  if (state === 'running') {
    // 三个点轮番上跳。相位差写在 CSS 的 animation-delay 里
    return (
      <span className="st-icon st-running" style={{ width: size, height: size }} aria-label="正在运行">
        <i /><i /><i />
      </span>
    )
  }
  if (state === 'approval') {
    return (
      <span className="st-icon st-approval" style={{ width: size, height: size }} aria-label="等待批准">
        <svg viewBox="0 0 24 24" width={size} height={size} fill="none">
          <circle cx="12" cy="12" r="9" strokeWidth="2" stroke="currentColor" />
          <path d="M12 7.5v5.5" strokeWidth="2" stroke="currentColor" strokeLinecap="round" />
          <circle cx="12" cy="16.5" r="1.1" fill="currentColor" />
        </svg>
      </span>
    )
  }
  // done：圈 + 对勾，**对勾右端超出圆的边缘**
  return (
    <span className="st-icon st-done" style={{ width: size, height: size }} aria-label="已完成">
      <svg viewBox="0 0 24 24" width={size} height={size} fill="none" style={{ overflow: 'visible' }}>
        <circle cx="11" cy="12" r="8" strokeWidth="2" stroke="currentColor" />
        {/* 终点 x=23 > 圆的右缘 x=19：对勾右端**故意**探出圆外，
            这是规格点名的形态。svg 的 overflow:visible 不能省，否则会被 viewBox 裁掉。 */}
        <path d="M7 12.5l3.2 3.2L23 4.5" strokeWidth="2.2" stroke="currentColor"
              strokeLinecap="round" strokeLinejoin="round" className="st-check" />
      </svg>
    </span>
  )
}
```

- [ ] **Step 2: 样式与动效**

创建 `src/renderer/src/features/status/status.css`：

```css
/* ---------- 三个状态 icon ---------- */
.st-icon {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  flex: none;
  line-height: 0;
}

/* running：三个点轮番上跳。
   曲线用 cubic-bezier(.34,1.56,.64,1) —— 与画布抽屉的弹簧同一条，
   超过 1 的那一段就是「轻微回弹」。相位差 120ms 让它读起来是「一个波」而不是三个点各跳各的。 */
.st-running { gap: 2px; }
.st-running i {
  width: 3px;
  height: 3px;
  border-radius: 50%;
  background: currentColor;
  animation: st-bounce 0.9s cubic-bezier(0.34, 1.56, 0.64, 1) infinite;
}
.st-running i:nth-child(2) { animation-delay: 0.12s; }
.st-running i:nth-child(3) { animation-delay: 0.24s; }
@keyframes st-bounce {
  0%, 60%, 100% { transform: translateY(0); }
  30% { transform: translateY(-4px); }
}

/* approval：整体缓慢呼吸。三者中唯一「持续动但不前进」的，对应「卡住」这个语义。 */
.st-approval { animation: st-breathe 1.8s ease-in-out infinite; }
@keyframes st-breathe {
  0%, 100% { opacity: 0.55; transform: scale(0.94); }
  50% { opacity: 1; transform: scale(1.04); }
}

/* done：入场时对勾描一次，之后静止。
   58 是这条路径的大致长度，取整即可——只要 ≥ 真实长度，描边动画就完整。 */
.st-check {
  stroke-dasharray: 58;
  stroke-dashoffset: 0;
  animation: st-draw 0.42s ease-out 1;
}
@keyframes st-draw {
  from { stroke-dashoffset: 58; }
  to { stroke-dashoffset: 0; }
}

@media (prefers-reduced-motion: reduce) {
  .st-running i, .st-approval, .st-check { animation: none; }
}
```

- [ ] **Step 3: 真机验证三个 icon**

起隔离实例，用 CDP 把三种状态都造出来，**用 Read 工具打开截图肉眼确认**：

```bash
SCRATCH=/private/tmp/claude-501/-Users-biily-Biily-Projects-vibe-coding-terminal/4c6d81ad-16bd-45be-a334-58f82d8868c9/scratchpad
npx electron . --user-data-dir="$SCRATCH/statusicon" --remote-debugging-port=9460 --no-sandbox &
```

逐条确认：三个 icon 同尺寸同基线；三个点是「一个波」不是各跳各的；
approval 在呼吸；**done 的对勾右端确实探出了圆的边缘**（这条是规格点名的，必须肉眼看）。

- [ ] **Step 4: 提交**

```bash
git add src/renderer/src/features/status/StatusIcon.tsx src/renderer/src/features/status/status.css
git commit -m "feat(status): 三个状态 icon 的唯一实现

running 三点轮番上跳（弹簧曲线与抽屉同一条）、approval 呼吸（唯一持续动
但不前进的，对应「卡住」）、done 圈加对勾且对勾右端探出圆外。"
```

---

## Task 4: 抽屉项目行改成 icon

**Files:**
- Modify: `src/renderer/src/features/canvas/CanvasDrawer.tsx:470-495`

**Interfaces:**
- Consumes: Task 2 的 `useProjectRows`、Task 3 的 `StatusIcon`

- [ ] **Step 1: 换掉文字徽章**

`CanvasDrawer.tsx` 现在是（约 489 行）：

```tsx
                    {projectHasAttention(p.id) ? (
                      <span className="cd-proj-badge attn">待处理</span>
                    ) : onCanvas ? (
                      <span className="cd-proj-badge">画布中</span>
                    ) : null}
```

改成：

```tsx
                    {(() => {
                      const row = rows.find((r) => r.projectId === p.id)
                      if (!row) return null
                      return (
                        <span className="cd-proj-badge st">
                          <StatusIcon state={row.top} size={13} />
                          {row.count > 1 && <b>{row.count}</b>}
                        </span>
                      )
                    })()}
```

**`画布中` 那一支整个删掉** —— 它是「这个项目在不在画布上」这个**位置信息**，
不是任务状态。和状态摆在同一个位置会让人以为它是状态的第三种取值。
连带 `const onCanvas = frames.some(...)`（约 470 行）也删掉，它没有别的用途。

组件顶部加：

```tsx
  const rows = useProjectRows()
```

import 补：

```tsx
import { useProjectRows } from '../status/useStatus'
import { StatusIcon } from '../status/StatusIcon'
import '../status/status.css'
```

- [ ] **Step 2: 徽章样式**

`status.css` 末尾加：

```css
/* 抽屉项目行右侧的状态徽章：只有 icon 和计数，没有文字 */
.cd-proj-badge.st {
  display: inline-flex;
  align-items: center;
  gap: 3px;
  background: none;
  padding: 0;
}
.cd-proj-badge.st b {
  font-size: 10px;
  font-weight: 700;
  opacity: 0.75;
}
```

- [ ] **Step 3: 类型检查 + 构建**

Run: `npm run typecheck && npm run build && npm test`
Expected: 三个都过

- [ ] **Step 4: 真机验证**

起隔离实例，造一个有 agent 在跑的终端（往终端发 `printf '\033]0;⠋ 干活中\007'` 触发
spinner 判定），确认抽屉项目行右侧出现三点跳动的 icon；停掉 spinner 后变成对勾。
**确认 `画布中` 字样再也不出现。**

- [ ] **Step 5: 提交**

```bash
git add src/renderer/src/features/canvas/CanvasDrawer.tsx src/renderer/src/features/status/status.css
git commit -m "feat(status): 抽屉项目行改成动态 icon，删掉「画布中」

「画布中」是位置信息不是任务状态，和状态摆在同一个位置会被当成第三种取值。"
```

---

## Task 5: 双击列表改成 icon + 排序

**Files:**
- Modify: `src/renderer/src/features/canvas/CanvasStage.tsx:525-540`
- Modify: `src/renderer/src/features/canvas/CanvasContextMenu.tsx`（菜单项支持 icon）

**Interfaces:**
- Consumes: Task 2 的 `useProjectRows` / `focusTerminal`、Task 3 的 `StatusIcon`

- [ ] **Step 1: 菜单项支持 icon**

`CanvasContextMenu.tsx` 的 `CanvasMenuItem` 接口现在有 `hint?: string`（右侧灰字）。
加一个字段：

```ts
  /** 右侧的状态 icon。和 hint 二选一——两个都给时 icon 优先。 */
  icon?: JSX.Element
```

渲染处（`hint` 的旁边）加：

```tsx
        {it.icon ?? (it.hint ? <span className="cm-hint">{it.hint}</span> : null)}
```

- [ ] **Step 2: 项目列表改用 icon 并排序**

`CanvasStage.tsx` 现在是（约 525 行）：

```tsx
    const items: CanvasMenuItem[] = st.projects.map((p) => {
      const exist = st.canvas.frames.find((f) => f.projectId === p.id)
      return {
        label: p.name,
        hint: exist ? '已在画布' : undefined,
        onClick: () => {
          if (exist) centerOnFrame(exist)
          else void addProjectFrame(p.id, wx - 60, wy - 17)
        }
      }
    })
```

改成：

```tsx
    // 有状态的排前面（approval > done > running），其余保持原顺序。
    // rowOf 用的是同一份 useProjectRows 结果，不在这里另算一遍。
    const rank = new Map(rows.map((r, i) => [r.projectId, i]))
    const ordered = [...st.projects].sort(
      (a, b) => (rank.get(a.id) ?? 999) - (rank.get(b.id) ?? 999)
    )
    const items: CanvasMenuItem[] = ordered.map((p) => {
      const exist = st.canvas.frames.find((f) => f.projectId === p.id)
      const row = rows.find((r) => r.projectId === p.id)
      return {
        label: p.name,
        // 「已在画布」删掉：位置信息不是状态（同 CanvasDrawer 那处的理由）
        icon: row ? <StatusIcon state={row.top} size={13} /> : undefined,
        onClick: () => {
          // 两档，先判上面这档，不是叠加：
          // 有 approval/done 就聚焦到最紧急的那个终端（这本身会把视图挪过去），
          // 并按规格 §1.2 清掉它的状态
          if (row && row.top !== 'running') {
            focusTerminal(row.focusPtyId)
            return
          }
          if (exist) centerOnFrame(exist)
          else void addProjectFrame(p.id, wx - 60, wy - 17)
        }
      }
    })
```

`rows` 从组件顶部拿：`const rows = useProjectRows()`。
**注意**：`onViewportDblClick` 是普通函数不是 hook，`rows` 必须在组件作用域里取好被闭包捕获。

- [ ] **Step 3: 类型检查 + 构建 + 测试**

Run: `npm run typecheck && npm run build && npm test`
Expected: 三个都过

- [ ] **Step 4: 真机验证**

双击画布空白处 → 确认项目列表里有状态的项目排在前面、右侧是 icon 不是文字、
**`已在画布` 字样再也不出现**；点一个有 done 的项目 → 视图跳到那个终端 → 该项目的 icon 消失。

- [ ] **Step 5: 提交**

```bash
git add src/renderer/src/features/canvas/CanvasStage.tsx src/renderer/src/features/canvas/CanvasContextMenu.tsx
git commit -m "feat(status): 双击列表改成状态 icon + 动态排序

删掉「已在画布」；有 approval/done 的项目点击改为聚焦到最紧急的那个终端。"
```

---

## Task 6: 右上角气泡回到右边 + 已完成列表

**Files:**
- Create: `src/renderer/src/features/status/DoneList.tsx`
- Modify: `src/renderer/src/features/canvas/CanvasDrawer.tsx:435-446`
- Modify: `src/renderer/src/features/canvas/canvas.css`（`.cd-attn-bubble` 的定位）

**Interfaces:**
- Consumes: Task 2 的 `useDoneRows` / `focusTerminal`、Task 3 的 `StatusIcon`

- [ ] **Step 1: 气泡挪回右上角**

`canvas.css` 里 `.cd-attn-bubble` 现在是 `left: 48px; top: 14px`。

改成：

```css
  /* 回到**右上角**。左上角是运行监视 .crm 的地盘（left:14px、宽 186px、z-index 42），
     而本气泡是 z-index 31 —— 2026-08-13 抽屉左右对调时把它挪到了左边，
     结果被运行监视完全盖住，用户报「右上角的通知没有了」。
     .crm 那条规则上方就写着当初的约定：「两类状态分列左右才不混」。别再挪回左边。 */
  right: 16px;
  top: 14px;
```

（把原来的 `left: 48px;` 那一行删掉）

- [ ] **Step 2: 已完成列表**

创建 `src/renderer/src/features/status/DoneList.tsx`：

```tsx
// 右上角气泡点开的已完成列表。**只列 done**——approval 那类要去灵动岛或终端处理，
// 混进来会让这个列表变成「所有待办」，失去「哪些跑完了可以验收」这个明确用途。
import { useEffect, useRef } from 'react'
import { createPortal } from 'react-dom'
import { StatusIcon } from './StatusIcon'
import { focusTerminal, useDoneRows } from './useStatus'

export function DoneList({ onClose }: { onClose: () => void }): JSX.Element | null {
  const rows = useDoneRows()
  const boxRef = useRef<HTMLDivElement>(null)

  // 点外面关掉。用 mousedown 而不是 click：click 要等 mouseup，
  // 期间用户可能已经在拖别的东西了
  useEffect(() => {
    const onDown = (e: MouseEvent): void => {
      if (e.target instanceof Node && boxRef.current?.contains(e.target)) return
      onClose()
    }
    window.addEventListener('mousedown', onDown)
    return () => window.removeEventListener('mousedown', onDown)
  }, [onClose])

  if (!rows.length) return null

  // portal 到 body：气泡在 .canvas-viewport 里，那是 overflow:clip，
  // 不 portal 出去列表会被切掉（同 ConfirmDialog 的做法）
  return createPortal(
    <div className="st-donelist" ref={boxRef}>
      {rows.map((r) => (
        <button
          key={r.ptyId}
          className="st-done-row"
          onClick={() => {
            focusTerminal(r.ptyId)
            onClose()
          }}
        >
          <StatusIcon state="done" size={13} />
          <span className="st-done-proj">{r.project}</span>
          <span className="st-done-term">{r.term}</span>
        </button>
      ))}
    </div>,
    document.body
  )
}
```

- [ ] **Step 3: 气泡点击改为开列表**

`CanvasDrawer.tsx` 现在是（约 437 行）：

```tsx
          {attentionCount > 0 && (
            <button className="cd-attn-bubble" data-tip="有待处理内容，点击展开" onClick={() => setOpen(true)}>
              {attentionCount}
            </button>
          )}
```

改成：

```tsx
          {doneCount > 0 && (
            <>
              <button
                className="cd-attn-bubble"
                data-tip="有任务完成了，点击查看"
                onClick={() => setDoneOpen((v) => !v)}
              >
                {doneCount}
              </button>
              {doneOpen && <DoneList onClose={() => setDoneOpen(false)} />}
            </>
          )}
```

组件顶部加：

```tsx
  const [doneOpen, setDoneOpen] = useState(false)
  const doneRows = useDoneRows()
  const doneCount = doneRows.length
```

并**删掉已经没人用的** `const attentionCount = projects.filter((p) => projectHasAttention(p.id)).length`
（约 83 行）—— 它原本只服务这个气泡。`projectHasAttention` 本身留到 Task 8 一起清，
因为项目行那处（`' breathing'` 类名）还在用它。

- [ ] **Step 4: 列表样式**

`status.css` 末尾加：

```css
/* ---------- 已完成列表 ---------- */
.st-donelist {
  position: fixed;
  right: 16px;
  top: 44px;
  z-index: 1000; /* 同右键菜单一档：它是点出来的瞬时浮层，要压在所有画板 chrome 之上 */
  min-width: 220px;
  max-height: 50vh;
  overflow-y: auto;
  padding: 5px;
  border-radius: var(--radius-md);
  border: 1px solid var(--glass-border);
  background: var(--glass-3);
  backdrop-filter: var(--blur);
  box-shadow: 0 12px 40px rgba(0, 0, 0, 0.45);
  animation: st-pop 0.16s cubic-bezier(0.34, 1.56, 0.64, 1);
}
@keyframes st-pop {
  from { opacity: 0; transform: translateY(-6px) scale(0.97); }
  to { opacity: 1; transform: none; }
}
.st-done-row {
  display: flex;
  align-items: center;
  gap: 7px;
  width: 100%;
  padding: 6px 8px;
  border: none;
  border-radius: var(--radius-sm);
  background: none;
  color: var(--fg);
  font-size: 12px;
  text-align: left;
  cursor: pointer;
}
.st-done-row:hover { background: var(--glass-2); }
.st-done-proj { font-weight: 600; }
.st-done-term { opacity: 0.6; margin-left: auto; }
```

- [ ] **Step 5: 类型检查 + 构建 + 测试**

Run: `npm run typecheck && npm run build && npm test`
Expected: 三个都过

- [ ] **Step 6: 真机验证 —— 这一条是本任务的重点**

必须**同时让运行监视有内容**（否则测不出那个 bug 修没修好）：

1. 在画布上开两个终端，都发 `printf '\033]0;⠋ 干活中\007'` 让它们进 running
2. 用 CDP 确认 `.crm` **真的在 DOM 里**（不是 `.crm-mini`、不是 null）
3. 让其中一个停下（发一个不带 spinner 的标题）→ 它进 done
4. **肉眼确认右上角的气泡看得见**（这是 2026-08-13 起就坏掉的那条）
5. 点气泡 → 出已完成列表，里面只有那一个 done，没有还在跑的那个
6. 点列表项 → 视图跳到那个终端 → 该项消失、气泡计数减 1
7. 点列表外面 → 列表关闭

- [ ] **Step 7: 提交**

```bash
git add src/renderer/src/features/status/ src/renderer/src/features/canvas/CanvasDrawer.tsx src/renderer/src/features/canvas/canvas.css
git commit -m "fix(status): 右上角气泡回到右边，点开是已完成列表

2026-08-13 抽屉左右对调时把气泡挪到了 left:48px/top:14px，
那正是运行监视 .crm 的地盘（left:14px、宽 186px、z-index 42），
气泡 z-index 31 被完全盖住——用户报的「右上角通知没有了」就是这个。
.crm 那条规则上方写着「两类状态分列左右才不混」，搬动的人没看到，
因为它在另一个文件的另一个位置。"
```

---

## Task 7: 运行监视与灵动岛搬家

**Files:**
- Create: `src/renderer/src/features/status/RunMonitor.tsx`（从 `canvas/CanvasRunMonitor.tsx` 搬）
- Create: `src/renderer/src/features/status/useIslandFeed.ts`（从 `island/useIslandFeed.ts` 搬）
- Delete: `src/renderer/src/features/canvas/CanvasRunMonitor.tsx`
- Delete: `src/renderer/src/features/island/useIslandFeed.ts`（目录随之空掉）
- Modify: `src/renderer/src/features/canvas/CanvasStage.tsx`、`src/renderer/src/features/board/BoardStage.tsx`、`src/renderer/src/App.tsx`（改 import 路径）

**Interfaces:**
- Consumes: Task 1 的 `locate` / `cleanTitle`、Task 2 的 `useStatus`

- [ ] **Step 1: 搬运行监视**

`git mv src/renderer/src/features/canvas/CanvasRunMonitor.tsx src/renderer/src/features/status/RunMonitor.tsx`

改两处：
1. 组件名 `CanvasRunMonitor` → `RunMonitor`（两个调用点跟着改）
2. 它内部那段自己走 tabs/leaves 找终端的 `rows` 计算（约 36-59 行）**删掉**，
   改用 `machine.locate`：

```tsx
  const rows = useMemo<Row[]>(() => {
    if (!runningPtys.length) return []
    const ctx = { tabs, frames, projects }
    return runningPtys
      .map((ptyId) => locate(ptyId, ctx))
      .filter((l): l is NonNullable<typeof l> => !!l)
      .map((l) => ({
        ptyId: l.ptyId,
        leafId: l.leafId,
        frameId: l.frameId,
        nodeId: l.nodeId,
        project: l.project,
        term: shortName(l.term)
      }))
  }, [runningPtys, tabs, projects, frames])
```

**位置仍在左上角**，CSS 不动。

- [ ] **Step 2: 搬灵动岛数据源**

`git mv src/renderer/src/features/island/useIslandFeed.ts src/renderer/src/features/status/useIslandFeed.ts`

改一处：文件内那个私有的 `locate(ptyId)`（45-75 行）和 `cleanTitle`（41-43 行）**删掉**，
改成 import `machine.ts` 的版本。调用点从 `locate(ptyId)` 改成 `locate(ptyId, ctx)`，
`ctx` 在 push 函数开头取一次：

```ts
      const st = useStore.getState()
      const ctx = { tabs: st.tabs, frames: st.canvas.frames, projects: st.projects }
```

`Located` 接口里灵动岛特有的字段（`cwd` / `model` / `effort` / `sessionId` / `agent`）
**留在本文件**，用一个薄封装补上：

```ts
/** 灵动岛还要几个 machine.Located 不管的字段（模型、耗时档、会话 id、跑的哪个 CLI）——
 *  那些只有灵动岛的卡片用，放进状态机会让它认识一堆与「状态」无关的东西。 */
function locateForIsland(ptyId: string, ctx: LocateCtx): (Located & IslandExtra) | null {
  const base = locate(ptyId, ctx)
  if (!base) return null
  const st = useStore.getState()
  const frame = st.canvas.frames.find((f) => f.id === base.frameId)
  const node = frame?.nodes.find((n) => n.id === base.nodeId)
  const agent = node?.agent
  const kind = agent?.kind ?? 'claude'
  const project = st.projects.find((p) => p.id === base.projectId)
  return {
    ...base,
    cwd: project?.path,
    model: agent?.model?.[kind],
    effort: agent?.effort?.[kind],
    sessionId: agent?.session?.[kind],
    agent: kind
  }
}
```

**推送节流（250ms）、审批过期判定（1.5s）、transcript 异步读取、`pruneSilenced`
这些灵动岛特有的东西全部留在本文件，不进状态机。**

- [ ] **Step 3: 改所有 import 路径**

Run: `grep -rn "CanvasRunMonitor\|features/island" src/renderer/src`
把找到的每一处改成新路径。预期是 `CanvasStage.tsx`、`BoardStage.tsx`、`App.tsx` 三处。

- [ ] **Step 4: 确认旧目录已空并删除**

Run: `ls src/renderer/src/features/island/`
Expected: 空 → `rmdir src/renderer/src/features/island`

- [ ] **Step 5: 类型检查 + 构建 + 测试**

Run: `npm run typecheck && npm run build && npm test`
Expected: 三个都过

- [ ] **Step 6: 真机验证 —— 灵动岛必须一个像素都没变**

1. 起隔离实例，开终端跑起 spinner，把应用切到后台 → **灵动岛照常出现**
2. 卡片上的项目名、终端名、耗时、模型档位**都还在**（这些走的是 `locateForIsland`）
3. 点卡片跳回 → 跳到正确的终端
4. 造一个审批场景 → 审批直通按钮还在、点了能写回
5. 运行监视仍在**左上角**，内容正确

- [ ] **Step 7: 提交**

```bash
git add -A src/renderer/src/features/
git commit -m "refactor(status): 运行监视与灵动岛数据源搬进 features/status

两者原先各自走了一遍「ptyId 落在哪个 tab/Frame/节点」的树遍历，
连同 useIslandFeed 里那份，同一件事有三份实现。现在统一走 machine.locate。
灵动岛特有的字段（模型/耗时档/会话 id/CLI 种类）用薄封装补，
不塞进状态机——那些与「状态」无关。外观与交互零改动。"
```

---

## Task 8: 全链路回归 + 清掉旧读取口

**Files:**
- Modify: `src/renderer/src/features/canvas/CanvasDrawer.tsx`（删掉 `projectHasAttention`）

**Interfaces:**
- Consumes: 前七个任务的全部产出

- [ ] **Step 1: 找出还在绕过状态机直接读那六个字段的地方**

Run: `grep -rn "attentionPtys\|runningPtys\|ptyApproval" src/renderer/src --include=*.tsx`

预期只剩这些合法读取方：`features/status/useStatus.ts`、`features/status/useIslandFeed.ts`、
`features/canvas/AgentCmdBar.tsx`（它读 `runningPtys` 判单个终端忙不忙，是合理的局部用途）。

`CanvasDrawer.tsx` 里的 `projectHasAttention`（约 72 行）现在已经没人用了 —— 删掉。
**若发现别的绕过点，一并改成走 `useStatus`**，并在报告里列出来。

- [ ] **Step 2: 类型检查 + 构建 + 测试**

Run: `npm run typecheck && npm run build && npm test`
Expected: 三个都过，测试数 = 72 + Task 1 新增的 13

- [ ] **Step 3: 全链路真机回归**

一个终端跑完整条链路，逐条确认：

1. 终端开始跑 → 抽屉项目行出现三点跳动 · 运行监视左上角出现 · 灵动岛（后台时）显示
2. 终端跑完 → 三处同时变成对勾 · **右上角气泡出现**
3. 点气泡 → 已完成列表里有它
4. 点列表项 → 跳到那个终端 → **抽屉、气泡、列表三处同时消失**（这条最能说明"只有一个真相"）
5. 双击画布 → 项目列表里该项目排在前面、右侧是 icon
6. 造一个审批场景 → 三处都显示呼吸的感叹号、排在 done 前面
7. **切到看板模式再切回来** → 状态显示不错乱

- [ ] **Step 4: 提交**

```bash
git add -A src/renderer/src/features/canvas/CanvasDrawer.tsx
git commit -m "refactor(status): 清掉绕过状态机的旧读取口

至此「一个终端此刻在干什么」在渲染层只有一个说法。"
```

---

## 实现顺序与依赖

```
Task 1（状态机+测试基建）
   ├─→ Task 2（useStatus）
   │      ├─→ Task 4（抽屉项目行）
   │      ├─→ Task 5（双击列表）
   │      ├─→ Task 6（气泡+已完成列表）
   │      └─→ Task 7（运行监视+灵动岛搬家）
   └─→ Task 3（三个 icon）→ Task 4 / 5 / 6 都要它
                                    └─→ Task 8（回归+清旧读取口）
```

Task 1 和 Task 3 可以并行（icon 只依赖 `TermState` 这个类型）。
Task 4/5/6/7 互相独立，做完 Task 2 和 3 之后随便哪个先做。
Task 8 必须最后。

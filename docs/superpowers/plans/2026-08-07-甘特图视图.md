# 甘特图视图 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 Eas-Term 加第四个视图「甘特图」，把「你发了什么 → agent 干完」画成时间轴。

**Architecture:** 采集在渲染层两个点（输入框提交、终端键盘输入），候选文本挂起 3 秒等
`setPtyRunning(true)` 确认后才落一条记录；记录通过 IPC 存到主进程的 `gantt.json`，
写入时剔除 7 天前的。视图是一个新的 `viewMode`，一行一个项目、并行任务行内堆叠。

**Tech Stack:** Electron 37 / React 18 / zustand / TypeScript。**本项目没有测试框架**
（无 vitest/jest/playwright，`package.json` 无 test 脚本），所以每个任务的验证步骤是
`npm run typecheck` + CDP 真机脚本，不是单元测试。

## Global Constraints

- **视觉（已验过对比度，不要改数值）**：进行中条 `var(--accent)`；已完成条
  `color-mix(in srgb, var(--accent) 70%, var(--pane-solid))`（蓝主题 5.21、粉主题 4.69，
  两者都 ≥4.5 所以条内放得下深墨文字）；异常中断 `#ef6f7b` 固定值**且必须带图标**
  （status 不跟主题走，也不能只靠颜色表达）。
- **条内标签用深墨 `#0b0d13`**——它和 `--pane-solid` 同色，所以「填充 vs 底」的对比数
  就是「文字 vs 填充」的对比数，两者不能分开调。条宽不足以放下 10 字时标签移到条右侧，
  改用 `var(--fg)`。
- 刻度文字 `var(--fg-dim)`；条圆角 4px；条与条之间留 2px 表面色间隙；网格线和轴要克制。
- 进行中的条加呼吸动效。**单一系列不加图例**——标题已经说明它是什么。
- `prompt` 和 `follow[]` 每条截断到 2000 字。
- 保留 7 天，在写入时剔除，不设定时器。
- 采集只记**用户发出去的**文本，agent 输出一律不记。
- CDP 验证的坑（每个脚本都要防）：窗口只有 1147×656，坐标要夹在窗口内；
  `/json/list` 里有灵动岛和 webview，选 target 必须
  `p.type === 'page' && p.url.includes('out/renderer') && !p.url.includes('island')`，
  选不中要 throw；生产构建没有 `window.__store`，状态只能查 DOM。

---

### Task 1: 数据类型与主进程存储

**Files:**
- Modify: `src/shared/types.ts`（末尾追加）
- Create: `src/main/gantt.ts`
- Modify: `src/main/index.ts:5`（加 import）、`src/main/index.ts:294`（加注册调用）
- Modify: `src/preload/index.ts:124`（`board:` 那一段之后加 `gantt:`）

**Interfaces:**
- Consumes: 无
- Produces: `GanttTask` 类型；`window.api.gantt.list(): Promise<GanttTask[]>`、
  `window.api.gantt.push(t: GanttTask): Promise<void>`、
  `window.api.gantt.finish(id: string, endAt: number): Promise<void>`、
  `window.api.gantt.follow(id: string, text: string): Promise<void>`

- [ ] **Step 1: 加类型**

在 `src/shared/types.ts` 末尾追加：

```ts
/** 甘特图上的一条任务：一次「你发了什么 → agent 干完」。
 *  prompt/follow 都是**用户发出去的**文本，agent 的输出不记。 */
export interface GanttTask {
  id: string
  projectId: string
  ptyId: string
  /** 点条能跳回那个终端 */
  leafId: string
  /** 原文，超过 2000 字截断 */
  prompt: string
  /** 这一轮里补发的指令（agent 已经在跑时又发的），各自也截断 2000 字 */
  follow?: string[]
  startAt: number
  /** null = 还在跑 */
  endAt: number | null
  /** 上次被强杀导致没能写 endAt，读取时补的标记。不编造结束时间。 */
  aborted?: true
}
```

- [ ] **Step 2: 写主进程存储**

创建 `src/main/gantt.ts`：

```ts
// 甘特图的任务记录。照 board.ts 的范式：主进程管文件、逐条校验、坏数据整条丢。
//
// 为什么保留期在写入时执行而不是定时器：定时器要考虑休眠唤醒、时钟跳变、
// 多窗口重复触发；而写入时清理天然只在有新数据时发生，没有这些问题。
import { app, ipcMain } from 'electron'
import fs from 'fs'
import path from 'path'

import type { GanttTask } from '../shared/types'

const storeFile = (): string => path.join(app.getPath('userData'), 'gantt.json')
const KEEP_MS = 7 * 24 * 60 * 60 * 1000
const MAX_TEXT = 2000

const clip = (s: string): string => (s.length > MAX_TEXT ? s.slice(0, MAX_TEXT) : s)

/** 逐条校验。少一个必填字段就整条丢 —— 半条记录跑进 UI 是最难查的那种问题。 */
function valid(t: unknown): t is GanttTask {
  if (!t || typeof t !== 'object') return false
  const o = t as Record<string, unknown>
  return (
    typeof o.id === 'string' &&
    typeof o.projectId === 'string' &&
    typeof o.ptyId === 'string' &&
    typeof o.leafId === 'string' &&
    typeof o.prompt === 'string' &&
    typeof o.startAt === 'number' &&
    (o.endAt === null || typeof o.endAt === 'number')
  )
}

function load(): GanttTask[] {
  try {
    const raw: unknown = JSON.parse(fs.readFileSync(storeFile(), 'utf8'))
    return Array.isArray(raw) ? raw.filter(valid) : []
  } catch {
    return []
  }
}

function save(list: GanttTask[]): void {
  try {
    fs.writeFileSync(storeFile(), JSON.stringify(list, null, 2))
  } catch (e) {
    console.error('[gantt] 写盘失败', e)
  }
}

/** 保留期在这里执行 */
function prune(list: GanttTask[]): GanttTask[] {
  const cut = Date.now() - KEEP_MS
  return list.filter((t) => t.startAt >= cut)
}

export function registerGanttHandlers(): void {
  ipcMain.handle('gantt:list', () => {
    // 没写完的：endAt 还是 null 的都是上次被强杀留下的（正常退出会走 finish）。
    // 打 aborted 标记、endAt 留 null，让 UI 画成开放条。不编一个结束时间——
    // 编出来的数字会被当成真的。
    return load().map((t) => (t.endAt === null ? { ...t, aborted: true as const } : t))
  })

  ipcMain.handle('gantt:push', (_e, t: GanttTask) => {
    if (!valid(t)) return
    const list = prune(load())
    list.push({ ...t, prompt: clip(t.prompt) })
    save(list)
  })

  ipcMain.handle('gantt:finish', (_e, id: string, endAt: number) => {
    const list = load()
    const hit = list.find((t) => t.id === id)
    if (!hit) return
    hit.endAt = endAt
    save(prune(list))
  })

  ipcMain.handle('gantt:follow', (_e, id: string, text: string) => {
    const list = load()
    const hit = list.find((t) => t.id === id)
    if (!hit) return
    hit.follow = [...(hit.follow ?? []), clip(text)]
    save(list)
  })
}
```

- [ ] **Step 3: 注册 handler**

`src/main/index.ts` 第 5 行附近（`import { registerBoardHandlers } from './board'` 旁边）加：

```ts
import { registerGanttHandlers } from './gantt'
```

第 294 行 `registerBoardHandlers()` 下面一行加：

```ts
  registerGanttHandlers()
```

- [ ] **Step 4: preload 暴露**

`src/preload/index.ts` 的 `board: { ... }` 块之后加：

```ts
  gantt: {
    list: (): Promise<GanttTask[]> => ipcRenderer.invoke('gantt:list'),
    push: (t: GanttTask): Promise<void> => ipcRenderer.invoke('gantt:push', t),
    finish: (id: string, endAt: number): Promise<void> =>
      ipcRenderer.invoke('gantt:finish', id, endAt),
    follow: (id: string, text: string): Promise<void> =>
      ipcRenderer.invoke('gantt:follow', id, text)
  },
```

同文件顶部的 type import 里加上 `GanttTask`。

- [ ] **Step 5: 验证类型通过**

Run: `npm run typecheck`
Expected: 无输出（两个 tsconfig 都过）

- [ ] **Step 6: 验证落盘真的能用**

Run:

```bash
npm run build && \
node -e "
const p=require('path'),f=require('fs'),os=require('os');
const d=p.join(os.homedir(),'Library/Application Support/Eas-Term');
console.log('gantt.json 存在?', f.existsSync(p.join(d,'gantt.json')));
"
```

Expected: 打印 `false`（还没有任何记录，文件尚未创建 —— 这一步只是确认路径算对了，
不报错即通过）

- [ ] **Step 7: 提交**

```bash
git add src/shared/types.ts src/main/gantt.ts src/main/index.ts src/preload/index.ts
git commit -m "feat(gantt): 任务记录的类型与主进程存储

保留期放在写入时执行而不是定时器：定时器要考虑休眠唤醒、时钟跳变、
多窗口重复触发，而写入时清理天然只在有新数据时发生。

没写完的记录（endAt 仍为 null）读取时打 aborted 标记、endAt 留 null，
让 UI 画成开放条 —— 不编造一个结束时间，编出来的数字会被当成真的。"
```

---

### Task 2: 采集器

**Files:**
- Create: `src/renderer/src/features/gantt/collector.ts`
- Modify: `src/renderer/src/features/terminal/TerminalInput.tsx:148`（`send` 里 write 之后）
- Modify: `src/renderer/src/features/terminal/TerminalView.tsx:449`（`term.onData` 那行）
- Modify: `src/renderer/src/features/terminal/TerminalView.tsx:478`（`setPtyRunning(ptyId, spinner)` 那行）

**Interfaces:**
- Consumes: Task 1 的 `window.api.gantt.*`、`GanttTask`
- Produces:
  - `noteSubmitted(ptyId: string, text: string): void` — 输入框提交时调
  - `feedKeystroke(ptyId: string, data: string): void` — `term.onData` 里调，内部做行缓冲
  - `noteRunning(ptyId: string, running: boolean, ctx: {projectId: string, leafId: string}): void`
    — spinner 起落时调

- [ ] **Step 1: 写采集器**

创建 `src/renderer/src/features/gantt/collector.ts`：

```ts
// 甘特图的采集：把「用户发出去的文本」和「agent 跑起来/停下」两件事对上。
//
// 为什么放渲染层而不是主进程的 pty:write（那才是输入的唯一汇合点）：
// 「agent 在不在跑」是 TerminalView 靠 spinner 判定的，主进程拿不到这个信号，
// 跨进程同步只会多一层竞态。而两条输入路径在渲染层同样齐全 ——
// term.onData 就是键盘输入写进 PTY 的唯一出口。
import type { GanttTask } from '../../../../shared/types'

/** 候选产出后等多久必须等到 agent 跑起来。不做成设置项 —— 多一个旋钮不会让它更准。 */
const PENDING_TTL_MS = 3000

/** 每个 pty 一份键盘缓冲 */
const keyBuf = new Map<string, string>()
/** 每个 pty 挂起的候选文本（等 running 确认） */
const pending = new Map<string, { text: string; at: number }>()
/** 每个 pty 当前那条还没结束的记录 id */
const active = new Map<string, string>()

/** 输入框提交：整段文本直接就是一条候选 */
export function noteSubmitted(ptyId: string, text: string): void {
  const t = text.trim()
  if (t) pending.set(ptyId, { text: t, at: Date.now() })
}

/**
 * 键盘输入：还原成「一条输入」。
 *
 * 准确性边界（有意不追）：TUI 有自己的行编辑和历史。按 ↑ 调出上一条命令时，
 * 画面上的内容不来自这个缓冲，产出的候选会和实际发出的不符；Tab 补全同理。
 * 追平意味着重新实现一遍各家 TUI 的编辑器，而且随它们改版失效。
 * **时间轴不受影响** —— 起止取自 setPtyRunning，与文本采集无关。
 */
export function feedKeystroke(ptyId: string, data: string): void {
  let buf = keyBuf.get(ptyId) ?? ''
  let i = 0
  while (i < data.length) {
    const ch = data[i]
    // bracketed paste：取中间内容整段入缓冲
    if (data.startsWith('\x1b[200~', i)) {
      const end = data.indexOf('\x1b[201~', i)
      buf += end < 0 ? data.slice(i + 6) : data.slice(i + 6, end)
      i = end < 0 ? data.length : end + 6
      continue
    }
    // 其它 CSI 控制序列（方向键、Home/End…）整段跳过，别让它们进缓冲
    if (ch === '\x1b') {
      const m = /^\x1b\[[0-9;?]*[ -/]*[@-~]/.exec(data.slice(i))
      i += m ? m[0].length : 1
      continue
    }
    if (ch === '\r' || ch === '\n') {
      const t = buf.trim()
      if (t) pending.set(ptyId, { text: t, at: Date.now() })
      buf = ''
      i++
      continue
    }
    if (ch === '\x7f' || ch === '\b') {
      buf = buf.slice(0, -1)
      i++
      continue
    }
    // 其余控制字符不入缓冲（Ctrl-C 之类）
    if (ch < ' ') {
      i++
      continue
    }
    buf += ch
    i++
  }
  keyBuf.set(ptyId, buf)
}

/** spinner 起落。running=true 时把挂起的候选转成一条记录；false 时收尾。 */
export function noteRunning(
  ptyId: string,
  running: boolean,
  ctx: { projectId: string; leafId: string }
): void {
  if (running) {
    const p = pending.get(ptyId)
    pending.delete(ptyId)
    // 没有候选，或候选早就过期了（说明这次跑起来跟用户刚才敲的没关系）→ 不记
    if (!p || Date.now() - p.at > PENDING_TTL_MS) return
    if (!ctx.projectId) return // 不属于任何项目的终端不进甘特图
    const task: GanttTask = {
      id: 'gt-' + Date.now().toString(36) + '-' + Math.random().toString(36).slice(2, 7),
      projectId: ctx.projectId,
      ptyId,
      leafId: ctx.leafId,
      prompt: p.text,
      startAt: Date.now(),
      endAt: null
    }
    active.set(ptyId, task.id)
    void window.api.gantt.push(task)
    return
  }
  const id = active.get(ptyId)
  active.delete(ptyId)
  if (id) void window.api.gantt.finish(id, Date.now())
}

/**
 * agent 正在跑时又发一条（补充指令、排队）。
 * 这时不会有新的 false→true 转换，按上面的规则会被超时丢掉，而边跑边补是常态。
 * 附加到当前那条记录上，不为它单开一根条 —— 它没有自己的起止，
 * 硬拆只会让图上多出一堆零长度的条。
 */
export function drainFollow(ptyId: string): void {
  const id = active.get(ptyId)
  if (!id) return
  const p = pending.get(ptyId)
  if (!p) return
  pending.delete(ptyId)
  void window.api.gantt.follow(id, p.text)
}
```

- [ ] **Step 2: 接输入框提交**

`src/renderer/src/features/terminal/TerminalInput.tsx`，在第 148 行
`window.api.pty.write(ptyId, body)` 的**下一行**加：

```ts
    // 甘特图采集：记的是 payload（含图片路径）之外的纯文本意图
    noteSubmitted(ptyId, text)
    drainFollow(ptyId)
```

文件顶部加 import：

```ts
import { noteSubmitted, drainFollow } from '../gantt/collector'
```

- [ ] **Step 3: 接键盘输入**

`src/renderer/src/features/terminal/TerminalView.tsx` 第 449 行，把

```ts
    const dataDisp = term.onData((data) => window.api.pty.write(ptyId, data))
```

改成

```ts
    const dataDisp = term.onData((data) => {
      // 甘特图采集要在写进 PTY 之前拿到原始按键 —— 这是键盘输入的唯一出口
      feedKeystroke(ptyId, data)
      drainFollow(ptyId)
      window.api.pty.write(ptyId, data)
    })
```

- [ ] **Step 4: 接 running 信号**

同文件第 478 行，把

```ts
      useStore.getState().setPtyRunning(ptyId, spinner)
```

改成

```ts
      const st = useStore.getState()
      st.setPtyRunning(ptyId, spinner)
      // 找这个 pty 属于哪个项目、哪个 leaf —— 甘特图按项目分行、按 leaf 跳回
      const tab = st.tabs.find((t) => collectLeaves(t.root).some((l) => l.pane.ptyId === ptyId))
      const leaf = tab && collectLeaves(tab.root).find((l) => l.pane.ptyId === ptyId)
      if (tab?.projectId && leaf) {
        noteRunning(ptyId, spinner, { projectId: tab.projectId, leafId: leaf.id })
      }
```

同文件第 446 行的 `setPtyRunning(ptyId, false)`（pty 退出那处）后面加一行收尾：

```ts
      noteRunning(ptyId, false, { projectId: '', leafId: '' })
```

顶部 import 加：

```ts
import { feedKeystroke, noteRunning, drainFollow } from '../gantt/collector'
import { collectLeaves } from '../../layout'
```

（`collectLeaves` 若文件里已 import 则不要重复加。）

- [ ] **Step 5: 验证类型**

Run: `npm run typecheck`
Expected: 无输出

- [ ] **Step 6: 真机验证采集**

Run:

```bash
npm run build
SP=/private/tmp/claude-501/-Users-biily-Biily-Projects-vibe-coding-terminal/4c6d81ad-16bd-45be-a334-58f82d8868c9/scratchpad
rm -rf "$SP/udata-g" && mkdir -p "$SP/udata-g"
cp ~/Library/Application\ Support/Eas-Term/{canvas.json,projects.json} "$SP/udata-g/"
nohup npx electron . --remote-debugging-port=9444 --user-data-dir="$SP/udata-g" > "$SP/devg.log" 2>&1 &
sleep 10
```

然后在应用里：切到终端模式 → 在某个属于项目的终端里输入框敲 `sleep 5` 并发送 →
等它跑完 → 检查：

```bash
cat "$SP/udata-g/gantt.json"
```

Expected: 一条记录，`prompt` 是 `sleep 5`，`startAt`/`endAt` 都是数字且相差约 5000ms。

若 `gantt.json` 不存在，说明 `noteRunning` 没拿到 `projectId`（终端不属于项目）——
换一个在画布 Frame 里的终端重试。

- [ ] **Step 7: 提交**

```bash
git add src/renderer/src/features/gantt/collector.ts \
        src/renderer/src/features/terminal/TerminalInput.tsx \
        src/renderer/src/features/terminal/TerminalView.tsx
git commit -m "feat(gantt): 采集用户发出去的文本 + agent 起止

两条输入路径都接：输入框的 send()，和 term.onData（键盘写进 PTY 的唯一出口）。
候选文本挂起 3 秒等 setPtyRunning(true) 确认才落记录 —— cd/ls 不会让 agent
跑起来，自动就被滤掉了。

agent 正在跑时补发的指令没有新的 false→true 转换，会被超时丢掉，
而边跑边补是常态，所以走 drainFollow 附加到当前记录上。

采集不准的边界写在 collector.ts 的注释里（TUI 自己的行编辑和历史），
时间轴不受影响 —— 起止取自 setPtyRunning，与文本采集无关。"
```

---

### Task 3: viewMode 加 'gantt' + 模式切换下拉

**Files:**
- Modify: `src/renderer/src/store/canvas/types.ts:13`
- Modify: `src/renderer/src/store/canvasSlice.ts:138`
- Modify: `src/renderer/src/App.tsx:256-278`
- Modify: `src/renderer/src/features/workspace/workspace.css`（`.view-seg` 那一段之后追加）

**Interfaces:**
- Consumes: 无
- Produces: `ViewMode` 多一个 `'gantt'`；`.mode-switch` / `.mode-menu` 两个类名

- [ ] **Step 1: 扩类型**

`src/renderer/src/store/canvas/types.ts` 第 13 行改成：

```ts
export type ViewMode = 'split' | 'canvas' | 'board' | 'gantt'
```

- [ ] **Step 2: 存档恢复允许 gantt**

`src/renderer/src/store/canvasSlice.ts` 第 138 行改成：

```ts
    if (scene.viewMode === 'canvas' || scene.viewMode === 'board' || scene.viewMode === 'gantt')
      set({ viewMode: scene.viewMode })
```

- [ ] **Step 3: 三段控件换成下拉按钮**

`src/renderer/src/App.tsx` 把第 256–278 行整块（`<div className="view-seg">` 到它的
`</div>`）替换成：

```tsx
          <ModeSwitch />
```

在 `src/renderer/src/App.tsx` 顶部 import 区加：

```tsx
import { ModeSwitch } from './features/workspace/ModeSwitch'
```

- [ ] **Step 4: 写 ModeSwitch 组件**

创建 `src/renderer/src/features/workspace/ModeSwitch.tsx`：

```tsx
// 四个视图收进一个按钮。三段控件到第四项就装不下了 —— 标题栏中间那块宽度有限，
// 再塞一段会把窗口标题挤掉。
import { useEffect, useRef, useState } from 'react'

import { useStore } from '../../store'
import type { ViewMode } from '../../store/canvas/types'
// 大写 I —— 文件就叫 Icons.tsx。
// 注意 IconProps 在那个文件里**没有 export**（第 4 行是裸 interface），
// 所以这里用 typeof 取一个现成图标的类型，别去 import 它。
import { TerminalIcon, CanvasIcon, BoardIcon, GanttIcon } from '../../ui/Icons'

const MODES: { key: ViewMode; label: string; Icon: typeof BoardIcon }[] = [
  { key: 'split', label: '终端', Icon: TerminalIcon },
  { key: 'canvas', label: '画布', Icon: CanvasIcon },
  { key: 'board', label: '看板', Icon: BoardIcon },
  { key: 'gantt', label: '甘特图', Icon: GanttIcon }
]

export function ModeSwitch(): JSX.Element {
  const viewMode = useStore((s) => s.viewMode)
  const setViewMode = useStore((s) => s.setViewMode)
  const [open, setOpen] = useState(false)
  const wrapRef = useRef<HTMLDivElement>(null)

  // 点外面关掉。捕获阶段 —— 菜单项自己的 onClick 要先跑完
  useEffect(() => {
    if (!open) return
    const onDown = (e: MouseEvent): void => {
      if (!wrapRef.current?.contains(e.target as Node)) setOpen(false)
    }
    document.addEventListener('mousedown', onDown)
    return () => document.removeEventListener('mousedown', onDown)
  }, [open])

  const cur = MODES.find((m) => m.key === viewMode) ?? MODES[0]

  return (
    <div className="mode-switch-wrap" ref={wrapRef}>
      <button
        className={`mode-switch${open ? ' on' : ''}`}
        onClick={() => setOpen((v) => !v)}
        data-tip="模式切换"
      >
        <cur.Icon size={13} />
        {cur.label}
        <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" strokeWidth="2.4">
          <path d="M6 9l6 6 6-6" />
        </svg>
      </button>
      {open && (
        <div className="mode-menu">
          {MODES.map((m) => (
            <button
              key={m.key}
              className={`mode-menu-item${m.key === viewMode ? ' on' : ''}`}
              onClick={() => {
                setViewMode(m.key)
                setOpen(false)
              }}
            >
              <m.Icon size={13} />
              {m.label}
            </button>
          ))}
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 5: 加 GanttIcon**

文件是 `src/renderer/src/ui/Icons.tsx`（**大写 I**），里面所有图标都走 `Svg` 包装器 +
`IconProps`，不要写裸 `<svg>`。在 `BoardIcon`（第 254 行）下面加：

```tsx
export const GanttIcon = (p: IconProps): JSX.Element => (
  <Svg {...p}>
    <rect x="3" y="5" width="10" height="3" rx="1.5" />
    <rect x="7" y="10.5" width="12" height="3" rx="1.5" />
    <rect x="5" y="16" width="8" height="3" rx="1.5" />
  </Svg>
)
```

- [ ] **Step 6: 样式**

`src/renderer/src/features/workspace/workspace.css` 里 `.view-seg` 那一段之后追加：

```css
/* 四个视图收进一个按钮。原来的三段控件 .view-seg 已经删掉，
   它的样式若还留着不影响 —— 但别再往里加第四段，标题栏中间放不下。 */
.mode-switch-wrap {
  position: relative;
}

.mode-switch {
  display: flex;
  align-items: center;
  gap: 6px;
  height: 28px;
  padding: 0 10px;
  border: 1px solid var(--glass-border);
  border-radius: 999px;
  background: var(--glass-3);
  color: var(--fg);
  font-size: 12.5px;
  cursor: pointer;
}

.mode-switch:hover,
.mode-switch.on {
  border-color: var(--glass-border-strong);
  background: var(--accent-soft);
}

.mode-menu {
  position: absolute;
  top: 34px;
  left: 0;
  min-width: 132px;
  padding: 5px;
  border: 1px solid var(--glass-border-strong);
  border-radius: var(--radius-md);
  background: var(--glass-3);
  backdrop-filter: var(--blur);
  box-shadow: 0 12px 32px rgba(0, 0, 0, 0.45);
  z-index: 3300;
}

.mode-menu-item {
  display: flex;
  align-items: center;
  gap: 8px;
  width: 100%;
  height: 30px;
  padding: 0 9px;
  border: none;
  border-radius: var(--radius-sm);
  background: none;
  color: var(--fg-dim);
  font-size: 12.5px;
  cursor: pointer;
}

.mode-menu-item:hover {
  background: rgba(255, 255, 255, 0.07);
  color: var(--fg);
}

.mode-menu-item.on {
  color: var(--fg);
  background: var(--accent-soft);
}
```

- [ ] **Step 7: 验证类型**

Run: `npm run typecheck`
Expected: 无输出

- [ ] **Step 8: 真机验证切换**

Run:

```bash
npm run build
SP=/private/tmp/claude-501/-Users-biily-Biily-Projects-vibe-coding-terminal/4c6d81ad-16bd-45be-a334-58f82d8868c9/scratchpad
pkill -f "udata-g"; sleep 1
nohup npx electron . --remote-debugging-port=9444 --user-data-dir="$SP/udata-g" > "$SP/devg.log" 2>&1 &
sleep 10
cat > "$SP/mode.mjs" << 'EOF'
import { connect, wait } from './cdp.mjs'
const { ws, send, evalJs } = await connect()
const click = async (x, y) => { for (const t of ['mousePressed','mouseReleased'])
  await send('Input.dispatchMouseEvent', { type: t, x, y, button: 'left', clickCount: 1, buttons: t==='mousePressed'?1:0 }); await wait(500) }
const b = await evalJs(`(() => { const e = document.querySelector('.mode-switch'); if (!e) return null
  const r = e.getBoundingClientRect(); return [r.left+r.width/2, r.top+r.height/2] })()`)
if (!b) throw new Error('找不到 .mode-switch')
await click(b[0], b[1])
console.log('菜单项:', await evalJs(`[...document.querySelectorAll('.mode-menu-item')].map(e => e.textContent.trim())`))
const g = await evalJs(`(() => { const e = [...document.querySelectorAll('.mode-menu-item')].find(x => x.textContent.trim() === '甘特图')
  if (!e) return null; const r = e.getBoundingClientRect(); return [r.left+r.width/2, r.top+r.height/2] })()`)
await click(g[0], g[1]); await wait(800)
console.log('切过去后按钮显示:', await evalJs(`document.querySelector('.mode-switch')?.textContent.trim()`))
ws.close(); process.exit(0)
EOF
node "$SP/mode.mjs"
```

Expected: 菜单项打印 `[ '终端', '画布', '看板', '甘特图' ]`，切过去后按钮文字含「甘特图」

- [ ] **Step 9: 提交**

```bash
git add src/renderer/src/store/canvas/types.ts src/renderer/src/store/canvasSlice.ts \
        src/renderer/src/App.tsx src/renderer/src/features/workspace/ModeSwitch.tsx \
        src/renderer/src/ui/icons.tsx src/renderer/src/features/workspace/workspace.css
git commit -m "feat(gantt): 第四个视图 + 模式切换收进下拉

三段控件到第四项就装不下了——标题栏中间那块宽度有限，再塞一段会把窗口标题挤掉。
换成一个按钮，点开下拉四选一。

存档恢复那处也要放行 gantt，否则关在甘特图、重开回到分屏。"
```

---

### Task 4: 甘特图视图

**Files:**
- Create: `src/renderer/src/features/gantt/GanttStage.tsx`
- Create: `src/renderer/src/features/gantt/gantt.css`
- Modify: `src/renderer/src/App.tsx:311`（`{viewMode === 'canvas' && <CanvasStage />}` 附近）

**Interfaces:**
- Consumes: Task 1 的 `window.api.gantt.list()` 与 `GanttTask`；Task 3 的 `'gantt'` viewMode
- Produces: `<GanttStage />`

- [ ] **Step 1: 写视图**

创建 `src/renderer/src/features/gantt/GanttStage.tsx`：

```tsx
// 甘特图：横轴时间，一行一个项目，同项目并行的任务在行内上下堆叠。
//
// 为什么并行要堆叠而不是重叠半透明：叠住的标题读不出来，非 hover 不可，
// 而这张图的价值就在于扫一眼知道当时在干什么。
import { useEffect, useMemo, useState } from 'react'

import { useStore } from '../../store'
import type { GanttTask } from '../../../../shared/types'
import './gantt.css'

/** 一屏的时间跨度：默认最近 8 小时。刻度到分。 */
const SPAN_MS = 8 * 60 * 60 * 1000
const MIN_BAR_PX = 3
/** 条内放得下 10 个字大约需要这么宽；不够就把标签移到条右边 */
const LABEL_INSIDE_MIN_PX = 86

const two = (n: number): string => String(n).padStart(2, '0')
const hhmm = (t: number): string => {
  const d = new Date(t)
  return two(d.getHours()) + ':' + two(d.getMinutes())
}
const dur = (ms: number): string => {
  const s = Math.round(ms / 1000)
  if (s < 60) return s + ' 秒'
  const m = Math.round(s / 60)
  return m < 60 ? m + ' 分钟' : (m / 60).toFixed(1) + ' 小时'
}

/** 同一行内把重叠的任务分到不同层 —— 贪心：能放下就放，放不下开新层 */
function layer(tasks: GanttTask[], now: number): GanttTask[][] {
  const rows: GanttTask[][] = []
  for (const t of [...tasks].sort((a, b) => a.startAt - b.startAt)) {
    const end = t.endAt ?? now
    const row = rows.find((r) => (r[r.length - 1].endAt ?? now) <= t.startAt)
    if (row) row.push(t)
    else rows.push([t])
  }
  return rows
}

export function GanttStage(): JSX.Element {
  const projects = useStore((s) => s.projects)
  const setViewMode = useStore((s) => s.setViewMode)
  const setBoardFullscreen = useStore((s) => s.setBoardFullscreen)
  const [tasks, setTasks] = useState<GanttTask[]>([])
  const [now, setNow] = useState(() => Date.now())
  const [hover, setHover] = useState<{ t: GanttTask; x: number; y: number } | null>(null)

  // 进来读一次，之后每 20 秒刷一次（有任务在跑时右端要跟着长）
  useEffect(() => {
    let alive = true
    const pull = (): void => {
      void window.api.gantt.list().then((l) => {
        if (alive) setTasks(l)
      })
      setNow(Date.now())
    }
    pull()
    const id = window.setInterval(pull, 20000)
    return () => {
      alive = false
      window.clearInterval(id)
    }
  }, [])

  const t1 = now
  const t0 = now - SPAN_MS

  const byProject = useMemo(() => {
    const m = new Map<string, GanttTask[]>()
    for (const t of tasks) {
      if ((t.endAt ?? now) < t0) continue // 整条都在窗口左边，跳过
      const arr = m.get(t.projectId) ?? []
      arr.push(t)
      m.set(t.projectId, arr)
    }
    return m
  }, [tasks, t0, now])

  /** 刻度：每小时一根，标到分 */
  const ticks = useMemo(() => {
    const out: number[] = []
    const first = Math.ceil(t0 / 3600000) * 3600000
    for (let t = first; t <= t1; t += 3600000) out.push(t)
    return out
  }, [t0, t1])

  const pct = (t: number): number => ((t - t0) / SPAN_MS) * 100

  const jump = (t: GanttTask): void => {
    setViewMode('board')
    setBoardFullscreen(t.leafId)
  }

  const rows = projects.filter((p) => byProject.has(p.id))

  return (
    <div className="gantt">
      <div className="gantt-head">
        <div className="gantt-title">最近 8 小时 · 每根条是一次「你发出去的话 → agent 干完」</div>
      </div>
      <div className="gantt-grid">
        <div className="gantt-axis">
          {ticks.map((t) => (
            <div key={t} className="gantt-tick" style={{ left: pct(t) + '%' }}>
              <span>{hhmm(t)}</span>
            </div>
          ))}
        </div>
        {rows.length === 0 && (
          <div className="gantt-empty">
            这段时间还没有记录。数据从装上这一版才开始记 —— 让 agent 跑一件事就会出现。
          </div>
        )}
        {rows.map((p) => {
          const layers = layer(byProject.get(p.id) ?? [], now)
          return (
            <div className="gantt-row" key={p.id}>
              <div className="gantt-rowname" title={p.name}>
                {p.name}
              </div>
              <div className="gantt-lanes">
                {ticks.map((t) => (
                  <div key={t} className="gantt-vline" style={{ left: pct(t) + '%' }} />
                ))}
                {layers.map((ln, li) => (
                  <div className="gantt-lane" key={li}>
                    {ln.map((t) => {
                      const s = Math.max(t.startAt, t0)
                      const e = Math.min(t.endAt ?? now, t1)
                      const left = pct(s)
                      const w = Math.max(pct(e) - left, 0)
                      const state = t.aborted ? 'aborted' : t.endAt === null ? 'running' : 'done'
                      const wide = (w / 100) * 900 > LABEL_INSIDE_MIN_PX
                      const label = t.prompt.slice(0, 10)
                      return (
                        <div
                          key={t.id}
                          className={`gantt-bar ${state}${wide ? ' wide' : ''}`}
                          style={{ left: left + '%', width: `max(${MIN_BAR_PX}px, ${w}%)` }}
                          onMouseEnter={(ev) =>
                            setHover({ t, x: ev.clientX, y: ev.currentTarget.getBoundingClientRect().bottom })
                          }
                          onMouseLeave={() => setHover(null)}
                          onClick={() => jump(t)}
                        >
                          {state === 'aborted' && (
                            <svg className="gantt-abort-ico" viewBox="0 0 24 24" width="10" height="10"
                              fill="none" stroke="currentColor" strokeWidth="2.6">
                              <path d="M12 8v5M12 16.5v.5" />
                              <circle cx="12" cy="12" r="9" />
                            </svg>
                          )}
                          <span className="gantt-bar-label">{label}</span>
                        </div>
                      )
                    })}
                  </div>
                ))}
              </div>
            </div>
          )
        })}
      </div>
      {hover && (
        <div className="gantt-pop" style={{ left: hover.x, top: hover.y + 8 }}>
          <div className="gantt-pop-time">
            {hhmm(hover.t.startAt)} → {hover.t.endAt ? hhmm(hover.t.endAt) : '进行中'}
            {hover.t.endAt && <span className="gantt-pop-dur">{dur(hover.t.endAt - hover.t.startAt)}</span>}
          </div>
          {hover.t.aborted && <div className="gantt-pop-abort">上次没有正常结束，结束时间未知</div>}
          <div className="gantt-pop-text">{hover.t.prompt}</div>
          {hover.t.follow?.map((f, i) => (
            <div className="gantt-pop-follow" key={i}>
              追加：{f}
            </div>
          ))}
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2: 样式**

创建 `src/renderer/src/features/gantt/gantt.css`：

```css
/* 甘特图。配色跑过对比度验证，数值别随手改：
   进行中 = var(--accent) 原色，vs 底 9.76(蓝) / 8.65(粉)
   已完成 = accent 混 70% 底色，vs 底 5.21(蓝) / 4.69(粉) —— ≥4.5 才放得下条内文字
   条内标签用 #0b0d13，它和 --pane-solid 同色，所以「填充 vs 底」的对比数
   就是「文字 vs 填充」的对比数，两者不能分开调。
   异常中断固定 #ef6f7b 且必须带图标：状态色不跟主题走，也不能只靠颜色表达。 */
.gantt {
  position: absolute;
  inset: 0;
  overflow: auto;
  padding: 14px 18px 24px;
  background: var(--pane-solid);
}

.gantt-head {
  margin-bottom: 12px;
}

.gantt-title {
  color: var(--fg-dim);
  font-size: 12px;
}

.gantt-grid {
  position: relative;
}

.gantt-axis {
  position: relative;
  height: 20px;
  margin-left: 132px;
}

.gantt-tick {
  position: absolute;
  top: 0;
  transform: translateX(-50%);
}

.gantt-tick span {
  color: var(--fg-dim);
  font-size: 11px;
  font-variant-numeric: tabular-nums;
}

.gantt-row {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  padding: 7px 0;
  border-top: 1px solid rgba(255, 255, 255, 0.05);
}

.gantt-rowname {
  flex: none;
  width: 120px;
  padding-top: 2px;
  color: var(--fg);
  font-size: 12.5px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.gantt-lanes {
  position: relative;
  flex: 1;
  min-width: 0;
}

/* 网格线要克制——它是背景，不是内容 */
.gantt-vline {
  position: absolute;
  top: -4px;
  bottom: -4px;
  width: 1px;
  background: rgba(255, 255, 255, 0.045);
}

.gantt-lane {
  position: relative;
  height: 22px;
  /* 层与层之间留 2px 表面色，条挨着条时不会糊成一块 */
  margin-bottom: 2px;
}

.gantt-lane:last-child {
  margin-bottom: 0;
}

.gantt-bar {
  position: absolute;
  top: 0;
  height: 22px;
  display: flex;
  align-items: center;
  gap: 3px;
  padding: 0 6px;
  border-radius: 4px;
  cursor: pointer;
  overflow: hidden;
  white-space: nowrap;
  /* 相邻条之间的 2px 间隙用表面色描边做，比 margin 可靠（条是绝对定位的） */
  box-shadow: 0 0 0 2px var(--pane-solid);
}

.gantt-bar.done {
  background: color-mix(in srgb, var(--accent) 70%, var(--pane-solid));
}

.gantt-bar.running {
  background: var(--accent);
  animation: gantt-breathe 2.4s ease-in-out infinite;
}

.gantt-bar.aborted {
  background: #ef6f7b;
  /* 右端渐隐 = 结束时间未知。不编一个结束时间，编出来的会被当成真的 */
  mask-image: linear-gradient(90deg, #000 72%, transparent 100%);
  color: #0b0d13;
}

@keyframes gantt-breathe {
  0%,
  100% {
    filter: brightness(1);
  }
  50% {
    filter: brightness(1.18);
  }
}

/* 条内标签：深墨。窄条放不下就整个藏起来，别让半个字露在外面 */
.gantt-bar-label {
  color: #0b0d13;
  font-size: 11.5px;
  font-weight: 500;
  opacity: 0;
}

.gantt-bar.wide .gantt-bar-label {
  opacity: 1;
}

.gantt-abort-ico {
  flex: none;
  color: #0b0d13;
}

.gantt-empty {
  padding: 40px 0 0 132px;
  color: var(--fg-dim);
  font-size: 12.5px;
}

.gantt-pop {
  position: fixed;
  z-index: 3400;
  max-width: 380px;
  padding: 9px 11px;
  border: 1px solid var(--glass-border-strong);
  border-radius: var(--radius-md);
  background: var(--tooltip-bg);
  backdrop-filter: var(--blur);
  box-shadow: 0 14px 36px rgba(0, 0, 0, 0.5);
  pointer-events: none;
}

.gantt-pop-time {
  display: flex;
  gap: 8px;
  color: var(--fg-dim);
  font-size: 11px;
  font-variant-numeric: tabular-nums;
}

.gantt-pop-dur {
  color: var(--accent);
}

.gantt-pop-abort {
  margin-top: 3px;
  color: #ef6f7b;
  font-size: 11px;
}

.gantt-pop-text {
  margin-top: 5px;
  color: var(--fg);
  font-size: 12.5px;
  line-height: 1.6;
  white-space: pre-wrap;
  word-break: break-word;
}

.gantt-pop-follow {
  margin-top: 5px;
  padding-left: 8px;
  border-left: 2px solid var(--glass-border-strong);
  color: var(--fg-dim);
  font-size: 12px;
  line-height: 1.55;
  white-space: pre-wrap;
  word-break: break-word;
}
```

- [ ] **Step 3: 挂进 App**

`src/renderer/src/App.tsx` 第 311 行 `{viewMode === 'canvas' && <CanvasStage />}` 的下一行加：

```tsx
            {viewMode === 'gantt' && <GanttStage />}
```

顶部 import 加：

```tsx
import { GanttStage } from './features/gantt/GanttStage'
```

- [ ] **Step 4: 验证类型**

Run: `npm run typecheck`
Expected: 无输出

- [ ] **Step 5: 造数据并真机验证三态**

Run:

```bash
npm run build
SP=/private/tmp/claude-501/-Users-biily-Biily-Projects-vibe-coding-terminal/4c6d81ad-16bd-45be-a334-58f82d8868c9/scratchpad
pkill -f "udata-g"; sleep 1
rm -rf "$SP/udata-g" && mkdir -p "$SP/udata-g"
cp ~/Library/Application\ Support/Eas-Term/{canvas.json,projects.json} "$SP/udata-g/"
# 造三态各一条 + 一组并行，用真实 projectId
node -e "
const f=require('fs'),p=require('path');
const dir='$SP/udata-g';
const pr=JSON.parse(f.readFileSync(p.join(dir,'projects.json'),'utf8'));
const list=Array.isArray(pr)?pr:(pr.projects||[]);
const id=n=>list[n]&&list[n].id;
const now=Date.now(), M=60000;
const mk=(o)=>Object.assign({id:'gt-'+Math.random().toString(36).slice(2),ptyId:'x',leafId:'y',follow:[]},o);
f.writeFileSync(p.join(dir,'gantt.json'), JSON.stringify([
  mk({projectId:id(0), prompt:'修复导出索引格式，顺便把预览也对齐', startAt:now-95*M, endAt:now-70*M}),
  mk({projectId:id(0), prompt:'并行的第二件事，用来验证行内堆叠', startAt:now-90*M, endAt:now-60*M}),
  mk({projectId:id(1), prompt:'这条是进行中，应该有呼吸动效', startAt:now-12*M, endAt:null}),
  mk({projectId:id(2), prompt:'这条是异常中断，右端要渐隐且有图标', startAt:now-200*M, endAt:null, aborted:true})
], null, 2));
console.log('造了 4 条，projectId:', id(0), id(1), id(2));
"
nohup npx electron . --remote-debugging-port=9444 --user-data-dir="$SP/udata-g" > "$SP/devg.log" 2>&1 &
sleep 10
cat > "$SP/gantt-check.mjs" << 'EOF'
import { connect, wait } from './cdp.mjs'
import fs from 'fs'
const { ws, send, evalJs } = await connect()
const click = async (x, y) => { for (const t of ['mousePressed','mouseReleased'])
  await send('Input.dispatchMouseEvent', { type: t, x, y, button: 'left', clickCount: 1, buttons: t==='mousePressed'?1:0 }); await wait(500) }
let b = await evalJs(`(() => { const e = document.querySelector('.mode-switch'); if(!e) return null
  const r = e.getBoundingClientRect(); return [r.left+r.width/2, r.top+r.height/2] })()`)
await click(b[0], b[1])
b = await evalJs(`(() => { const e = [...document.querySelectorAll('.mode-menu-item')].find(x=>x.textContent.trim()==='甘特图')
  if(!e) return null; const r = e.getBoundingClientRect(); return [r.left+r.width/2, r.top+r.height/2] })()`)
await click(b[0], b[1]); await wait(1200)
console.log(await evalJs(`(() => {
  const bars = [...document.querySelectorAll('.gantt-bar')]
  const st = e => { const s = getComputedStyle(e); return { cls: e.className, bg: s.backgroundColor, w: Math.round(e.getBoundingClientRect().width) } }
  return JSON.stringify({
    行数: document.querySelectorAll('.gantt-row').length,
    条数: bars.length,
    层数最多的行: Math.max(0, ...[...document.querySelectorAll('.gantt-lanes')].map(l => l.querySelectorAll('.gantt-lane').length)),
    刻度: [...document.querySelectorAll('.gantt-tick span')].map(e=>e.textContent).slice(0,4),
    条: bars.map(st)
  }, null, 1)
})()`))
// hover 第一根条，验浮层
const p = await evalJs(`(() => { const e = document.querySelector('.gantt-bar'); if(!e) return null
  const r = e.getBoundingClientRect(); return [r.left + r.width/2, r.top + r.height/2] })()`)
await send('Input.dispatchMouseEvent', { type: 'mouseMoved', x: p[0], y: p[1] }); await wait(500)
console.log('浮层:', await evalJs(`document.querySelector('.gantt-pop')?.textContent.trim().slice(0,60) ?? '（没出来）'`))
const s = await send('Page.captureScreenshot', { format: 'png' })
fs.writeFileSync(process.argv[2] + '/gantt.png', Buffer.from(s.result.data,'base64'))
ws.close(); process.exit(0)
EOF
node "$SP/gantt-check.mjs" "$SP"
```

Expected:
- `行数` ≥ 3、`条数` = 4
- `层数最多的行` = 2（第一个项目的两条并行）
- `刻度` 是 `HH:MM` 形式
- 三种 `cls` 各出现：`gantt-bar done` / `running` / `aborted`
- 浮层文字非空

- [ ] **Step 6: 看截图**

用 Read 工具打开 `$SP/gantt.png`，确认：三态颜色能分辨、并行确实上下堆叠不重叠、
刻度不重叠、条内标签在宽条上可读、窄条上标签隐藏而不是半个字露在外面。
**这一步不能跳** —— 前面的脚本只验数据和类名，版面问题只有看图才发现得了。

- [ ] **Step 7: 提交**

```bash
git add src/renderer/src/features/gantt/GanttStage.tsx \
        src/renderer/src/features/gantt/gantt.css src/renderer/src/App.tsx
git commit -m "feat(gantt): 甘特图视图

一行一个项目，同项目并行的任务在行内上下堆叠——叠住的标题读不出来，
非 hover 不可，而这张图的价值就在于扫一眼知道当时在干什么。

配色跑过对比度验证：已完成条是 accent 混 70% 底色（蓝 5.21 / 粉 4.69），
≥4.5 才放得下条内深墨文字；底色和深墨同色，所以「填充 vs 底」和
「文字 vs 填充」是同一个数，不能分开调。异常中断固定 #ef6f7b 且带图标——
状态色不跟主题走，也不能只靠颜色表达。

异常中断画成右端渐隐的开放条，不编造结束时间。"
```

---

### Task 5: 发 0.4.13

**Files:**
- Modify: `package.json`（version）
- Modify: `CHANGELOG.md`（顶部加一节）
- Modify: `src/renderer/src/features/workspace/PaneLayer.tsx`（**已改好未提交**，本任务只负责提交它）

**Interfaces:**
- Consumes: Task 1–4 的全部产出
- Produces: 线上 0.4.13

- [ ] **Step 1: 先把看板修复单独提交**

工作区里 `PaneLayer.tsx` 已有一处改好但没提交的修复（effect 依赖漏了 `boardFullscreen`，
导致看板全屏点开一次之后就再打不开，实测改前 12 次空 11 次、改后 12/12）。

```bash
git add src/renderer/src/features/workspace/PaneLayer.tsx
git commit -m "fix(board): 看板里点开终端一片空白

PaneLayer 量槽位的 effect 依赖是 [viewMode, canvas.frames]，两个观察器绑的是
进入 effect 那一刻的 .board 元素。而看板全屏走的是
\`if (fullOf) return <div className=\"board-fs\">\` —— .board 整棵被卸载，
退出全屏时 React 建的是新元素。依赖里没有全屏状态，effect 不重跑，
观察器就一直盯着一个已经不在文档里的旧节点，measure 再也不跑，
boardByLeaf 空，hidden={!bp} 把终端整个藏起来（连控制条和输入框都没有）。

间歇性：切到看板后第一次通常正常（卸载那一下恰好还能触发一次），之后全废，
直到切走再切回看板才恢复 —— 所以现象是「只有几个打不开」，
很容易误以为是某几个终端自己的问题。实测改前 12 次空 11 次，改后 12/12。"
```

- [ ] **Step 2: 升版本**

把 `package.json` 的 `"version": "0.4.12"` 改成 `"0.4.13"`，然后：

```bash
npm install --package-lock-only --silent
node -e "console.log(require('./package-lock.json').version)"
```

Expected: 打印 `0.4.13`

- [ ] **Step 3: 写 CHANGELOG**

`CHANGELOG.md` 里 `## 0.4.12 — 2026-08-06` 之前插入：

```markdown
## 0.4.13 — 2026-08-07

### 新增

- **甘特图**。第四个视图：横轴是时间，一行一个项目，每根条是一次「你发出去的话 → agent 干完」。条上显示前十个字，鼠标停上去看全文和耗时，点一下跳回那个终端。同一个项目同时跑几件事时，条会上下堆叠而不是叠在一起
- 四个视图收进了一个「模式切换」按钮，点开下拉四选一 —— 原来的三段控件放不下第四项

### 修复

- **看板里点开终端一片空白**。而且是间歇性的：切到看板后第一次通常正常，之后就全打不开，直到切走再切回看板。所以看着像「只有某几个终端有问题」，其实跟是哪个终端无关，取决于你是第几次点开
```

- [ ] **Step 4: 生成官网页并提交**

**`CHANGELOG.md` 才是源头** —— `scripts/changelog.mjs` 从它生成官网页和应用内更新提示，
`publish-site.sh` 发布前会校验这一版在不在里面。只改 `site/changelog.html` 会被挡下。

```bash
node scripts/changelog.mjs html
git add package.json package-lock.json CHANGELOG.md site/changelog.html
git commit -m "chore: 0.4.12 → 0.4.13"
git push origin feat/secrets-group-vars
```

- [ ] **Step 5: 触发 Windows CI**

```bash
gh workflow run "Build Windows" --ref feat/secrets-group-vars
sleep 8 && gh run list --workflow="Build Windows" --limit 1
```

Windows 包只能云端构建（node-pty 的原生模块要在 Windows 上编译）。约 4 分钟。

- [ ] **Step 6: 本地打 mac 包**

```bash
EAS_NOTARIZE=1 npm run dist
```

不加 `EAS_NOTARIZE` 只签名不公证，别人下载会看到「已损坏」。公证凭证在钥匙串档案
`eas-notary`。公证是 staple 到 `.app` 上的，**DMG 本身不带票据是正常的**，验证要验 .app：

```bash
for D in mac-arm64 mac; do
  xcrun stapler validate ~/Eas-Term-release/$D/Eas-Term.app && \
  spctl -a -t exec -vv ~/Eas-Term-release/$D/Eas-Term.app
done
```

Expected: 两个都 `The validate action worked!` + `accepted`

- [ ] **Step 7: 取 Windows 包**

```bash
SP=/private/tmp/claude-501/-Users-biily-Biily-Projects-vibe-coding-terminal/4c6d81ad-16bd-45be-a334-58f82d8868c9/scratchpad
RUN=$(gh run list --workflow="Build Windows" --limit 1 --json databaseId --jq '.[0].databaseId')
rm -rf "$SP/win" && mkdir -p "$SP/win"
gh run download "$RUN" --name eas-term-win --dir "$SP/win"
cp "$SP/win/Eas-Term-0.4.13-x64-setup.exe" ~/Eas-Term-release/
[ "$(stat -f%z "$SP/win/Eas-Term-0.4.13-x64-setup.exe")" = "$(stat -f%z ~/Eas-Term-release/Eas-Term-0.4.13-x64-setup.exe)" ] && echo "✓ 字节一致"
```

- [ ] **Step 8: 发布**

```bash
bash scripts/publish-site.sh 2>&1 | tee /tmp/publish.log
```

**判据是日志末尾有没有 `✓ 发布完成`，不是退出码** —— `| tee` 的退出码是 tee 的，恒为 0。

**传大包会断**（2026-08-07 发 0.4.12 时，第五个包传到 68% 被 `Connection reset by peer`
掐断）。断了的话：单独 `scp` 重传那一个文件，比对 SHA256 一致，再整跑一遍脚本走完后续
步骤（脚本对包是无条件重传、不跳过已存在的，所以重跑安全但要重传 ~460MB）。
中断时网页已经更新、`latest.json` 还没写 —— 这个「先传包、最后写 latest.json」的顺序
是安全网，别改。

- [ ] **Step 9: 线上核对**

```bash
curl -s https://eas.biily.top/download/latest.json | python3 -m json.tool | head -12
for n in arm64.dmg arm64.zip x64.dmg x64.zip x64-setup.exe; do
  curl -sI "https://eas.biily.top/download/v0.4.13/Eas-Term-0.4.13-$n" | head -1
done
```

Expected: `latest.json` 的 version 是 `0.4.13`；五个包全部 `HTTP/2 200`

- [ ] **Step 10: 回写服务器档案**

在 `~/.claude/servers/39.105.40.173-阿里云.md` 的「## 变更记录」下面加一节
`### 2026-08-07 · Eas-Term 0.4.13 发布`，记：包数与落点、`latest.json` 指向、
`KEEP=2` 清掉了哪一版、reload 前后五个站的状态码、发布前后的磁盘余量。
**发现档案与现实不符就当场补** —— 脱节的档案会在最忙的时候咬人。

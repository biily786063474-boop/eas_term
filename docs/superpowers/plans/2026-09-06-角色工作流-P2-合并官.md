# 角色工作流 · P2（合并官 + 两个工具 + 回归命令）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 一个内置的「合并官」角色，配两个只读工具（合并预检、依赖波及）与项目级回归命令，从分支徽标菜单一键起会话把角色分支合进主干。

**Architecture:** 纯解析逻辑放 `src/shared/mergePreflight.ts` / `src/shared/repoImpact.ts`（零依赖、裸测）；主进程 `src/main/mergeTools.ts` 用 `gitExec` 与 `analyzeProject` 取事实并暴露 IPC `merge:preflight` / `merge:impact`；MCP 工具 `merge_preflight` / `repo_impact` 在 mcpHandler 里调 IPC；`Project.testCmd` 走 projects.ts 与侧栏行内编辑；`merger` 角色卡内置；徽标菜单「合并到主干」在同一 Frame 起合并官节点并**预填**首条消息（不自动发）。

**Tech Stack:** TypeScript · Electron · React · `node --test`（Node 26 直接跑 `.ts`）· git ≥ 2.38（`merge-tree --write-tree`）。

**Spec:** `docs/superpowers/specs/2026-09-05-角色工作流-分支合并官协同板-design.md` 第七节（7.1-7.4）。P1 已合入 main（`af3ee16`）。

## Global Constraints

- 两个工具**只读**：`merge_preflight` 不改工作区（冲突探测用 `git merge-tree --write-tree --name-only`，不做试合并）；`repo_impact` 只读依赖图。
- git < 2.38 或 `merge-tree` 失败 → 返回 `conflicts: null` 与一句「无法预检，直接合并看结果」，不抛。
- 主干判定：`git symbolic-ref -q refs/remotes/origin/HEAD` 有值取其分支名；否则 `main` 存在取 `main`，否则 `master`；都没有 → 报错「找不到主干分支」。
- `testCmd` 来源顺序：`Project.testCmd` → `package.json` 的 `scripts.test`（标注「推断」）→ 无（合并官必须报「未跑回归」）。
- 合并官角色 `caps` 为空、`isolation: 'none'`；契约里「只改冲突文件」是软约束，报告必须列出改过的文件。
- 徽标菜单「合并到主干」起的节点：同一 Frame、cwd 项目根、`roleId: 'merger'`、首条消息**预填不发送**。
- 所有 `-c` / git 参数走参数数组（`gitExec`），不经 shell；工具入参来自模型，`branch` 只认 `^[A-Za-z0-9._/-]+$` 且不以 `-` 开头。
- 测试 `npm test`；类型检查 `npm run typecheck`；每任务结束都绿。提交信息中文，`feat(merge): …` 风格，末尾 `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`。
- 工作目录：`/Users/biily/Biily/Projects/vibe coding/terminal-wt/roles-workflow-p2`（分支 `feat/roles-workflow-p2`）。每条 Bash 命令 4 分钟内返回。

---

## 文件结构

| 文件 | 责任 |
|---|---|
| `src/shared/mergePreflight.ts`（新）+ test | `parseMergeTreeNameOnly`、`parseWorktreeList`、`pickDefaultBranch`、`inferTestCmd`、`isSafeRef`、`PreflightResult` 类型 |
| `src/shared/repoImpact.ts`（新）+ test | `impactFrom(graph, files, opts)`：反向依赖（直接 + 二级）、涉及的环、建议回归文件 |
| `src/main/mergeTools.ts`（新） | `preflight(projectPath, branch)`、`impact(projectPath, files)`（`analyzeProject` 5 分钟缓存）、IPC 注册 |
| `src/main/index.ts`、`src/preload/index.ts`（改） | 注册 + `merge.preflight/impact`、`projects.setTestCmd` |
| `src/shared/types.ts`、`src/main/projects.ts`（改） | `Project.testCmd`、IPC `projects:setTestCmd` |
| `src/renderer/src/store/projectsSlice.ts`（或 `projects` 所在 slice，改） | `setProjectTestCmd` |
| `src/renderer/src/features/workspace/projectMenu.ts`、`Sidebar.tsx`（改） | 菜单项「回归命令…」→ 行内编辑 mode `'testCmd'` |
| `mcp/eas-mcp.mjs`、`src/renderer/src/mcpHandler.ts`（改） | `merge_preflight` / `repo_impact` |
| `src/main/builtinRoles.ts` + test（改） | `merger` 角色卡 |
| `src/renderer/src/layout.ts`、`store/tabsSlice.ts`、`store/canvas/persist.ts`（改） | agent pane `draft?: string`（预填，不持久化）、`clearAgentDraft` |
| `src/renderer/src/features/agentChat/AgentChatView.tsx`（改） | 预填 effect；徽标菜单「合并到主干」启用 |
| `docs/architecture/03、10、11、13`、spec（改） | 图纸同步 + 真机验证记录 |

---

### Task 1: 纯解析层（mergePreflight + repoImpact）

**Files:**
- Create: `src/shared/mergePreflight.ts`、`src/shared/mergePreflight.test.ts`
- Create: `src/shared/repoImpact.ts`、`src/shared/repoImpact.test.ts`

**Interfaces（Produces）:**
```ts
// mergePreflight.ts
export interface WorktreeEntry { path: string; head: string; branch: string | null }
export function parseWorktreeList(porcelain: string): WorktreeEntry[]
export function parseMergeTreeNameOnly(stdout: string, exitCode: number): { ok: true; tree: string; conflicts: string[] } | { ok: false }
export function pickDefaultBranch(symbolicRef: string | null, hasMain: boolean, hasMaster: boolean): string | null
export function inferTestCmd(packageJsonText: string | null): string | null
export function isSafeRef(name: string): boolean
export interface PreflightResult {
  ok: true
  branch: string; base: string; defaultBranch: string
  worktree: string | null
  changed: string[]
  conflicts: string[] | null           // null = 无法预检
  conflictNote?: string
  overlaps: { file: string; branches: string[] }[]
  testCmd: { value: string | null; source: 'project' | 'package.json' | 'none' }
}
// repoImpact.ts
export interface ImpactResult {
  files: string[]                       // 输入里在图上找得到的
  unknown: string[]                     // 图上没有的（非源码 / 未扫到）
  dependents: { file: string; direct: string[]; transitive: string[] }[]
  cycles: string[][]                    // 涉及输入文件的环（每个环是节点 id 列表）
  suggestedTests: string[]              // 同目录 *.test.ts / *.test.mjs（含 dependents 的）
}
export function impactFrom(graph: { nodes: { id: string }[]; edges: { from: string; to: string; circular?: boolean }[]; cycles: { edges: { from: string; to: string }[] }[] }, files: string[], testFiles: string[]): ImpactResult
```

- [ ] **Step 1: 写失败的测试**

`src/shared/mergePreflight.test.ts`：

```ts
import assert from 'node:assert/strict'
import { test } from 'node:test'

import { inferTestCmd, isSafeRef, parseMergeTreeNameOnly, parseWorktreeList, pickDefaultBranch } from './mergePreflight.ts'

test('parseWorktreeList：按块解析，主工作区 branch 为 refs 去前缀，detached 为 null', () => {
  const out = `worktree /p\nHEAD aaaa\nbranch refs/heads/main\n\nworktree /p/.worktrees/builder-t1\nHEAD bbbb\nbranch refs/heads/eas/builder/t1\n\nworktree /p/.worktrees/x\nHEAD cccc\ndetached\n\n`
  assert.deepEqual(parseWorktreeList(out), [
    { path: '/p', head: 'aaaa', branch: 'main' },
    { path: '/p/.worktrees/builder-t1', head: 'bbbb', branch: 'eas/builder/t1' },
    { path: '/p/.worktrees/x', head: 'cccc', branch: null }
  ])
})

test('parseMergeTreeNameOnly：有冲突 → exit 1，第一行树对象，其后到空行为止是冲突文件', () => {
  const out = `ebfbfc12\nf.txt\nsrc/a.ts\n\nAuto-merging f.txt\nCONFLICT (content): Merge conflict in f.txt\n`
  assert.deepEqual(parseMergeTreeNameOnly(out, 1), { ok: true, tree: 'ebfbfc12', conflicts: ['f.txt', 'src/a.ts'] })
})

test('parseMergeTreeNameOnly：无冲突 → exit 0 只有树对象', () => {
  assert.deepEqual(parseMergeTreeNameOnly('b90cedd\n', 0), { ok: true, tree: 'b90cedd', conflicts: [] })
})

test('parseMergeTreeNameOnly：exit ≥ 2 或输出为空 → 不可预检', () => {
  assert.deepEqual(parseMergeTreeNameOnly('', 128), { ok: false })
  assert.deepEqual(parseMergeTreeNameOnly('fatal: bad', 2), { ok: false })
})

test('pickDefaultBranch：origin/HEAD 优先，其次 main，再 master，都没有 null', () => {
  assert.equal(pickDefaultBranch('refs/remotes/origin/develop', true, true), 'develop')
  assert.equal(pickDefaultBranch(null, true, true), 'main')
  assert.equal(pickDefaultBranch(null, false, true), 'master')
  assert.equal(pickDefaultBranch(null, false, false), null)
})

test('inferTestCmd：读 scripts.test；没有或坏 JSON → null', () => {
  assert.equal(inferTestCmd(JSON.stringify({ scripts: { test: 'node --test' } })), 'npm test')
  assert.equal(inferTestCmd(JSON.stringify({ scripts: {} })), null)
  assert.equal(inferTestCmd('{bad'), null)
  assert.equal(inferTestCmd(null), null)
})

test('isSafeRef：只认字母数字 ._/-，不以 - 开头，不含 ..', () => {
  assert.ok(isSafeRef('eas/builder/ab12ef'))
  assert.ok(isSafeRef('main'))
  for (const bad of ['-x', 'a b', 'a..b', 'a;b', '', 'a\nb']) assert.ok(!isSafeRef(bad), bad)
})
```

`src/shared/repoImpact.test.ts`：

```ts
import assert from 'node:assert/strict'
import { test } from 'node:test'

import { impactFrom } from './repoImpact.ts'

const graph = {
  nodes: [{ id: 'src/a.ts' }, { id: 'src/b.ts' }, { id: 'src/c.ts' }, { id: 'src/d.ts' }],
  edges: [
    { from: 'src/b.ts', to: 'src/a.ts' },   // b 依赖 a
    { from: 'src/c.ts', to: 'src/b.ts' },   // c 依赖 b
    { from: 'src/a.ts', to: 'src/d.ts', circular: true },
    { from: 'src/d.ts', to: 'src/a.ts', circular: true }
  ],
  cycles: [{ edges: [{ from: 'src/a.ts', to: 'src/d.ts' }, { from: 'src/d.ts', to: 'src/a.ts' }] }]
}
const tests = ['src/a.test.ts', 'src/c.test.ts', 'other/x.test.ts']

test('反向依赖：direct 是直接 import 它的，transitive 是再往上一层（不重复、不含自己）', () => {
  const r = impactFrom(graph, ['src/a.ts'], tests)
  assert.deepEqual(r.files, ['src/a.ts'])
  assert.deepEqual(r.dependents[0], { file: 'src/a.ts', direct: ['src/b.ts', 'src/d.ts'], transitive: ['src/c.ts'] })
})

test('图上没有的文件进 unknown，不当成零依赖', () => {
  const r = impactFrom(graph, ['docs/x.md', 'src/a.ts'], tests)
  assert.deepEqual(r.unknown, ['docs/x.md'])
})

test('涉及输入文件的环被列出', () => {
  const r = impactFrom(graph, ['src/a.ts'], tests)
  assert.deepEqual(r.cycles, [['src/a.ts', 'src/d.ts']])
  assert.deepEqual(impactFrom(graph, ['src/c.ts'], tests).cycles, [])
})

test('建议回归：输入文件与其 dependents 同目录同名的测试文件，去重排序', () => {
  const r = impactFrom(graph, ['src/a.ts'], tests)
  assert.deepEqual(r.suggestedTests, ['src/a.test.ts', 'src/c.test.ts'])
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test src/shared/mergePreflight.test.ts src/shared/repoImpact.test.ts` → 找不到模块

- [ ] **Step 3: 实现**

`src/shared/mergePreflight.ts`：

```ts
// 合并预检的纯解析。git 调用在 main/mergeTools.ts；这里零依赖、裸测。
export interface WorktreeEntry { path: string; head: string; branch: string | null }

export function parseWorktreeList(porcelain: string): WorktreeEntry[] {
  const out: WorktreeEntry[] = []
  for (const block of porcelain.split(/\n\s*\n/)) {
    const lines = block.split('\n').filter(Boolean)
    if (!lines.length) continue
    let path = '', head = '', branch: string | null = null
    for (const l of lines) {
      if (l.startsWith('worktree ')) path = l.slice(9)
      else if (l.startsWith('HEAD ')) head = l.slice(5)
      else if (l.startsWith('branch ')) branch = l.slice(7).replace(/^refs\/heads\//, '')
    }
    if (path) out.push({ path, head, branch })
  }
  return out
}

/** `git merge-tree --write-tree --name-only A B`：exit 0 无冲突（只有树对象）；exit 1 有冲突，
 *  第一行树对象，其后到第一个空行是冲突文件名；exit ≥ 2 是 git 本身失败（版本太老等）。 */
export function parseMergeTreeNameOnly(stdout: string, exitCode: number): { ok: true; tree: string; conflicts: string[] } | { ok: false } {
  if (exitCode > 1) return { ok: false }
  const lines = stdout.split('\n')
  const tree = (lines[0] ?? '').trim()
  if (!/^[0-9a-f]{7,40}$/.test(tree)) return { ok: false }
  if (exitCode === 0) return { ok: true, tree, conflicts: [] }
  const conflicts: string[] = []
  for (const l of lines.slice(1)) {
    if (!l.trim()) break
    conflicts.push(l.trim())
  }
  return { ok: true, tree, conflicts }
}

export function pickDefaultBranch(symbolicRef: string | null, hasMain: boolean, hasMaster: boolean): string | null {
  if (symbolicRef) {
    const m = symbolicRef.trim().match(/^refs\/remotes\/[^/]+\/(.+)$/)
    if (m) return m[1]
  }
  if (hasMain) return 'main'
  if (hasMaster) return 'master'
  return null
}

/** package.json 有 scripts.test 就推断为 `npm test`（不照抄脚本内容 —— 用户跑的也是这一句） */
export function inferTestCmd(packageJsonText: string | null): string | null {
  if (!packageJsonText) return null
  try {
    const j = JSON.parse(packageJsonText) as { scripts?: Record<string, string> }
    return typeof j.scripts?.test === 'string' && j.scripts.test.trim() ? 'npm test' : null
  } catch {
    return null
  }
}

/** 工具入参来自模型：分支名只认这几种字符，不以 - 开头（防被当成选项），不含 .. */
export function isSafeRef(name: string): boolean {
  return /^[A-Za-z0-9][A-Za-z0-9._/-]*$/.test(name) && !name.includes('..')
}

export interface PreflightResult {
  ok: true
  branch: string
  base: string
  defaultBranch: string
  worktree: string | null
  changed: string[]
  conflicts: string[] | null
  conflictNote?: string
  overlaps: { file: string; branches: string[] }[]
  testCmd: { value: string | null; source: 'project' | 'package.json' | 'none' }
}
```

`src/shared/repoImpact.ts`：

```ts
// 依赖波及的纯计算。图来自 main/codeGraphAnalyze 的 analyzeProject；这里只做反查。
export interface ImpactResult {
  files: string[]
  unknown: string[]
  dependents: { file: string; direct: string[]; transitive: string[] }[]
  cycles: string[][]
  suggestedTests: string[]
}

interface GraphLike {
  nodes: { id: string }[]
  edges: { from: string; to: string; circular?: boolean }[]
  cycles: { edges: { from: string; to: string }[] }[]
}

const dirOf = (f: string): string => f.split('/').slice(0, -1).join('/')
const stem = (f: string): string => (f.split('/').pop() ?? '').replace(/\.[^.]+$/, '')

export function impactFrom(graph: GraphLike, files: string[], testFiles: string[]): ImpactResult {
  const known = new Set(graph.nodes.map((n) => n.id))
  const inputs = files.filter((f) => known.has(f))
  const unknown = files.filter((f) => !known.has(f))
  const rev = new Map<string, string[]>()
  for (const e of graph.edges) rev.set(e.to, [...(rev.get(e.to) ?? []), e.from])
  const dependents = inputs.map((file) => {
    const direct = [...new Set(rev.get(file) ?? [])].sort()
    const seen = new Set([file, ...direct])
    const transitive = [...new Set(direct.flatMap((d) => rev.get(d) ?? []))].filter((x) => !seen.has(x)).sort()
    return { file, direct, transitive }
  })
  const inputSet = new Set(inputs)
  const cycles = graph.cycles
    .map((c) => [...new Set(c.edges.flatMap((e) => [e.from, e.to]))].sort())
    .filter((ids) => ids.some((id) => inputSet.has(id)))
  const candidates = new Set([...inputs, ...dependents.flatMap((d) => [...d.direct, ...d.transitive])])
  const suggestedTests = testFiles
    .filter((t) => [...candidates].some((c) => dirOf(t) === dirOf(c) && stem(t) === `${stem(c)}.test`))
    .sort()
  return { files: inputs, unknown, dependents, cycles, suggestedTests: [...new Set(suggestedTests)] }
}
```

- [ ] **Step 4: 验证并提交**

Run: `node --test src/shared/mergePreflight.test.ts src/shared/repoImpact.test.ts && npm run typecheck && npm test`

```bash
git add src/shared/mergePreflight.ts src/shared/mergePreflight.test.ts src/shared/repoImpact.ts src/shared/repoImpact.test.ts
git commit -m "feat(merge): 合并预检与依赖波及的纯解析层（merge-tree / worktree list / 主干判定 / 反向依赖）

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 2: 主进程 `mergeTools.ts` + `Project.testCmd` + IPC/preload

**Files:**
- Create: `src/main/mergeTools.ts`
- Modify: `src/shared/types.ts`（`Project.testCmd?: string`）、`src/main/projects.ts`（`projects:setTestCmd`）、`src/main/index.ts`（`registerMergeHandlers()`）、`src/preload/index.ts`（`merge.preflight/impact`、`projects.setTestCmd`）

**Interfaces（Produces）:** IPC `merge:preflight(projectPath, branch) → PreflightResult | { ok:false; error }`、`merge:impact(projectPath, files: string[]) → { ok:true } & ImpactResult | { ok:false; error }`、`projects:setTestCmd(id, cmd) → Project[]`。

- [ ] **Step 1: `src/main/mergeTools.ts`**

```ts
// 合并官的两个只读工具的事实来源。**不改工作区**：冲突用 merge-tree 算，依赖用现成的 analyzeProject。
import fs from 'fs'
import path from 'path'
import { ipcMain } from 'electron'

import { gitExec, parseNameOnly } from './gitExec'
import { analyzeProject } from './codeGraphAnalyze'
import { collectRows } from './collabBoard'
import { inferTestCmd, isSafeRef, parseMergeTreeNameOnly, parseWorktreeList, pickDefaultBranch, type PreflightResult } from '../shared/mergePreflight'
import { impactFrom, type ImpactResult } from '../shared/repoImpact'
import { projectRootOf } from '../shared/roleWorktree'
import type { Project } from '../shared/types'

type Fail = { ok: false; error: string }

let projectsSource: () => Project[] = () => []
/** projects.ts 启动时注入（同 collabBoard 的注入手法，避免互相 import） */
export function setProjectsSource(fn: () => Project[]): void {
  projectsSource = fn
}

async function refExists(cwd: string, ref: string): Promise<boolean> {
  return (await gitExec(cwd, ['rev-parse', '--verify', '-q', `refs/heads/${ref}`])).ok
}

export async function preflight(projectPathRaw: string, branch: string): Promise<PreflightResult | Fail> {
  const projectPath = projectRootOf(projectPathRaw)
  if (!isSafeRef(branch)) return { ok: false, error: `分支名「${branch}」不合法` }
  if (!(await gitExec(projectPath, ['rev-parse', '--is-inside-work-tree'])).ok) return { ok: false, error: '这个目录不是 git 仓库' }
  if (!(await refExists(projectPath, branch))) return { ok: false, error: `分支 ${branch} 不存在` }
  const sym = await gitExec(projectPath, ['symbolic-ref', '-q', 'refs/remotes/origin/HEAD'])
  const defaultBranch = pickDefaultBranch(sym.ok ? sym.out : null, await refExists(projectPath, 'main'), await refExists(projectPath, 'master'))
  if (!defaultBranch) return { ok: false, error: '找不到主干分支（没有 origin/HEAD、main 或 master）' }
  const baseR = await gitExec(projectPath, ['merge-base', defaultBranch, branch])
  if (!baseR.ok) return { ok: false, error: `算不出合并基点：${baseR.out.slice(0, 200)}` }
  const base = baseR.out.trim()
  const changedR = await gitExec(projectPath, ['diff', '--name-only', `${base}..${branch}`])
  const changed = changedR.ok ? parseNameOnly(changedR.out) : []
  const mt = await gitExec(projectPath, ['merge-tree', '--write-tree', '--name-only', defaultBranch, branch])
  const parsed = parseMergeTreeNameOnly(mt.out, mt.code)
  const wl = await gitExec(projectPath, ['worktree', 'list', '--porcelain'])
  const worktree = wl.ok ? (parseWorktreeList(wl.out).find((w) => w.branch === branch)?.path ?? null) : null
  // 协同板上其他活跃分支与这条分支碰到的同一文件。不用 findOverlaps —— 它只数「≥2 条活分支同改一文件」，
  // 而合并官要的是「我要合的这条 vs 别人」，哪怕我这条会话已经退出（工匠干完就走了）也要算
  const rows = await collectRows(projectPath, {})
  const changedSet = new Set(changed)
  const byFile = new Map<string, Set<string>>()
  for (const r of rows) {
    if (!r.alive || r.branch === branch || r.cwd === projectPath) continue
    for (const f of r.files) if (changedSet.has(f)) byFile.set(f, (byFile.get(f) ?? new Set()).add(r.branch))
  }
  const overlaps = [...byFile.entries()].map(([file, b]) => ({ file, branches: [...b].sort() })).sort((a, b) => a.file.localeCompare(b.file))
  const project = projectsSource().find((p) => p.path === projectPath)
  let testCmd: PreflightResult['testCmd'] = { value: null, source: 'none' }
  if (project?.testCmd?.trim()) testCmd = { value: project.testCmd.trim(), source: 'project' }
  else {
    const pkg = path.join(projectPath, 'package.json')
    const inferred = inferTestCmd(fs.existsSync(pkg) ? fs.readFileSync(pkg, 'utf8') : null)
    if (inferred) testCmd = { value: inferred, source: 'package.json' }
  }
  return {
    ok: true, branch, base, defaultBranch, worktree, changed,
    conflicts: parsed.ok ? parsed.conflicts : null,
    ...(parsed.ok ? {} : { conflictNote: '无法预检冲突（git merge-tree --write-tree 不可用，需要 git ≥ 2.38）；直接合并看结果，失败就 git merge --abort' }),
    overlaps, testCmd
  }
}

// analyzeProject 一次几百毫秒到几秒；合并官一轮里会连着问几次，按项目缓存 5 分钟
const graphCache = new Map<string, { at: number; graph: Awaited<ReturnType<typeof analyzeProject>> }>()
async function graphFor(root: string): Promise<Awaited<ReturnType<typeof analyzeProject>>> {
  const hit = graphCache.get(root)
  if (hit && Date.now() - hit.at < 5 * 60_000) return hit.graph
  const graph = await analyzeProject(root)
  graphCache.set(root, { at: Date.now(), graph })
  return graph
}

function listTestFiles(root: string): string[] {
  const out: string[] = []
  const walk = (dir: string, depth: number): void => {
    if (depth > 8) return
    for (const ent of fs.readdirSync(dir, { withFileTypes: true })) {
      if (ent.name === 'node_modules' || ent.name.startsWith('.') || ent.name === 'out' || ent.name === 'dist') continue
      const p = path.join(dir, ent.name)
      if (ent.isDirectory()) walk(p, depth + 1)
      else if (/\.test\.(ts|tsx|mjs|js)$/.test(ent.name)) out.push(path.relative(root, p).split(path.sep).join('/'))
    }
  }
  try { walk(root, 0) } catch { /* 读不了就空 */ }
  return out
}

export async function impact(projectPathRaw: string, files: string[]): Promise<({ ok: true } & ImpactResult) | Fail> {
  const root = projectRootOf(projectPathRaw)
  if (!fs.existsSync(root)) return { ok: false, error: '项目目录不存在' }
  const clean = files.filter((f) => typeof f === 'string' && f && !f.startsWith('/') && !f.includes('..')).map((f) => f.replace(/\\/g, '/'))
  try {
    const graph = await graphFor(root)
    return { ok: true, ...impactFrom(graph, clean, listTestFiles(root)) }
  } catch (e) {
    return { ok: false, error: e instanceof Error ? e.message : String(e) }
  }
}

export function registerMergeHandlers(): void {
  ipcMain.handle('merge:preflight', (_e, p: unknown, b: unknown) =>
    typeof p === 'string' && typeof b === 'string' ? preflight(p, b) : { ok: false, error: '参数不对' })
  ipcMain.handle('merge:impact', (_e, p: unknown, files: unknown) =>
    typeof p === 'string' && Array.isArray(files) ? impact(p, files as string[]) : { ok: false, error: '参数不对' })
}
```

（`collectRows(projectPath, roleNames, now?)` 的第二参是「roleId → 名字」表，overlaps 不用名字所以传 `{}`。`BoardRow` 有 `branch/alive/files/cwd`。）

- [ ] **Step 2: `Project.testCmd` 与 IPC**

`types.ts` `Project` 加 `/** 回归测试命令（合并官合并前后各跑一次）。空 = 从 package.json 推断 */ testCmd?: string`。
`projects.ts` 加 handler：

```ts
  ipcMain.handle('projects:setTestCmd', (_e, id: string, cmd: string) => {
    const trimmed = String(cmd ?? '').trim().slice(0, 200)
    const list = loadProjects()
    const p = list.find((x) => x.id === id)
    if (p) {
      if (trimmed) p.testCmd = trimmed
      else delete p.testCmd
    }
    saveProjects(list)
    return list
  })
```
并在 `registerProjectHandlers()` 开头 `setProjectsSource(loadProjects)`（import 自 `./mergeTools`）。`index.ts` 在 `registerCollabBoardHandlers()` 之后 `registerMergeHandlers()`。

preload：`projects` 命名空间加 `setTestCmd: (id: string, cmd: string): Promise<Project[]> => ipcRenderer.invoke('projects:setTestCmd', id, cmd)`；新命名空间：

```ts
  merge: {
    preflight: (projectPath: string, branch: string): Promise<PreflightResult | { ok: false; error: string }> =>
      ipcRenderer.invoke('merge:preflight', projectPath, branch),
    impact: (projectPath: string, files: string[]): Promise<({ ok: true } & ImpactResult) | { ok: false; error: string }> =>
      ipcRenderer.invoke('merge:impact', projectPath, files)
  },
```

- [ ] **Step 3: 验证并提交**

Run: `npm run typecheck && npm test`

```bash
git add src/main/mergeTools.ts src/main/projects.ts src/main/index.ts src/shared/types.ts src/preload/index.ts
git commit -m "feat(merge): 主进程合并预检 / 依赖波及 IPC，Project.testCmd

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 3: MCP 工具 `merge_preflight` / `repo_impact` + 回归命令的侧栏入口

**Files:**
- Modify: `mcp/eas-mcp.mjs`（`board_read` 之后加两个定义）、`src/renderer/src/mcpHandler.ts`
- Modify: `src/renderer/src/store/`（projects 所在 slice：`setProjectTestCmd`）、`src/renderer/src/features/workspace/projectMenu.ts`、`Sidebar.tsx`

- [ ] **Step 1: 工具定义**

```js
  {
    name: 'merge_preflight',
    description:
      '合并前的**只读预检**：给一条角色分支（如 eas/builder/ab12ef），返回主干名、合并基点、这条分支改了哪些文件、' +
      '直接合并会冲突的文件（git merge-tree 算的，不动工作区）、协同板上其他活跃分支与它碰到的同一文件、' +
      '以及该项目的回归命令（用户设的或从 package.json 推断，都没有就 null —— 那时你必须在报告里写「未跑回归」）。' +
      '合并官动手前先调它。conflicts 为 null 表示这台机器的 git 太老无法预检。',
    inputSchema: { type: 'object', properties: { branch: { type: 'string', description: '要合进主干的分支名' } }, required: ['branch'] }
  },
  {
    name: 'repo_impact',
    description:
      '依赖波及（只读）：给一组文件（相对项目根），返回谁 import 了它们（直接与再上一层）、它们卷入的循环依赖、' +
      '以及建议的回归范围（同目录同名的 *.test 文件）。图来自代码地图的分析，第一次要几秒。' +
      '拿 merge_preflight 的 changed 喂给它，就知道这次合并会波及到哪。',
    inputSchema: { type: 'object', properties: { files: { type: 'array', items: { type: 'string' } } }, required: ['files'] }
  },
```

- [ ] **Step 2: mcpHandler**

```ts
  if (tool === 'merge_preflight' || tool === 'repo_impact') {
    const where = resolveFrame(ctx)
    const projectPath = where?.projectPath || ctx.project || ''
    if (!projectPath) throw new Error('找不到你所在的项目')
    if (tool === 'merge_preflight') {
      const branch = String(args.branch ?? '').trim()
      if (!branch) throw new Error('缺少 branch')
      const r = await window.api.merge.preflight(projectPath, branch)
      if (!r.ok) throw new Error(r.error)
      return r
    }
    const files = Array.isArray(args.files) ? args.files.map(String) : []
    if (!files.length) throw new Error('files 不能为空')
    const r = await window.api.merge.impact(projectPath, files)
    if (!r.ok) throw new Error(r.error)
    return r
  }
```

- [ ] **Step 3: 回归命令入口**

store（projects 所在 slice）加 `setProjectTestCmd: async (id, cmd) => { const list = await window.api.projects.setTestCmd(id, cmd); set({ projects: list }) }`（照 `renameProject` 的写法）。
`Sidebar.tsx` 的 `editingProject.mode` 联合类型加 `'testCmd'`：进入该模式时 input 初值 `p.testCmd ?? ''`、placeholder「回归命令，留空则从 package.json 推断」；`onBlur` 里 `mode === 'testCmd'` → `void setProjectTestCmd(p.id, val)` 后 return。
`projectMenu.ts` 在「改显示名」之后加 `{ label: '回归命令…', hint: p.testCmd ? p.testCmd : '未设，合并官会从 package.json 推断', onClick: () => onStartRename(projectId, 'testCmd') }`（`onStartRename` 的 mode 类型同步扩）。

- [ ] **Step 4: 验证并提交**

Run: `npm run typecheck && npm test`

```bash
git add mcp/eas-mcp.mjs src/renderer/src/mcpHandler.ts src/renderer/src/store src/renderer/src/features/workspace/projectMenu.ts src/renderer/src/features/workspace/Sidebar.tsx
git commit -m "feat(merge): MCP 工具 merge_preflight / repo_impact；项目菜单可设回归命令

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 4: 合并官角色卡 + 徽标菜单触发（预填首条消息）

**Files:**
- Modify: `src/main/builtinRoles.ts` + `builtinRoles.test.ts`（`merger` 加进 id 快照与「其余角色三家参数全空」组）
- Modify: `src/renderer/src/layout.ts`（agent pane `draft?: string`，**不持久化**）、`store/tabsSlice.ts`（`openAgentPane` 收 `draft`；`clearAgentDraft`）
- Modify: `src/renderer/src/features/agentChat/AgentChatView.tsx`（预填 effect；徽标菜单「合并到主干」）

- [ ] **Step 1: 角色卡**（放在 `inspector` 之后、`prototyper` 之前，`group: 'main'`）

```ts
  {
    id: 'merger',
    name: '合并官',
    desc: '把角色分支合进主干；懂依赖波及，合并前后跑回归',
    group: 'main',
    color: '#a5b4fc',
    kind: 'auto',
    isolation: 'none',
    model: { claude: 'opus' },
    effort: { claude: 'high', codex: 'high' },
    // caps 故意为空：要跑 git、要跑测试、要改冲突文件，硬约束一条都不能收
    contract: [
      '你这一轮的职责是把一条角色分支合进主干，不是继续开发。',
      '流程固定：1 用 board_read 看协同板；2 用 merge_preflight 看主干名、改动文件、冲突文件、别的分支是否在改同一文件、回归命令；',
      '3 把 changed 交给 repo_impact 看波及范围；4 在那条分支的 worktree 目录里跑回归命令；5 在主干 git merge --no-ff；',
      '6 在主干再跑一次回归；7 失败就 git merge --abort 或 git reset --hard 回到合并前并报告。',
      '只改冲突文件，不顺手重构。回归命令为 null 时必须在报告里写「未跑回归」，不许说「测试通过」。',
      '完成判据：主干回归绿；报告列出冲突文件、你改过的文件、两次回归的命令与结果；提醒用户在那个节点的分支徽标里删掉 worktree。'
    ].join('\n')
  },
```

`builtinRoles.test.ts`：id 快照加 `'merger'`；「其余内置角色三家参数全空」那组的 id 列表加 `merger`；隔离默认表加 `merger: 'none'`。

- [ ] **Step 2: 预填**

`layout.ts` agent pane 加 `/** 首条消息预填（只填进输入框，不发送；不持久化）。徽标菜单「合并到主干」用 */ draft?: string`。`tabsSlice.ts`：`openAgentPane` opts 加 `draft?: string` 并写进 pane；`clearAgentDraft(tabId, leafId)`（照 `clearAgentInitialMessage`）。`canvas/types.ts` 的 `addAgentNode` opts 同步加 `draft?`（它原样转给 `openAgentPane`）。
`AgentChatView.tsx`：订阅 `draft`（照 `initialMessage` 的 selector），effect：

```ts
  const draftFiredRef = useRef(false)
  useEffect(() => {
    if (draftFiredRef.current || !draft) return
    draftFiredRef.current = true
    setText(draft)
    clearAgentDraft(tabId, leafId)
  }, [draft, tabId, leafId, clearAgentDraft])
```

- [ ] **Step 3: 徽标菜单**

把 `{ label: '合并到主干', hint: 'P2 接合并官', disabled: true, onClick: () => {} }` 改为：

```ts
        {
          label: '合并到主干',
          hint: '起一个合并官会话，首条消息已预填',
          onClick: () => {
            const S = useStore.getState()
            const frame = S.canvas.frames.find((f) => f.nodes.some((n) => n.leafId === leafId))
            if (!frame) return
            void S.addAgentNode(frame.id, {
              cli: selected?.id,
              roleId: 'merger',
              draft: `把 ${worktree.branch} 合进主干。先 merge_preflight，再 repo_impact，回归前后各一次。`
            })
          }
        },
```

- [ ] **Step 4: 验证并提交**

Run: `node --test src/main/builtinRoles.test.ts && npm run typecheck && npm test`

```bash
git add src/main/builtinRoles.ts src/main/builtinRoles.test.ts src/renderer/src/layout.ts src/renderer/src/store/tabsSlice.ts src/renderer/src/store/canvas/types.ts src/renderer/src/features/agentChat/AgentChatView.tsx
git commit -m "feat(roles): 合并官角色卡；徽标菜单「合并到主干」起合并官会话并预填首条消息

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 5: 图纸同步 + 真机验证

**Files:**
- Modify: `docs/architecture/03-agent角色边界.md`（合并官一行、`testCmd`）、`10-模块领地图.md`（`mergeTools.ts`、`shared/mergePreflight.ts`、`shared/repoImpact.ts`）、`11-MCP工具网络.md`（两个工具）、`13-所有权矩阵.md`（`Project.testCmd` 进 persist 字段清单；builtinRoles 快照提醒）
- Modify: spec 状态行 →「P1、P2 已实现；P3 未动」，第七节末尾加「已实现」指向模块名

- [ ] **Step 1: 图纸**（按事实写）

- [ ] **Step 2: 真机验证（隔离实例，每条命令限时）**

用 `/tmp` 临时 git 仓库造场景：`main` 改 `f.txt` 第 2 行为 `bb`；分支 `eas/builder/t1` 改同一行为 `B` 并新增 `n.txt`；再建一条 `eas/e2e/t2` 改 `n.txt`（模拟并行分支）。把仓库塞进实例 `projects.json`。

| # | 场景 | 判据 |
|---|---|---|
| 1 | `window.api.merge.preflight(P, 'eas/builder/t1')` | `defaultBranch='main'`、`changed=['f.txt','n.txt']`、`conflicts=['f.txt']`、`worktree` 指向 `.worktrees/…`（若已 `git worktree add`）、`testCmd.source='none'` |
| 2 | 在 `package.json` 写 `scripts.test`，再 preflight | `testCmd = { value:'npm test', source:'package.json' }`；项目菜单「回归命令…」设一条后 → `source='project'` |
| 3 | `window.api.merge.impact(<本仓库路径>, ['src/shared/board.ts'])` | `dependents[0].direct` 含 `src/main/collabBoard.ts`；`suggestedTests` 含 `src/shared/board.test.ts` |
| 4 | 不合法分支名 `-x` / `a;b` | `{ ok:false }`，错误是人话 |
| 5 | 工匠节点徽标菜单「合并到主干」 | 同一 Frame 多一个合并官节点，输入框预填「把 eas/builder/… 合进主干…」，**没有自动发送** |
| 6 | 合并官会话发送后（真实 Claude，最小提示词「只调 merge_preflight 然后把结果原样报给我，别合并」） | 回复里出现 conflicts 与 testCmd |
| 7 | 角色管理里合并官卡片 | 无 caps、隔离「在主工作区」、契约七步 |

结果写进 spec 新小节「P2 真机验证（日期）」，每条一行写「看到了什么」。

- [ ] **Step 3: 提交**

```bash
git add docs/architecture/03-agent角色边界.md docs/architecture/10-模块领地图.md docs/architecture/11-MCP工具网络.md docs/architecture/13-所有权矩阵.md "docs/superpowers/specs/2026-09-05-角色工作流-分支合并官协同板-design.md"
git commit -m "docs(architecture): 角色工作流 P2 —— 合并官、merge_preflight / repo_impact、testCmd 入图纸，真机验证记录

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

## 自查记录

- **Spec 覆盖**：7.1 角色卡 → T4；7.2 两个工具 → T1/T2/T3；7.3 回归命令 → T2/T3（推断 + 侧栏入口）；7.4 触发 → T4（预填不发送）；「台账」相关字句从契约里去掉（P3 未做），改为「写进报告」。
- **类型一致性**：`PreflightResult` / `ImpactResult` 在 shared 定义，main、preload、mcpHandler 都从它引；`draft` 在 layout / tabsSlice / canvas types / AgentChatView 一致。
- **裁定**：预填走新字段 `draft`（现有 `initialMessage` 会自动发送，语义不同）；`testCmd` 入口复用侧栏行内编辑的第三个 mode，不新造弹窗；`repo_impact` 的图按项目缓存 5 分钟。

# 设置层级视觉稿落地
目标：照已确认 docs/prototype/2026-09-10-settings-hierarchy.html，保留设置行为、权限和存储，只重构信息层级。
范围：SettingsPanel、独立导航元数据/样式、MCP 跳转入口；主进程/IPC/安全逻辑不改。复用当前干净隔离 worktree，不接触根工作区未提交资产。
- [x] 测试先行：10 页/3 组、关键词搜索、无效路由保护；MCP 独立页与诊断归属。
- [x] 实现：分组侧栏、搜索、页标题、分组卡片、说明折叠、OMP 三档。保留隐私写入说明、全部扩展/手机/CLI 子组件。
- [x] 回归：类型/静态/全量；独立数据真实 Electron 逐页、搜索、审批控件禁用/详情、窄窗口/亮色、关闭与快捷键入口。
- [x] 同步架构与验证记录；开发实例保持打开，不自行发版。

验证边界详见 docs/verification/settings-hierarchy/README.md；外部写入类操作未执行，不把保留处理器当成端到端验证。

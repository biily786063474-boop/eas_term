# 角色工作流 · P1（分支隔离 + 协同板）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 写码角色的对话会话自动落在自己的 worktree 与分支上；一张由 app 从事实算出来的协同板让每条会话起步就看见别人在改什么，随时能查。

**Architecture:** 角色卡加显式字段 `isolation`。渲染层在第一次发消息前经新 IPC 建 worktree（复用 `teamWorktreeOps` 的 git 逻辑与三道前置检查），pane 记住 `{ relPath, branch }` 并持久化，起会话的 cwd 指向 worktree。主进程新模块 `board.ts` 从会话表 + `git diff` 生成 `.eas/board.md`（纯渲染函数可裸测），在会话起 / 每轮结束 / 退出 / worktree 增删时刷新；起会话时把板文全文附进系统提示（≤20 行），另给一个只读 MCP 工具 `board_read`。对话工具栏显示分支徽标，菜单里能开终端与删 worktree。

**Tech Stack:** TypeScript · Electron · React · `node --test`（Node 26 直接跑 `.ts`）。

**Spec:** `docs/superpowers/specs/2026-09-05-角色工作流-分支合并官协同板-design.md`（第五节 5.2 台账**不在** P1；第六、八节是 P1；6.3 的「合并到主干」菜单项在 P1 只占位禁用，P2 接）。

## Global Constraints

- **不推断角色写不写代码**：只看 `AgentRole.isolation === 'worktree'`。内置默认 `e2e` / `builder` / `prototyper` = `worktree`，其余 = `none`。
- worktree 路径 `.worktrees/<roleId>-<6 位短 id>`，分支 `eas/<roleId>/<短 id>`（与团队的 `eas-team/` 前缀区分）；短 id 用 6 位 `[a-z0-9]`。
- 不是 git 仓库 → **不静默降级**：弹确认「这个目录不是 git 仓库，<角色名>会直接改主工作区」，点「继续」才起。
- 会话结束不删 worktree；删除只从徽标菜单点，未提交改动一律拒删（沿用 `team:worktreeRemove` 的纪律）。
- 协同板**只由事实生成**：会话表 + git；模型不能写它。写文件用临时文件 + rename。`.eas/` 写进项目 `.git/info/exclude`，不改用户 `.gitignore`。
- 板文注入系统提示：≤20 行全文，超过截断到 20 行并附一句「完整内容见 `.eas/board.md`，用 `board_read` 读」。
- 测试命令 `npm test`；类型检查 `npm run typecheck`。每个任务结束两者都要绿。
- 提交信息中文，`feat(roles): …` / `feat(board): …` 风格，末尾 `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`。
- 在 node 下裸跑的源文件相对 import 带 `.ts` 后缀；渲染层 import shared 不带。
- 真机验证在隔离配置目录（`npm run verify`）里做，不碰用户真实 `~/.eas/roles.json`。

---

## 文件结构

| 文件 | 责任 |
|---|---|
| `src/shared/types.ts`（改） | `AgentRole.isolation?: 'worktree' \| 'none'` |
| `src/main/rolesSchema.ts`（改）+ test | 清洗 `isolation` |
| `src/main/builtinRoles.ts`（改）+ test | 三个写码角色默认 `worktree` |
| `src/shared/roleWorktree.ts`（新）+ test | 命名：`roleWorktreeName(roleId, id)`、`roleWorktreeBranch`、`newShortId()`、`projectRootOf(cwd)` |
| `src/main/teamWorktreeOps.ts`（改） | 抽出 `addWorktree(projectPath, relPath, branch)` 供两处用；新增 IPC `role:worktreeAdd(projectPath, roleId)` |
| `src/shared/board.ts`（新）+ test | `BoardRow`、`renderBoard(rows, now)`、`findOverlaps(rows)`、`clipForPrompt(text)` |
| `src/main/board.ts`（新） | `collectRows(projectPath)`（会话表 + git）、`refreshBoard(projectPath)`（防抖、写文件、exclude）、`readBoard(projectPath)`、IPC `board:refresh` / `board:read` |
| `src/main/agentChat/session.ts`（改） | `StartOpts.roleId`、`boardText`；起会话前读板、起后 / `turn.done` / exit 刷板 |
| `src/main/agentChat/sessionState.ts`、`src/shared/agentChat.ts`（改） | 两个新字段 |
| 三个 adapter / `omp/launch.ts`（改）+ tests | 系统提示末尾附板文 |
| `src/preload/index.ts`（改） | `roles.worktreeAdd`、`board.refresh` / `board.read` |
| `src/renderer/src/layout.ts`、`store/tabsSlice.ts`、`store/canvas/persist.ts`（改） | agent pane 加 `worktree`；`setAgentWorktree`；持久化 |
| `src/renderer/src/features/agentChat/AgentChatView.tsx`（改） | 首次起会话前建 worktree（含非 git 确认）；`effectiveCwd`；传 `roleId`；分支徽标 + 菜单 |
| `src/renderer/src/features/agentChat/agentChat.css`（改） | `.ac-branch` |
| `src/renderer/src/features/canvas/CanvasRoleEditor.tsx`（改） | 「起会话时」两档开关 |
| `mcp/eas-mcp.mjs`、`src/renderer/src/mcpHandler.ts`（改） | `board_read` |
| `docs/architecture/03、10、11、13`、spec（改） | 图纸同步 |

---

### Task 1: `isolation` 字段 + 命名纯函数

**Files:**
- Modify: `src/shared/types.ts`（`AgentRole`，`raw?` 之后）
- Modify: `src/main/rolesSchema.ts` + `src/main/rolesSchema.test.ts`
- Modify: `src/main/builtinRoles.ts` + `src/main/builtinRoles.test.ts`
- Create: `src/shared/roleWorktree.ts` + `src/shared/roleWorktree.test.ts`

**Interfaces:**
- Produces: `AgentRole.isolation`；`roleWorktreeName(roleId, id) → string | null`、`roleWorktreeBranch(roleId, id) → string | null`、`newShortId(rand?) → string`、`projectRootOf(cwd) → string`。

- [ ] **Step 1: 写失败的测试**

`src/shared/roleWorktree.test.ts`：

```ts
import assert from 'node:assert/strict'
import { test } from 'node:test'

import { newShortId, projectRootOf, roleWorktreeBranch, roleWorktreeName } from './roleWorktree.ts'

test('路径与分支：.worktrees/<roleId>-<id> 与 eas/<roleId>/<id>', () => {
  assert.equal(roleWorktreeName('builder', 'ab12ef'), '.worktrees/builder-ab12ef')
  assert.equal(roleWorktreeBranch('builder', 'ab12ef'), 'eas/builder/ab12ef')
})

test('roleId 不合法（含大写 / 空格 / 路径符）→ null，不拼路径', () => {
  for (const bad of ['Builder', 'a b', '../x', '']) {
    assert.equal(roleWorktreeName(bad, 'ab12ef'), null, bad)
    assert.equal(roleWorktreeBranch(bad, 'ab12ef'), null, bad)
  }
})

test('短 id 是 6 位 [a-z0-9]，可注入随机源', () => {
  const id = newShortId(() => 0.5)
  assert.match(id, /^[a-z0-9]{6}$/)
  assert.match(newShortId(), /^[a-z0-9]{6}$/)
})

test('projectRootOf：worktree 路径回到项目根，其余原样', () => {
  assert.equal(projectRootOf('/p/.worktrees/builder-ab12ef'), '/p')
  assert.equal(projectRootOf('/p/.worktrees/builder-ab12ef/src'), '/p')
  assert.equal(projectRootOf('C:\\p\\.worktrees\\x'), 'C:\\p')
  assert.equal(projectRootOf('/p'), '/p')
})
```

`src/main/rolesSchema.test.ts` 追加：

```ts
test('sanitizeRoles：isolation 只认 worktree，其余当没写', () => {
  const out = sanitizeRoles({ roles: [
    { id: 'a', name: 'A', isolation: 'worktree' },
    { id: 'b', name: 'B', isolation: 'none' },
    { id: 'c', name: 'C', isolation: 'yes' }
  ] })
  assert.equal(out[0].isolation, 'worktree')
  assert.equal(out[1].isolation, undefined)
  assert.equal(out[2].isolation, undefined)
})
```

`src/main/builtinRoles.test.ts` 追加：

```ts
test('内置角色的隔离默认：e2e / builder / prototyper 进 worktree，其余不隔离 —— 系统不替角色猜写不写码', () => {
  const iso = Object.fromEntries(BUILTIN_ROLES.map((r) => [r.id, r.isolation ?? 'none']))
  assert.deepEqual(iso, {
    e2e: 'worktree', scout: 'none', builder: 'worktree', inspector: 'none',
    prototyper: 'worktree', writer: 'none', illustrator: 'none', runner: 'none'
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test src/shared/roleWorktree.test.ts src/main/rolesSchema.test.ts src/main/builtinRoles.test.ts`
Expected: 新增的都 FAIL

- [ ] **Step 3: 实现**

`src/shared/types.ts` 的 `AgentRole` 在 `raw?` 之后加：

```ts
  /** 起会话时是否隔离到自己的 worktree。**缺省 none，不推断**（沿用 team_spawn 的纪律：
   *  系统不会替你判断这个角色写不写代码）。写码角色选 worktree，主工作区就不被动。 */
  isolation?: 'worktree' | 'none'
```

`src/main/rolesSchema.ts` 的 `sanitizeRoles` 输出对象里加 `...(r.isolation === 'worktree' ? { isolation: 'worktree' as const } : {})`。

`src/main/builtinRoles.ts`：`e2e` / `builder` / `prototyper` 三条各加 `isolation: 'worktree',`（放在 `kind` 之后），并在 `builder` 那条上方加注释：`// isolation: 写码的角色进自己的 worktree（P1）。scout / inspector 只读，writer / illustrator 产出不在代码里，runner 是逃生口 —— 都不隔离。`

`src/shared/roleWorktree.ts`：

```ts
// 角色会话的 worktree 命名与定位。**纯函数、零依赖。** git 操作在 main/teamWorktreeOps.ts。
//
// 与团队派活的 `.worktrees/<批次>-<role>` / `eas-team/…` 并存：目录同一个，前缀不同，
// `git branch` 里一眼分得清哪些是派活的、哪些是角色会话自己开的。
import { WORKTREE_DIR } from './teamWorktree'

const ROLE_RE = /^[a-z][a-z0-9-]*$/

export function roleWorktreeName(roleId: string, id: string): string | null {
  if (!ROLE_RE.test(roleId) || !/^[a-z0-9]{6}$/.test(id)) return null
  return `${WORKTREE_DIR}/${roleId}-${id}`
}

export function roleWorktreeBranch(roleId: string, id: string): string | null {
  if (!ROLE_RE.test(roleId) || !/^[a-z0-9]{6}$/.test(id)) return null
  return `eas/${roleId}/${id}`
}

/** 6 位 [a-z0-9]。随机源可注入，测试才钉得住 */
export function newShortId(rand: () => number = Math.random): string {
  const alphabet = 'abcdefghijklmnopqrstuvwxyz0123456789'
  let s = ''
  for (let i = 0; i < 6; i++) s += alphabet[Math.floor(rand() * alphabet.length) % alphabet.length]
  return s
}

/** 会话 cwd → 项目根：worktree 里的 cwd 剥到 `.worktrees/` 之前；其余原样。两种分隔符都认。 */
export function projectRootOf(cwd: string): string {
  for (const sep of ['/', '\\']) {
    const i = cwd.indexOf(`${sep}${WORKTREE_DIR}${sep}`)
    if (i >= 0) return cwd.slice(0, i)
    if (cwd.endsWith(`${sep}${WORKTREE_DIR}`)) return cwd.slice(0, cwd.length - WORKTREE_DIR.length - 1)
  }
  return cwd
}
```

（`teamWorktree.ts` 的 `WORKTREE_DIR` 是 `'.worktrees'`，那文件零依赖，可以 import。）

- [ ] **Step 4: 验证并提交**

Run: `node --test src/shared/roleWorktree.test.ts src/main/rolesSchema.test.ts src/main/builtinRoles.test.ts && npm run typecheck && npm test`

```bash
git add src/shared/types.ts src/shared/roleWorktree.ts src/shared/roleWorktree.test.ts src/main/rolesSchema.ts src/main/rolesSchema.test.ts src/main/builtinRoles.ts src/main/builtinRoles.test.ts
git commit -m "feat(roles): 角色卡加 isolation 字段，写码角色默认进 worktree；角色 worktree 命名纯函数

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 2: 协同板的纯渲染层

**Files:**
- Create: `src/shared/board.ts` + `src/shared/board.test.ts`

**Interfaces:**
- Produces: `BoardRow { branch: string; roleName: string; roleId?: string; alive: boolean; idleMs: number; startedAt: number; files: string[]; cwd: string }`、`findOverlaps(rows) → { file: string; branches: string[] }[]`、`renderBoard(rows, now, overlaps?) → string`、`clipForPrompt(text, maxLines = 20) → string`、`BOARD_REL = '.eas/board.md'`。

- [ ] **Step 1: 写失败的测试**

```ts
import assert from 'node:assert/strict'
import { test } from 'node:test'

import { BOARD_REL, clipForPrompt, findOverlaps, renderBoard, type BoardRow } from './board.ts'

const row = (p: Partial<BoardRow>): BoardRow => ({
  branch: 'eas/builder/ab12ef', roleName: '工匠', roleId: 'builder', alive: true, idleMs: 0,
  startedAt: Date.UTC(2026, 8, 5, 5, 10), files: [], cwd: '/p/.worktrees/builder-ab12ef', ...p
})

test('BOARD_REL 固定', () => assert.equal(BOARD_REL, '.eas/board.md'))

test('空板：一句「没有活跃分支」而不是空表', () => {
  const s = renderBoard([], Date.UTC(2026, 8, 5, 6))
  assert.ok(s.startsWith('# 协同板'))
  assert.ok(s.includes('勿手改'))
  assert.ok(s.includes('没有活跃分支'))
})

test('一行：分支 / 角色 / 状态（活跃 or 闲置 N 分钟）/ 起于 / 触及（≤3 个文件全列，多了按目录归并 + 计数）', () => {
  const now = Date.UTC(2026, 8, 5, 6, 0)
  const s = renderBoard([
    row({ files: ['src/a.ts', 'src/b.ts'] }),
    row({ branch: 'eas/e2e/9c0d11', roleName: '全流程', alive: true, idleMs: 40 * 60_000, startedAt: Date.UTC(2026, 8, 5, 3, 2),
      files: ['src/renderer/src/features/agentChat/x.tsx', 'src/renderer/src/features/agentChat/y.tsx', 'src/renderer/src/features/agentChat/z.tsx', 'src/renderer/src/features/agentChat/w.tsx'] })
  ], now)
  assert.ok(s.includes('| eas/builder/ab12ef | 工匠 | 活跃 |'))
  assert.ok(s.includes('src/a.ts, src/b.ts'))
  assert.ok(s.includes('| eas/e2e/9c0d11 | 全流程 | 闲置 40 分钟 |'))
  assert.ok(s.includes('src/renderer/src/features/agentChat/**（4 文件）'))
})

test('进程没了但记录还在 → 状态「已停」', () => {
  const s = renderBoard([row({ alive: false, idleMs: 5 * 60_000 })], Date.UTC(2026, 8, 5, 6))
  assert.ok(s.includes('| 已停 |'))
})

test('findOverlaps：两条分支碰同一文件才算，且只报活跃的', () => {
  const rows = [
    row({ files: ['src/shared/types.ts', 'src/a.ts'] }),
    row({ branch: 'eas/e2e/9c0d11', files: ['src/shared/types.ts'] }),
    row({ branch: 'eas/scout/000000', alive: false, files: ['src/a.ts'] })
  ]
  assert.deepEqual(findOverlaps(rows), [{ file: 'src/shared/types.ts', branches: ['eas/builder/ab12ef', 'eas/e2e/9c0d11'] }])
})

test('renderBoard 把交集写成 ⚠ 行', () => {
  const rows = [row({ files: ['src/shared/types.ts'] }), row({ branch: 'eas/e2e/9c0d11', files: ['src/shared/types.ts'] })]
  const s = renderBoard(rows, Date.UTC(2026, 8, 5, 6), findOverlaps(rows))
  assert.ok(s.includes('⚠ 两条分支都改了 src/shared/types.ts（eas/builder/ab12ef, eas/e2e/9c0d11）'))
})

test('clipForPrompt：≤20 行原样；超过截到 20 行并附路径与 board_read 提示', () => {
  const short = Array.from({ length: 5 }, (_, i) => `l${i}`).join('\n')
  assert.equal(clipForPrompt(short), short)
  const long = Array.from({ length: 30 }, (_, i) => `l${i}`).join('\n')
  const c = clipForPrompt(long)
  assert.equal(c.split('\n').length, 21)
  assert.ok(c.endsWith('…（还有 10 行，完整内容见 .eas/board.md，用 board_read 读）'))
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test src/shared/board.test.ts` → FAIL（找不到模块）

- [ ] **Step 3: 实现 `src/shared/board.ts`**

```ts
// 协同板的纯渲染。**数据从哪来不归这里管**（main/board.ts 从会话表 + git 算），
// 这里只负责把 rows 排成人和模型都读得懂的一张表。零依赖，node --test 裸跑。
export const BOARD_REL = '.eas/board.md'

export interface BoardRow {
  branch: string
  roleName: string
  roleId?: string
  alive: boolean
  idleMs: number
  startedAt: number
  /** 相对项目根，已排序去重 */
  files: string[]
  cwd: string
}

export interface Overlap {
  file: string
  branches: string[]
}

const pad = (n: number): string => String(n).padStart(2, '0')
const hhmm = (t: number): string => {
  const d = new Date(t)
  return `${pad(d.getUTCHours())}:${pad(d.getUTCMinutes())}`
}
const stamp = (t: number): string => {
  const d = new Date(t)
  return `${d.getUTCFullYear()}-${pad(d.getUTCMonth() + 1)}-${pad(d.getUTCDate())} ${hhmm(t)}`
}

function status(r: BoardRow): string {
  if (!r.alive) return '已停'
  const min = Math.floor(r.idleMs / 60_000)
  return min >= 5 ? `闲置 ${min} 分钟` : '活跃'
}

/** ≤3 个文件全列；多了按最深公共目录归并成 `dir/**（N 文件）` */
function touched(files: string[]): string {
  if (!files.length) return '—'
  if (files.length <= 3) return files.join(', ')
  const parts = files.map((f) => f.split('/').slice(0, -1))
  let common = parts[0]
  for (const p of parts.slice(1)) {
    let i = 0
    while (i < common.length && i < p.length && common[i] === p[i]) i++
    common = common.slice(0, i)
  }
  const dir = common.length ? common.join('/') + '/' : ''
  return `${dir}**（${files.length} 文件）`
}

export function findOverlaps(rows: BoardRow[]): Overlap[] {
  const byFile = new Map<string, string[]>()
  for (const r of rows) {
    if (!r.alive) continue
    for (const f of r.files) byFile.set(f, [...(byFile.get(f) ?? []), r.branch])
  }
  return [...byFile.entries()].filter(([, b]) => b.length >= 2).map(([file, branches]) => ({ file, branches }))
}

export function renderBoard(rows: BoardRow[], now: number, overlaps: Overlap[] = findOverlaps(rows)): string {
  const head = `# 协同板 · ${stamp(now)} 自动生成，勿手改`
  if (!rows.length) return `${head}\n\n没有活跃分支。\n`
  const lines = [head, '', '| 分支 | 角色 | 状态 | 起于 | 触及 |', '|---|---|---|---|---|']
  for (const r of rows) lines.push(`| ${r.branch} | ${r.roleName} | ${status(r)} | ${hhmm(r.startedAt)} | ${touched(r.files)} |`)
  for (const o of overlaps) lines.push(`| ⚠ 两条分支都改了 ${o.file}（${o.branches.join(', ')}） |`)
  return lines.join('\n') + '\n'
}

/** 注入系统提示用：几行以内全文；超过 maxLines 截断并指路 */
export function clipForPrompt(text: string, maxLines = 20): string {
  const lines = text.replace(/\n$/, '').split('\n')
  if (lines.length <= maxLines) return lines.join('\n')
  const rest = lines.length - maxLines
  return [...lines.slice(0, maxLines), `…（还有 ${rest} 行，完整内容见 ${BOARD_REL}，用 board_read 读）`].join('\n')
}
```

- [ ] **Step 4: 验证并提交**

Run: `node --test src/shared/board.test.ts && npm run typecheck`

```bash
git add src/shared/board.ts src/shared/board.test.ts
git commit -m "feat(board): 协同板的纯渲染 —— 行状态、触及归并、交集告警、注入截断

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 3: 角色 worktree 的 IPC + 主进程协同板

**Files:**
- Modify: `src/main/teamWorktreeOps.ts`（抽 `addWorktree`，加 `role:worktreeAdd`）
- Create: `src/main/board.ts`
- Modify: `src/main/agentChat/session.ts`（导出 `allSessionRecords()`；`StartOpts.roleId` / `boardText`；三处刷板）
- Modify: `src/shared/agentChat.ts`、`src/main/agentChat/sessionState.ts`（`roleId?`、`boardText?` 两个字段，`effectiveOpts` 带上）
- Modify: `src/main/index.ts`（`registerBoardHandlers()`）
- Modify: `src/preload/index.ts`（`roles.worktreeAdd`、`board.refresh` / `board.read`）
- Test: `src/main/agentChat/sessionState.test.ts`（restart 带 `roleId` / `boardText`）

**Interfaces:**
- Consumes: Task 1 的命名函数；Task 2 的 `renderBoard` / `findOverlaps` / `BOARD_REL`。
- Produces: IPC `role:worktreeAdd(projectPath, roleId) → { ok: true, absPath, relPath, branch } | { ok: false, reason: 'not-git' | 'other', error }`；IPC `board:refresh(projectPath) → { ok: boolean }`、`board:read(projectPath) → { text: string; rows: BoardRow[]; overlaps: Overlap[] }`；`StartOpts.roleId?: string`、`StartOpts.boardText?: string`。

- [ ] **Step 1: `teamWorktreeOps.ts` 抽函数 + 新 IPC**

把 `team:worktreeAdd` handler 里从 `const inside = …` 到 `return { ok: true, absPath, branch }` 的逻辑抽成导出函数：

```ts
export async function addWorktree(
  projectPath: string, relPath: string, branch: string
): Promise<WorktreeResult & { reason?: 'not-git' | 'other' }> {
  const inside = await git(projectPath, ['rev-parse', '--is-inside-work-tree'])
  if (!inside.ok || inside.out !== 'true')
    return { ok: false, reason: 'not-git', error: '这个项目不是 git 仓库，起不了 worktree。写码 agent 必须隔离（并发写会静默覆盖），所以要么先 git init，要么这一批改成只读角色。' }
  const abs = path.join(projectPath, relPath)
  if (fs.existsSync(abs)) return { ok: false, reason: 'other', error: `${relPath} 已经存在 —— 可能是上一批留下的，先删掉再派` }
  const exists = await git(projectPath, ['rev-parse', '--verify', branch])
  if (exists.ok) return { ok: false, reason: 'other', error: `分支 ${branch} 已存在，先删掉再派` }
  const r = await git(projectPath, ['worktree', 'add', '-b', branch, relPath])
  if (!r.ok) return { ok: false, reason: 'other', error: `git worktree add 失败：${r.out.slice(0, 200)}` }
  return { ok: true, absPath: abs, branch }
}
```

`team:worktreeAdd` handler 改为参数校验后 `return addWorktree(projectPath, relPath, branch)`（返回形状兼容，多一个 `reason` 字段无害）。

在 `registerTeamWorktree()` 里加：

```ts
  // 角色会话自己开的 worktree：命名在 shared/roleWorktree.ts。**只有 isolation='worktree' 的角色会走到这**。
  ipcMain.handle('role:worktreeAdd', async (_e, projectPath: unknown, roleId: unknown) => {
    if (typeof projectPath !== 'string' || typeof roleId !== 'string') return { ok: false, reason: 'other', error: '参数不对' }
    // 短 id 撞车的概率极低，但撞了就换一个再试；三次都撞说明不是运气问题
    for (let i = 0; i < 3; i++) {
      const id = newShortId()
      const rel = roleWorktreeName(roleId, id)
      const br = roleWorktreeBranch(roleId, id)
      if (!rel || !br) return { ok: false, reason: 'other', error: `角色 id「${roleId}」没法拿来做工作树路径` }
      const r = await addWorktree(projectPath, rel, br)
      if (r.ok) {
        void refreshBoard(projectPath)
        return { ok: true, absPath: r.absPath, relPath: rel, branch: br }
      }
      if (r.reason === 'not-git' || !/已存在/.test(r.error ?? '')) return r
    }
    return { ok: false, reason: 'other', error: '连续三次撞上已存在的名字，先清理 .worktrees/' }
  })
```

顶部 import：`import { newShortId, roleWorktreeBranch, roleWorktreeName } from '../shared/roleWorktree'` 与 `import { refreshBoard } from './board'`。`team:worktreeRemove` 成功返回前也加 `void refreshBoard(projectPath)`。

- [ ] **Step 2: `src/main/board.ts`**

```ts
// 协同板：**从事实算**（会话表 + git），不是模型自报。渲染在 shared/board.ts。
//
// 写文件用临时文件 + rename：多条会话同时触发刷新时不会写出半截。
// `.eas/` 写进项目的 .git/info/exclude 而不是 .gitignore —— 不动用户提交的文件。
import { execFile } from 'child_process'
import fs from 'fs'
import path from 'path'
import { ipcMain } from 'electron'

import { BOARD_REL, findOverlaps, renderBoard, type BoardRow, type Overlap } from '../shared/board'
import { belongsToProject } from '../shared/teamWorktree'
import { BUILTIN_ROLES } from './builtinRoles'
import type { SessionRecord } from './agentChat/sessionState'

function git(cwd: string, args: string[]): Promise<{ ok: boolean; out: string }> {
  return new Promise((resolve) => {
    execFile('git', args, { cwd, timeout: 15_000 }, (err, stdout, stderr) => {
      resolve({ ok: !err, out: (err ? stderr || stdout : stdout).toString().trim() })
    })
  })
}

/** 这条分支从哪个提交长出来的：reflog 最早一条就是 `worktree add -b` 那一下 */
async function forkPoint(cwd: string, branch: string): Promise<string | null> {
  const r = await git(cwd, ['reflog', 'show', '--format=%H', branch])
  if (!r.ok || !r.out) return null
  const lines = r.out.split('\n').filter(Boolean)
  return lines[lines.length - 1] ?? null
}

/** 触及文件 = 相对 fork 点的已提交改动 ∪ 工作区未提交改动（含未跟踪） */
export async function touchedFiles(cwd: string, branch: string): Promise<string[]> {
  const set = new Set<string>()
  const base = await forkPoint(cwd, branch)
  if (base) {
    const d = await git(cwd, ['diff', '--name-only', base])
    if (d.ok) d.out.split('\n').filter(Boolean).forEach((f) => set.add(f))
  }
  const st = await git(cwd, ['status', '--porcelain', '--untracked-files=all'])
  if (st.ok) st.out.split('\n').filter(Boolean).forEach((l) => set.add(l.slice(3).trim()))
  return [...set].sort()
}

async function currentBranch(cwd: string): Promise<string> {
  const r = await git(cwd, ['rev-parse', '--abbrev-ref', 'HEAD'])
  return r.ok ? r.out : '?'
}

/** 会话表由 session.ts 启动时注入（它也要 import 本模块刷板，直接互相 import 会成环）。
 *  没注入时板永远是空的 —— 宁可空，不要在模块初始化期去碰 electron 那边的单例。 */
let sessionSource: () => SessionRecord[] = () => []
export function setSessionSource(fn: () => SessionRecord[]): void {
  sessionSource = fn
}

/** 会话表 + git → rows。只收「属于这个项目」的会话（含 .worktrees/ 下的） */
export async function collectRows(projectPath: string, roleNames: Record<string, string>, now = Date.now()): Promise<BoardRow[]> {
  const rows: BoardRow[] = []
  for (const rec of sessionSource()) {
    if (!belongsToProject(rec.cwd, projectPath)) continue
    if (!rec.roleId && rec.cwd === projectPath) continue // 主工作区里没角色的会话不上板
    const branch = await currentBranch(rec.cwd)
    const files = rec.cwd === projectPath ? [] : await touchedFiles(rec.cwd, branch)
    rows.push({
      branch: rec.cwd === projectPath ? `主工作区（${branch}）` : branch,
      roleName: (rec.roleId && roleNames[rec.roleId]) || rec.roleId || rec.role || '无角色',
      roleId: rec.roleId,
      alive: rec.alive,
      idleMs: Math.max(0, now - rec.lastActiveAt),
      startedAt: rec.startedAt,
      files,
      cwd: rec.cwd
    })
  }
  return rows
}

function ensureExcluded(projectPath: string): void {
  try {
    const gitDir = path.join(projectPath, '.git')
    if (!fs.existsSync(gitDir)) return
    const info = path.join(gitDir, 'info')
    fs.mkdirSync(info, { recursive: true })
    const f = path.join(info, 'exclude')
    const cur = fs.existsSync(f) ? fs.readFileSync(f, 'utf8') : ''
    if (!cur.split('\n').some((l) => l.trim() === '.eas/')) fs.appendFileSync(f, (cur.endsWith('\n') || !cur ? '' : '\n') + '.eas/\n')
  } catch {
    /* 排除失败不阻断：板照写，最多在 git status 里多一行 */
  }
}

const timers = new Map<string, NodeJS.Timeout>()
/** 防抖 500ms：一轮 turn.done 常常紧跟 exit，别连写两次 */
export function refreshBoard(projectPath: string): void {
  const t = timers.get(projectPath)
  if (t) clearTimeout(t)
  timers.set(projectPath, setTimeout(() => {
    timers.delete(projectPath)
    void writeBoard(projectPath)
  }, 500))
}

let roleNameLookup: () => Record<string, string> = () => Object.fromEntries(BUILTIN_ROLES.map((r) => [r.id, r.name]))
/** roles.ts 启动后把「含用户自建角色」的查询函数装进来，板上才显示得出自建角色的名字 */
export function setRoleNameLookup(fn: () => Record<string, string>): void {
  roleNameLookup = fn
}

export async function writeBoard(projectPath: string): Promise<void> {
  try {
    const rows = await collectRows(projectPath, roleNameLookup())
    const text = renderBoard(rows, Date.now())
    ensureExcluded(projectPath)
    const f = path.join(projectPath, BOARD_REL)
    fs.mkdirSync(path.dirname(f), { recursive: true })
    const tmp = `${f}.${process.pid}.tmp`
    fs.writeFileSync(tmp, text)
    fs.renameSync(tmp, f)
  } catch {
    /* 板写不出来不影响会话 */
  }
}

export function readBoard(projectPath: string): string {
  try {
    return fs.readFileSync(path.join(projectPath, BOARD_REL), 'utf8')
  } catch {
    return ''
  }
}

export function registerBoardHandlers(): void {
  ipcMain.handle('board:refresh', async (_e, projectPath: unknown) => {
    if (typeof projectPath !== 'string') return { ok: false }
    await writeBoard(projectPath)
    return { ok: true }
  })
  ipcMain.handle('board:read', async (_e, projectPath: unknown): Promise<{ text: string; rows: BoardRow[]; overlaps: Overlap[] }> => {
    if (typeof projectPath !== 'string') return { text: '', rows: [], overlaps: [] }
    const rows = await collectRows(projectPath, roleNameLookup())
    return { text: readBoard(projectPath), rows, overlaps: findOverlaps(rows) }
  })
}
```

`src/main/roles.ts` 的 `registerRoleHandlers()` 开头加 `setRoleNameLookup(() => Object.fromEntries(load().map((r) => [r.id, r.name])))`（import 自 `./board`）。`src/main/index.ts` 在 `registerTeamWorktree()` 之后加 `registerBoardHandlers()`（import 自 `./board`）。

- [ ] **Step 3: `session.ts` 三处**

1. 把会话表注进板（**不导出给 board.ts import**，否则 board ↔ session 成环）：在注册 `agentChat:*` IPC 的那个函数开头加
   ```ts
   setSessionSource(() => [...sessions.values()].map((l) => l.rec))
   ```
   （`setSessionSource` 从 `'../board'` import；`sessions` 是本文件那张 `Map<string, Live>`。）
2. `agentChat:start` 的 `rec` 加 `roleId: typeof p.roleId === 'string' && p.roleId ? p.roleId : undefined,` 与 `boardText: readBoardForPrompt(p.cwd)`，其中
   ```ts
   import { readBoard, refreshBoard, setSessionSource } from '../board'
   import { clipForPrompt } from '../../shared/board'
   import { projectRootOf } from '../../shared/roleWorktree'
   function readBoardForPrompt(cwd: string): string | undefined {
     const text = readBoard(projectRootOf(cwd)).trim()
     return text ? clipForPrompt(text) : undefined
   }
   ```
3. 刷板：spawn 成功之后（`sessions.set(id, live)` 后）、`handleEvent` 里 `e.k === 'turn.done'` 时、`proc.on('exit')` 里，各加 `refreshBoard(projectRootOf(live.rec.cwd))`。`agentChat:stop` 的 `sessions.delete(id)` 之后也加一次。

`src/shared/agentChat.ts` 的 `StartOpts` 加：

```ts
  /** 角色卡 id（协同板按它显示角色名；契约与边界另有字段） */
  roleId?: string
  /** 起会话那一刻的协同板文本（已按 clipForPrompt 截断）。**只在 spawn 时附进系统提示**，
   *  会话中途变化不推送 —— 三家 CLI 都没有中途注入系统提示的通道，契约要求改文件前先 board_read。 */
  boardText?: string
```

`sessionState.ts` 的 `SessionRecord` 同样加这两个字段，`effectiveOpts` 带上。`sessionState.test.ts` 照 `roleBounds` 那两条的写法加「restart 时带 roleId」「带 boardText」。

- [ ] **Step 4: preload**

`roles` 命名空间加：

```ts
    /** 给角色会话建 worktree（isolation='worktree' 的角色第一次发消息前调）。 */
    worktreeAdd: (projectPath: string, roleId: string): Promise<
      { ok: true; absPath: string; relPath: string; branch: string } | { ok: false; reason: 'not-git' | 'other'; error: string }
    > => ipcRenderer.invoke('role:worktreeAdd', projectPath, roleId),
```

新命名空间：

```ts
  board: {
    refresh: (projectPath: string): Promise<{ ok: boolean }> => ipcRenderer.invoke('board:refresh', projectPath),
    read: (projectPath: string): Promise<{ text: string; rows: BoardRow[]; overlaps: Overlap[] }> =>
      ipcRenderer.invoke('board:read', projectPath)
  },
```

（`BoardRow` / `Overlap` 从 `'../shared/board'` type-import。）

- [ ] **Step 5: 验证并提交**

Run: `npm run typecheck && npm test`

```bash
git add src/main/teamWorktreeOps.ts src/main/board.ts src/main/roles.ts src/main/index.ts src/main/agentChat/session.ts src/main/agentChat/sessionState.ts src/main/agentChat/sessionState.test.ts src/shared/agentChat.ts src/preload/index.ts
git commit -m "feat(board): 主进程协同板（会话表 + git 现算、防抖写、exclude）与角色 worktree IPC

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 4: 板文附进三家系统提示 + `board_read` 工具

**Files:**
- Modify: `src/main/agentChat/adapters/claude.ts`、`codex.ts`、`src/main/agentChat/omp/launch.ts` + `paths.ts`（`ompAcpArgs` 的契约参数改为「契约 + 板」拼好的一段，由 session.ts 拼）
- Modify: `src/main/agentChat/adapters/adapters.test.ts`、`src/main/agentChat/omp/paths.test.ts`
- Modify: `mcp/eas-mcp.mjs`（`board_read` 定义）、`src/renderer/src/mcpHandler.ts`

**Interfaces:**
- Consumes: `StartOpts.boardText`（Task 3）
- Produces: 三家系统提示末尾多一段 `## 协同板（起会话时的快照）\n<boardText>`；MCP 工具 `board_read`（无参）返回当前板文（先 `board.refresh` 再读）。

- [ ] **Step 1: 写失败的测试**

`adapters.test.ts` 追加：

```ts
test('Claude：boardText 附在系统提示末尾，带标题', () => {
  const { args } = getAdapter('claude')!.buildArgs({ cwd: '/p', roleContract: '契约', boardText: '# 协同板\n| a |' })
  const sp = args[args.indexOf('--append-system-prompt') + 1]
  assert.ok(sp.includes('契约'))
  assert.ok(sp.includes('## 协同板（起会话时的快照）\n# 协同板\n| a |'))
})

test('Claude：没有 boardText 时系统提示与今天逐字相同', () => {
  const a = getAdapter('claude')!.buildArgs({ cwd: '/p', roleContract: '契约' }).args
  const b = getAdapter('claude')!.buildArgs({ cwd: '/p', roleContract: '契约', boardText: '' }).args
  assert.deepEqual(a, b)
})

test('Codex：boardText 压成单行接在 instructions 后面', () => {
  const { args } = getAdapter('codex')!.buildArgs({ cwd: '/p', roleContract: '契约', boardText: '# 协同板\n| a |' })
  const v = args.find((a) => a.startsWith('instructions=')) ?? ''
  assert.ok(!v.includes('\n'))
  assert.ok(v.includes('契约') && v.includes('协同板') && v.includes('| a |'))
})

test('Codex：只有 boardText 没有契约也要拼 instructions', () => {
  const { args } = getAdapter('codex')!.buildArgs({ cwd: '/p', boardText: '# 协同板' })
  assert.ok(args.some((a) => a.startsWith('instructions=') && a.includes('协同板')))
})
```

`paths.test.ts`：`ompAcpArgs` 已经收一段 `roleContract` 文本；session.ts 负责把契约与板拼成一段传进去，`paths.ts` 不改。加一条测试钉住「传进去的多行文本原样进 `--append-system-prompt=`」（现有行为，防止有人为了板改它）。

- [ ] **Step 2: 跑测试确认失败** → Claude / Codex 四条 FAIL

- [ ] **Step 3: 实现**

`claude.ts` 拼系统提示那段（`OUTPUT_STYLE_PROMPT` / `ASK_FIRST_PROMPT` / `roleContract` 三段 join）再加第四段：

```ts
        opts.boardText?.trim() ? `## 协同板（起会话时的快照）\n${opts.boardText.trim()}` : ''
```

`codex.ts`：`const contract = [opts.roleContract?.trim(), opts.boardText?.trim() ? `协同板（起会话时的快照）：${opts.boardText.trim()}` : ''].filter(Boolean).join(' ').replace(/\s*\n\s*/g, ' ')`，非空才 `-c instructions=`。

`session.ts` 里 omp 的 `openOmpProcess({ roleContract: live.rec.roleContract })` 改为传拼好的：

```ts
          roleContract: [live.rec.roleContract?.trim(), live.rec.boardText?.trim() ? `## 协同板（起会话时的快照）\n${live.rec.boardText.trim()}` : '']
            .filter(Boolean).join('\n\n') || undefined,
```

`mcp/eas-mcp.mjs` 在 `todo_list` 之后加：

```js
  {
    name: 'board_read',
    description:
      '读这个项目的**协同板**：现在有哪些会话在哪条分支上改了哪些文件、谁闲置了、哪两条分支碰了同一个文件。' +
      '由 app 从会话表和 git 现算，不是谁写的。**改文件前先读一遍** —— 起会话时看到的那份是快照，' +
      '别人这会儿可能已经动了同一处。只读，没有参数。',
    inputSchema: { type: 'object', properties: {} }
  },
```

`src/renderer/src/mcpHandler.ts` 在 `todo_list` 的分支旁加：

```ts
  if (tool === 'board_read') {
    const where = resolveFrame(ctx)
    const projectPath = where?.projectPath || ctx.project || ''
    if (!projectPath) throw new Error('找不到你所在的项目，读不了协同板')
    await window.api.board.refresh(projectPath)
    const { text } = await window.api.board.read(projectPath)
    return { content: [{ type: 'text', text: text || '协同板是空的：这个项目现在没有活跃分支。' }] }
  }
```

（返回形状照 `todo_list` 那条现有写法。）

- [ ] **Step 4: 验证并提交**

Run: `node --test src/main/agentChat/adapters/adapters.test.ts src/main/agentChat/omp/paths.test.ts && npm run typecheck && npm test`

```bash
git add src/main/agentChat/adapters/claude.ts src/main/agentChat/adapters/codex.ts src/main/agentChat/adapters/adapters.test.ts src/main/agentChat/omp/paths.test.ts src/main/agentChat/session.ts mcp/eas-mcp.mjs src/renderer/src/mcpHandler.ts
git commit -m "feat(board): 板文附进三家系统提示；MCP 工具 board_read 随时查

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 5: 渲染层 —— pane 记 worktree、首发前建树、非 git 确认、传 roleId

**Files:**
- Modify: `src/renderer/src/layout.ts:47`（agent pane 加 `worktree?: { relPath: string; branch: string }`）
- Modify: `src/renderer/src/store/tabsSlice.ts`（`setAgentWorktree`）
- Modify: `src/renderer/src/store/canvas/persist.ts`（存 / 读 `worktree`）
- Modify: `src/renderer/src/features/agentChat/AgentChatView.tsx`

**Interfaces:**
- Consumes: `window.api.roles.worktreeAdd`（Task 3）
- Produces: `pane.worktree`、`setAgentWorktree(tabId, leafId, wt | undefined)`、`effectiveCwd`（Task 6 的徽标读它）

- [ ] **Step 1: 类型与 store**

`layout.ts` agent pane 加：

```ts
      /** 这个节点的会话落在哪棵 worktree（相对项目根）与分支。**由 app 在第一次起会话前建**，
       *  之后每次起会话（含恢复）cwd 都指过去。删 worktree 时清掉。 */
      worktree?: { relPath: string; branch: string }
```

`tabsSlice.ts` 接口加 `setAgentWorktree: (tabId: string, leafId: string, wt: { relPath: string; branch: string } | undefined) => void`，实现照 `setAgentRole` 的写法（同值不造新对象）。

`persist.ts` 存的 pane 对象加 `worktree: pane.worktree`；读回时 `worktree: a.worktree && typeof a.worktree.relPath === 'string' && typeof a.worktree.branch === 'string' ? { relPath: a.worktree.relPath, branch: a.worktree.branch } : undefined`。

- [ ] **Step 2: AgentChatView**

订阅 `worktree`（照 `roleId` 那条 selector 的写法），并：

```ts
  const setAgentWorktree = useStore((s) => s.setAgentWorktree)
  /** 真正起会话的目录：有 worktree 就是它，否则项目目录 */
  const effectiveCwd = worktree ? `${cwd}/${worktree.relPath}` : cwd
```

在 `start` 前（`const identity = …` 之前）加：

```ts
      // 写码角色第一次起会话前先把 worktree 建好，cwd 直接指过去 —— 模型没有「不开分支」的选项。
      // 只在**全新**会话且还没有 worktree 时建；恢复会话沿用 pane 上记的那棵。
      let startCwd = effectiveCwd
      if (role?.isolation === 'worktree' && !worktree && !savedResumeId) {
        const r = await window.api.roles.worktreeAdd(cwd, role.id)
        if (r.ok) {
          setAgentWorktree(tabId, leafId, { relPath: r.relPath, branch: r.branch })
          startCwd = r.absPath
        } else if (r.reason === 'not-git') {
          // 不静默降级：告诉用户会直接改主工作区，点「继续」才起
          const go = await new Promise<boolean>((resolve) =>
            requestConfirm({
              message: `这个目录不是 git 仓库，「${role.name}」会直接改主工作区。\n\n要继续吗？`,
              confirmLabel: '继续',
              onConfirm: () => resolve(true),
              onCancel: () => resolve(false)
            })
          )
          if (!go) { setStarting(false); return }
        } else {
          setStarting(false)
          setStartError(`建不了分支：${r.error}`)
          return
        }
      }
```

（`PendingConfirm` 若没有 `onCancel` 字段，看 `uiSlice.ts` 的类型补一个可选 `onCancel?: () => void`，`ConfirmDialog.tsx` 的取消按钮调它。）

两处 `agentChat.start({ cwd, … })` 的 `cwd` 改为 `startCwd`，并各加 `...(role?.id ? { roleId: role.id } : {})`。工具栏 `.ac-ctxbar-item` 的目录显示与 `data-tip` 改用 `effectiveCwd`。

- [ ] **Step 3: 验证并提交**

Run: `npm run typecheck && npm test`

```bash
git add src/renderer/src/layout.ts src/renderer/src/store/tabsSlice.ts src/renderer/src/store/canvas/persist.ts src/renderer/src/store/uiSlice.ts src/renderer/src/ui/ConfirmDialog.tsx src/renderer/src/features/agentChat/AgentChatView.tsx
git commit -m "feat(roles): 写码角色第一次发消息前自动建 worktree，cwd 指过去；非 git 仓库先确认

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 6: 分支徽标与菜单 + 编辑器的隔离开关

**Files:**
- Modify: `src/renderer/src/features/agentChat/AgentChatView.tsx`（ctxbar 里 RolePicker 之后）
- Modify: `src/renderer/src/features/agentChat/agentChat.css`
- Modify: `src/renderer/src/features/canvas/CanvasRoleEditor.tsx`

- [ ] **Step 1: 徽标**

在 `<RolePicker …/>` 之后：

```tsx
          {worktree && (
            <button
              type="button"
              className={`ac-ctxbar-item as-btn ac-branch${branchOverlap ? ' warn' : ''}`}
              data-tip={`${effectiveCwd}${branchOverlap ? '\n⚠ 有别的分支在改同一个文件，改前先 board_read' : ''}`}
              onClick={(e) => openBranchMenu(e)}
            >
              <GitBranchIcon size={12} />
              <span className="ac-ctxbar-name">{worktree.branch}</span>
            </button>
          )}
```

（`GitBranchIcon` 若 `ui/Icons.tsx` 里没有，加一个 12px 的分支线图标；有同义的就用现成的。）

状态与菜单（照 `cliMenuAt` / `cliMenuItems` / `CanvasContextMenu` 的现成写法）：

```ts
  const [branchMenuAt, setBranchMenuAt] = useState<{ x: number; y: number } | null>(null)
  const [branchOverlap, setBranchOverlap] = useState(false)
  const openBranchMenu = (e: React.MouseEvent): void => {
    const r = (e.currentTarget as HTMLElement).getBoundingClientRect()
    setBranchMenuAt({ x: r.left, y: r.bottom + 4 })
  }
  // 交集告警：打开菜单时刷一次板，读自己这条分支在不在 ⚠ 里
  useEffect(() => {
    if (!worktree || !branchMenuAt) return
    void window.api.board.read(cwd).then((b) => setBranchOverlap(b.overlaps.some((o) => o.branches.includes(worktree.branch))))
  }, [branchMenuAt, worktree, cwd])
  const branchMenuItems: CanvasMenuItem[] = worktree
    ? [
        { label: '打开终端到这个 worktree', onClick: () => void openTerminal({ cwd: effectiveCwd }) },
        { label: '合并到主干', hint: 'P2 接合并官', disabled: true, onClick: () => {} },
        { sep: true, label: '' },
        {
          label: '删除 worktree', danger: true,
          onClick: () => {
            void window.api.agentChat.worktreeRemove(cwd, worktree.relPath, worktree.branch, false).then((r) => {
              if (r.ok) { setAgentWorktree(tabId, leafId, undefined); return }
              requestConfirm({
                message: `${r.error}\n\n仍要删？未提交的改动会丢，分支保留。`,
                confirmLabel: '删除',
                onConfirm: () => void window.api.agentChat.worktreeRemove(cwd, worktree.relPath, worktree.branch, true)
                  .then((r2) => { if (r2.ok) setAgentWorktree(tabId, leafId, undefined) })
              })
            })
          }
        }
      ]
    : []
```

`openTerminal` 从 store 取（`useStore((s) => s.openTerminal)`）。渲染 `<CanvasContextMenu>` 照 cliMenu 那处。板告警也在会话起 / 每轮结束后刷新一次：在收到 `turn.done` 事件的地方（AgentChatView 里 reduce 后）加同样的 `board.read` → `setBranchOverlap`，仅当 `worktree` 存在。

CSS（`agentChat.css`，`.rolepick-warn` 旁）：

```css
.ac-branch .ac-ctxbar-name { font-family: var(--mono, ui-monospace, monospace); }
.ac-branch.warn { background: rgba(var(--sem-warn-rgb), 0.16); }
```

- [ ] **Step 2: 编辑器开关**

`CanvasRoleEditor.tsx` 在「用哪个 CLI」那个 `re-field` 之后加：

```tsx
          <div className="re-field">
            <span className="re-label">起会话时</span>
            <div className="re-seg">
              {([
                { v: undefined, label: '在主工作区', note: '直接改项目目录' },
                { v: 'worktree', label: '独立分支', note: '建 .worktrees/<角色>-<id>，分支 eas/<角色>/<id>，主工作区不被动' }
              ] as const).map((x) => (
                <button key={String(x.v)} className={(draft.isolation ?? undefined) === x.v ? 'on' : ''} onClick={() => set({ isolation: x.v })} data-tip={x.note}>
                  {x.label}
                </button>
              ))}
            </div>
            <span className="re-hint">
              写代码的角色选「独立分支」。系统不替你猜这个角色写不写码 —— 不选就是主工作区。不是 git 仓库时会先问你。
            </span>
          </div>
```

- [ ] **Step 3: 验证并提交**

Run: `npm run typecheck && npm test`

```bash
git add src/renderer/src/features/agentChat/AgentChatView.tsx src/renderer/src/features/agentChat/agentChat.css src/renderer/src/features/canvas/CanvasRoleEditor.tsx src/renderer/src/ui/Icons.tsx
git commit -m "feat(roles): 对话工具栏分支徽标（开终端 / 删 worktree / 交集告警）；编辑器加「起会话时」隔离开关

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 7: 图纸同步 + 真机验证

**Files:**
- Modify: `docs/architecture/03-agent角色边界.md`（3A 加 `isolation`、协同板位置、`.eas/board.md`、`role:worktreeAdd`）
- Modify: `docs/architecture/10-模块领地图.md`（`shared/board.ts` `shared/roleWorktree.ts` `main/board.ts`；`.eas/` 目录）
- Modify: `docs/architecture/11-MCP工具网络.md`（`board_read`）
- Modify: `docs/architecture/13-所有权矩阵.md`（persist 字段清单加 `worktree`；`AgentRole.isolation` 同步行）
- Modify: spec 状态行 →「P1 已实现；P2 / P3 未动」

- [ ] **Step 1: 图纸**（照上面列的四处各补一段，措辞按代码事实）

- [ ] **Step 2: 真机验证（隔离实例）**

先 `pkill -f verify-app.mjs; lsof -i :9333` 确认空，再 `npm run build && node scripts/verify-app.mjs --seed`（跳过 `omp:fetch`）。CDP 驱动用 `[data-leaf-id="<leaf>"] textarea.ac-input` 定位、⌘+Enter 发送（见 memory「CDP 验证方法」第九个坑）。用 `git init` 一个临时项目 `/tmp/eas-p1-<id>`（含一次提交）作为验证项目，别用真实仓库。

| # | 场景 | 判据 |
|---|---|---|
| 1 | 在临时项目里开对话，选工匠，发「把 hello 写进 a.txt」 | `git worktree list` 多出 `.worktrees/builder-xxxxxx`，分支 `eas/builder/xxxxxx`；工具栏出现徽标；pane 持久化后（重启实例）徽标还在 |
| 2 | 同项目再开一个全流程会话，发「把 hi 写进 a.txt」 | `.eas/board.md` 两行，⚠ 行指出 a.txt；两个徽标变色 |
| 3 | 会话里让模型 `board_read` | 回复里出现两行表 |
| 4 | 新会话的系统提示 | `ps` 看 claude 进程 `--append-system-prompt` 里含「协同板」 |
| 5 | 非 git 目录选工匠 | 出现「不是 git 仓库…继续？」确认；点取消不起会话；点继续起在主工作区 |
| 6 | 徽标菜单「删除 worktree」 | 有未提交改动时先拒（提示里有数量），确认后删掉且徽标消失；`git worktree list` 少一项、分支还在 |
| 7 | 只读角色（勘探员）| 不建 worktree、无徽标、板上不出现（主工作区无角色会话不上板 / 有角色的显示「主工作区」）|
| 8 | `.git/info/exclude` | 含 `.eas/`；`git status` 不显示 board.md |

结果写进 spec 新小节「P1 真机验证」，每条一行写「看到了什么」，验不到的如实写。

- [ ] **Step 3: 提交**

```bash
git add docs/architecture/03-agent角色边界.md docs/architecture/10-模块领地图.md docs/architecture/11-MCP工具网络.md docs/architecture/13-所有权矩阵.md "docs/superpowers/specs/2026-09-05-角色工作流-分支合并官协同板-design.md"
git commit -m "docs(architecture): 角色工作流 P1 —— isolation、角色 worktree、协同板与 board_read 入图纸，真机验证记录

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

## 自查记录

- **Spec 覆盖**：6.1 字段与默认 → T1；6.2 流程 1-4 → T3（建树）+ T5（首发前建、非 git 确认、恢复沿用、不自动删）+ T6（删除拒删）；6.3 徽标与菜单 → T6（「合并到主干」占位）；8.2 板的数据来源、刷新时机、原子写、注入、`board_read`、交集告警 → T2/T3/T4/T6；8.1 静态层指针注入不在 P1（P3 章程）。
- **类型一致性**：`BoardRow` / `Overlap` 在 `shared/board.ts` 定义，main/board.ts、preload、AgentChatView 都从它引；`role:worktreeAdd` 返回形状在 T3 与 T5 一致；`pane.worktree` 形状在 layout / persist / tabsSlice / AgentChatView 一致。
- **裁定**：`board.ts` 与 `session.ts` 之间用 `setSessionSource` 注入而不是互相 import（避免循环）。worktree 在渲染层首发前建（spec 说在 `agentChat:start` 里）—— pane 需要知道分支才能画徽标与持久化，放渲染层一步到位，主进程 `agentChat:start` 不动。基点用分支 reflog 首条，不落标记文件。

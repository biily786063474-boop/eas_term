# 角色工作流 · P3（章程 + 台账 + board_note）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 每个角色在每个项目里有一份**章程**（`docs/roles/<roleId>.md`，进 git，首次用某角色时 app 生成初稿）和每条角色分支一份**台账**（`.eas/board/<branch>.md`，不进 git，app 维护头部、角色用 `board_note` 追加），起会话时把两者的**指针**附进系统提示。

**Architecture:** 纯文本逻辑（章程模板、台账渲染/解析/追加、路径命名、指针段、从 `.git` 文件读分支）全放 `src/shared/roleDocs.ts` 裸测；主进程 `src/main/roleCharter.ts`（首次生成章程）与 `src/main/branchLedger.ts`（台账头部随协同板刷新一起 upsert、`board_note` 追加，按项目排成一条链避免读改写互踩）；`collabBoard.ts` 的 `doWriteBoard` 顺手 upsert 台账、`board:read` 带回台账、新增 `board:note` IPC；`agentChat:start` 算 `roleDocs` 指针段，三家 adapter 附进系统提示；MCP 新工具 `board_note`。

**Tech Stack:** TypeScript · Electron · React · `node --test`（Node 直接跑 `.ts`，import 带 `.ts` 后缀）· git。

**Spec:** `docs/superpowers/specs/2026-09-05-角色工作流-分支合并官协同板-design.md` 第五节（5.1 章程、5.2 台账）、第八节 8.1（静态层指针）、7.1 注①（合并官契约里被去掉的台账字句现在补回）。P1/P2 已在 main。

## Global Constraints

- 章程 **只在不存在时生成**，已存在一个字节都不动（用户会改它）。模板顶部固定一行说明「这里写的是软约束，硬约束只有绑定层那份」。
- 指针注入，**不注全文**（托管区纪律：`syncRules` 文件头那条 99% 字符的教训）。指针段最多 4 行。
- 台账在 `.eas/board/` 下，`.eas/` 已由 `ensureExcluded` 写进 `.git/info/exclude`（不动 `.gitignore`）。头部由 app 从事实算，「记录」段只追加、不改写、不删。
- 台账/章程写文件都用 tmp + rename；同一项目的台账读改写走一条 promise 链（与 `writeBoard` 的链同一手法）。
- `agentChat:start` 是**同步** handler（返回 `AgentChatStartResult` 不是 Promise）——起会话时读分支**不许起 git 进程**，读 `.git` 文件（worktree 的 `.git` 是文件 `gitdir: …/.git/worktrees/<name>`，那里的 `HEAD` 是 `ref: refs/heads/<branch>`）。
- 主工作区会话（cwd = 项目根，含合并官）**没有自己的台账**；`board_note` 不带 `branch` 时报错说明，带 `branch` 时写到那条分支的台账（合并官写合并记录用）。
- 内置角色契约的改动只加字句不改 id / isolation / caps；`builtinRoles.test.ts` 三张表不动。
- 测试 `npm test`；类型 `npm run typecheck`；每任务结束都绿。提交信息中文，末尾 `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`。
- 工作目录：`/Users/biily/Biily/Projects/vibe coding/terminal-wt/roles-workflow-p3`（分支 `feat/roles-workflow-p3`，自 main `62f993f`）。每条 Bash 命令 4 分钟内返回。
- **别碰** `docs/architecture/03-agent角色边界.md` 3B 节列的静默失效区（fsGuard、`index.ts` 注册顺序、`LONG_WAITS`、persist 白名单、CanvasStage）。

---

## 文件结构

| 文件 | 责任 |
|---|---|
| `src/shared/roleDocs.ts`（新）+ test | `charterRel` / `renderCharter` / `ledgerRel` / `renderLedger` / `splitLedger` / `appendLedgerNote` / `clipTail` / `roleDocsPrompt` / `branchFromGitFiles` |
| `src/main/roleCharter.ts`（新） | `ensureCharter(root, role) → { created, rel }`，同步 fs |
| `src/main/branchLedger.ts`（新） | `upsertLedgers(root, rows)` / `appendNote(root, branch, note)` / `readLedgers(root, branches)`，按项目排链 |
| `src/main/collabBoard.ts`（改） | `doWriteBoard` 末尾 upsert 台账；`board:read` 返回加 `ledgers`；新 IPC `board:note`；export `roleNameOf(id)` |
| `src/shared/agentChat.ts`、`src/main/agentChat/sessionState.ts`、`session.ts`、`adapters/claude.ts`、`adapters/codex.ts`（改） | `StartOpts.roleDocs` 指针段；start 时生成章程 + 算指针；三家 adapter 附进系统提示；start 结果带 `charterCreated` |
| `src/preload/index.ts`（改） | `board.note`、`board.read` 返回类型加 `ledgers` |
| `mcp/eas-mcp.mjs`、`src/renderer/src/mcpHandler.ts`（改） | `board_note` 工具；`board_read` 返回 `ledgers` |
| `src/main/builtinRoles.ts`（改） | e2e / builder / prototyper 契约加「board_note 记一条」；merger 契约加「合并后 board_note 写合并记录、先读台账」 |
| `src/renderer/src/features/agentChat/AgentChatView.tsx`（改） | start 返回 `charterCreated` 时用 `fail()` 同一通道提示一句（非错误语气） |
| `docs/architecture/03、10、11、13`、spec（改） | 图纸 + 真机验证 |

---

### Task 1: 纯文本层 `src/shared/roleDocs.ts`

**Files:**
- Create: `src/shared/roleDocs.ts`、`src/shared/roleDocs.test.ts`

**Interfaces（Produces）:**
```ts
export const CHARTER_DIR = 'docs/roles'
export const LEDGER_DIR = '.eas/board'
export function charterRel(roleId: string): string | null            // 'docs/roles/builder.md'；roleId 不合法 → null
export function ledgerRel(branch: string): string | null             // '.eas/board/eas--builder--ab12ef.md'；空/含 .. → null
export interface CharterInput { roleId: string; roleName: string; contract: string; hardLines: string[] }
export function renderCharter(input: CharterInput): string
export interface LedgerHeader { branch: string; roleName: string; worktree: string | null; startedAt: number; lastActiveAt: number; alive: boolean; files: string[] }
export function renderLedger(h: LedgerHeader, notes: string, now: number): string
export function splitLedger(text: string): { notes: string }        // 「## 记录」之后的原文（不含标题行）；没有 → ''
export function appendLedgerNote(text: string, note: string, roleName: string, now: number): string
export function clipTail(text: string, maxLines: number): string     // 超出时保留最后 maxLines 行，顶上加「…（前面还有 N 行）」
export function roleDocsPrompt(o: { charterRel: string; ledgerRel: string | null }): string
export function branchFromGitFiles(cwd: string, read: (p: string) => string | null): string | null
```

- [ ] **Step 1: 写失败的测试**

`src/shared/roleDocs.test.ts`：

```ts
import assert from 'node:assert/strict'
import { test } from 'node:test'

import {
  appendLedgerNote, branchFromGitFiles, charterRel, clipTail, ledgerRel,
  renderCharter, renderLedger, roleDocsPrompt, splitLedger
} from './roleDocs.ts'

test('charterRel：只认 [a-z][a-z0-9-]*', () => {
  assert.equal(charterRel('builder'), 'docs/roles/builder.md')
  assert.equal(charterRel('my-role2'), 'docs/roles/my-role2.md')
  for (const bad of ['', 'A', '../x', 'a/b', '1a']) assert.equal(charterRel(bad), null, bad)
})

test('ledgerRel：/ 变 --，其余非 [A-Za-z0-9._-] 变 -，拒绝空与 ..', () => {
  assert.equal(ledgerRel('eas/builder/ab12ef'), '.eas/board/eas--builder--ab12ef.md')
  assert.equal(ledgerRel('feat x'), '.eas/board/feat-x.md')
  assert.equal(ledgerRel(''), null)
  assert.equal(ledgerRel('a..b'), null)
})

test('renderCharter：顶部软约束说明、四段、边界留空、契约进产出段、硬约束逐行', () => {
  const t = renderCharter({
    roleId: 'builder', roleName: '工匠',
    contract: '你是工匠。\n完成判据：测试全绿。',
    hardLines: ['不许生图 · Codex：skills.config 摘掉 imagegen（硬）']
  })
  assert.ok(t.startsWith('# 工匠 · 本项目章程\n'))
  assert.match(t, /软约束/)
  assert.match(t, /硬约束.*绑定层/)
  for (const h of ['## 边界', '## 产出与落点', '## 完成判据', '## 交接格式（写进台账的字段）']) assert.ok(t.includes(h), h)
  assert.match(t, /- 可以改：\n- 不要碰：/)
  assert.ok(t.includes('你是工匠。'))
  assert.ok(t.includes('完成判据：测试全绿。'))
  assert.ok(t.includes('- 不许生图 · Codex：skills.config 摘掉 imagegen（硬）'))
  assert.ok(t.endsWith('\n'))
})

test('renderCharter：没有硬约束时写「无」，契约为空时写占位', () => {
  const t = renderCharter({ roleId: 'x', roleName: 'X', contract: '', hardLines: [] })
  assert.match(t, /硬约束[^\n]*\n[^\n]*无/)
  assert.match(t, /（角色卡没有契约，请补）/)
})

const H = { branch: 'eas/builder/ab12ef', roleName: '工匠', worktree: '.worktrees/builder-ab12ef', startedAt: Date.UTC(2026, 8, 6, 6, 2), lastActiveAt: Date.UTC(2026, 8, 6, 6, 31), alive: true, files: ['src/a.ts', 'src/b.ts'] }

test('renderLedger：头部字段齐全，记录段原样保留', () => {
  const t = renderLedger(H, '- 14:05 [工匠] 决定：用 A 方案\n', Date.UTC(2026, 8, 6, 6, 31))
  assert.ok(t.startsWith('# 台账 · eas/builder/ab12ef\n'))
  assert.match(t, /头部由 app 维护/)
  assert.match(t, /- 角色：工匠/)
  assert.match(t, /- worktree：\.worktrees\/builder-ab12ef/)
  assert.match(t, /- 状态：活跃/)
  assert.match(t, /- 触及：src\/a\.ts, src\/b\.ts（2 文件）/)
  assert.ok(t.endsWith('## 记录\n- 14:05 [工匠] 决定：用 A 方案\n'))
})

test('renderLedger：已停 / 无 worktree / 无触及', () => {
  const t = renderLedger({ ...H, alive: false, worktree: null, files: [] }, '', 0)
  assert.match(t, /- 状态：已停/)
  assert.match(t, /- worktree：（无）/)
  assert.match(t, /- 触及：（无）/)
  assert.ok(t.endsWith('## 记录\n'))
})

test('splitLedger / appendLedgerNote：只动记录段，多行 note 缩进为续行', () => {
  const t0 = renderLedger(H, '', 0)
  assert.equal(splitLedger(t0).notes, '')
  const t1 = appendLedgerNote(t0, '决定：用 A 方案\n原因：B 太重', '工匠', Date.UTC(2026, 8, 6, 6, 5))
  const notes = splitLedger(t1).notes
  assert.match(notes, /^- \d\d:\d\d \[工匠\] 决定：用 A 方案\n  原因：B 太重\n$/)
  const t2 = appendLedgerNote(t1, '第二条', '工匠', 0)
  assert.equal(splitLedger(t2).notes.split('\n').filter(Boolean).length, 3)
  assert.equal(splitLedger('没有记录段的文本').notes, '')
  assert.ok(appendLedgerNote('没有记录段的文本', 'x', 'R', 0).includes('## 记录\n- '))
})

test('clipTail：不超不动，超出保留尾部并加提示行', () => {
  assert.equal(clipTail('a\nb\n', 5), 'a\nb\n')
  const c = clipTail(['1', '2', '3', '4', '5'].join('\n') + '\n', 2)
  assert.equal(c, '…（前面还有 3 行）\n4\n5\n')
})

test('roleDocsPrompt：有台账两行，没有一行；都是指针不是全文', () => {
  const p = roleDocsPrompt({ charterRel: 'docs/roles/builder.md', ledgerRel: '.eas/board/eas--builder--ab12ef.md' })
  assert.ok(p.startsWith('## 你的角色文档\n'))
  assert.match(p, /章程：`docs\/roles\/builder\.md`.*动手前先读/)
  assert.match(p, /台账：`\.eas\/board\/eas--builder--ab12ef\.md`.*board_note/)
  const q = roleDocsPrompt({ charterRel: 'docs/roles/scout.md', ledgerRel: null })
  assert.ok(!q.includes('台账'))
  assert.ok(q.split('\n').length <= 4)
})

test('branchFromGitFiles：worktree 的 .git 是文件，主工作区是目录，detached 为 null', () => {
  const files: Record<string, string> = {
    '/p/.worktrees/b-1/.git': 'gitdir: /p/.git/worktrees/b-1\n',
    '/p/.git/worktrees/b-1/HEAD': 'ref: refs/heads/eas/builder/ab12ef\n',
    '/p/.git/HEAD': 'ref: refs/heads/main\n',
    '/q/.git': 'gitdir: /q/../.git/wt\n',
    '/q/../.git/wt/HEAD': 'abcdef1234567890\n'
  }
  const read = (p: string): string | null => files[p] ?? null
  assert.equal(branchFromGitFiles('/p/.worktrees/b-1', read), 'eas/builder/ab12ef')
  assert.equal(branchFromGitFiles('/p', read), 'main')
  assert.equal(branchFromGitFiles('/q', read), null)
  assert.equal(branchFromGitFiles('/nope', read), null)
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test src/shared/roleDocs.test.ts` → 找不到模块

- [ ] **Step 3: 实现**

`src/shared/roleDocs.ts`：

```ts
// 角色章程（docs/roles/<roleId>.md，进 git）与分支台账（.eas/board/<branch>.md，不进 git）的
// 纯文本逻辑：模板、渲染、解析、追加、路径、指针段。**零依赖、裸测。** fs 与 git 在 main/ 那两个模块。
export const CHARTER_DIR = 'docs/roles'
export const LEDGER_DIR = '.eas/board'

const ROLE_RE = /^[a-z][a-z0-9-]*$/

export function charterRel(roleId: string): string | null {
  return ROLE_RE.test(roleId) ? `${CHARTER_DIR}/${roleId}.md` : null
}

/** 分支名 → 台账文件名：`/` 变 `--`（一眼看得出层级），别的怪字符变 `-` */
export function ledgerRel(branch: string): string | null {
  if (!branch.trim() || branch.includes('..')) return null
  const name = branch.replace(/\//g, '--').replace(/[^A-Za-z0-9._-]/g, '-')
  return `${LEDGER_DIR}/${name}.md`
}

export interface CharterInput { roleId: string; roleName: string; contract: string; hardLines: string[] }

/** 首次在某项目用某角色时的初稿。边界段留空给用户填；其余从角色卡来。 */
export function renderCharter(i: CharterInput): string {
  const contract = i.contract.trim() || '（角色卡没有契约，请补）'
  const hard = i.hardLines.length ? i.hardLines.map((l) => `- ${l}`).join('\n') : '- 无'
  const judge = contract.split('\n').filter((l) => /完成判据|算做完|算完成/.test(l))
  return [
    `# ${i.roleName} · 本项目章程`,
    '',
    '> 这份文件写的是**软约束**：模型会读、会照做，但没有任何东西拦着它。',
    '> 硬约束只有绑定层那份（角色卡里的能力开关，由 CLI / 沙箱 / hook 执行），列在下面「硬约束」段。',
    `> 由 Eas-Term 首次使用「${i.roleName}」时生成，之后不再覆盖 —— 放心改。`,
    '',
    '## 边界',
    '- 可以改：',
    '- 不要碰：',
    '',
    '### 硬约束（绑定层执行，改角色卡才会变）',
    hard,
    '',
    '## 产出与落点',
    '（来自角色卡契约，按本项目改写）',
    '',
    contract,
    '',
    '## 完成判据',
    judge.length ? judge.map((l) => `- ${l.trim()}`).join('\n') : '- （从上面的契约里提炼，用户可改）',
    '',
    '## 交接格式（写进台账的字段）',
    '每完成一个可交付的小步，用 `board_note` 记一条，按需带这些字段：',
    '- 决定：做了什么选择、为什么',
    '- 未完成：还差什么、卡在哪',
    '- 给合并官：合并时要注意的（冲突预期、必须一起改的文件、要跑的回归）',
    '- 怎么验：命令或步骤',
    ''
  ].join('\n')
}

export interface LedgerHeader {
  branch: string; roleName: string; worktree: string | null
  startedAt: number; lastActiveAt: number; alive: boolean; files: string[]
}

const pad = (n: number): string => String(n).padStart(2, '0')
const hhmm = (t: number): string => { const d = new Date(t); return `${pad(d.getHours())}:${pad(d.getMinutes())}` }
const stamp = (t: number): string => { const d = new Date(t); return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())} ${hhmm(t)}` }

const NOTES_HEAD = '## 记录'

/** 头部从事实算（会话表 + git），记录段原样接回。 */
export function renderLedger(h: LedgerHeader, notes: string, now: number): string {
  const head = [
    `# 台账 · ${h.branch}`,
    `<!-- 头部由 app 维护，勿手改；「${NOTES_HEAD.slice(3)}」段由角色通过 board_note 追加。${stamp(now)} 更新 -->`,
    `- 角色：${h.roleName}`,
    `- 分支：${h.branch}`,
    `- worktree：${h.worktree ?? '（无）'}`,
    `- 起于：${stamp(h.startedAt)} · 最后活动：${hhmm(h.lastActiveAt)}`,
    `- 状态：${h.alive ? '活跃' : '已停'}`,
    `- 触及：${h.files.length ? `${h.files.join(', ')}（${h.files.length} 文件）` : '（无）'}`,
    '',
    NOTES_HEAD,
    ''
  ].join('\n')
  return head + notes
}

export function splitLedger(text: string): { notes: string } {
  const i = text.indexOf(`\n${NOTES_HEAD}\n`)
  if (i < 0) return { notes: '' }
  return { notes: text.slice(i + NOTES_HEAD.length + 2) }
}

/** 只追加，不改写：多行 note 从第二行起缩两格当续行，列表结构不散 */
export function appendLedgerNote(text: string, note: string, roleName: string, now: number): string {
  const lines = note.trim().split('\n').map((l) => l.trimEnd())
  const entry = `- ${hhmm(now)} [${roleName}] ${lines[0] ?? ''}` + lines.slice(1).map((l) => `\n  ${l}`).join('') + '\n'
  const has = text.includes(`\n${NOTES_HEAD}\n`) || text.startsWith(`${NOTES_HEAD}\n`)
  const base = has ? text : `${text.trimEnd()}\n\n${NOTES_HEAD}\n`
  return (base.endsWith('\n') ? base : base + '\n') + entry
}

export function clipTail(text: string, maxLines: number): string {
  const lines = text.split('\n')
  if (lines[lines.length - 1] === '') lines.pop()
  if (lines.length <= maxLines) return text
  const keep = lines.slice(-maxLines)
  return `…（前面还有 ${lines.length - maxLines} 行）\n${keep.join('\n')}\n`
}

/** 起会话时附进系统提示的指针段。**只有指针**：全文注入违反托管区纪律。 */
export function roleDocsPrompt(o: { charterRel: string; ledgerRel: string | null }): string {
  const lines = [
    '## 你的角色文档',
    `- 本项目对你这个角色的章程：\`${o.charterRel}\`，动手前先读（软约束；硬约束由绑定层执行）`
  ]
  if (o.ledgerRel)
    lines.push(`- 你这条分支的台账：\`${o.ledgerRel}\`。每完成一个可交付的小步用 board_note 记一条（决定 / 未完成 / 给合并官 / 怎么验）`)
  return lines.join('\n')
}

/** 不起 git 进程读当前分支：worktree 的 .git 是文件 `gitdir: <path>`，主工作区是目录。
 *  `read` 读不到返回 null（目录也返回 null）。detached / 读不到 → null。 */
export function branchFromGitFiles(cwd: string, read: (p: string) => string | null): string | null {
  const dotGit = `${cwd}/.git`
  const asFile = read(dotGit)
  let gitDir: string
  if (asFile !== null) {
    const m = asFile.match(/^gitdir:\s*(.+?)\s*$/m)
    if (!m) return null
    gitDir = m[1].startsWith('/') || /^[A-Za-z]:/.test(m[1]) ? m[1] : `${cwd}/${m[1]}`
  } else gitDir = dotGit
  const head = read(`${gitDir}/HEAD`)
  if (!head) return null
  const r = head.match(/^ref:\s*refs\/heads\/(.+?)\s*$/m)
  return r ? r[1] : null
}
```

- [ ] **Step 4: 验证并提交**

Run: `node --test src/shared/roleDocs.test.ts && npm run typecheck && npm test`

```bash
git add src/shared/roleDocs.ts src/shared/roleDocs.test.ts
git commit -m "feat(roles): 章程与台账的纯文本层（模板 / 渲染 / 追加 / 路径 / 指针段 / 从 .git 文件读分支）

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 2: 主进程 —— 章程生成、台账维护、`board:note` / `board:read` 带台账

**Files:**
- Create: `src/main/roleCharter.ts`、`src/main/branchLedger.ts`
- Modify: `src/main/collabBoard.ts`（`doWriteBoard` 末尾、`board:read`、新 `board:note`、export `roleNameOf`）、`src/preload/index.ts`（`board.note`、`board.read` 类型）

**Interfaces（Produces）:**
```ts
// roleCharter.ts
export function ensureCharter(root: string, role: { roleId: string; roleName: string; contract: string; bounds?: RoleBounds }): { created: boolean; rel: string } | null
// branchLedger.ts
export function upsertLedgers(root: string, rows: BoardRow[], now?: number): Promise<void>
export function appendNote(root: string, branch: string, note: string, roleName: string): Promise<{ ok: true; rel: string } | { ok: false; error: string }>
export function readLedgers(root: string, branches: string[], maxLines?: number): Record<string, string>
// collabBoard.ts
export function roleNameOf(roleId: string | undefined): string | undefined
// IPC
'board:note'(cwdOrRoot: string, note: string, branch?: string) → { ok: true; rel: string } | { ok: false; error: string }
'board:read'(projectPath) → { text, rows, overlaps, ledgers: Record<string,string> }
```

- [ ] **Step 1: `src/main/roleCharter.ts`**

```ts
// 首次在某项目用某角色 → 生成章程初稿。**已存在就一个字节不动**（用户会改它）。
// 同步 fs：调用点 agentChat:start 是同步 handler。
import fs from 'fs'
import path from 'path'

import { capMatrix, CAP_LABEL, HARNESS_LABEL, LEVEL_LABEL, type RoleBounds } from '../shared/roleBinding'
import { charterRel, renderCharter } from '../shared/roleDocs'

/** 绑定层「硬约束」几行：点亮的 cap × 三家的落法 */
function hardLines(bounds: RoleBounds | undefined): string[] {
  const out: string[] = []
  for (const row of capMatrix(bounds)) {
    if (!row.active) continue
    for (const [h, cell] of Object.entries(row.cells)) {
      if (!cell) continue
      out.push(`${CAP_LABEL[row.cap]} · ${HARNESS_LABEL[h as keyof typeof HARNESS_LABEL]}：${cell.how}（${LEVEL_LABEL[cell.level]}）`)
    }
  }
  return out
}

export function ensureCharter(
  root: string,
  role: { roleId: string; roleName: string; contract: string; bounds?: RoleBounds }
): { created: boolean; rel: string } | null {
  const rel = charterRel(role.roleId)
  if (!rel) return null
  const abs = path.join(root, rel)
  if (fs.existsSync(abs)) return { created: false, rel }
  try {
    fs.mkdirSync(path.dirname(abs), { recursive: true })
    const tmp = `${abs}.${process.pid}.tmp`
    fs.writeFileSync(tmp, renderCharter({ roleId: role.roleId, roleName: role.roleName, contract: role.contract, hardLines: hardLines(role.bounds) }))
    fs.renameSync(tmp, abs)
    return { created: true, rel }
  } catch {
    // 写不出章程不阻断起会话：指针照附，模型读不到会自己说
    return { created: false, rel }
  }
}
```

（`capMatrix` 的 `MatrixRow.cells` 是 `Partial<Record<HarnessId, BindingLine>>`，`BindingLine { cap, level, how }`；以 `src/shared/roleBinding.ts:409` 为准。）

- [ ] **Step 2: `src/main/branchLedger.ts`**

```ts
// 分支台账 .eas/board/<branch>.md：头部由 app 从事实算（协同板每次刷新时 upsert），
// 「记录」段只追加（board_note）。**同一项目的读改写排成一条链** —— upsert 与 append
// 都是读→改→写，叠在一起会把对方那次改动写丢。
import fs from 'fs'
import path from 'path'

import type { BoardRow } from '../shared/board'
import { appendLedgerNote, clipTail, ledgerRel, renderLedger, splitLedger } from '../shared/roleDocs'

const chain = new Map<string, Promise<unknown>>()
function enqueue<T>(root: string, fn: () => T | Promise<T>): Promise<T> {
  const next = (chain.get(root) ?? Promise.resolve()).catch(() => {}).then(fn)
  chain.set(root, next)
  next.finally(() => { if (chain.get(root) === next) chain.delete(root) })
  return next
}

function readText(abs: string): string {
  try { return fs.readFileSync(abs, 'utf8') } catch { return '' }
}
function writeAtomic(abs: string, text: string): void {
  fs.mkdirSync(path.dirname(abs), { recursive: true })
  const tmp = `${abs}.${process.pid}.tmp`
  fs.writeFileSync(tmp, text)
  fs.renameSync(tmp, abs)
}

/** 协同板每次刷新时调：板上每条**分支**（主工作区那行没有台账）都 upsert 一次头部。 */
export function upsertLedgers(root: string, rows: BoardRow[], now = Date.now()): Promise<void> {
  return enqueue(root, () => {
    for (const r of rows) {
      if (r.cwd === root) continue // 主工作区会话：没有自己的台账
      const rel = ledgerRel(r.branch)
      if (!rel) continue
      const abs = path.join(root, rel)
      const { notes } = splitLedger(readText(abs))
      const worktree = path.relative(root, r.cwd).split(path.sep).join('/') || null
      try {
        writeAtomic(abs, renderLedger(
          { branch: r.branch, roleName: r.roleName, worktree, startedAt: r.startedAt, lastActiveAt: now - r.idleMs, alive: r.alive, files: r.files },
          notes, now
        ))
      } catch { /* 台账写不出不影响板 */ }
    }
  })
}

export function appendNote(root: string, branch: string, note: string, roleName: string): Promise<{ ok: true; rel: string } | { ok: false; error: string }> {
  const rel = ledgerRel(branch)
  if (!rel) return Promise.resolve({ ok: false, error: `分支名「${branch}」不能当台账文件名` })
  const text = note.trim().slice(0, 4000)
  if (!text) return Promise.resolve({ ok: false, error: 'note 不能为空' })
  return enqueue(root, () => {
    const abs = path.join(root, rel)
    try {
      writeAtomic(abs, appendLedgerNote(readText(abs), text, roleName, Date.now()))
      return { ok: true as const, rel }
    } catch (e) {
      return { ok: false as const, error: `台账写不进去：${e instanceof Error ? e.message : String(e)}` }
    }
  })
}

/** 给 board_read 带回去的台账（每份只留尾部，合并官要的是最近的决定与提醒） */
export function readLedgers(root: string, branches: string[], maxLines = 30): Record<string, string> {
  const out: Record<string, string> = {}
  for (const b of branches) {
    const rel = ledgerRel(b)
    if (!rel) continue
    const t = readText(path.join(root, rel))
    if (t) out[b] = clipTail(t, maxLines)
  }
  return out
}
```

- [ ] **Step 3: `collabBoard.ts` 接线**

- `doWriteBoard` 里 `fs.renameSync(tmp, f)` 之后加 `void upsertLedgers(projectPath, rows)`（在 try 内；`rows` 为空时 upsert 什么都不做）。**位置在 `if (!rows.length && !fs.existsSync(f)) return` 之后** —— 一条会话都没有的目录不该被造出 `.eas/`。
- export：
  ```ts
  export function roleNameOf(roleId: string | undefined): string | undefined {
    return roleId ? roleNameLookup()[roleId] : undefined
  }
  ```
- `board:read` 返回加 `ledgers: readLedgers(root, rows.filter((r) => r.cwd !== root).map((r) => r.branch))`，返回类型同步。
- 新 handler：
  ```ts
  ipcMain.handle('board:note', async (_e, cwd: unknown, note: unknown, branch?: unknown) => {
    if (typeof cwd !== 'string' || typeof note !== 'string') return { ok: false, error: '参数不对' }
    const root = projectRootOf(cwd)
    // 自己这条分支：从会话表找 cwd 相同的那条拿角色名与分支；没有会话就现读 .git 文件
    const rec = sessionSource().find((r) => r.cwd === cwd)
    const own = cwd !== root ? branchFromGitFiles(cwd, readIfFile) : null
    const target = typeof branch === 'string' && branch.trim() ? branch.trim() : own
    if (!target) return { ok: false, error: '主工作区没有自己的台账。要写到某条分支的台账，带上 branch 参数' }
    const roleName = roleNameOf(rec?.roleId) ?? rec?.roleId ?? (cwd === root ? '主工作区' : '无角色')
    return appendNote(root, target, note, roleName)
  })
  ```
  `readIfFile`（本文件私有）：`(p) => { try { return fs.statSync(p).isFile() ? fs.readFileSync(p, 'utf8') : null } catch { return null } }`。

- [ ] **Step 4: preload**

`board` 命名空间加：
```ts
    /** 往某条分支的台账「记录」段追加一条。cwd 在 worktree 里时默认写自己那条；主工作区必须带 branch */
    note: (cwd: string, note: string, branch?: string): Promise<{ ok: true; rel: string } | { ok: false; error: string }> =>
      ipcRenderer.invoke('board:note', cwd, note, branch),
```
`read` 的返回类型加 `ledgers: Record<string, string>`。

- [ ] **Step 5: 验证并提交**

Run: `npm run typecheck && npm test`

```bash
git add src/main/roleCharter.ts src/main/branchLedger.ts src/main/collabBoard.ts src/preload/index.ts
git commit -m "feat(roles): 章程首次生成、分支台账随协同板 upsert、board:note / board:read 带台账

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 3: 起会话时附「角色文档」指针段（三家 harness）

**Files:**
- Modify: `src/shared/agentChat.ts`（`StartOpts.roleDocs?: string`；`AgentChatStartResult` ok 分支加 `charterCreated?: string`）、`src/main/agentChat/sessionState.ts`（`SessionRecord.roleDocs?`、`effectiveOpts` 带上）、`src/main/agentChat/session.ts`（start 时算；omp 路径拼接）、`src/main/agentChat/adapters/claude.ts`、`adapters/codex.ts`、`adapters/adapters.test.ts`

**Interfaces（Consumes）:** Task 2 的 `ensureCharter`、`roleNameOf`；Task 1 的 `roleDocsPrompt`、`ledgerRel`、`branchFromGitFiles`。

- [ ] **Step 1: 写失败的测试（adapters.test.ts）**

找到现有钉 `boardText` 进系统提示的那几条测试（`grep -n boardText src/main/agentChat/adapters/adapters.test.ts`），在同一组后面加：

```ts
test('claude：roleDocs 指针段附在协同板之后', () => {
  const args = claudeAdapter.buildArgs({ ...baseOpts, roleContract: '你是工匠', boardText: '# 板', roleDocs: '## 你的角色文档\n- 章程：`docs/roles/builder.md`' })
  const sp = args[args.indexOf('--append-system-prompt') + 1]
  assert.ok(sp.indexOf('## 协同板') < sp.indexOf('## 你的角色文档'))
  assert.ok(sp.endsWith('- 章程：`docs/roles/builder.md`'))
})

test('claude：没有 roleDocs 时系统提示不多一段', () => {
  const args = claudeAdapter.buildArgs({ ...baseOpts, roleContract: '你是工匠' })
  assert.ok(!args[args.indexOf('--append-system-prompt') + 1].includes('角色文档'))
})

test('codex：roleDocs 压成单行拼进 instructions', () => {
  const args = codexAdapter.buildArgs({ ...baseOpts, roleContract: '你是工匠', roleDocs: '## 你的角色文档\n- 章程：`docs/roles/builder.md`' })
  const v = args[args.indexOf('-c', 0) >= 0 ? args.findIndex((a) => a.startsWith('instructions=')) : -1]
  assert.match(v, /^instructions=你是工匠 你的角色文档：- 章程：`docs\/roles\/builder\.md`$/)
})
```

（`baseOpts` / adapter 变量名以文件里现有测试为准；断言的拼接格式以 Step 3 的实现为准，两边必须逐字一致。）

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test src/main/agentChat/adapters/adapters.test.ts`

- [ ] **Step 3: 实现**

`shared/agentChat.ts`：`StartOpts` 在 `boardText` 之后加
```ts
  /** 起会话时附进系统提示的「角色文档」指针段（章程 + 台账路径，**只有指针不含全文**）。
   *  与 boardText 一样只在 spawn 时附；restart 由 effectiveOpts 带上。 */
  roleDocs?: string
```
`AgentChatStartResult` ok 分支：`{ ok: true; sessionId: string; charterCreated?: string }`（值是章程相对路径，仅首次生成时有）。

`sessionState.ts`：`SessionRecord.roleDocs?: string`；`effectiveOpts` 加 `roleDocs: s.roleDocs`；`toStartOpts`/序列化处若有字段白名单一并加。

`session.ts` start handler，在 `boardText: readBoardForPrompt(p.cwd)` 附近：
```ts
    // 角色文档指针（P3）：章程首次生成 + 台账路径。只对带 roleId 的会话；主工作区会话没有台账。
    let roleDocs: string | undefined
    let charterCreated: string | undefined
    const roleId = typeof p.roleId === 'string' && p.roleId ? p.roleId : undefined
    if (roleId) {
      const root = projectRootOf(p.cwd)
      const ch = ensureCharter(root, {
        roleId, roleName: roleNameOf(roleId) ?? roleId,
        contract: typeof p.roleContract === 'string' ? p.roleContract : '',
        bounds: roleBounds
      })
      if (ch) {
        if (ch.created) charterCreated = ch.rel
        const branch = p.cwd !== root ? branchFromGitFiles(p.cwd, readIfFile) : null
        roleDocs = roleDocsPrompt({ charterRel: ch.rel, ledgerRel: branch ? ledgerRel(branch) : null })
      }
    }
```
`rec` 加 `roleDocs`；返回 `{ ok: true, sessionId: id, ...(charterCreated ? { charterCreated } : {}) }`（找到现有 `return { ok: true, sessionId: id }` 改）。`readIfFile` 同 Task 2 那个（放 `session.ts` 顶部私有函数，或从 `collabBoard.ts` export 共用 —— 二选一，别写第三份）。

omp 路径（`session.ts` 约 :1200 `roleContract:` 数组）加第三段 `live.rec.roleDocs?.trim()`。

`adapters/claude.ts` 系统提示数组在协同板那行之后加 `opts.roleDocs?.trim() ?? ''`。
`adapters/codex.ts` 的 `contract` 数组加第三段：`opts.roleDocs?.trim() ? opts.roleDocs.trim().replace(/^## /, '').replace(/\n/, '：') : ''`（把「## 你的角色文档\n- …」压成「你的角色文档：- …」，随后整段统一压单行）。

- [ ] **Step 4: 验证并提交**

Run: `node --test src/main/agentChat/adapters/adapters.test.ts src/main/agentChat/sessionState.test.ts && npm run typecheck && npm test`

```bash
git add src/shared/agentChat.ts src/main/agentChat/sessionState.ts src/main/agentChat/session.ts src/main/agentChat/adapters/claude.ts src/main/agentChat/adapters/codex.ts src/main/agentChat/adapters/adapters.test.ts
git commit -m "feat(roles): 起会话附「角色文档」指针段（章程 + 台账），三家 harness 同步；首次生成章程回告渲染层

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 4: MCP `board_note`、`board_read` 带台账、契约字句、首次生成提示

**Files:**
- Modify: `mcp/eas-mcp.mjs`（`board_read` 描述补 `ledgers`；新 `board_note`）、`src/renderer/src/mcpHandler.ts`、`src/main/builtinRoles.ts`、`src/renderer/src/features/agentChat/AgentChatView.tsx`

- [ ] **Step 1: 工具定义**

`board_read` 描述末尾改为：「返回 JSON：`board` 是协同板的 Markdown 原文；`ledgers` 是各分支台账的尾部（分支名 → 文本，只有记过东西的分支才有）——合并官合并前先看目标分支的「给合并官」条目。」

`board_read` 之后加：
```js
  {
    name: 'board_note',
    description:
      '往你这条分支的**台账**（.eas/board/<分支>.md 的「记录」段）追加一条。只追加，不改写。' +
      '每完成一个可交付的小步记一条，按需带：决定 / 未完成 / 给合并官 / 怎么验。' +
      '在主工作区（没有自己的分支）时必须带 branch —— 合并官合并完写「已合并进 <主干>（<sha>）」就是这么用的。' +
      '返回 JSON：`rel` 是台账相对项目根的路径。',
    inputSchema: {
      type: 'object',
      properties: {
        note: { type: 'string', description: '要追加的内容，可多行' },
        branch: { type: 'string', description: '写到哪条分支的台账；不给 = 自己所在的分支' }
      },
      required: ['note']
    }
  },
```

- [ ] **Step 2: mcpHandler**

`board_read` 分支：`const { text, ledgers } = await window.api.board.read(projectPath)`，返回对象加 `ledgers`。
新分支（放 `board_read` 之后）：
```ts
  if (tool === 'board_note') {
    // 台账按**调用方的 cwd** 定位（ctx.project 是起会话时注进 MCP 环境的 cwd，worktree 里就是 worktree 路径）。
    // 不能用 resolveFrame 的 projectPath —— 那是项目根，会把每条分支的记录都写到「主工作区」去。
    const cwd = ctx.project || resolveFrame(ctx)?.projectPath || ''
    if (!cwd) throw new Error('找不到你所在的目录，写不了台账')
    const note = String(args.note ?? '').trim()
    if (!note) throw new Error('缺少 note')
    const branch = typeof args.branch === 'string' && args.branch.trim() ? args.branch.trim() : undefined
    const r = await window.api.board.note(cwd, note, branch)
    if (!r.ok) throw new Error(r.error)
    return { rel: r.rel }
  }
```
先确认 `ctx.project` 在角色会话里确实是 worktree 路径：看 `session.ts` 里 `mcpEnv({ project: cwd })` 那行（cwd 是会话的 cwd）。

- [ ] **Step 3: 契约字句（`builtinRoles.ts`）**

- `e2e`、`builder`、`prototyper` 三张卡的 `contract` 末尾各加一行：`'每完成一个可交付的小步，用 board_note 往你这条分支的台账记一条（决定 / 未完成 / 给合并官 / 怎么验）；动手前读一遍系统提示里指的章程。'`
- `merger` 契约：第 1 步改为「1 用 board_read 看协同板，**读目标分支台账里「给合并官」的条目**」；完成判据后加一句 `'合并成功后用 board_note（带 branch=<那条分支>）写一条「已合并进 <主干>（<merge 提交 sha>）」。'`
- 跑 `node --test src/main/builtinRoles.test.ts` 确认三张表不受影响。

- [ ] **Step 4: 首次生成章程的提示（AgentChatView）**

两处 `window.api.agentChat.start(...)` 之后（`result.ok` 时），加：
```ts
      if (result.ok && result.charterCreated)
        useStore.getState().requestConfirm({
          message: `已为这个角色生成本项目章程：${result.charterCreated}\n边界段（可以改 / 不要碰）留给你填，其余从角色卡来。之后不会再覆盖。`,
          confirmLabel: '知道了',
          onConfirm: () => {}
        })
```
（`requestConfirm` 是单槽，若此时已有确认框会覆盖 —— 已知留项，这里概率极低：首次用角色那一刻不会同时弹别的。）

- [ ] **Step 5: 验证并提交**

Run: `node --test src/main/builtinRoles.test.ts && npm run typecheck && npm test`

```bash
git add mcp/eas-mcp.mjs src/renderer/src/mcpHandler.ts src/main/builtinRoles.ts src/renderer/src/features/agentChat/AgentChatView.tsx
git commit -m "feat(roles): MCP board_note；board_read 带各分支台账；写码角色与合并官契约接台账；首次生成章程提示

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 5: 图纸同步 + 真机验证

**Files:**
- Modify: `docs/architecture/03-agent角色边界.md`（3A：章程与台账的位置、指针注入、谁写谁读）、`10-模块领地图.md`（`roleCharter.ts` / `branchLedger.ts` / `shared/roleDocs.ts`、`docs/roles/`、`.eas/board/`）、`11-MCP工具网络.md`（`board_note`；`board_read` 返回 `ledgers`）、`13-所有权矩阵.md`（`StartOpts.roleDocs` ↔ `SessionRecord.roleDocs` ↔ `effectiveOpts` 同步链；`.eas/board/` 与 `.eas/board.md` 并存）
- Modify: spec 状态行「P1、P2、P3 已实现」；第五节末尾「已实现」小段；新小节「十六、P3 真机验证」

- [ ] **Step 1: 图纸**（按事实写，先读代码）

- [ ] **Step 2: 真机验证（隔离实例，每条命令限时；参考 `~/.claude/projects/…/memory/eas-term-cdp-验证方法.md` 十个坑）**

用 `/tmp` 临时 git 仓库（有 `package.json`），注册进实例 `projects.json`；在画布上给它开 Frame，起一个 **工匠** 节点（真实 Claude，最小提示词）。

| # | 场景 | 判据 |
|---|---|---|
| 1 | 首次起工匠会话 | `docs/roles/builder.md` 出现，四段齐全、边界留空、硬约束段来自角色卡（内置工匠无 caps → 「无」）；渲染层弹「已为这个角色生成本项目章程」；`git status` 里它是未跟踪文件（进 git 由用户决定） |
| 2 | 手改章程加一行，再起一次工匠会话 | 文件一字不动 |
| 3 | 会话起来后 | `.eas/board/eas--builder--<id>.md` 出现，头部：角色工匠、分支、worktree 相对路径、状态活跃、触及（无）；`git status` 看不见它（`.eas/` 在 exclude） |
| 4 | 提示词让它「在 worktree 里新建 x.txt，然后用 board_note 记『决定：建了 x.txt』」 | 一轮后台账「记录」段多一条 `- HH:MM [工匠] 决定：建了 x.txt`，头部「触及」变 `x.txt（1 文件）`，头部重写没有吃掉记录段 |
| 5 | `window.api.board.read(P)` | `ledgers['eas/builder/<id>']` 是那份台账的尾部 |
| 6 | 在项目根起一个合并官节点，提示「只调 board_note，note=『测试』，不带 branch」 | 工具报错「主工作区没有自己的台账…带上 branch」；再让它带 `branch=eas/builder/<id>` → 写进工匠那条台账，署名「合并官」 |
| 7 | 系统提示 | 从会话进程的 argv（`ps -o args -p <pid>` 或 `--append-system-prompt` 内容）确认末尾有「## 你的角色文档」两行指针，**没有**章程全文 |
| 8 | 停掉工匠会话 | 台账头部状态变「已停」 |

结果写进 spec「十六、P3 真机验证」，每条一行写「看到了什么」。

- [ ] **Step 3: 提交**

```bash
git add docs/architecture/03-agent角色边界.md docs/architecture/10-模块领地图.md docs/architecture/11-MCP工具网络.md docs/architecture/13-所有权矩阵.md "docs/superpowers/specs/2026-09-05-角色工作流-分支合并官协同板-design.md"
git commit -m "docs(architecture): 角色工作流 P3 —— 章程 / 台账 / board_note 入图纸，真机验证记录

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

## 自查记录

- **Spec 覆盖**：5.1 章程（生成初稿、已存在不动、指针注入、软约束说明）→ T1 模板 + T2 `ensureCharter` + T3 指针；5.2 台账（app 维护头部、`board_note` 追加、契约要求记一条）→ T1/T2/T4；8.1 静态层指针 → T3；7.1 合并官台账字句 → T4；十三图纸 → T5。
- **类型一致性**：`roleDocs` 在 `StartOpts` / `SessionRecord` / `effectiveOpts` / 三家 adapter 同名；`board:read` 的 `ledgers` 在 main / preload / mcpHandler 同名；`appendNote` 返回 `{ok, rel}` 与 IPC / preload / mcpHandler 一致；`roleNameOf` 由 T2 导出、T3 消费。
- **裁定**：起会话读分支不起 git 进程（handler 同步）；台账 upsert 挂在 `doWriteBoard`（同一刷新节奏，不另起钩子）；主工作区无台账、合并官带 `branch` 写；首次生成提示复用 `requestConfirm` 不新造 toast。

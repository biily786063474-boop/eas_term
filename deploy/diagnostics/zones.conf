# Included at http scope in this host's vhost file. In-memory IP counters only.
limit_req_zone $binary_remote_addr zone=eas_diagnostic_rate:1m rate=1r/m;
limit_conn_zone $binary_remote_addr zone=eas_diagnostic_conn:1m;

# 更新日志

写给用户看的，不是 git log 的复述——只记「你能感觉到的变化」。
格式被 `scripts/changelog.mjs` 解析成官网的更新日志页和应用内的更新提示，
所以标题那行的写法别改：`## <版本> — <YYYY-MM-DD>`，下面用 `### 新增 / 改进 / 修复` 分组。

## 0.4.69 — 2026-08-30

### 修复

- **Windows 上「涉及终端就卡、甚至整个软件未响应」**。终端会自动识别输出里的文件路径（就是那些能 Cmd/Ctrl 点开的链接），而识别时要挨个去查这些路径存不存在——这件事原来是**卡着主程序做的**，鼠标每划过一行就查一次，一行最多 15 个。Mac 上一次不到一毫秒感觉不到；Windows 上有杀毒软件盯着每一次文件读写，一行就能卡住几十到几百毫秒，鼠标在终端里一动就连续卡，严重时整个窗口没反应。现在改成后台查 + 记住结果，不再挡住界面
- **终端里一个孤零零的 `:42` 不会再被画成链接**（它以前会指向项目根目录）

### 新增

- **设置里多了「性能」一栏**：显示这台电脑的图形加速有没有真的生效。有些显卡或驱动下，界面渲染会整个退回用 CPU 画——那时候卡顿跟显卡好不好没关系，跟这一项有没有开有关系。**只在本机显示，不上传任何东西**；旁边有个「复制完整诊断」，卡的时候可以把它发给我定位

## 0.4.68 — 2026-08-30

### 改进

- **画布平移和缩放不卡了**。之前拖动画布时掉帧率有 24%，拖起来一顿一顿的。查出来是每拖一帧都会把画面上每个节点的正文重新排版一遍（对话记录、文档节点都算），三秒钟里做了将近五万次。现在同样的内容只排版一次，**掉帧率从 24% 降到 0%，平均每帧从 25 毫秒降到 8.4 毫秒**。节点越多、对话越长，改善越明显
- **缩小画布时，终端不再头重脚轻**。以前缩小只是「少显示几行」，字号不变——可输入框只有一行、没有内容可省，于是越缩它越显眼：缩到 35% 时输入框要占掉四分之一的高度，提示文字还被截断。现在整个终端等比缩小，**任何缩放下比例都一样**，看起来就是一张缩小的照片。缩放手势结束时那一下轻微跳动也一并没了。想看内容就放大，或者双击最大化

## 0.4.67 — 2026-08-29

### 修复

- **Codex 登录不再要求你先去 ChatGPT 网站改设置**。上一版用的是设备码登录，而那条路有个**默认关着**的前置条件——点开授权页会被顶回来，让你先去「ChatGPT 安全设置」里启用「设备代码授权」再重来一次。现在换成普通登录，没有这个前置条件，授权完通常会自己跳回来，连码都不用输

### 改进

- **登录改成独立的弹窗**。以前它内嵌在对话框里，而画布上的节点常常只有三四百像素高，面板一展开就把输入框和聊天记录全挤没了，那条长长的登录网址还要在里面横着滚。现在是一个居中的弹窗，按 Esc 或点旁边就能关（**安装进行中不会被误关**——那一下会取消一个跑了几分钟的安装，要放弃得点右上角的 ×）
- **登录弹窗右上角多了一个问号**：万一网页打不开、被地区或公司网络挡住，鼠标移上去会告诉你怎么办；点开还能一键开个终端把登录命令跑起来，或者把命令复制走。登录完回到软件里，状态会自动刷新。**默认仍然全程在界面里完成**，这只是走不通时的出口
- **不再让你以为必须手动粘授权码**。两个 CLI 授权完其实都会自己跳回来，那个输入框是兜底用的（网页真给了你一串码时才用得上）——以前的说法像是必做的一步，会让人坐在那儿等一个不会出现的码

## 0.4.66 — 2026-08-29

### 新增

- **第一次打开有引导页了**。这软件自己不会说话，干活的是 Claude Code 或 Codex —— 但以前打开只看到一块空画布，没人告诉你还要装东西、装完还要登录。现在首次启动会列出这两个 CLI 各自的状态（没装 / 装了但没登录 / 可以用了），当场就能装、能登。**可以跳过**，跳过之后第一次选 CLI 时还会再提醒一次
- **甘特图多了里程碑模式和项目模式**。原来只记「你发了哪些消息」，一条条平铺。里程碑模式把阶段性的任务和回复也算进来；项目模式按「一段工作」聚合 —— AI 最后一次输出之后 30 分钟没有新输入，就算这个阶段结束，结束时间以最后一次返回为准
- **AI 在正文里问「选哪个」时，选项变成可以点的卡片**。以前它列 1/2/3，你得自己照着敲一遍。现在直接点，点完填进输入框（不自动发送——选完常常还要补一句「但是…」）。Claude Code 和 Codex 都认

### 改进

- **安装和登录全都在 AI 对话里完成，不再开终端**。以前点「安装」会弹出一个终端把命令跑起来，你盯着满屏输出不知道该不该按回车；装完还得自己敲 `claude login`。现在是进度条 + 一行「安装器正在做什么」，装完直接接上登录，全程不用离开对话框。**装之前命令原文仍然摆在你眼前**，你看得见自己在同意什么；装失败时会把安装器的原始报错展开给你看，并留一条「把命令填进终端，我自己来」的退路——公司网络、代理、权限这类问题，只有原始报错才解得开
- **登录不再自己弹浏览器**。改成一颗「点我去登录」按钮：左键才打开，**右键可以复制登录链接**，拿去你自己信任的浏览器里打开。Codex 那条路会把一次性码放大显示，Claude 那条路直接在面板里粘授权码——两边都不用回终端
- **AI 对话加了内存上限，并且跟着上下文压缩一起丢历史**。以前一个对话开久了会把所有记录都留在内存里；现在 CLI 那侧压缩上下文时，这边同步丢掉对应的历史，长会话不再越用越沉

### 修复

- **没登录时输入内容，不再是莫名其妙的「CLI 进程退出」**。这是这一版最要紧的一条：以前没登录的人打一句话发出去，CLI 照常启动、撞上认证失败、反复重试、进程死掉，而界面上只留一句红色的「CLI 进程退出（code 1）」——看起来像「我一打字它就崩了」。现在发送之前就先问一句，没登录直接引导你去登录，**你打的字留在输入框里**，登完回来接着发。会话跑到一半掉线（登录过期）也认得出来，当场给登录入口
- **在登录面板上右键，不会再弹出画布的「关闭终端」**。那里的右键是「复制登录链接」——想复制链接却差一点把整个节点关掉

## 0.4.65 — 2026-08-27

### 改进

- **Windows 上「未响应」的两个成因已经处理掉**。一是判断终端忙不忙时会起一个 PowerShell 去枚举全系统进程——终端多的时候这件事每秒都在做，现在换成更轻的方式并加了缓存；二是画布上的网页节点原本豁免了后台节流，软件切到后台时十几个网页仍在满速空转。这两条都只在 Windows 上明显，Mac 上感觉不到区别
- **灵动岛的圆角翻了一倍**，鼠标移上去有和画布模块同款的边缘高亮
- **CLI 没装时，那颗「启动」按钮直接变成「安装」**。以前没装的 CLI 在菜单里是灰的、按钮也是灰的写着「未检测到 xx 命令」——它告诉你不能用，却没给出任何一条能走的路。现在没装的也能选中（旁边标「未安装」），按钮点下去就问装不装，同意就开终端装上。一个 CLI 都没装的新机器上，这条控制条也不再整条消失

## 0.4.64 — 2026-08-27

### 新增

- **灵动岛可以收起来了**。顶上那条常驻的黑块会压住内容，现在**右键它**就缩成摄像头左边一颗呼吸的小圆点，点圆点再展开回去。收起不是关掉——通知照收，圆点的颜色跟着状态走（等审批是琥珀色、呼吸也更快；在跑是绿色；闲着是灰色），右上角还带待处理条数。收起状态会记住，重启后仍是收着的
- **看板上也能看到 AI 对话了**。以前只统计终端，一个项目哪怕有三个 AI 对话在跑，卡片上也写着「还没有终端」。现在终端数和 AI 对话数分开显示，每一行前面有图标区分，「在跑 / 等处理」的状态两者都认
- **名词词典点条目，可以插进 AI 对话的输入框**。以前只能插进终端。现在按你最后碰过哪个输入框来分流：刚点过 AI 对话就插那儿，刚点过终端就插终端
- **CLI 没装时，点一下就问你装不装**。同意的话直接开个终端把安装命令跑起来，不用再自己敲回车。命令会原样列在确认框里，你看得见自己在同意什么；也可以选「只把命令填进终端」自己看着办

### 改进

- **额度悬停显示「还有多久刷新」**，不再是「8月28日 09:00 重置」那种要自己算的绝对时刻
- **动效词条的演示重做了一轮**。删掉 6 条在选型台里根本不动的（演了也是静止画面），修好 36 条：有些演的交互组件压根没有反应（比如给一个纯滚轮驱动的组件演悬停）、有些点的位置根本不在功能上、有些画面裁得太小看不出是什么。开头静止超过半秒的也都重录了——hover 上去要等半秒才动，那半秒里你已经在怀疑它是不是坏的

### 修复

- **全屏时顶栏没有通天**，上方按钮统一成「文字 + 竖线分隔」的样式，整体也收窄了
- **分屏模式左侧的项目与文件区可以收起来了**，收起后留一条窄边带项目首字
- **设置菜单改成标签页**，不再一页堆完
- **待办清单面板的语音按钮**挪进输入框右下角，输入框底部也和窗口留出了距离
- **模块最大化时，在它的内容区右键不再弹出画布的菜单**（那时候画布根本看不见，弹「删除节点」等于对着看不见的东西操作）

## 0.4.26 — 2026-08-14

### 修复

- **切一趟画布再回来，「有终端在等你处理」的标记会消失**。这个问题存在有一阵子了，之前一直以为是切视图触发终端重新计算尺寸、撞上了 shell 自己设标题的钩子——这次加埋点实测，发现完全不是：切视图期间标题压根没变过。真正的原因是切回终端视图时，当前那个终端会被**自动**聚焦一下，而「终端获得焦点」一直被当成「你已经看到了」，标记就被顺手清掉了。你什么都没做，只是切了个视图，agent 卡在那儿等你这件事就再也不会提醒你。现在「看到了」只认真实的鼠标点击和键盘输入。**副作用**：切标签路过一个亮着标记的终端时，标记不再自动消失，得点它一下或者敲个键——多亮一会儿只是碍眼，而清错了你可能永远不知道有个 agent 在等
- **画布上的动效没有响应系统的「减弱动态效果」**。开了辅助功能里那个开关的话，其它界面的呼吸、脉冲都会停，唯独画布这块照旧在动。现在补上了：循环的呼吸/脉冲会停，拖拽排序、hover 放大这类位移不再演过程直接到位；而颜色和透明度的淡入保留（那本来就是这个设置推荐的替代方案），抽屉、工具条、灯箱该展开还是展开——它们的位置本身就是靠位移表达的，一起停掉会让抽屉再也打不开

## 0.4.25 — 2026-08-14

### 新增

- **待办清单**。在画布任意位置右键就能建一块，右上角 `+` 加一条。点前面的圆圈打勾，卡片会收进可折叠的「已完成」区；右侧的把手能拖着上下排序，别的卡片会实时让位而不是松手才跳。点开卡片弹出灯箱写详细正文，点灯箱外面就关。**拍快照时不会被清掉** —— 只有矩形/箭头/批注这类标注才随快照清空，待办是要留着的
- **skill 管理面板**。知识库抽屉左上角的名称现在是个下拉，切到「Skill」就能看各 CLI 的 skill：选中某个项目 Frame 时显示那个项目的 skill，没选中就显示全局的。点 skill 名字展开它的文件树，文件能拖到画布上直接改。skill 可以右键复制、粘贴到别的目录（**重名一律拒绝并告诉你**，不覆盖也不自动改名成 `xxx-2`），也可以右键临时禁用 —— 禁用只写一份清单，**硬盘上的文件一个字节都不动**（代价是 CLI 自己仍会加载它，面板上有说明）
- **skill 目录不再写死**。除了内置的几个默认位置，可以自己添加目录 —— 你把 skill 搬去 `design-skills` / `motion-skills` 做渐进式披露那种组织方式，面板认得
- **agent 可以帮你整理 skill 分类**。跟它说「整理一下我的 skill」，它会读出你所有 skill 并归成扁平一层的分类，直接显示在面板上、每类一个可折叠的头。分类只写 Eas-Term 自己的配置，不碰你的 skill 文件，也不影响任何 CLI 怎么加载它们

### 改进

- **Frame 整理时，终端优先排到第一行左上**。以前整理完终端可能被挤到下面，要找一下
- **「便签」改叫「批注」**
- **画布外的东西现在盖得住画布内的东西**。文件信息、知识库抽屉、周围那些悬浮图标，层级都高于画布内的一切元素（包括矩形、箭头、批注）
- **鼠标滚轮不再被标记截断**。以前鼠标一挪到矩形或批注上，画布就滚不动也缩放不了；现在只有选中它们时滚轮才归它们（批注的长文本仍然能滚）

## 0.4.24 — 2026-08-14

### 新增

- **右上角那个提醒气泡点开是一份列表**，只列已经干完活的项目，点一条直接跳到对应的那个终端

### 改进

- **四个显示状态的地方现在口径统一了**：灵动岛、右上角提醒、项目右侧的状态、双击项目弹出的列表 —— 以前各算各的，会出现「灵动岛说在跑、列表说完了」这种自相矛盾。现在它们共用同一个状态机
- **双击项目弹出的列表会动态排序**：等审批的排最前，然后是干完的，最后是还在跑的。状态不再显示文字，改成动态图标 —— 三个点轮流上跳表示在跑，圈加对勾表示完成，圈加感叹号在呼吸表示等你审批。「已在画布中」这个没用的状态去掉了

### 修复

- **右上角的通知不见了**。上一版把左右两个抽屉对调时，这个气泡跟着跑到了左边，落进运行监视那块面板的盒子里被盖住。现在挪回右上角，知识库抽屉展开时它会自己让位
- **从灵动岛点进软件后输入法打不了字、鼠标悬停也没反应**。原因是那种激活方式只把应用推到前台，没有真正把窗口变成「键盘窗口」，而输入法和鼠标指针只认后者。现在改成等价于「右键 Dock 图标 → 显示所有窗口」的行为
- **agent 还在跑的时候叫你，五个显示状态的地方会重新亮起来**（以前有一条路径会把「等审批」的标记冲掉）
- **那份「已完成」列表会在你没点气泡的时候自己弹出来**
- **MCP 桥、密钥对话框、第二实例这三处会挑中灵动岛而不是主窗口**。表现是弹窗弹到一个 40 像素高的小条上，或者干脆看不见

## 0.4.23 — 2026-08-13

### 新增

- **画布上可以做标记了**：矩形、箭头、便签，圈出你要说的那块地方
- **框选拍快照直接给 agent 看**。在画布上框一块区域，截图连同你画的标记一起发给 agent，省掉「截图 → 存文件 → 拖进来」这一串

### 修复

- **标记层会吃掉落点和右键**。画了矩形之后，那块区域上的右键菜单和拖放会失灵
- **版本管理落在非 git 目录时整个界面会白掉**（既有问题，与这一版的新功能无关）
- **拍快照偶尔拍到别的项目**：如果此前最大化过某个节点，残留的选中状态会把快照引到错的地方
- 图形的右键菜单里不该出现「关闭终端」

## 0.4.22 — 2026-08-12

### 修复

- **「正在工作中 / 工作完成」和灵动岛的状态监视全都不见了**。原因不在 Eas-Term：Claude Code 2.1.229 换了写终端标题的方式——以前干活时标题前面是盲文转圈字符（⠋⠙⠹…），现在改成了半黑圆圈（◐◑ 交替），空闲时则是 ✳。而「这个终端在不在干活」这件事，整个应用只认那一个盲文信号，认不出就等于永远空闲，于是三个功能一起哑掉。现在新旧字符都认；**而且加了一层不认字符的兜底**——只要标题开头那个字在变、后面的名字没变，就当成在转圈，下次它再换符号也不会又静默失效。标签名里那个字符也会被剥掉，不会再看到 `◐` 或 `✳` 挂在名字前面
- **终端偶发「既没有输出、状态也不动」**。一个只在特定时序下才会踩到的问题：终端订阅输出时，如果此前已经有缓冲的内容要回放，回放会在订阅那一刻同步发生，而当时有段代码还没初始化完，异常会把后面所有的事件注册（标题、缩放、响铃）全带走。把那段代码提到订阅之前就没有这个窗口了

## 0.4.21 — 2026-08-12

### 新增

- **知识库可以按你自己的分类建了**。原来是写死的八个目录（收件箱 / 我 / 人物 / 方法 / 领域 / 项目 / 素材 / 模板），做研究的想按「课题 / 文献 / 实验」分，做内容的想按「选题 / 素材 / 成稿」分，只能忍着。现在跟 agent 说一句「知识库分类不合适，我想按自己的方式建」，它会先把你现在库里的分布摊给你看（哪个目录堆着、哪个空着），再一次一个问题问清你平时往里放的东西大致分几类、每类一句话怎么描述，给出方案等你点头，然后建好新库切过去
- **你现有的库一个字节都不会动** —— 不搬、不改、不删。新库建在别处，随时能指回去
- 新库享有和内置库完全一样的能力：查询、图谱、体检、收件箱徽章、归档、双链，全都照常工作

### 改进

- **给 AI 的使用指引改成按需加载**。原来是一份 300 行的说明，每次对话都要整份读一遍；现在常驻的只剩不到 100 行的「什么时候该做什么」，画布操作、生图、密钥、知识库四块的详细做法拆成单独文件，用到哪块才读哪块。在 Codex 里表现最明显 —— 以前在那儿改一行代码，都要先付一整份画板指南的 token

### 修复

- **「扩展能力」面板里的落点清单少报了文件**。它是告诉你「我们往你机器上写了什么、卸载会删哪些」的那张表，之前只列得出其中一个文件，现在按实际写进去的每个文件列全

## 0.4.20 — 2026-08-11

### 新增

- **分屏模式的终端也有命令按钮了**，跟画布上那排一样。它自己会认出这个终端里跑的是 Claude 还是 Codex —— 判据是终端里真实的进程，不管你是从控制台点「启动」起的、还是自己敲 `claude` 起的，都认得出；退出之后按钮也会自己消失

### 修复

- **CLI 修好了但软件一直说「未检测到」**。这次真撞上了：Claude Code 的自动更新被 npm 的脚本策略拦掉、原生二进制没就位，修好之后 Eas-Term 还是一口咬定它不存在，连带 slash 命令按钮也消失，只能重启软件才恢复。原因是那个探测**一个面板只跑一次、永不重探**，而「两个 CLI 都没有」才会挂的那个补救监听，偏偏漏掉了「只坏了其中一个」这种最常见的情况。现在改成窗口每次重新获得焦点都重探一次 —— 你去别处装好/修好，切回来就认出来了

## 0.4.19 — 2026-08-11

### 新增

- **画布终端的控制条下面多了一排命令按钮**，点一下直接发给 agent，不用自己打斜杠命令。六个常用的常驻：压缩上下文、看上下文占用、计划模式、新开一轮、复制上条回复、用量花费；点「⋯」还有审查改动、安全审查、恢复会话、换模型、思考档位、重载技能、生成项目说明。按钮上是中文语义，Claude 和 Codex 底下发的命令不一样，你不用记
- **不可逆的操作会先问一句**，而且说清会失去什么：压缩会把对话换成摘要（细节不可恢复）、新开一轮会清空上下文（旧会话还在磁盘上）、审查会花时间和额度。二级菜单里这几条旁边标了「需确认」，点之前就知道
- **按钮只在真的跑着 agent 的终端上出现**：裸 shell 不显示（不然点了只会得到一句 `command not found`），agent 正在干活时按钮变灰不可点（那会儿插命令会被它当成粘贴吃掉）

## 0.4.18 — 2026-08-10

### 新增

- **Markdown 里的代码块右上角有复制按钮了**，点一下整块拷走，图标变成对勾表示成功。文件预览和知识库都有。复制出来的是原文，`<`、`&`、引号都不会变成网页转义符

### 修复

- **从灵动岛点进来之后打不了中文、鼠标指针也不跟着变**。上一版为了确认「真的切到前台了」加了一道核实，而那道核实问的是辅助功能那一层——**它看不见 Eas-Term**（实测：本软件真在前台时，那句查询连报 40 次「找不到任何前台进程」；同一秒把访达切到前台却答得又快又准）。于是每次跳转都被判成「没切过来」，接着执行补救动作；而那个补救动作有 3/8 的概率把前台交给一个毫不相干的后台软件，窗口却还亮在你眼前——看着在前面、其实系统没把输入法和鼠标指针给它。修复本身成了故障源。现在换了一条不经辅助功能的问法，并且**问不出来时什么都不做**（不知道就别乱动），补救动作也换成了实测零副作用的那条
- **知识库里点不开笔记**。右边「左边选一篇笔记」那块占位提示铺满了整个面板，把左边的笔记列表整个盖住了——看得见、点不着
- **画布上的视频，你去点别的地方就自动暂停了**。以前点开一个视频它会一直放下去——你已经在敲终端了，旁边还在解码，白耗电。现在点到别处就停，播放位置留着，点回来接着看
- **点发送按钮的时候麦克风会收掉了**。以前只有按 ⌘↵ 才收麦，点旁边那个发送按钮不算——消息发出去了麦还在录，你说的下一句被接在了后面。现在不管从哪儿发都收麦，要再说就自己点一下麦克风
- **让 AI 生图时不用先自己去开笔纵画板了**。以前画板没开着的话，说一句「画个封面」只会莫名其妙失败——画板的接口只在它运行时才在，而没有任何环节会去把它拉起来。现在调用前会自动在后台打开画板（不抢你正在用的窗口），等它就绪再继续；本来就开着的话没有任何延迟
- **画板版本太旧时不再配一条永远连不上的通道**。旧版画板（1.21.20 之前）的安装包里没带 MCP 需要的依赖，装了也用不了，而报错只有一句「连接失败」，根本看不出是该更新画板。现在装之前先验一次，验不过就不配——界面上一眼能看出没接上，而不是配了个假的。另外接入方式改成用画板自带的运行时，你机器上装没装 node、装的什么版本都不影响
- **修好了「AI 说在生成、结果永远等不到」**。画板为了防止 AI 擅自花钱，生成是两步的：先写参数，再由你确认（或明确声明无人值守）才真正开始。而我们给 AI 的说明书里漏了第二步——它会一直轮询一个根本没开始的任务。顺带补上了另外几条它以前会做错的：多张参考图必须在提示词里点名是哪张、连线的参数名、提示词用中文写
- **删终端现在四个模式一起关，不再留下点不掉的空白框**。以前画布和分屏各删各的：从哪边删，另一边都留着半个。留在画布上的那半个尤其难缠——终端节点是靠它里面那个终端面板被右键菜单认出来的，面板都没了，右键它菜单里一个选项都没有，只能重启才消失。现在从任何一处删，四个模式同时收掉；关标签、删项目也一并清干净

## 0.4.17 — 2026-08-09

### 新增

- **甘特图上右键一根条，可以选去哪个模式看那个终端**：画布、终端、看板三选一。选过一次之后，左键点条就直接去你上次选的那个模式（第一次默认看板）。去画布时会把视口移到那个节点上，不用自己找

### 修复

- **填新密钥时切出去复制，回来面板还在**。以前去别处复制 key、点回软件的第一下就被当成「点了外面」，面板当场关掉，填的内容全丢。现在只有点保存、点关闭或按 Esc 才关
- **确认框不再被面板盖住点不到**。删密钥、删甘特图记录、启动时的回溯确认——这些弹出来的确认框层级比调用它的面板还低，看得见点不着。这不只是密钥柜的问题，六个地方都会撞上，一并修了
- **灵动岛崩了以后会自己重建**。以前它的显示进程一旦出问题就永久坏着，直到你重启整个软件——用久了变成一行没有样式的裸文字，多半就是这个
- 灵动岛出问题时现在会在日志里留下记录。以前它出任何事都是悄无声息的：连样式文件加载失败这种，系统层面都不会报任何错，所以「看不到报错长什么样」不是没找到，是真的没有

## 0.4.16 — 2026-08-09

### 新增

- **终端输入框右键可以插入待办清单**。就在输入框正上方，加几条要干的事，干完自己点掉。跟着终端走，各个终端互不干扰，关掉软件再打开还在
- **甘特图能缩放时间线了**：⌃+滚轮或触控板双指捏合，最大三天、最小一个屏幕里显示一小时，以光标为锚（缩放时光标底下那个时刻不会跑掉）。原来的「24 小时 / 3 天」两个按钮留着当快捷跳转
- **一个项目开了几个终端就显示几行**，项目名成了可折叠的分组头。原来是一个项目挤一行、并行的任务上下叠着，看不出哪条属于哪个终端
- **可以自己删记录了**：鼠标停在某根条上删单条，图上还有「清空这段」和「清空全部」，按钮上直接写明会删几条

### 修复

- **甘特图的数据出问题不会再拖累别的**。它现在有自己的一层保护，坏数据被挡在画图之前，就算真崩了也只掉甘特图这一块 —— 终端、画布、看板照常能用，切过去就行

## 0.4.15 — 2026-08-08

### 修复

- **视频拖进度条会断**。播放器要跳到某个时间点，靠的是只取文件中间那一段的请求，而软件内部那个取本地文件的通道从来只会整份返回、不认这种请求 —— 于是每次跳进度都静默失败。现在正确支持了，跳到哪都能接着播
- **视频点全屏按钮没反应**。是权限那一层把全屏一起拒了，跟画面怎么摆无关。现在能全屏了，退出后视频回到画布原位，跟着画布平移缩放照旧
- **甘特图鼠标停上去，详情框贴到窗口右边会被压扁**（文字挤成每行两三个字的竖排）。现在会自动收回窗口内；靠近底部时翻到上方展开；内容特别长时框内可以滚

## 0.4.14 — 2026-08-08

### 修复

- **甘特图上鼠标停上去，详情里混进一大段乱码**（形如 `<35;22;13M<35;23;13M…`）。那不是乱码，是鼠标在终端里移动时终端上报的坐标 —— 记录你说了什么的那段代码没认出它是控制序列，把它当成你敲的字收了进去。现在改成按标准结构识别**任何**控制序列并整段跳过，不再逐个枚举，以后遇到没见过的序列也不会漏进来

> 这一版之前记下的内容里已经混了这些坐标，它们不会自动消失 —— 会跟着记录到期（保留一周）。时间轴本身是准的，只有文字部分受影响。

## 0.4.13 — 2026-08-07

### 新增

- **甘特图**。第四个视图：横轴是时间，一行一个项目，每根条是一次「你发出去的话 → agent 干完」。条上显示前十个字，鼠标停上去看全文和耗时，点一下跳回那个终端。同一个项目同时跑几件事时，条会上下堆叠而不是叠在一起
- **底部有一条导航带**，画完整一周的活动疏密，拖上面那个框选看哪一段；主区跨度可以在 24 小时和 3 天之间切。有异常中断的位置在导航带上是红的，一眼能找到
- **图上直接拖也能平移**，底部那个框实时跟着走；反过来拖框上面也跟着动
- 四个视图收进了一个「模式切换」按钮，点开下拉四选一 —— 原来的三段控件放不下第四项

### 修复

- **看板里点开终端一片空白**。而且是间歇性的：切到看板后第一次通常正常，之后就全打不开，直到切走再切回看板。所以看着像「只有某几个终端有问题」，其实跟是哪个终端无关，取决于你是第几次点开
- **从灵动岛跳回软件后，偶尔整个窗口点不动**（画布缩放没反应、输入框点不了），要去「显示所有窗口」再切回来才好。原因在系统层面：macOS 不认「后台程序自己把自己切到前台」这件事，程序以为自己已经激活了，但键盘鼠标其实还在喂给上一个应用。改成交给系统去打开自己 —— 实测同一台机器上，原来的做法 0/10 次成功，现在 10/10

## 0.4.12 — 2026-08-06

### 新增

- **装了「笔纵画板」就能在终端里直接出图**。跟 agent 说「画张封面」，它会自己开项目、建节点、挑模型、发起生成、等结果，画完把图摆到画布上给你看，不用再切过去手动点一遍。画板放在「应用程序」里就会被自动认出来，不用填任何配置 —— 但生成的时候画板得开着（工具是连到运行中的画板上的）
- 视频、抠图、放大、扩图这些画板的能力，同样可以直接在终端里说

### 修复

- **模块放大铺满时，右下角的画布工具条和缩放条不再透出来**。它们本来就被盖在下面，但模块背景是半透明玻璃，于是那排图标和「100%」浮在终端输入框那一行上，跟发送、语音挤成一团。铺满的时候画布整个被盖住了，缩放画布本来也没有意义

## 0.4.11 — 2026-08-06

### 修复

- **从灵动岛跳回软件后，第一次点击不再被吃掉**。以前跳过来伸手点模块，那一下被系统当成「激活窗口」用掉了，看着像点不动 —— 现在看见什么就能点什么
- **模块放大铺满后直接就能滚**，不用再点一次选中它。画布已经被整个盖住了，那会儿滚轮本来就该归模块内容
- 看板里点开终端后，上方那条控制条（角色 / 模型 / 思考档位）显示的**和画布上同一个终端一致**了 —— 之前它读不到配置，一律显示默认值，改了也存不住
- 看板全屏时，上方那条可以拖动窗口了
- 「启动」弹出的回溯确认框不再越出窗口右边缘
- 看板左上角「进行中」那个浮标不再跟着横向滚动滑走

## 0.4.10 — 2026-08-06

### 改进

- **每个终端的内存占用大幅下降**。终端保留的历史行数从 2 万调到 5 千 —— 一个终端刷满长日志时，占用从 +68MB 降到 +2MB。开十几二十个终端长时间工作时差别很明显。往回翻仍有约 60 屏，agent 的审批解析不受影响

## 0.4.9 — 2026-08-06

### 改进

- **画布只渲染你看得见的那部分**。以前视口外几千像素的终端也一直在画，三十个终端摊开时内存会随着平移一路爬（实测 536 → 668 MB）；现在稳定在 378 MB 不动
- **网页节点离屏两分钟就连进程一起回收**，滚回来自动重建并回到你上次停留的页面。以前一个网页节点只要被看过一次，它那个进程就活到关软件为止
- **看板里能关终端了**：卡片上的终端行鼠标移上去出一个 ×，全屏时顶部条也有。以前得切回终端或画布才能关

### 修复

- **看板里点开的终端翻不动历史**。滚轮被画布那套「在模块上滚 = 平移画板」的逻辑截走了 —— 而且它还在偷偷平移画布，所以你在看板滚几下、切回画布会发现画面整个跑掉了
- **画面偶尔整个往上跳、顶部被切掉**。浏览器为了让某个元素可见会去滚它认为能滚的容器，而那几层容器设计上根本不该滚。改用 `overflow: clip` 从根上堵住，不再靠事后复位

## 0.4.8 — 2026-08-06

### 新增

- **看板模式**。第三种视图，跟「终端」「画布」并排。按状态分列，一个项目一张卡片：开了几个终端、每个正在忙什么、哪个在等你处理，一屏看全。点卡片铺满干活，Esc 或点「看板」回来
- **看板的列由你自己定**：新建、双击改名、删都行。删一个看板不会删掉里面的项目，它们回到「未分类」。一屏正好四列，多的往右滑
- **拖着卡片就能改状态**，而且三个视图是同一份数据 —— 画布上那个项目的框会跟着变色，分屏里右键项目也能打同一个标
- **分屏里也能给项目打状态标了**。以前状态记在画布的框上，没进过画布的项目根本无处可打

### 改进

- 看板里滚轮永远滚列表，除非你真的点进了某个终端 —— 指针只是路过时不会误翻某个终端的历史
- 往上滚出去的卡片会折叠成一摞收在顶部，滚回来自动展开

## 0.4.7 — 2026-08-05

### 新增

- **卡片和按钮跟着鼠标发光**。鼠标靠近画布节点的边框，那条边会渗出一圈光；主按钮划过去有一道白光斜扫而过
- **主按钮有了立体感**，按下去是真的凹进去——受光面会翻过来，不是整个往下滑
- **终端上的 Claude / Codex 切换收成了左边一枚星星按钮**。当前用哪个看图标就认得出，不再占两个文字按钮的位置；角色钉死了 CLI 时按钮会锁住并告诉你原因，以前那种情况点了没有任何反应
- **角色选择改成翻页式**：顶上一排点、中间一页角色、底下左右翻，也可以点任意一个点直接跳过去。打开时停在你正在用的那个角色上
- **角色的「禁用工具」不用再手打名字了**。按「改文件 / 跑命令 / 读代码 / 联网 / 协作 / MCP」分好组摆出来，点一下就禁，每项鼠标停上去有一句说明它是干什么的。通配符这类还是能手写，收在「手写补充」里

### 改进

- 密钥柜排版松开了，标题和正文拉开距离，变量名改成两列
- 角色列表里每项前面的彩色圆点改成了灰点，只有当前选中的那个亮——原来那些颜色看着像有分类含义，其实没有

### 修复

- **灵动岛上点「知道了」不再把整个软件拽到前台**。你在别的应用里干活时顺手点一下，Eas-Term 会安安静静待着——那条通知你本来就只是想让它别再响，没打算切过来
- **角色编辑面板里的字不再叠成一团**。窗口不够高时「职责契约」那一栏会被挤成零高度，里面的内容原地溢出，压在下面两栏上
- 手快连着点两个要禁用的工具时，不会再丢掉其中一个

## 0.4.6 — 2026-08-04

### 修复

- **你在软件里干活时，灵动岛不再自己冒出来**。它是个层级很高的窗口，macOS 为了显示它会把你从当前桌面拽走——全屏用的时候就是「被强制切了窗口」。任务完成照旧有提示音和标题栏的待处理计数，切走之后灵动岛才登场；想主动看一眼可以右键 Dock 图标
- **终端里的链接不再拉起 Safari**。用来把链接留在软件内的那层拦截其实一直没生效（被系统的 PATH 重排挤掉了），所以 agent 一打开网页，整个应用就被顶到后台
- 标签栏不再跟着 agent 的转圈动画一直跳字——那个字每 100 毫秒换一个，紧挨着标题栏，看着就是顶上一条在闪
- 在终端输入框里点发送（或 ⌘↵），内容有时会停在 agent 自己的输入框里不发出去，还得再按一次回车——带图片时尤其明显。现在文本和回车分开发，中间留一点时间给 agent 反应
- 多行内容不再被拆成好几条发出去，整段算一条

## 0.4.5 — 2026-08-04

### 新增

- 画布上框选一片模块后，按**删除键**可以一次性删掉。Mac 键盘那个 delete 键现在也认了（原来只认 Fn+delete，按下去没反应）
- 选中里含终端时会问一次「其中几个正在运行命令，删除会终止它们」，确认后一起删——不会逐个弹窗

### 改进

- 任务完成时灵动岛**只折叠着播报**，不再摊开卡片挡住屏幕顶部；等待审批才自动展开（那种要你做决定，卡片上就有能直接点的选项）
- 点折叠条只做「展开」这一件事：看清哪些完成了、哪些还在跑，不会顺手把待处理清掉
- 展开后点灵动岛以外的地方，它会自己收起来

## 0.4.4 — 2026-08-04

### 新增

- Dock 图标**右键**可以直接看「谁完成了、谁还在跑」，点一条直达那个终端——不用打开软件，也不用等灵动岛冒出来

### 改进

- **在软件里干活时，灵动岛不再弹卡片压住屏幕顶部**：只留一条折叠胶囊，上面写明是「任务完成」还是「需要审批」，想看详情自己点一下展开
- 展开后是一份列表：完成了哪些、还在跑哪些，点任意一条跳到那个终端
- 「知道了」不再把任务当成处理完了——它只是不再提醒你，待处理标记留着，等你真去那个终端才消掉
- 语音输入插在**光标处**，不再总是接到最后面

### 修复

- Windows 上用鼠标滚轮缩放画布，一格就直接掉到 20%（缩放步长是照着 macOS 触控板写的，而 Windows 滚轮一格的跨度是它的十倍）

## 0.4.3 — 2026-08-03

### 新增

- 有新版本时标题栏会提示，点开能看到这一版改了什么，一键下载对应你芯片的包并打开
- 官网多了一页[更新日志](https://eas.biily.top/changelog.html)，每个版本改了什么都记在那里
- **新增匿名使用统计，默认开启，可以关**：只有使用时长、启动次数、版本与系统大类、各功能的使用次数。
  终端内容、命令、文件路径、项目名、与 AI 的对话、密钥，一个字节都不会离开你的电脑，
  也不生成任何能跨天认出你的标识。不想要就去设置 → 隐私关掉，关掉后一条请求都不会发。
  详情见[隐私说明第四节](https://eas.biily.top/privacy.html#usage)——这一条是对此前「不做使用统计」那句话的改动，所以单独写出来。

### 改进

- 设置里能看到当前版本号，并可手动检查更新

## 0.4.2 — 2026-08-03

### 新增

- 终端多了一个文字输入框：长提示词先写好再发，中文输入法不再错位，粘贴多行也不会被逐行当成命令执行
- 输入框支持粘贴 / 拖入图片，⌘↵ 时把图片路径连同文字一起发给 agent
- 语音输入的结果现在落进输入框，说错了能改完再发（原先是直接写进终端）

### 改进

- 设置移到标题栏最右，两种视图下位置一致；主题和提示音都收在里面
- 画布右侧抽屉的项目高亮和文件树，会跟着你正在操作的项目走（选中 Frame 或点进终端节点）

### 修复

- 语音按钮和输入框在右下角叠成一团
- 你正盯着某个终端、它跑完了却一声不响——现在无论是否聚焦都会提醒
- 主窗口在前台时灵动岛不再完全沉默：有新通知会露面几秒，等待审批的会一直留着
- 贴着窗口右缘的悬浮提示被挤成一列竖排字

## 0.4.1 — 2026-08-03

### 新增

- **灵动岛**：把软件收起来后，屏幕顶部出现一条状态胶囊，显示有几个项目在跑；
  任务完成或需要审批时弹出通知，写明项目、耗时、模型和这一轮的问答梗概，点一下直接跳回那个会话
- 需要审批的打断可以在灵动岛上直接处理，不用切回来
- 待处理提示音（木琴音色），任务完成和等待审批是两个不同的音，可在设置里关闭或调音量
- 画布 Frame 支持颜色状态标签：蓝色进行中、黄色待执行、红色已完结

### 改进

- Windows 包恢复跟随发布（此前需要手动补传）

## 更早的版本

0.4.0 及更早没有整理更新日志。0.3.x 主要是画布、知识库和密钥柜的迭代。

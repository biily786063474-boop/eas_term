// 角色编辑器里契约输入框的占位提示。
//
// 单独一个文件是为了不让一大段中文示例挤在组件里；也方便以后按语言切。
export const BUILTIN_HINT = `例如：

你这一轮的职责是实现。
完成判据：改完代码必须构建通过，并且亲眼验证过效果才能说「已修复」。
构建失败、测试失败一律原样报告，不粉饰。

写「产出什么、落在哪、什么算做完」，不要写「你是一个资深工程师」——
后者对模型行为几乎没有影响。`
